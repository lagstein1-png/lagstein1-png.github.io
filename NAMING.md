# שמות האפליקציות

מסמך ההחלטה. עד עכשיו השמות נקבעו אפליקציה־אפליקציה ונוצרו הבדלים
בין דף הבית לאפליקציות עצמן. הכלל כאן הוא מקור האמת; כשמוסיפים
אפליקציה או שפה, ממלאים לפיו.

## שם האתר: ״למידה שנשמעת״ — Learning That Sounds

השם האנגלי הוא שם האתר בוויקס, ונמסר ב-5.9.2026. השם תיאורי ולכן
מתורגם ולא מתועתק, כמו ״תאוריה מדברת״: בערבית **تعلُّم يُسمَع**
וברוסית **Учёба, которую слышно** — אותו ביטוי שכבר עמד בכותרת
הראשית (`h1a`) של דף הבית בשתי השפות.

השם חי בארבעה מקומות בשורש, וכולם חייבים להסכים: המחרוזת `me`
ב-`STR` של `index.html` בארבע השפות (הלוגו, וכותרת הכרטיסייה
שנבנית ממנה ב-`paint()`), ה-`<title>` הסטטי, ו-`name` ו-`short_name`
ב-`manifest.json`. בטלפון צר (עד 720px) השם בלוגו מוסתר והסמל
נשאר, מפני שלוגו + ארבע שפות + מצב כהה לא נכנסו ב-360px.

## הכלל: שם מותג מתועתק, תיאור מתורגם

**השם נשאר אותו שם בכל ארבע השפות, בתעתיק פונטי.** ״ניב״ הוא Niv,
Нив ו-نِيف — לא Idiom ולא لهجة. **התיאור שאחרי הקו המפריד מתורגם**
לפי משמעות: `ניב — אנגלית מדוברת וספרותית` הופך ל-
`Niv — spoken and written English`.

הנימוק: זה מוצר אחד, והשם הוא מה שילד מחפש, מבטא בקול ואומר לחבר.
תרגום לפי משמעות יוצר ארבעה מותגים שונים שאיש אינו יכול להפנות
מאחד לשני. שלושה מתוך ארבעה השמות כבר נהגו כך; ״מפנה״ היה החריג
ותוקן (היה Turning Point ‏/ ‏Поворот ‏/ ‏منعطف).

**מספרי סדרה מקומיים.** אקסיומה ב׳ ו-ג׳ נכתבות II ו-III באנגלית
וברוסית, ו-‏٢ ו-‏٣ בערבית — ספרה בכתב היעד, לא האות העברית.

## הטבלה

| תיקייה | עברית | ערבית | רוסית | אנגלית |
|---|---|---|---|---|
| `math-app` | מתמטיקה לחכמים שמתקשים | رياضيات للأذكياء الذين يجدون صعوبة | Математика для умных, которым трудно | Math for Smart Strugglers |
| `math-teen` | שלב | شلاف | Шлав | Shlav |
| `math-uni` | אקסיומה | أكسيوما | Аксиома | Axioma |
| `math-uni2` | אקסיומה ב׳ | أكسيوما ٢ | Аксиома II | Axioma II |
| `math-uni3` | אקסיומה ג׳ | أكسيوما ٣ | Аксиома III | Axioma III |
| `bagrut-806` | 806 | ٨٠٦ | 806 | 806 |
| `ulpan` | אולפן | أولبان | Ульпан | Ulpan |
| `english` | ניב | نِيف | Нив | Niv |
| `history` | מפנה | مفنيه | Мифне | Mifne |
| `reader` | מקריא קולי | قارئ صوتي | Голосовой чтец | Voice reader |
| `theory` | תאוריה מדברת | نظرية ناطقة | Говорящая теория | Talking Theory |
| `voice` | בדיקת קול | — | — | — |

״תאוריה מדברת״ יושבת בריפו נפרד תחת אותו חשבון אבל נספרת
בפורטפוליו, ולכן היא בטבלה. המזהה שלה ב-`DATA.APPS` הוא `theory`.

## השם האנגלי הישן — אסור, ונמחק

**השם האנגלי שקדם ל״תאוריה מדברת״ אסור לשימוש** — חשש לזכויות
יוצרים וסימן מסחר, 3.9.2026. אין שם חלופי, ואין להמציא אחד.

**ב-4.9.2026 ההחלטה הורחבה: למחוק אותו מהמאגר כליל** — גם באות
קטנה, גם כמזהה פנימי, וגם בתוך הערות ומסמכים. **השם הרשמי היחיד
הוא ״תאוריה מדברת״**, ובשלוש השפות האחרות לפי שורת הטבלה שלמעלה.

מה שהשתנה בפועל:

| היה | היום |
|---|---|
| `"id":"drivewise"` ב-`DATA.APPS` | `"id":"theory"` |
| `ICONS["drivewise"]` | `ICONS["theory"]` |
| `EXTERNAL` ב-`apps.js` | `theory` |
| אזכורים ב-`marketing/`, ב-`ARCHITECTURE.md`, ב-`learning-core/` ובקובצי הסוכנים | ״תאוריה מדברת״ |

### המופע האחד שנשאר — הנתיב, ולמה הוא חזר

האפליקציה מתפרסמת מריפו נפרד, ו-GitHub Pages מפרסם פרויקט תמיד
תחת **שם הריפו**. לכן הכרטיס נושא את שם הריפו כנתיב, בשדה `u`.

**ב-4.9 השדה נמחק בהנחה שהריפו הנפרד כבר שונה ל-`theory`. ההנחה
לא נכונה.** נמדד ב-5.9.2026: רשימת הריפוזיטוריז של החשבון מחזירה
**שניים בלבד** — `lagstein1-png/lagstein1-png.github.io` ו-
`lagstein1-png/drivewise`. כלומר הכתובת `/theory/` אינה קיימת,
והכרטיס בדף הבית הוביל ל-404 חי לכל מי שלחץ עליו.

לכן השדה הוחזר, ו-`naming.js` מחריג **מחרוזת אחת מדויקת** —
`"u":"/drivewise/"` בתוך `index.html` — ולא את הקובץ. כל מופע אחר
באותו קובץ עדיין נכשל, ואומת בפועל: הוספת המילה בהערה בראש הקובץ
הפילה את הבדיקה.

**מה שסוגר את זה סופית אינו בריפו הזה:** שינוי שם הריפו הנפרד
ל-`theory`. ביום שזה קורה — מוחקים את השדה, מוחקים את ההחרגה
ב-`naming.js`, ומוחקים את סעיף 4.5 ב-`apps.js`. עד אז השדה הוא
נתיב טכני, ולא שם.

### הבדיקה שאוכפת

`node .claude/qa/naming.js` סורק את כל קובצי הטקסט במאגר ונכשל על
כל מופע של השם, **בכל אות** — הביטוי הוא `/drivewise/i` ולא רק
צורת המותג באות גדולה. שני קבצים מוחרגים והם רשימה סגורה:
`NAMING.md`, שכן הוא המסמך שמנסח את הכלל, ו-`naming.js` עצמו.
קובץ שלישי שיזדקק למילה הוא סימן שמשהו זולג, ולא סיבה להאריך
את הרשימה. ומעליהם החרגה אחת שאינה קובץ אלא **מחרוזת**: שדה
הנתיב שבכרטיס, כמתואר למעלה.

ו-`node .claude/qa/apps.js` אוכף את הצד השני של אותו מטבע: אפליקציה
שרשומה ב-`EXTERNAL` — כלומר אין לה תיקייה כאן — **חייבת** נתיב
מפורש בכרטיס, אחרת המזהה לבדו מוביל ל-404. שתי הבדיקות ביחד
מונעות גם את החזרת השם, וגם את הקישור השבור.

הבדיקה מוודאת גם שארבע המחרוזות המוצגות בכרטיס הן עדיין השם
הרשמי — כי סריקת המילה האסורה לבדה לא הייתה תופסת החלפה בשם
שלישי תמים, והנזק זהה.

## 806 — מספר שאלון, ולכן ספרה בכתב היעד

״806״ אינו שם שהומצא כאן אלא מספר השאלון הרשמי, והוא מה שהתלמיד
מחפש. הוא נכתב בספרות של שפת היעד — `٨٠٦` בערבית — לפי אותו כלל
שחל על אקסיומה ב׳ ו-ג׳.

**התיאור שאחריו היה נפרד, ואוחד.** ב-`<title>` וב-`manifest.json`
היה כתוב ״חמש יחידות״, וב-`DATA.APPS` שבדף הבית ״5 יח״ל״. שלושת
המקומות אומרים היום **״806 — בגרות במתמטיקה, 3–4 ו-5 יח״ל״**,
וזה גם מדויק יותר: האפליקציה מכילה אוספים בשתי הרמות, ולכל אוסף
תווית רמה משלו.

זו דוגמה לפער שבין 4 ל-5 ברשימת הבדיקה שלמטה — לא בשם עצמו, אלא
בתיאור שאחריו. **גם הוא נבדק אחרי כל שינוי שם**, ולא רק המילה
הראשונה.

## שלוש אפליקציות בלי שם מותג

ל-`math-app`, ל-`reader` ול-`theory` יש תיאור במקום שם, וזו
החלטה ולא פער: ״מתמטיקה לחכמים שמתקשים״ אומר למי האפליקציה
מיועדת, וזה בדיוק מה שההורה מחפש. שלושתן מתורגמות לפי משמעות בכל
שפה, כי אין בהן שם לתעתק. אם יוחלט למתג אותן, הכלל שלמעלה חל
עליהן כמו על השאר.

`voice` הוא כלי בדיקה פנימי — אין לו שם מותג, אין לו מניפסט רב־לשוני
והוא אינו מופיע ברשימת האפליקציות בדף הבית. זה מכוון.

## איפה שם אחד חי — רשימת בדיקה

שם אחד מופיע בחמישה מקומות, וכולם חייבים להסכים:

1. `<title>` בראש `index.html` של האפליקציה — `שם — תיאור`
2. הערת הכותרת שמתחתיו
3. `name` ו-`short_name` ב-`manifest.json` של האפליקציה (עברית בלבד;
   `name` הוא `שם — תיאור`, `short_name` הוא השם לבדו)
4. מילון השפה בתוך האפליקציה — `app:` ו-`tagline:` בכל אחת מארבע
   השפות, וכן מחרוזות ה״גרסה״ וכותרת המסך הראשי שמשלבות את השם
5. `DATA.APPS` ב-`index.html` של דף הבית — האובייקט `n` בארבע שפות

הפער הנפוץ הוא בין 4 ל-5: דף הבית נערך בנפרד מהאפליקציה ומתפצל
ממנה בשקט. אחרי כל שינוי שם, לגרוף את המאגר על השם הישן בארבע
השפות ולוודא אפס תוצאות.

## ידוע ופתוח

״תאוריה מדברת״ מופיעה ברשימת האפליקציות בדף הבית ומקשרת אל ריפו
נפרד, ולכן אין לה תיקייה במאגר הזה. השם תיאורי ומתורגם לפי
משמעות, כמו שתי האפליקציות שלמעלה.

**והנתיב עדיין נושא את השם הישן** — ראו ״המופע האחד שנשאר״
למעלה. זה ייסגר כששם הריפו הנפרד ישונה ל-`theory`, וזה טרם קרה.
