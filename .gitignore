# קבצים רגישים
.env
.env.local
.env.production

# תיקיות מערכת
node_modules/
.DS_Store
Thumbs.db

# קבצי לוג
*.log
npm-debug.log*

# תיקיות build
dist/
build/

# מפתחות וסודות — לעולם לא נכנסים ל-commit.
# הדפוסים רחבים בכוונה: קובץ שנוצר בחיפזון עם שם דומה ייתפס גם הוא.
*API_TOKEN*
*api_token*
*.token
*_TOKEN.txt
CLOUDFLARE_*
secrets.*
*.pem
*.key
