# הארכיטקטורה — מה כאן מנוע ומה כאן תוכן

המסמך הזה ממפה את הקוד הקיים לשתי שכבות: **מנוע** — מה שחוזר כמעט
זהה בכל אפליקציה, ו**תוכן** — מה ששייך לאפליקציה אחת בלבד.

כל מספר כאן נמדד בפקודה שרצה על `83ddf43`. מי שרוצה לאמת — הפקודה
כתובה ליד המספר. **אין כאן מספר שלא נמדד.**

---

## 0. נקודת החזרה

    git checkout engine-baseline-2026-09-03

תג מוער על `83ddf43`, שהוא בדיוק `origin/main`. זה המצב העובד, ואלה
האימותים שרצו עליו בסשן שיצר את התג:

| בדיקה | תוצאה |
|---|---|
| `node .claude/qa/smoke.js` ×11 עמודים | כולם `clean`, אפס שגיאות JS |
| `node .claude/qa/cache.js` | 11 `sw.js` נבדקו, 0 ממצאים |
| `node .claude/qa/exam.js` | 8 אפליקציות, כולן ✓ |
| `node .claude/qa/exam806.js` | 2 בחינות, 0 ממצאים |
| `parse.js`, `entropy.js`, `options.js` | exit 0 |

`smoke.js` ו-`exam.js` דורשים את השרת המקומי:
`node .claude/qa/serve.js &`.

---

## 1. שלוש משפחות, לא אחת

`ls */index.html` מראה תיקיות. הוא אינו מראה שהקוד שבתוכן שייך
לשלושה שלדים שונים, ומי שמעתיק בין משפחות מעתיק לתוך מבנה שלא
מקבל אותו.

| משפחה | אפליקציות | סכימת תוכן | שלד הקובץ |
|---|---|---|---|
| **חידון** | `english`, `history`, `ulpan` | `{q, a, options:[{h,ok,why}], hint}` | 12 סעיפים ממוספרים |
| **מחוללים** | `math-teen`, `math-uni`, `math-uni2`, `math-uni3` | `{ask, expr, ans, wrong:[W], steps:[S], hint}` | 19 סעיפים ממוספרים |
| **יחיד** | `math-app` | `hint1`/`hint2`/`explain` + `{a}`/`{b}`/`{ans}` | בלי מספור סעיפים |
| **מודולרי** | `bagrut-806` | `EXAM/QUESTION/SUB` ב-`data/exams.js` | **קבצים נפרדים** |

`math-app` הוא היחיד בלי מספור סעיפים — `grep -n '^   [0-9]\+\. '`
מחזיר בו שורה אחת בלבד (סעיף 18), לעומת 12 ב-`ulpan` ו-19 ב-`math-uni`.
הוא גם היחיד בלי מנגנון תרגום פעיל (ראו §5).

---

## 2. `bagrut-806` הוא כבר הארכיטקטורה שאליה רוצים להגיע

זה הממצא המרכזי של ה-audit. אין צורך להמציא מבנה למנוע — הוא כבר
קיים ועובד בייצור:

    bagrut-806/index.html   473 שורות   — שלד HTML + CSS בלבד
    bagrut-806/app.js      1066 שורות   — המנוע
    bagrut-806/speech.js    405 שורות   — מנוע ההקראה
    bagrut-806/data/exams.js 402 שורות  — התוכן, ורק הוא

*(`wc -l` על ארבעת הקבצים.)*

הטעינה היא שש תגיות `<script>` רגילות ב-`index.html:466-471`, לפי סדר:

    /legal/terms.js  →  /legal/protect.js  →  vendor/katex  →
    data/exams.js  →  speech.js  →  app.js

`app.js` סומך על `window.EXAMS` שכבר קיים. אין build, אין import, אין
fetch — ולכן זה עובד גם אופליין וגם מ-`file://`. **זה התקדים, וכל
אפליקציה חדשה צריכה להיבנות כך.**

---

## 3. כמה מהמנוע כבר משותף — במדידה

113 שמות פונקציה מופיעים ב**כל שמונה** האפליקציות של המשפחות
חידון+מחוללים+יחיד — נמדד ב-`grep -o 'function [A-Za-z_$][A-Za-z0-9_$]*'`
לכל קובץ, ואז חיתוך של שמונה הרשימות.

והמדידה החזקה ביותר בכל ה-audit, כי היא לא תלויה בשום פרסור:

    demoEl … demoMount     155 שורות     md5 = 22fce56073423d8e

**אותו md5 בדיוק בכל שמונה האפליקציות.** בלוק של 155 שורות, זהה
בייט־בייט, מ-`math-app` ועד `ulpan`.

*(סקריפט נוסף שמחלץ גופי פונקציות מצא 62 פונקציות זהות בשמונה.
המספר הזה **פחות אמין** — מונה הסוגריים שבו נשבר על ליטרלים של
ביטויים רגולריים ב-`esc` וב-`csvCell`. הוא מובא כאומדן, לא כמדידה.
מדידת ה-155 שורות וטבלת ה-`diff` שלמטה אינן תלויות בו.)*

חשוב מהמספר: **איפה** הדמיון יושב. השוואת סעיף מול סעיף בין שלוש
אפליקציות משפחת החידון, בשורות שונות (`diff | grep -c '^[<>]'`):

| סעיף | גודל | english↔ulpan | english↔history | history↔ulpan |
|---|---|---|---|---|
| 1. עזרים | 15 | **0** | **0** | **0** |
| 3. אייקונים | 29 | **0** | **0** | **0** |
| 4. מצב ושמירה | 73 | 6 | 6 | 6 |
| 5. מצבי קריאות | 22–28 | 8 | **0** | 8 |
| 6. קול | 578–617 | 85 | 22 | 83 |
| 9. מסכים | 409–449 | 38 | 168 | 174 |
| 10. אירועים | 197–212 | 31 | 86 | 99 |
| 11. הפעלה | 22 | 4 | 2 | 4 |
| 18. מבחן כיתתי | 1120–1209 | 38 | 133 | 137 |

ושלוש השורות שמפרידות בין מודול המצב של `english` למודול המצב של
`ulpan` — כל ששת ההבדלים — הן אלה:

    var BUILD="n34 · 2026-09-02";     ↔  var BUILD="a4 · 2026-09-02";
    var SKEY="niv-state-v1";          ↔  var SKEY="ulpan-state-v1";
    var GKEY_STORE="niv-gkey";        ↔  var GKEY_STORE="ulpan-gkey";

**זה כבר מנוע עם בלוק קונפיגורציה.** הוא פשוט משוכפל בהעתקה במקום
להיטען מקובץ.

---

## 4. רכיבי המנוע — מה ניתן למחזר

### 4.1 שאלות ותשובות

| רכיב | איפה | ניתן למחזר |
|---|---|---|
| `buildQ` — בניית שאלה | כל 8 | ✓ החתימה; הגוף תלוי־סכימה |
| `shuffle`, `pick`, `ri`, `uniq`, `clamp` | כל 8 | ✓ זהות |
| `withSeed`, `hash32` | כל 8, **זהות בייט־בייט** | ✓ הגרלה משוחזרת |
| `scoreAnswer` / `onAnswer` / `record` | לפי משפחה | ✓ בממשק אחיד |
| מחוללים (`gen*`) | משפחת המחוללים | ✗ **תוכן** |
| מאגר שאלות כתוב | משפחת החידון, סעיף 7 | ✗ **תוכן** |

`withSeed` ו-`hash32` זהות בשמונה. הן הבסיס למבחן הכיתתי: אותו זרע
אצל המורה ואצל התלמיד מייצר בדיוק אותן שאלות, בלי שרת.

### 4.2 TTS והקראה — הרכיב הגדול ביותר

סעיף 6 (״קול״), 578–617 שורות במשפחת החידון. שכבתי:

1. **Web Speech מקומי** — `speakDevice`, `pickVoice`/`bestVoice`,
   `voicesReady`. מטפל בארבע התקלות המתועדות ב-`bagrut-806/speech.js:1-27`:
   קולות שנטענים אסינכרונית, אמירה ארוכה שנבלעת, פיסוק בין ספרות,
   וכרום שעוצר אחרי ~15 שניות.
2. **Gemini TTS ענני, אופציונלי** — `gSynth`, `pcmToWav`.
   `grep -rn 'generativelanguage.googleapis.com'` מוצא אותו ב-9 קבצים.
   **המפתח לעולם אינו בריפו** — הוא נכנס בהגדרות ונשמר ב-localStorage,
   ויש שרשרת נפילה־לאחור בין אפליקציות (`math-uni2/index.html:1015-1018`:
   `axiom2-gkey` → `shlav-gkey` → `reader-gkey`).
3. **הדגשת המילה הנאמרת** — `hlPrep`, `hlAt`, `hlWhole`, `hlClear`.
   זהות ב-7 מתוך 8.
4. **`segments` / `sayseg`** — חיתוך טקסט למקטעים קצרים.

✓ **כל השכבה ניתנת למחזור כמו שהיא.** היא הרכיב הבשל ביותר בפרויקט.

### 4.3 ניקוד והגייה

זה **אינו** רכיב מנוע היום — הוא מפוזר:

| יכולת | איפה | מצב |
|---|---|---|
| ניקוד אוטומטי (Dicta API) | `reader` בלבד — `reader/index.html:696` | ייחודי לאפליקציה אחת |
| טקסט מנוקד כתוב מראש | `ulpan` (21 התאמות ל-`ניקוד`) | תוכן |
| חוקי הגייה | `drivewise/data/speech-rules.json` | **ריפו אחר** |
| מילון הגייה | `english`, `ulpan`, `bagrut-806/speech.js` | 2 התאמות בכל אחד |

⚠️ **הפער הגדול ביותר במנוע.** אין כאן היום רכיב אחד; יש ארבעה
מימושים חלקיים בארבעה מקומות. הקובץ `speech-rules.json` בריפו התאוריה
הוא היחיד שנראה כמו מנגנון כללי.

### 4.4 רמזים

שלוש סכימות שאינן מתורגמות זו לזו:

| משפחה | שדות |
|---|---|
| `math-app` | `hint1`, `hint2`, `explain` + מצייני מיקום, סיומת לכל פעולה |
| מחוללים | `hint` + `steps:[S(כותרת, הסבר, נוסחה)]` + `wrong:[W(טקסט, למה)]` |
| חידון | `hint` + `options:[{h, ok, why}]` |
| `bagrut-806` | `steps:[{hint, detail}]` — רמז שאינו פותר, ואחריו השלב המלא |

✓ **מנגנון החשיפה** (רמז אחד־אחד, לא הכול בבת אחת) ניתן למחזור.
✗ **תוכן הרמזים** הוא תוכן.

הניסוח של `bagrut-806` — ״רמז שאינו פותר, ואחריו השלב המלא״ — הוא
המודל הכללי ביותר, וכדאי שהמנוע יאמץ אותו.

### 4.5 מעקב התקדמות

| רכיב | חידון | מחוללים | math-app |
|---|---|---|---|
| `mastery` / `masteryBar` | ✓ | ✓ | ✗ |
| `touchDay` / `dayRec` — רצף יומי | ✓ | ✓ | ✗ |
| `streak` | ✓ | ✓ | ✓ |
| XP / מדבקות / חיית מחמד | ✗ | ✗ | ✓ בלבד |
| חולשות (`weak*`) | `history` | ✓ | ✓ |

*(מ-`grep -c` על כל מחרוזת בכל קובץ.)*

✓ `mastery`, `touchDay`, `dayRec` — ניתנים למחזור.
✗ המדבקות, החיה והקונפטי של `math-app` — **תוכן וזהות**, לא מנוע.
משפחת המחוללים אף מסמנת זאת במפורש: סעיף 14 שם נקרא
״משפטי ליווי — קצרים, בוגרים, **בלי קונפטי**״.

### 4.6 התאמה ללומד מתקשה

`applyModes` — מודול קצר (22–28 שורות) שמחליף מחלקות על `<html>`:

| דגל | מה הוא עושה |
|---|---|
| `--fs` | 17px ↔ 20px |
| `clear` / `clearfont` | פונט קריא לדיסלקציה |
| `spaced` | ריווח אותיות ושורות |
| `hc` | ניגודיות גבוהה |
| `rm` | `prefers-reduced-motion` |
| `data-theme` | כהה / בהיר / אוטומטי |

⚠️ **דריפט אמיתי:** `grep -o 'classList.toggle("[a-z]*"'` מראה
`clearfont` ב-`math-teen`/`math-uni`, אבל `clear` ב-`english`/
`history`/`ulpan`. ל-`math-app` יש רק `hit` ו-`spaced` — **אין לו
כלל ניגודיות, פונט קריא, תנועה מופחתת או ערכת צבע**, והוא דווקא
האפליקציה של הקהל הצעיר ביותר.

### 4.7 תמיכה בשפות — ארבעה מנגנונים שאינם מדברים זה עם זה

| מנגנון | אפליקציות | המפתח | שפות |
|---|---|---|---|
| `T` + `t("key")` | `english`, `history`, `ulpan` | מזהה אנגלי | 4 |
| `TR` + `_("מחרוזת עברית")` | `math-uni`, `math-uni2`, `math-uni3` | **המחרוזת העברית** | 4 |
| `TR_AR` + `trHTML` | `math-teen` | **המחרוזת העברית** | 2 (he, ar) |
| `STR` + `LANGS[].label/.flag` | `math-app` | מזהה | 4, **לא מחוברות** |

*(מ-`grep -c` על `TR_AR`, `TR_KEYS`, `_(`, `trHTML`, `function t(`.)*

גם צורת `LANGS` שונה: `math-app` משתמש ב-`label`+`flag`, כל השאר
ב-`name`. מי שמעתיק בין משפחות שובר את בורר השפה בשקט.

⚠️ **מלכודת המיזוג.** ב-`math-teen` המילון הוא **שורה אחת של 219,430
תווים** (`awk '{print length}' | sort -rn | head -1`). כל שינוי בה
מתנגש במלואו, ו-`git` יציע לבחור צד — בחירה מוחקת מאות תרגומים.
משפחת `math-uni` פתרה זאת נכון: `TR_KEYS` הוא מערך שורה־למפתח
(`math-uni/index.html:6793` ואילך), והשורה הארוכה ביותר שם היא
10,022 תווים. **הפורמט של `math-uni` הוא הנכון למנוע.**

### 4.8 UI/UX לימודי

| רכיב | היקף | מחזור |
|---|---|---|
| `render` + `afterRender` | כל 8 | ✓ תבנית |
| מסכים (`view*` / `screen*`) | 409–449 שורות | ✓ שלד, ✗ תוכן |
| `announce` — `aria-live` | כל 8 | ✓ |
| אייקונים SVG בקוד | 29 שורות, **זהות בייט־בייט** | ✓ |
| `ic()` — עטיפת אייקון | כל 8 | ✓ |
| אונבורדינג / הדגמה (`demo*`) | 13 פונקציות, **כולן זהות ב-8** | ✓ |
| שער תנאי שימוש (`te*`) | 18 פונקציות | ✓ |

בלוק ה-`demo*` — `demoEl`, `demoShow`, `demoTap`, `demoClick`,
`demoType`, `demoQuiet`, `demoMountUI`, `demoKey`, `demoStart`,
`demoNext`, `demoStop` — הוא **155 שורות עם אותו `md5` בדיוק בכל
שמונה האפליקציות** (`22fce56073423d8e`). הוא מודול מוכן, בלי עבודה.
`demoMount` שאחריו הוא נקודת החיבור לאפליקציה.

### 4.9 מבחן כיתתי — המודול הגדול והנייד ביותר

סעיף 18, 1120–1209 שורות. 30 פונקציות `ex*`/`exam*`/`sit*`, ורובן
זהות בייט־בייט בשמונה: `examId`, `encodeExam`, `decodeExam`,
`examQuestions`, `exNewSeed`, `exSlot`, `exBuildSlot`, `examUrl`,
`waLink`, `exLoad`, `exSave`, `encodeResult`, `decodeResult`,
`gradeResult`, `sitNext`, `sitGo`, `sitGrade` ועוד.

הוא מייצר קישור וואטסאפ, מקבל תשובות ומחשב ציונים — **בלי שרת
ובלי חשבון.** `node .claude/qa/exam.js` מאשר שהוא עובד זהה בשמונה.

✓ **הרכיב הכי מוכן להוצאה כמודול.**

### 4.10 Service worker ו-PWA

`diff math-app/sw.js <כל אחד מהעשרה>` מחזיר **שני הבדלים בלבד**,
שניהם אותה מחרוזת:

    const CACHE = "math-app-" + V;                    שורה 13
    keys.filter(k => k.startsWith("math-app-") ...)   שורה 25

עשרה `sw.js` — `math-app`, `english`, `history`, `math-teen`,
`math-uni`, `math-uni2`, `math-uni3`, `ulpan`, `reader`, והשורש —
זהים לחלוטין חוץ מהמחרוזת הזאת. `bagrut-806/sw.js` שונה (81 שורות
במקום 61) כי הוא מצרף גם את KaTeX.

✓ **תבנית עם פרמטר אחד.**

---

## 5. מה ספציפי לאפליקציית התאוריה

**אפליקציית התאוריה — ״תאוריה מדברת״ — אינה בריפו הזה.** היא ריפו
נפרד, `lagstein1-png/drivewise`, ומתפרסמת ב-`lagstein1-png.github.io/drivewise/`.
ה-audit נעשה על עותק קריאה שנשלף בסשן הזה.

*(השם ״DriveWise״ אסור כשם מותג — ראו `NAMING.md`. המילה `drivewise`
ב-`id`, ב-`ICONS` וב-`/drivewise/` היא **נתיב**, לא שם.)*

### 5.1 היא כמעט אינה חולקת מנוע עם הריפו הזה

מתוך 140 שמות פונקציה ב-`drivewise/index.html`, החיתוך עם 113
פונקציות המנוע האוניברסליות הוא **שש**:

    load  render  save  shuffle  speak  voicesReady

ואלה שמות גנריים, לא מימוש משותף. **זו לא אותה בסיס־קוד.** מי
שמתכנן ״מנוע אחד לכולם״ חייב לדעת שהתאוריה תצטרך פורט מלא, לא
התאמה.

### 5.2 מה בה ששייך למנוע ולא קיים כאן

| רכיב | למה הוא חשוב |
|---|---|
| **תוכן ב-JSON חיצוני** — `data/questions.{he,ar,en,ru}.json` | 4 שפות, קבצים נפרדים. הריפו הזה מטמיע הכול ב-`index.html` |
| **רמזים בקובץ נפרד** — `data/hints.{he,ar,en,ru}.json` | הפרדת רמזים מתוכן |
| **הקראה מוקלטת מראש** — 6,823 קובצי mp3, 137MB (`find audio -type f \| wc -l`, `du -sh audio`) | לא תלוי במנוע TTS של המכשיר כלל |
| **`data/speech-rules.json`** | מנגנון ההגייה הכללי היחיד בכל הפורטפוליו |
| **מפתח קאש נגזר** — `sw.js?v=' + BUILD` (`index.html:1378`) | **מקור אמת אחד.** בריפו הזה המחרוזת מקודדת קשיח פעמיים |

**`sw.js?v=' + BUILD` הוא התיקון לבאג שמתועד ב-`CLAUDE.md`.**
התאוריה כבר פתרה את מלכודת שתי המחרוזות; הריפו הזה עדיין לא.

### 5.3 מה בה תוכן ולא מנוע

מאגר שאלות התאוריה, האייקונים של תמרורים (`signSVG`, `spotSVG`),
`catLabel`/`bestCategory`, ומשפטי העידוד (`getPraiseHe`, `milestoneFor`,
`rewardFor`, `challengeLine`, `shareLine`).

### 5.4 שני ממצאים שדורשים החלטה

**🔴 קריטי — טלמטריה מול הבטחת הפרטיות בדף הבית.**
`drivewise/index.html:1252` מגדיר `METRICS_URL` שמצביע ל-Google Apps
Script, ו-`Metrics.post` שולח בכל פתיחה ובכל סגירת מפגש: מזהה אקראי
קבוע (`dw-aid`), סוג מכשיר, האם מותקן כאפליקציה, שפה, גרסה, אורך
המפגש בשניות ומספר התשובות.

אין שם, אין אימייל ואין תוכן תשובות — אבל דף הבית של הריפו הזה
מבטיח, בארבע שפות: *״ההתקדמות נשמרת במכשיר שלכם בלבד ולא נשלחת
לשום מקום״* (`index.html`, מפתח `how3`). ההתקדמות עצמה באמת אינה
נשלחת; מונה התשובות כן. **הניסוח והקוד אינם תואמים, ואחד מהם צריך
להשתנות.** הכיבוי הוא מחרוזת ריקה ב-`METRICS_URL`, והאפליקציה עובדת
בדיוק אותו דבר.

**🟡 חשוב — קוד מת שקורא ל-API בתשלום.**
`drivewise/netlify/functions/explain.js` קורא ל-`api.anthropic.com`.
הוא **אינו** מכיל מפתח (רק `process.env.ANTHROPIC_API_KEY`), והוא
מכובה — `EXPLAIN = { url:'/.netlify/functions/explain', enabled:false }`
ב-`index.html:2979`. וגם: לפי `CLAUDE.md`, **Netlify לא קיים יותר**,
ולכן הנתיב מת ממילא. אין דליפה ואין חיוב, אבל זה קוד שסותר את הכלל
״לא שירות בתשלום״ ויושב בריפו ציבורי. כדאי למחוק אותו.

---

## 6. מה **אינו** מנוע — ואסור לגעת בו בשכפול

| דבר | למה |
|---|---|
| מאגרי שאלות ומחוללים | תוכן, ולפי `CLAUDE.md` **אסור למחוק** |
| הרמזים, ההסברים והתרגומים | אותו כלל |
| חיית המחמד, המדבקות והקונפטי של `math-app` | זהות של אפליקציה אחת |
| `reader` — Dicta, ניקוד, קריאת טקסט חופשי | כלי, לא אפליקציית לימוד |
| `voice/` | דף אבחון. `<title>בדיקת קול</title>`, בלי sw, בלי מטמון |
| `legal/` | עמוד תנאים משותף — ראו §7 |
| `img/`, `marketing/` | נכסים, לא דפים |

**המניין:** הפורטפוליו הוא **אחת־עשרה** אפליקציות, ו-`DATA.APPS`
ב-`index.html` הוא מקור האמת — 11 רשומות בדיוק, נספרו ב-
`JSON.parse` על הבלוק:

    math-app  math-teen  math-uni  math-uni2  math-uni3  bagrut-806
    ulpan  english  history  reader  drivewise

`ls */index.html` מחזיר **12**, ואין כאן סתירה: עשר מתוך האחת־עשרה
הן תיקיות כאן (`drivewise` בריפו אחר), ומצטרפות אליהן `legal/`
ו-`voice/` שאינן אפליקציות לימוד. 10 + 2 = 12.

המספר כתוב כמילה בארבע מחרוזות `badge` — ״אחת־עשרה״, ״أحد عشر״,
״Одиннадцать״, ״Eleven״ — ו**אינו נגזר מ-`DATA.APPS`**. הוספת
אפליקציה מחייבת לגעת בארבעתן ביד.

---

## 7. שלוש מלכודות שחייבות להיכנס לכל שכפול

### 7.1 גרסה = שני מקומות

    var BUILD="b40 · 2026-09-02";                          מה שהמשתמש רואה
    navigator.serviceWorker.register("sw.js?v=b40-pwa1");  מה שקובע את הקאש

`sw.js` גוזר את מפתח הקאש מ-`?v=`. **מחרוזת הרישום מקודדת קשיח ואינה
נגזרת מ-`BUILD`.** עדכון של אחד בלי השני = המשתמש לא יראה את התיקון
לעולם. `node .claude/qa/cache.js` תופס את זה.

לדף הבית ול-`reader` אין `BUILD` כלל (`13-pwa1`, `19-pwa1`) — אין
להם פוטר שמציג גרסה. זה תקין.

### 7.2 מטמון `legal/`

אחד־עשר קובצי `sw.js` מצרפים מראש את **אותם** `/legal/terms.js`
ו-`/legal/protect.js`, כל אחד למטמון בשם משלו. ההגשה היא
`caches.open(CACHE)` ואז `c.match(req)` — **לא** `caches.match` הגלובלי.
לכן עותק ישן במטמון של אפליקציה אחרת אינו עונה כאן, אבל העותק של
כל אפליקציה משתחרר רק כשהגרסה שלה עולה.

**נגעת ב-`legal/` — העלה את כל אחד־עשר המפתחות יחד.**

### 7.3 מספר הגרסה מוקצה במיזוג, לא בבנייה

כל סשן מתפצל מ-`main`, רואה `b40`, ומסמן `b41`. שני סשנים יעשו זאת
באותו יום, ושתי גרסאות שונות עם אותו מפתח = מי שקיבל אחת לא יקבל
את השנייה לעולם. **בונים ומשאירים; מי שממזג מקצה מספר טרי.**

---

## 8. הדרך קדימה — סדר, לא היקף

הסדר נגזר ממה שנמדד למעלה: מתחילים במה שכבר זהה, לא במה שהכי רצוי.

1. **`sw.js`** — שני הבדלים בלבד. תבנית + פרמטר.
2. **בלוק `demo*`** — 13 פונקציות זהות בייט־בייט. הוצאה בלי סיכון.
3. **שער התנאים (`te*`) והאייקונים** — זהים או כמעט.
4. **מבחן כיתתי (סעיף 18)** — הגדול והנייד ביותר, ויש לו בדיקה
   (`exam.js`) שתתפוס רגרסיה.
5. **שכבת הקול (סעיף 6)** — 86%–96% זהה. תאחד גם את פער ההגייה (§4.3).
6. **`applyModes`** — ותוך כדי, לסגור את הדריפט `clear`/`clearfont`
   ואת מה שחסר ל-`math-app`.
7. **תרגום** — הכי מסוכן, אחרון. הפורמט של `math-uni` (שורה למפתח)
   הוא היעד; `math-teen` (שורה אחת של 219,430 תווים) הוא מה שצריך
   לפרק.

**המבנה הסופי הוא זה של `bagrut-806`, לא משהו חדש:**

    <app>/index.html      שלד + CSS + בלוק קונפיגורציה
    <app>/data/*.js       תוכן, ורק תוכן
    <app>/sw.js           תבנית עם שם מטמון אחד
    /engine/*.js          מה שמשותף

**כלל אחד מעל הכול:** תיקון רוחבי שנשאר בענף יחזור וייפול, כי
אפליקציה חדשה נולדת מהעתקה של קובץ ב-`main`. **גמרת רוחבי — מזג
מיד.**
