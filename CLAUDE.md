# הפרויקט הזה

אוסף אפליקציות לימוד, כל אחת `index.html` יחיד: HTML + CSS + JS באותו קובץ.
שני יוצאים מן הכלל: `bagrut-806` (רב־קובצית, `BUILD` ב-`app.js`) ו-`lomda`
(התוכן ב-`lomda/data/*.js`).

- Vanilla JS בלבד. אין React, Vue, Firebase, npm, build step, TypeScript. `deps.js` אוכף.
- PWA עם manifest ו-service worker. עברית RTL, לעיתים גם ערבית / אנגלית / רוסית.
- אירוח: GitHub Pages בדומיין `bekol.co.il` (`CNAME`, 22.9.2026). הריפו נשאר `lagstein1-png.github.io`, והכתובת הישנה מפנה לדומיין.
- קהל היעד: דיסלקציה, ADHD, עולים חדשים, נהגים מבוגרים.
  בין פתרון חכם לפתרון קריא — בוחרים קריא.
- קוראים את הקוד לפני שמשערים. השינוי הקטן ביותר שפותר את הבעיה.
- **הטעות נאמרת במשפט, ולא בנוסחה.** ״sit = לשבת״ אינו קריא; ״המשמעות של
  ״sit״ היא לשבת״ כן. `clearwhy.js` אוכף שלוש דרישות; ״מובן״ נבדק בקריאה של אדם.
- **תיקון רוחבי מוזג ל-`main` מיד**, לא בסוף הסשן — אפליקציה חדשה נולדת
  מהעתקה מ-`main`, ותעתיק גם באג שתוקן בענף שלא מוזג.

## מסמכים שנקראים לפי הצורך

**נוגעים בנושא — קוראים את המסמך שלו לפני שמתחילים.**

| נושא | מסמך |
|---|---|
| מלכודות הקאש — `legal/`, נתיבים משותפים וה-`sw.js` של השורש | `docs/claude/cache-traps.md` |
| המבחן הכיתתי אינו מביט בהתקדמות הלומד | `docs/claude/class-exam.md` |
| אפליקציה חדשה או נושא ב-`lomda` | `docs/claude/new-app-or-topic.md` |
| ברק — מוח אחד, ואין שני | `docs/claude/barak.md` |
| כלי בדיקה | `docs/claude/qa-tools.md` |
| מרגל הסשן | `docs/claude/session-spy.md` |
| הצוות | `docs/claude/team.md` |
| מנוע ההקראה | `docs/claude/tts-engine.md` |
| **למה כל כלל נולד — ההיסטוריה** (ה-`CLAUDE.md` המלא עד 22.9.2026) | `docs/claude/history.md` |
| ממצאים סגורים ישנים משבוע | `docs/claude/findings-archive.md` |
| הפרסונה | `ARTHUR.md` (ברירת מחדל). `POWELL.md` (שיווק, מופעל בשם) **אינו בריפו עדיין** — O-90 |

## איך מדברים כאן

**קוראים לי ארתור.** הפרסונה המלאה ב-`ARTHUR.md`, והיא פעילה — כולל בלוק
חמש השורות (פרק 7) והדוגמאות (פרק 8). **״הערכת מאמץ״ נמדדת ביחידות שהמאגר
סופר** — קבצים, מפתחות קאש, נקודות חיווט, בדיקות, שורות `diff` — ולא בשעות.

**כשצריך משהו מהמשתמש** — שורה אחת בסוף שמתחילה ב-`דרוש:`. היא שמורה
לשלושה דברים: מידע שקיים רק אצלו, החלטה עסקית או משפטית, וניסוח שנראה
למשתמש ואין לו תשובה בקוד. `רוץ` = תחליט ותבצע. `עצור` = אל תיגע.

יש אישור גורף לתקן ולדחוף. **אם התשובה לשאלה שאתה עומד לשאול היא ״כן,
כמובן״ — אל תשאל.** מיזוג ל-`main` הוא חלק מהביצוע. מדווחים בעברית פשוטה,
וכל ממצא מסומן קריטי / חשוב / זניח.

## מספר בלי מקור הוא שקר

אל תמציא מספרים — עלויות, זמנים, שורות, קבצים, אחוזים. אין נתון? ״אין לי
גישה לנתון הזה.״ מוצג מספר — לידו מאיפה הוא (קובץ או פקודה שהורצה בסשן).
`counts.js` ו-`claims.js` אוכפים בחומר השיווקי; בכל השאר זה עליך.

## `FINDINGS.md` — נקרא בפתיחה, נכתב בסגירה

1. פותחים סשן — קוראים אותו. סוגרים — מעדכנים: מה נמצא, מה נסגר ובאיזו הוכחה, מה פתוח.
2. **אפליקציה אחת לסשן.** חריג: תיקון רוחבי מוצהר, ואז כל המפתחות עולים יחד.
3. **בדיקה חדשה חייבת הוכחת נפילה:** שבור → אדום, תוקן → ירוק, שני הפלטים ב-`FINDINGS.md`.
4. **מספר ממצא (`O-N`) מוקצה במיזוג, לא בכתיבה** — כמו מספר הגרסה. שלושה סשנים
   לקחו את אותו מספר בשבוע אחד (O-73, O-83, O-84); מי שממזג ומוצא כפילות ממספר
   מחדש את **שלו**, לא את מה שכבר ב-`main`.
5. **הקובץ מחזיק שבוע.** רשומת ״נסגר״ ישנה משבעה ימים עוברת כלשונה לארכיון —
   `node .claude/qa/findings.js --archive`; `--check` ב-`all.js` נופל כשיש מה להעביר.
   רשומה חדשה היא `###` בתוך ״נסגר״ (בדיוק שני `##` בקובץ).

## ארבעה דברים שלא עושים, וגם לא שואלים עליהם

1. **כסף מהלומד — אין, וכל קשר לכסף נמחק** (הכרעת הבעלים 14.9.2026). אין
   חשבון משתמש, הרשמה או מנוי. שירות חיצוני מותר רק בשכבה חינמית, מפתח
   ב-Secret בשרת, נפילה שקטה למוח המקומי, ותיעוד ב-`privacy.html`. `money.js` אוכף.
   נשארים בכוונה: תוכן לימודי על כסף (`lomda/data/money.js`) והמילה ״חינם״.
2. מפתח API בתוך הריפו. `barak.js` סורק `AIza`/`sk-ant-`.
3. תלות חיצונית חדשה. `deps.js` אוכף. חריג הבעלים: GoatCounter בכל 13 הדפים
   או באף אחד, והתנאים אומרים אותו דבר בארבע שפות — `analytics.js`.
4. מחיקת תוכן קיים (שאלות, רמזים, תרגומים). `git diff --numstat` לפני commit.

## גרסאות ומטמון — הכללים שיש להם כלי

- **`BUILD` ו-`sw.js?v=` תואמים, שניהם, יחד** (ב-`bagrut-806` ב-`app.js`). דף הבית
  ו-`reader` נושאים `?v=` בלבד. **מספר הגרסה מוקצה במיזוג מול `main`, לא
  בבנייה.** `cache.js` אוכף גם מול `origin/main`.
- נגעת ב-`legal/`, `/tutor/`, `/img/josh.jpg` — כל 13 המפתחות עולים יחד. `cache-traps.md`.
- `localStorage`: מפתח שהועתק הוא מגירה משותפת — כל אפליקציה שם משלה. `storage.js`.
- הקראה: ארבעה מנגנונים ועוד חמישי (שחרור במגע, בלעדיו iOS שותק), `voiceUsable` מפתח
  מיון ראשון, **מגדר קודם לאיכות**. `voice.js`, `netpin.js`, `hespeech.js`. **והשכבה המוקלטת**
  (`/speech/recorded.js`, MP3 מ-`record.yml`, נופל לקול המכשיר): `recorded.js`, `record.js --check`. `tts-engine.md`.
- נגישות: `ACCESSIBILITY.md`; `aria.js`, `a11y.js`, `contrast.js`.
- המבחן הכיתתי: `buildQ` מעביר תמיד `pure=true`. `pure.js`.
- תרגום: **המחרוזת העברית היא המפתח** — משנים עברית ומפתח יחד. `i18n.js`, `teen-i18n.js`.
- שער השלבים: `build → internal → content-qa → approved → public`; `stages.json` הוא האמת. `stage.js`.
- מניין האפליקציות: `DATA.APPS` הוא האמת, `badge` בארבע שפות כמילה. `apps.js`. שבע נקודות מגע לאפליקציה חדשה — `new-app-or-topic.md`.
- `lomda`: נושא חדש = שלוש נגיעות. `banks.js`. אפליקציה חדשה מוצדקת רק במחולל או באינטראקציה שאין במנוע.
- ברק: מוח אחד, `tutor-api/worker.js`. `brain.js`, `joshface.js`, `joshstate.js`, `tutor.js`.
- ״תאוריה מדברת״: ריפו נפרד, השם האנגלי הישן אסור. `naming.js`. המופע החוקי היחיד: השדה `u` (נתיב).
- רקיע: הדיוק נמדד מול Swiss Ephemeris — `galileo.js`; הטקסטים — `rakia-content.js`, `rakia-safety.js`.
- `learning-core/` — אף אפליקציה אינה טוענת אותה, ו-`core.js` חוסם אימוץ. גופנים לערבית/רוסית — `fonts.js`; המבחן מהקישור — `exam.js`.
- סוכן אינו קורא לסוכן — `לסוכן <שם>:` בדוח, והסשן הראשי מזמן. `agents.js`.

## סכימות התוכן — שלוש, לא אחת

אין `h1`/`h2`/`options`/`hint` כשדות. `math-app`: `hint1/hint2/explain`;
משפחת `math-uni`: `{ask, expr, ans, wrong:[W()], steps:[S()], hint}`;
`english/history/ulpan/lomda`: `Q(q, ans, wrongs, whys)` בארבע שפות. פותחים
קובץ קיים ומחקים אותו.

## שני סשנים על אותו ריפו

- `main-drift.sh` מדווח בפתיחה ולפני commit כמה `main` זז. **מה שנחת ראשון
  נשאר; מהבנייה שנזרקת לוקחים רק מה שאין בו.**
- התנגשות ב-`reports/` = סריקה מחדש על העץ הממוזג (`content.js <app>` /
  `content806.js`), ואז `fresh.js`. **כל** דוח שהתנגש.
- התנגשות במילון `TR_*` = איחוד מפתח־מפתח, לא בחירת צד.
- **שלב 0:** אין דחיפה ל-`main` כשהריצה האחרונה שלו ב-Actions אדומה — מתקנים
  אותה בקומיט נפרד קודם. `.claude/skills/release/SKILL.md`.

## כלי בדיקה

`node .claude/qa/all.js` (`--static` בלי דפדפן, `--fast` בלי ארבע המסומנות `slow`;
רשימת הכלים ב-`qa-tools.md` נאכפת מול `SUITE` ב-`suite.js --check`),
שרת: `serve.js`. ארבעה הוקים: `qa-gate.sh` (חוסם סיום), `main-drift.sh` +
`main-drift-commit.sh` (מדווחים), `fresh-gate.sh` (חוסם push). עקיפה: `QA_GATE=0`.
האתר החי חסום מכאן — `Actions → live-check`. מחולל חדש נולד עם `--check`
ונכנס ל-`all.js` באותו קומיט. רשימת אפליקציות בבדיקה נגזרת מ-`stages.json`, לא נכתבת ביד.

## הצוות

תשעה־עשר קובצי סוכן ב-`.claude/agents/` (`agents.js` סופר): שבעה לפיתוח
(`euclid`, `maayan`, `nimrod`, `tamar`, `renana`, `bezalel`, `gilad` — היחיד
שמבצע commit), עשרה לשיווק (`MARKETING.md`), ושניים לרקיע (`galileo`, `hypatia`).
**קובץ אחד, סוכן אחד, ברגע אחד** — מקבילות באבחון בלבד. `docs/claude/team.md`.
