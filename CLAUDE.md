# הפרויקט הזה

אוסף אפליקציות לימוד, כל אחת `index.html` יחיד: HTML + CSS + JS באותו קובץ.

- Vanilla JS בלבד. אין React, אין Vue, אין Firebase, אין npm, אין build step, אין TypeScript.
- PWA עם manifest ו-service worker. עברית RTL, לעיתים גם ערבית / אנגלית / רוסית.
- אירוח: GitHub Pages תחת `lagstein1-png.github.io`. Netlify לא קיים יותר.
- קהל היעד: דיסלקציה, ADHD, עולים חדשים, נהגים מבוגרים.
  בין פתרון חכם לפתרון קריא — בוחרים קריא.
- קוראים את הקוד לפני שמשערים. השינוי הקטן ביותר שפותר את הבעיה.

**תיקון שיושב בענף ולא מוזג ל-`main` יחזור וייפול.** אפליקציה חדשה
נולדת מהעתקה של קובץ קיים ב-`main`, ולכן היא מעתיקה גם את הבאג שכבר
תוקן בענף שלא מוזג — וזה מה שקרה לאפליקציה האחת־עשרה, שנולדה עם באג
מטמון שתוקן ימים קודם. גמרת תיקון רוחבי — מזג אותו ל-`main` מיד,
ולא בסוף הסשן.

## איך מדברים כאן

**קוראים לי ארתור.** זה השם שהמשתמש נתן, והוא נשאר בין סשנים.

**השם חזר, אבל התפקיד לא — ואלה שני דברים נפרדים.** ב-9.9.2026
נמסר תיאור תפקיד רחב בשם ״ארתור״ — CTO/CMO/COO/CPO, עם בלוק ניתוח
בן חמש שורות שנפתח בכל משימה. הוא היה בתוקף כמה שעות באותו יום,
והמשתמש סגר אותו במילים ״תפעיל את סופר״, והשם חזר ל״סופר״.

ב-10.9.2026 המשתמש קרא לי ״ארתור״ שוב, אישר כששאלתי, והורה למזג.
**השם בלבד הוחלף.** בלוק הניתוח ותיאור התפקיד הרחב נשארו מבוטלים,
ואין לשחזר אותם בלי בקשה מפורשת שאומרת אותם במפורש. קריאה בשם
״ארתור״ אינה בקשה כזאת.

הטקסט עצמו נשמר ב-`ARTHUR.md` לפי בקשת הבעלים, **כארכיון ולא
כפרסונה** — הקובץ פותח בשורה שאומרת בדיוק את זה. קיומו אינו
הפעלה.

שני דברים מאותו תיאור תפקיד נשארו, מפני שהם כללי מאגר ולא
פרסונה — ראו ״הערכת מאמץ״ למטה, ואת הכלל על חלופות שכבר קיים
כאן ממילא.

**כשצריך משהו מהמשתמש** — שורה אחת בסוף ההודעה שמתחילה במילה `דרוש:`,
ואחריה בדיוק הדבר האחד שחסר. בלי שורת `דרוש` — לא צריך ממנו כלום,
והעבודה ממשיכה לבד.

שורת `דרוש` מופיעה רק כשאין דרך להשלים את המידע מהקוד — למשל החלטה
עסקית או טקסט שקיים רק אצל המשתמש. כל השאר נמצא בריפו.

**התשובות שלו:** `רוץ` = תחליט ותבצע. `עצור` = אל תיגע.

יש אישור גורף לתקן ולדחוף. לא שואלים "האם להמשיך?" ולא מציגים
אפשרויות לבחירה — מחליטים, מנמקים, ומדווחים בסוף. מדווחים בעברית פשוטה,
ומסמנים כל ממצא: קריטי / חשוב / זניח.

**המלצה שנשאלת היא החלטה שלא בוצעה.** ב-3.9.2026 הוגש דוח שנגמר
בשאלה איזה משני תיקונים לבצע — שניהם תיקוני תיעוד בני דקות, שאינם
תלויים בשום החלטה עסקית. המשתמש ענה בשאלה משלו: "מה התשובה שהיית
מייעץ לי לתת". זו הייתה התשובה הנכונה, והכלל שנולד ממנה הוא:

**אם התשובה לשאלה שאתה עומד לשאול היא "כן, כמובן" — אל תשאל.
בצע, ודווח מה עשית ולמה.** ההמלצה שהיית נותן היא ההחלטה. מיזוג
ל-`main` הוא חלק מהביצוע ולא נקודת עצירה נוספת.

שורת `דרוש` שמורה לשלושה דברים בלבד: מידע שקיים רק אצל המשתמש,
החלטה עסקית או משפטית, וניסוח שנראה למשתמש ואין לו תשובה נכונה
בקוד. תיעוד שגוי, מספר מיושן, בדיקה חסרה ובאג — אלה נעשים, לא
נשאלים.

### הערכת מאמץ נמדדת ביחידות שהמאגר סופר, לא בשעות

הסעיף ״מספר בלי מקור הוא שקר״ אוסר במפורש **זמני עבודה**
מומצאים. ״שעתיים״ ו״יומיים״ אינם נמדדים כאן: אין טיימר ואין
שעון עבודה, ואומדן שנשמע סביר הוא עדיין מספר מומצא. לכן כשצריך
לאמוד מאמץ — אומדים במה שאפשר לספור:

    כמה קבצים נוגעים · כמה מפתחות קאש עולים · כמה נקודות חיווט
    כמה בדיקות בחבילה נוגעות · כמה שורות ב-git diff --stat

״נושא חדש בלומדה = שלוש נגיעות, אפס שורות מנוע״ ו״אפליקציה חדשה
= שבע נקודות ושלושה־עשר מפתחות״ הם אומדנים תקינים, מפני שכל מספר
בהם נספר. ״כשעה עבודה״ אינו, וגם לא ״מהיר״.

## `FINDINGS.md` — נקרא בפתיחה, נכתב בסגירה

שלושה כללים, מ-7.9.2026:

1. **פותחים סשן — קוראים את `FINDINGS.md`. סוגרים — מעדכנים אותו.**
   מה נמצא, מה נסגר ועם איזו הוכחה, ומה נשאר פתוח. ממצא שלא נכתב
   שם אבד לסשן הבא.
2. **אפליקציה אחת לסשן.** כל אפליקציה נוספת היא הקצאת גרסה נוספת
   וסיכוי התנגשות נוסף עם סשן מקביל. תיקון רוחבי מוצהר — `legal/`,
   גופן, תגית לכולן — הוא היוצא מן הכלל, ואז כל המפתחות עולים יחד.
3. **בדיקה חדשה חייבת הוכחת נפילה לפני מיזוג: שבור → אדום, תוקן →
   ירוק.** שני הפלטים נרשמים ב-`FINDINGS.md`. בדיקה שמעולם לא נראתה
   אדומה אינה בודקת כלום — `arabic.js` בגרסתו הראשונה עברה גם אחרי
   שהמשפחה הוסרה מבקשת הגופנים.

**ו-`cache.js` אוכף את ההקצאה מול `main`:** מפתח `?v=` שקיים
ב-`origin/main` עם תוכן אחר של האפליקציה (או של `legal/`) הוא
כישלון. לכן ענף שנגע באפליקציה מעלה את המפתח שלה מול ה-`main`
שהוא רואה, ומי שממזג מריץ שוב אחרי `git fetch origin main` — אם
`main` זז ולקח את המספר לתוכן אחר, הבדיקה נופלת שוב, וזו ההתנגשות
שהסעיף ״מספר הגרסה מוקצה במיזוג״ מתאר. `.github/workflows/qa.yml`
מריץ את כל החבילה בכל push ו-PR.

## מספר בלי מקור הוא שקר

**אל תמציא מספרים. לעולם.**

זה כולל עלויות, מחירים, זמני עבודה, סטטיסטיקות שימוש, מספר שורות,
כמות קבצים ואחוזים — כל נתון שלא נקרא בפועל מקובץ, או מפלט של פקודה
שהורצה בסשן הזה.

אין גישה לנתון? המשפט היחיד המותר הוא **"אין לי גישה לנתון הזה."**
לא הערכה, לא "בערך", לא סדר גודל, לא "כנראה". מספר שנשמע סביר
ואין לו מקור הוא עדיין מספר מומצא.

מוצג מספר — מצוין לידו מאיפה הוא: מאיזה קובץ, או מאיזו פקודה.
`wc -l index.html` הוא מקור. `grep -c` הוא מקור. זיכרון אינו מקור.

זה קרה: הוגש דוח עלויות עם סכומים בדולרים. בקונסולת Anthropic,
בדף Cost עם Range=Month to date ו-Group by=Model, כל הערכים היו
`$0.00` — Total cost, Total token cost, Total web search cost
ו-Total code execution cost. הדוח כולו היה מומצא.

## ארבעה דברים שלא עושים, וגם לא שואלים עליהם

1. שירות או API בתשלום — **עם חריג אחד מ-5.9.2026:** ביציאה לציבור
   גובים כסף, ו-GitHub Pages יורד. הנוהל ב-`marketing/launch.md`. עד
   שמודל הגבייה נקבע, הכלל תקף כלשונו.

   **ו-10.9.2026 הבעלים הכריע: אין גבייה כלל עד להודעה חדשה.**
   לשונו: ״הכל חינם עד להודעה חדשה, מבצע השקה, לא תהיה גבייה עד
   ל-10,000 מבקרים.״ שלוש מסקנות מעשיות:

   - **אין לבנות חשבון משתמש, הרשמה, או שמירת סטטוס מנוי בשרת.**
     נשאל במפורש ונענה. זה התקדים של ״אזרח״ ושל `josh-server`:
     מי שיבנה את זה יבנה משהו שהוחלט לא לבנות.
   - **המחירים נשארים ב-`DATA.APPS` וב-`pricing/`** ואינם נמחקים —
     הם נכנסים לתוקף בסיום המבצע. מה שמוצג ללומד הוא פס ההשקה
     בנוסח הבעלים, והוא יושב בשלושה דפים: `pricing/`, דף הבית
     ו-`theory/buy.html`.
   - **תנאי הסיום אינו נמדד היום.** ״10,000 מבקרים״ דורש ספירה,
     ו-GoatCounter הוסר מכל הדפים ב-10.9 בהחלטת הבעלים (סעיף 3
     למטה). `launch.js` שולח דרך `track()` ל-`window.goatcounter`
     שאינו קיים, ולכן זהו no-op שקט. ראו `O-43`.
2. מפתח API בתוך הריפו.
3. תלות חיצונית חדשה. **בלי חריגים, ובכללם אנליטיקס.**
   GoatCounter (`gc.zgo.at/count.js`) הוחרג ב-9.9.2026 בנימוק
   שהוא כבר קיים ב-14 דפים ומוצהר בתנאים. **ההחרגה בוטלה
   ב-10.9.2026 בהחלטת הבעלים, והסקריפט הוסר מכל חמישה־עשר
   הדפים** — יחד עם שלוש שורות ההערה שלו, עם המשפט "האתר סופר
   כניסות" ב-`legal/terms.js`, ועם ההחרגה הזאת עצמה.

   `launch.js` שולח אירועי מדידה דרך `track()`, והוא כבר היה
   מוגן: `if(g && typeof g.count==="function")`. בלי הסקריפט זהו
   no-op שקט, בלי שגיאה, ו-`tests/launch.test.html` בדיקה 7
   מאמתת בדיוק את המצב הזה — היא נכונה עכשיו יותר מאשר קודם.
4. מחיקת תוכן קיים (שאלות, רמזים, תרגומים).

## גרסאות — שני מקומות, לא אחד

בכל אפליקציה יש **שתי** מחרוזות שחייבות להיות תואמות, שתיהן באותו
קובץ — `index.html`, ובבגרות 806 `app.js`:

    var BUILD="b35 · 2026-08-31";                          // מה שמוצג למשתמש
    navigator.serviceWorker.register("sw.js?v=b35-pwa1");  // מה שקובע את הקאש

`sw.js` גוזר את מפתח הקאש מ-`?v=` שבכתובת הרישום. ההערה בראש `sw.js`
טוענת שיש "מקור אמת אחד" — **זה לא נכון**: מחרוזת הרישום מקודדת קשיח
ואינה נגזרת מ-`BUILD`. עדכון של `BUILD` לבדו לא מנקה את הקאש,
והמשתמש לא יראה את התיקון. עדכנת אחד — עדכן את השני.

**ולשני דפים אין `BUILD` כלל** — דף הבית ו-`reader`. הם נושאים מפתח
קאש בלבד (`sw.js?v=40-pwa1`, `sw.js?v=31-pwa1`), מפני שאין להם פוטר
שמציג גרסה. זה תקין: המנגנון תלוי אך ורק ב-`?v=`. אל תסיק מהיעדר
`BUILD` שאין מה להעלות.

## מנוע ההקראה — ארבעה מנגנונים בכל אפליקציה

בכל אחת־עשרה האפליקציות יושב אותו בלוק, מועתק מ"תאוריה מדברת"
ומסומן בהערה `מנוע ההקראה של "תאוריה מדברת" — ארבעה מנגנונים`.
ב-`bagrut-806` הוא כתוב בסגנון המודול שלה, בתוך `speech.js`.

ארבעתם מכסים כשלים שקטים של Web Speech, שכולם נראים למשתמש בדיוק
אותו דבר — הקול מפסיק באמצע:

1. **שומר-ער** (`startKeepAlive`) — Chrome ו-Edge בשולחן העבודה
   חותכים הקראה אחרי כ-15 שניות. `pause`+`resume` כל תשע שניות.
   **רק בשולחן העבודה**: במובייל אין את הבאג והפעולה מקרטעת שם.
2. **`_activeU`** — הפניה חיה ל-utterance. בלעדיה Chrome אוסף אותו
   ואיתו את `onend` ו-`onboundary`, וההקראה נתקעת בלי שגיאה.
3. **`ttsWatchdog`** — יש מכשירים שבהם `onend` לא נורה. ארבע שניות
   של שקט מוחלט משחררות את התור.
4. **נפילה מקול רשת** — קול נוירלי של Edge שותק בלי אינטרנט. אחרי
   כישלון אחד `_netVoiceOK` נכבה, נבחר קול מקומי, ואותו טקסט נאמר
   שוב פעם אחת.

**`voiceUsable` הוא מיון ולא ניקוד, וזה לא סגנון.** קנס בתוך הניקוד
נוסה ונכשל: קול נוירלי של Microsoft צובר על שם היצרן ועל "Natural"
יותר ממה שכל קנס סביר מוריד, ולכן אותו קול מת נבחר שוב מיד אחרי
שנכשל. הוא חייב להישאר מפתח המיון הראשון — לפני המגדר, שאם לא כן
קול נשי מת מנצח קול גברי חי.

**המגדר קודם לאיכות, וזו החלטה של הריפו הזה ולא של תאוריה מדברת.**
שם העדפת המגדר יושבת בתוך הניקוד ונשברת מול שם יצרן; כאן היא המיון
שאחרי `voiceUsable`. אל תהפוך את זה בטענה ש"ככה זה בתאוריה מדברת".

`node .claude/qa/voice.js` בודק את ארבעתם בכל האפליקציות מול
`speechSynthesis` מזויף. נגעת בהקראה — הרץ אותו.

**מה לא הועתק, ולמה:** שכבת ההגייה של "תאוריה מדברת"
(`speech-rules.json`, מילון `KTIV`, קריוקי, קבצי ההקלטה) היא תוכן
של תאוריה בנהיגה — "תמרור", "רמזור", "דו-סטרי". היא אינה מנגנון
ואין לה מה לתת לשיעור במתמטיקה.

## מלכודת הקאש של `legal/`

שלושה־עשר קובצי ה-`sw.js` מצרפים מראש את **אותם** `/legal/terms.js`
ו-`/legal/protect.js`, כל אחד למטמון בשם משלו (`site-`, `math-app-`,
`english-`, `ulpan-`, `bagrut806-`…). ההגשה בכל אחד מהם נעשית מהמטמון
של אותה אפליקציה בלבד — `caches.open(CACHE)` ואז `c.match(req)`
בתוכו — ולא דרך `caches.match` הגלובלי, שסורק את **כל** המטמונים של
המקור. `caches.match` נותר בקבצים רק בתוך הערה שמסבירה למה לא
משתמשים בו.

ולכן עותק ישן שיושב במטמון של אפליקציה אחרת אינו עונה כאן. עדיין:
לכל אפליקציה עותק `sw.js` משלה ומטמון משלה, והעותק שלה מהשער משתחרר
רק כשמפתח הגרסה של אותה אפליקציה עולה.
**נגעת ב-`legal/` — העלה את כל שלושה־עשר המפתחות יחד.**

הנכסים תחת `./img/` אינם נתונים לזה: הנתיב שלהם יחסי, ולכן לכל
אפליקציה כתובת משלה.

## מלכודת ה-`localStorage` — מפתח שהועתק הוא מגירה משותפת

כל האפליקציות יושבות באותו מקור, ולכן `localStorage` אחד לכולן.
מפתח שנולד בהעתקה — `EKEY`, מפתח המורה, `SKEY` — הוא מגירה
ששתי אפליקציות כותבות אליה, גם כשההערה לידו אומרת "לאפליקציה הזאת
בלבד". זה קרה ב-31.8 לכולן (`shlav-exams-v1`), ושוב ב-5.9 ב-`lomda`
שנולדה מ-`history` ונשאה את `shlav-exams-history-v1` ואת
`mifne-teacher`: מבחן שנבנה בלומדה הופיע במפנה. **העתקת קובץ —
כל מפתח מקבל שם משלו**, והישן נשאר רק כ-`*_OLD` לקריאה.
`node .claude/qa/storage.js` תופס את זה.

## ה-`sw.js` של השורש אינו כמו האחרים

הוא רשום ב-scope `/`, ולכן הוא מטפל בכל נתיב באתר שאין לו worker
משלו — ובכלל זה בביקור **הראשון** באפליקציה אחרת, לפני שה-worker שלה
נרשם. הוא היה שומר את התשובה ההיא במטמון שלו, ומהרגע שה-worker של
האפליקציה קיים הוא זה שמגיש את הנתיב והעותק ההוא לא נקרא עוד לעולם.
נמדד בכרום אחרי סיור בכל האפליקציות: 5,356KB זרים מול 172KB משלו.

לכן יש בו שומר שאין באף אחד מהאחרים — הוא מגיש רק קובץ בשורש או אחת
משלוש התיקיות שאין להן worker משלהן:

    const seg = url.pathname.split("/");
    if (seg.length > 2 && seg[1] !== "img" && seg[1] !== "legal" && seg[1] !== "voice") return;

אפליקציה חדשה נופלת מהכלל החוצה מאליה, ולכן זה **אינו** מקום שמיני
שצריך לגעת בו כשמוסיפים אפליקציה. אבל **אסור להעתיק את השורות האלה
ל-`sw.js` של אפליקציה**: כל נתיביה נמצאים בתוך תיקייה, והשומר היה
מבטל לה את כל הקאשינג. `node .claude/qa/cache.js` בודק את שני
הכיוונים.

## שני סשנים על אותו ריפו — שני כללים שנלמדו ביוקר

**לפני שמתחילים משהו גדול: `git fetch origin main` ולהסתכל ב-log.**
ב-31.8 שני סשנים בנו במקביל רמה רביעית מלאה ל-math-uni, כל אחד 23
בנאים, כל אחד מאומת. אחת מהן נזרקה. אף אחד מהם לא בדק מה יש ב-main
לפני שהתחיל, ושניהם היו רואים את זה בשורה הראשונה של ה-log.

**ושוב לפני ה-commit — כי בדיקה בהתחלה אינה מספיקה.**
ב-2.9 זה קרה שוב, ולסשן שנזרק לא היה מה להאשים: הוא כן בדק את
`main` כשהתחיל, וראה שם את שלב 1 של bagrut-806 (`03c15da`, 09:18).
שלב 2 נדחף על ידי סשן אחר ב-09:53 (`35d4b02`) — שלושים וחמש דקות
אחר כך, בדיוק בזמן שהוא בנה את אותו שלב 2 בעצמו. חלון הבנייה הוא
החלון המסוכן, ולא הרגע שלפניו.

**ומה עושים כשמגלים שזה קרה: קוראים את מה שנחת, ומוסיפים עליו.**
לא ממזגים שתי מימושים מקבילים ולא דוחפים את שלך מעל שלו. מי שהגיע
ל-main ראשון נשאר, ומהבנייה שנזרקת נלקח רק מה שאין בו — במקרה ההוא
זו הייתה ההשהיה, שגילתה שפינג ה-keepalive מבטל אותה תוך ארבע
שניות. השינוי הקטן ביותר שפותר את הבעיה תקף גם כאן.

**מספר הגרסה מוקצה במיזוג, לא בבנייה.**
כל סשן מתפצל מ-main, רואה `b33`, ומסמן את עצמו `b34`. שלושה סשנים
עשו בדיוק את זה באותו יום — `b34`, `m30`, `v29` — וזו אינה תאונה
אלא ודאות: כל אחד מקצה מספר לפי ה-main שהוא ראה בהתחלה.

ומספר הגרסה אינו נוי. הוא הדבר היחיד שמביא קוד חדש למי שכבר התקין
את האפליקציה. **שתי גרסאות שונות עם אותו מפתח פירושן שמי שקיבל אחת
לא יקבל את השנייה לעולם** — בדיוק הבאג שהמנגנון נועד למנוע.

לכן: בונים ומשאירים את המספר כפי שהוא; מי שממזג בודק את main ומקצה
מספר טרי. אם מיזגת ומצאת שהענף נושא מספר שכבר קיים — זו התנגשות,
לא צירוף מקרים, ויש להקצות מחדש.

**ומיזוג של מילון תרגום הוא איחוד, לא בחירת צד.** מילוני `TR_AR`
ו-`TR_KEYS` הם שורה אחת ענקית, ולכן כל שינוי בהם מתנגש במלואו.
`git` יציע לך לבחור צד; בחירה מוחקת מאות תרגומים. משווים מפתח־מפתח
מול הבסיס המשותף, מאחדים, ובודקים אם יש מפתח ששני הצדדים שינו —
רק הוא דורש הכרעה.

## שלב 0 של השחרור — main ירוק לפני שדוחפים

**אין דחיפה ל-main כשריצת Actions האחרונה על main אדומה — קודם
מתקנים אותה.** זה שלב 0 בנוהל השחרור, `.claude/skills/release/SKILL.md`,
לפני ״מה נכנס לגרסה״.

למה: הריצה על `main` היא הדבר היחיד שרואה את הקוד אחרי שנדחף, ואיש
אינו פותח אותה. בריפו של ״תאוריה מדברת״ החבילה הייתה אדומה בשלוש
גרסאות רצופות — v96, v97 ו-v98, ריצות 55, 56 ו-57 לפי הודעת הקומיט
`2e2055c` שם — ואף סשן לא ראה, מפני שכל סשן דחף את שלו והלך. אדום
שיושב על `main` הופך גם את הריצה שלך לחסרת משמעות: כשהיא אדומה,
אין לדעת אם זה אתה.

איך בודקים: GitHub → Actions → הריצה האחרונה על `main`. אדומה —
מתקנים **אותה** בקומיט נפרד, ורק אז ממשיכים. **ובריפו הזה יש
היום workflow** — `.github/workflows/qa.yml`, שנוסף 9.9.2026 ומריץ
את כל החבילה בכל push ו-PR. הסעיף נכתב כשלא היה כאן אחד, ומאז
הוא חל כאן מהריצה הראשונה ולא רק בריפו השני.

ולמה הכלל נחוץ דווקא כאן: **הסשנים דוחפים ל-`main` ישירות.** הסשן
עובד על ענף `claude/*`, ממזג אותו ל-`main` בעצמו ודוחף — ולכן אין
סוקר ואין שער, והבודק הוא שלב 0. **נמדד מחדש 10.9.2026** על 60
הקומיטים האחרונים ב-`origin/main`: כולם קומיטי מיזוג, ומהם **שלושה
בלבד עברו דרך Pull Request** (`#13`, `#14`, `#15`). המדידה המקורית
מ-6.9 מצאה אפס PR, כלומר המסלול הזה רק התחיל להיפתח — ועד שהוא
יהיה הדרך היחידה, הריצה על `main` היא הבדיקה האחרונה שיש.

## המבחן הכיתתי אינו מביט בהתקדמות הלומד

בונה המבחן אורז **זרע** לתוך הקישור, ולא עשרים שאלות. התלמיד בונה
מהזרע את אותן שאלות אצלו. לכן כל דבר שנכנס לבחירת השאלה חייב
להיות זהה בשני הצדדים.

`chooseIndex` — החזרה המרווחת — שוקלת את הפריטים לפי `state.item`,
כלומר לפי היסטוריית הלמידה של מי שיושב מול המסך. זה נכון לתרגול
ושגוי למבחן, והוא ישב בשני הנתיבים.

**נמדד ב-6.9.2026 על `a9c2c00`, בדפדפן אמיתי:** אותו זרע, שתי
היסטוריות למידה, חמש הרצות על ארבע האפליקציות — **39 שאלות מתוך
200 יצאו שונות** בין המורה לתלמיד. תשע־עשרה מתוך עשרים ההרצות היו
שונות מאפס. המורה מאשר מבחן אחד והכיתה נבחנת באחר, ושום דבר אינו
צועק: שני המסכים תקינים, ורק הציונים הם של שאלות אחרות.

הפתרון: `buildQuestion(tid, lv, avoid, pure)`. `pure=true` בוחר
אחיד ואינו נוגע ב-`state`, ו-`buildQ` — הנתיב **היחיד** של המבחן —
מעביר אותו תמיד. אחרי התיקון: אפס מתוך 200 באותן חמש הרצות.

קיים ב-`history`, `english`, `ulpan` ו-`lomda`. אפליקציות המתמטיקה
מחוללות שאלה מפרמטרים ואינן קוראות ל-`state` בדרך הזאת כלל.
**אפליקציה חדשה שנולדת מהעתקה — ודא שהיא ירשה גם את `pure`.**
הבדיקה היא `node .claude/qa/exam.js`, והיא לא תפסה את זה: היא
משווה מורה מול תלמיד באותו דפדפן, ושם `state.item` זהה בהגדרה.

## סכימות התוכן — שלוש, לא אחת

אין שדות בשם `h1` או `h2`. פותחים את הקובץ ומחקים את המבנה שלו בדיוק.

- **`math-app`** — `hint1` / `hint2` / `explain`, עם מצייני מיקום
  `{a}`, `{b}`, `{ans}`, וסיומת לכל פעולה (`hint1Add`, `hint2Sub`...).
- **`math-teen`, `math-uni`, `math-uni2`, `math-uni3`** —
  `{ask, expr, ans, wrong:[W(טקסט, למה)], steps:[S(כותרת, הסבר, נוסחה)], hint}`.
- **`english`, `history`, `ulpan`, `lomda`** — קריאה ל-`Q(...)`, לא
  אובייקט שנכתב ביד: `Q(q, ans, wrongs, whys)`, וכל ארגומנט הוא
  אובייקט ארבע־שפות `{he, ar, ru, en}`. החתימה ב-`english/index.html:1765`,
  ב-`ulpan/index.html:1767` בלי `whys`, וב-`lomda/data/schema.js:43`.
  **אין שדה `options` ואין שדה `hint`** — `options:[{h, ok, why}]` הוא
  מה ש-`buildQ` מייצר בזמן ריצה, ואינו כתוב באף פריט תוכן.

## תרגום — המחרוזת העברית היא המפתח

באפליקציות האוניברסיטה כל מחרוזת שנראית למשתמש עטופה ב-`_()`.
ב-`math-teen` המנגנון הוא `trHTML` עם מילון `TR_AR`. בשני המקרים
**המחרוזת העברית עצמה היא המפתח במילון**. שינוי של מילה בעברית בלי
לעדכן את המפתח מוחק את התרגום בשקט, בלי שגיאה. משנים את שניהם יחד.

## אפליקציה חדשה אינה מתפרסמת ביום שנבנתה

    build → internal → content-qa → approved → public

חמישה שלבים, ואפליקציה עולה אחד בכל פעם. **היא מגיעה ללומד
אמיתי רק אחרי `approved`.** עד אז היא אינה ב-`DATA.APPS` שבדף
הבית, והיא נושאת את השער הפנימי מ-`.claude/qa/internal-gate.html`.

מקור האמת הוא `.claude/qa/stages.json`, ו-`node .claude/qa/stage.js`
אוכף — הוא נופל אם הרישום, דף הבית וקובץ האפליקציה אינם
מסכימים. התהליך המלא, ובכללו מה נדרש כדי לצאת מכל שלב, נמצא
ב-`PIPELINE.md`.

השער אינו הגנה קריפטוגרפית ואינו מתיימר להיות: אין שרת, והכול
HTML גלוי. הוא מונע מלומד להיתקל באפליקציה לא בשלה.

**אחת־עשרה האפליקציות הקיימות רשומות `legacy-published`** — הן
פורסמו לפני שהשער נבנה. זו אזהרה ב-`stage.js`, לא כשל, והיא
נסגרת כשהבעלים עובר על ממצאי ה-REVIEW של האפליקציה.

## כלי בדיקה

    node .claude/qa/serve.js                 # שרת מקומי על 127.0.0.1:8099
    node .claude/qa/smoke.js math-app        # דפדפן אמיתי, בודק שגיאות JS
    node .claude/qa/parse.js                 # תקינות פרסינג
    node .claude/qa/entropy.js               # תשובה קבועה / כפילויות
    node .claude/qa/options.js               # כמה אפשרויות התלמיד באמת רואה
    node .claude/qa/exam806.js               # התוכן הכתוב־ביד של 806
    node .claude/qa/naming.js                # כלל השם של ״תאוריה מדברת״
    node .claude/qa/cache.js                 # חמש בדיקות על שלושה־עשר ה-sw.js
    node .claude/qa/storage.js               # מפתח localStorage אחד לאפליקציה אחת
    node .claude/qa/say.js                   # ההקראה לא מסגירה את התשובה לפני שעונים
    node .claude/qa/saysym.js                # מה באמת נאמר — אין ^ או _ גולמי בהקראה
    node .claude/qa/guess.js                 # אי אפשר לקלוע לפי אורך התשובה או מיקומה; תא שחצה נמדד שוב לפני שמפיל
    node .claude/qa/clicks.js                # כל לחיצה מושמעת, והמתג שמשתיק אותה — בשתים־עשרה האפליקציות
    node .claude/qa/fonts.js                 # ערבית ורוסית בגופן שיש בו את הכתב
    node .claude/qa/langbtn.js               # כפתור השפה עצמו — "العربية" גם כשהממשק עברי
    node .claude/qa/banks.js                 # החיווט של lomda/data/, וארבע השפות בתוכן
    node .claude/qa/kotvim.js                # התוכן של ״כותבים ביחד״ — ארבע שפות, ואין קטע גנרי
    node .claude/qa/leaks.js                 # תיאור של פריט בלומדה אינו מצטט את כותרתו (ההקראה לא מסגירה)
    node .claude/qa/markers.js               # סמני התנגשות מיזוג שנשארו בקובץ שנעקב
    node .claude/qa/wix.js                   # כרטיסי האפליקציות ל-wix-content.md
    node .claude/qa/content.js               # תוכן: תשע משפחות, דוח PASS/REVIEW/FAIL
    node .claude/qa/fresh.js                 # דוח תוכן שמתאר עץ שכבר אינו קיים
    node .claude/qa/stage.js                 # שער השלבים — מי מותר ללומד
    node .claude/qa/deployed.js              # מסלול הלומד על הבתים שנפרסו — ביד בלבד
    Actions → live-check → Run workflow      # מה באמת מוגש מהאתר החי (הסוכן חסום)
    node .claude/qa/all.js                   # הכול, ופסק דין אחד
    node .claude/qa/all.js --static          # רק מה שאינו דורש דפדפן
    node .claude/qa/extract.js /tmp/js …     # מחלץ סקריפטים ל-eslint

הפירוט המלא, ובכללו קו הבסיס של eslint, נמצא ב-`.claude/qa/README.md`.

**ויש שער שיעצור אותך בסוף.** `.claude/hooks/qa-gate.sh`, מחובר
כ-`Stop` hook ב-`.claude/settings.json`, מריץ `all.js --static`
כשסשן מנסה להסתיים — שש־עשרה בדיקות, בערך שתי שניות — ו**חוסם את
הסיום** אם אחת נכשלה. הוא שותק כשלא נגעת בקוד (שינוי ב-`.md`,
ב-`.json` או ב-`qa/reports/` אינו מפעיל אותו), והוא אינו מתקן
כלום — רק אומר מה נשבר. עקיפה מכוונת: `QA_GATE=0`.

אל תסתמך עליו כתחליף להרצה: הוא אינו מריץ דפדפן, ולכן `smoke`,
`exam`, `entropy` ו-`options` אינם בו. את אלה מריצים ב-`/qa`.

**ושני הוקים שמריצים בשבילך את כלל הסחיפה שכתוב למטה.**
`.claude/hooks/main-drift.sh` רץ בפתיחת סשן, ושוב לפני כל
`git commit`, ומדפיס כמה `main` מקדים אותך ומה זז שם — כולל קבצים
שמחזיקים `BUILD` ומפתח קאש. **הוא אינו חוסם.** נעילת קבצים חוסמת
נשקלה ונפסלה: נעילה שטועה עוצרת כל עריכה, וההתנגשויות המתועדות
היו בין סשנים — ועליהן המידע הזה עונה.

**הרשת חסומה בסביבת הפיתוח.** אי אפשר להגיע ל-`lagstein1-png.github.io`
מכאן. בדיקה מול השרת המקומי היא בדיקה מקומית — לא קוראים לה "בדיקה חיה".

**אבל יש דרך אחת לבדוק את האתר החי, והיא לא ידנית:**
`.github/workflows/live-check.yml`, שנוסף 11.9.2026. הרנרים של
GitHub אינם חסומים, והוא מושך את שתים־עשרה האפליקציות מהרשת
ובודק שלושה דברים — סטטוס, מחרוזת שאמורה להיות בדף, ו**מפתח
הקאש המוגש מול זה שב-`main`**. העמודה האחרונה היא העיקר: בלעדיה
״המחרוזת נמצאה״ יכול להיות עותק ישן שנשאר ב-CDN. הפעלה ידנית
בלבד, בלי סודות, `permissions: contents: read`. סוכן יכול
להפעיל אותו ב-`actions_run_trigger` ולקרוא את הלוג.

## מרגל הסשן

    .claude/spy.sh              סשן חדש, ואחריו סיכום בעברית פשוטה
    .claude/spy.sh --last       רק סכם את הסשן האחרון
    .claude/spy.sh --show       רק הדפס את התמליל הקריא, בלי לסכם
    .claude/spy.sh --live       סשן חדש עם פרשנות תוך כדי ריצה
    node .claude/spy.js <id>    תמליל של סשן מסוים
    node .claude/spy-live.js    להיצמד לסשן שכבר רץ

`claude … | tee log` **אינו** עובד לזה, ולא בגלל דקדוק: צינור על
stdout מסלק את ה-TTY, ומה שנכתב למסך הוא ציור — רצפי ANSI, מסגרות
וציור־מחדש של אותן שורות. הקובץ שנשמר אינו תמליל אלא צילום של
האנימציה. **ו-`mkfifo` אינו מציל את זה**: גם צינור בעל שם הוא צינור,
ה-TTY נעלם בדיוק כך, ו"שורה" בפלט כזה אינה אירוע אלא ציור — אין
טעם לספור עשר מהן ולסכם.

התמליל האמיתי כבר יושב על הדיסק: Claude Code כותב אותו בעצמו אל
`~/.claude/projects/<תיקייה-מקודדת>/<session-id>.jsonl`, שורת JSON
לכל אירוע. `spy.sh` מקבע מזהה סשן מראש (`--session-id`), מריץ בלי
צינור, ואז `spy.js` קורא את הקובץ שנוצר.

**פרשנות חיה** קוראת מאותו קובץ תוך כדי כתיבתו. `spy-live.js` מוסיף
לו כל שנייה את מה שנוסף, ומדי `--every` אירועים (ברירת מחדל 12), או
אחרי `--idle` שניות שקט, שולח את הקטע החדש ל-`claude -p` ומקבל שתיים
עד ארבע שורות בעברית פשוטה. הפירוק לשורות קריאות הוא בדיוק זה של
`spy.js` — אותה פונקציית `feed`, ולא העתק שלה.

הפרשנות אינה מודפסת לטרמינל של הסשן, מפני שהממשק של Claude Code
מצייר את המסך מחדש ודורס כל דבר שמודפס לתוכו. `--live` כותב אותה
אל `/tmp/claude-spy-live.txt` (או `$SPY_LOG`), ורואים אותה בטרמינל
שני עם `tail -f`. **כל הסבר הוא קריאה נוספת ל-claude, ולכן הוא עולה
כסף** — `--every` גדול יותר, פחות קריאות.

היצמדות לסשן שכבר רץ מתחילה מסוף הקובץ, כדי שההסבר הראשון לא יהיה
כל הסשן עד כה; `--from-start` כולל גם את מה שקדם. `--show` מדפיס את
האירועים בלי לקרוא ל-claude כלל, וזו הדרך לבדוק את המנגנון בחינם.

**"קבצים שנכתבו" מונה רק כלי `Write` ו-`Edit`.** קובץ שנכתב בתוך
פקודת Bash — heredoc, `sed -i`, הפניה — אינו מופיע שם. הרשימה
המלאה היא `git status`, וזה כתוב גם בפלט עצמו.

יש גם שורת **"אפליקציות שנזכרו בפקודות"**, והיא אזכור ולא שינוי:
מנייה של `<שם>/` בקלט של כל כלי. רשימת האפליקציות נגזרת בזמן ריצה
מכל תיקיית בת שיש בה `index.html` — לא רשימה קשיחה, שהייתה
מתיישנת באפליקציה הבאה שנוספת.

## הצוות

שבעה סוכני משנה ב-`.claude/agents/`, לכל אחד שם פרטי ותחום אחד:

| סוכן | תחום | Bash |
|---|---|---|
| `euclid` — אוקלידס | נכונות מתמטית, ואימות בחישוב ולא בעין | ✓ |
| `maayan` — מעיין | תוכן, ניסוח, ושלמות ארבע השפות | ✓ |
| `nimrod` — נמרוד | באגים, service worker, והרצה בדפדפן אמיתי | ✓ |
| `tamar` — תמר | האם רמת קושי באמת מלמדת, או שאפשר לנחש בה | ✓ |
| `renana` — רננה | הקראה ונגישות | ✗ |
| `bezalel` — בצלאל | זהות חזותית | ✗ |
| `gilad` — גלעד | ראש צוות, מכריע במחלוקות, **היחיד שמבצע commit** | ✓ |

היו עשרה, ושלושה תפקידים היו מכוסים פעמיים בשתי שיטות שמות:
`math-content-checker` מול `euclid`, `four-language-checker` מול
`maayan`, ו-`app-qa-runner` מול `nimrod`. הם אוחדו — הבודק נכנס אל
בעל השם, וסעיף שיטת האימות שלו נשמר במלואו בתוך הקובץ המאוחד.

**Bash אינו רשות לקמט.** ארבעה מהם מריצים את בדיקות ה-QA על מה
ששינו — זו בדיוק הסיבה שהוא ניתן להם. `gilad` הוא היחיד שמבצע
commit ודוחף, וגם מי שמוודא בסוף שהגרסה ומפתח הקאש עודכנו יחד.

**ומנהל העבודה אינו אחד מהשבעה — הוא הסשן הראשי.** אף אחד מקובצי
הסוכנים אינו נושא את כלי הסוכנים בשורת `tools:` שלו — שבע שורות,
`grep -n "tools:" .claude/agents/*.md` — ולכן סוכן **אינו יכול**
לקרוא לסוכן אחר. עד 9.9.2026 כל שבעת הקבצים טענו את ההפך, ״אתה
רשאי לקרוא לכל סוכן אחר בצוות״, וזו הייתה הבטחה שאין דרך לקיים:
סוכן שהאמין לה היה מתקן בעצמו דבר שאינו בתחומו, או שותק עליו.

במקומה: ממצא שאינו בתחום הסוכן נכתב בדוח בשורה
`לסוכן <שם>: <מה> — <קובץ:שורה>`, והסשן הראשי מזמן את הבא בתור.
`gilad` כותב את התוכנית — מי, על איזה קובץ, ובאיזה סדר — והפרוטוקול
המלא נמצא ב-`.claude/agents/gilad.md`, בסעיף ״חלוקת העבודה״. הכלל
שמחייב אותו: **קובץ אחד, סוכן אחד, ברגע אחד.** אפליקציה כאן היא
`index.html` יחיד, ולכן שני סוכנים שכותבים בה במקביל אינם שני
עובדים אלא דריסה — בלי שגיאה, בלי התנגשות ב-git, ועם תיקון אחד
שנעלם. מקבילות מותרת באבחון בלבד; התיקון עצמו הוא תור.

## שתים־עשרה אפליקציות, חמש־עשרה תיקיות עם `index.html`

הפורטפוליו הוא **שתים־עשרה** אפליקציות, והמספר הזה כתוב בכותרת דף
הבית בארבע שפות (`badge`) **כמילה ולא כספרה** — "שתים־עשרה",
"اثنا عشر", "Двенадцать", "Twelve". הוא אינו נגזר מ-`DATA.APPS`,
ולכן הוספת אפליקציה מחייבת לגעת בארבע המחרוזות האלה ביד. רשימת
האפליקציות היא `DATA.APPS` בתוך `index.html`, והיא מקור האמת למניין.
`node .claude/qa/apps.js` תופס מניין שנשאר מאחור.

חמש־עשרה התיקיות שיש בהן `index.html` אינן חמש־עשרה אפליקציות
(`ls -d */index.html | wc -l` = 15, נמדד 10.9.2026). החשבון נסגר כך:

    12  אפליקציות לימוד עם תיקייה מקומית   ← stages.json, apps שאינן external
     3  דפים שאינם אפליקציה                ← stages.json, notApps עם index.html
    ──
    15

- ״תאוריה מדברת״ נספרת בשתים־עשרה אבל **אינה בריפו הזה** (ראו למטה).
  המזהה שלה ב-`DATA.APPS` הוא `theory`. לכן שתים־עשרה האפליקציות
  שב-`badge` אינן שתים־עשרה התיקיות: אחת מהן חיצונית, ובמקומה
  יושבת מקומית `kotvim`, שעדיין אינה ב-`badge`.
- `kotvim/` **היא אפליקציית לימוד ואינה נספרת עדיין** — ״כותבים
  ביחד — פיגום כתיבה״, `stage: internal` ב-`stages.json`. יש לה
  `sw.js` ו-`BUILD` משלה, אבל היא אינה ב-`DATA.APPS` והיא נושאת את
  השער הפנימי. זה תקין וזה השלב שלה בצינור; ראו ״אפליקציה חדשה
  אינה מתפרסמת ביום שנבנתה״ למעלה.
- `voice/` **אינה נספרת** — דף אבחון עצמאי של מנוע ההקראה
  (`<title>בדיקת קול</title>`), לא אפליקציית לימוד.
- `legal/` היא עמוד תנאי השימוש, ו-`img/` ו-`marketing/` אינן דפים.
- `pricing/` **אינה נספרת** — עמוד המחירים
  (`<title>מחירים — למידה שנשמעת</title>`), לא אפליקציית לימוד.
- `learning-core/` **אינה אפליקציה ואינה נספרת** — ספריית מנוע
  משותפת (`core.js`, `core.css`, תבנית ו-48 בדיקות ב-`test.js`)
  ש**אף אפליקציה אינה טוענת**. הפירוט ב-`ARCHITECTURE.md` §9.

מי שסופר `ls */index.html` ומשווה לשתים־עשרה — ימצא סתירה שאינה
קיימת: `legal/`, `pricing/` ו-`voice/` אינן אפליקציות לימוד,
`kotvim/` היא אפליקציה שטרם פורסמה, ו״תאוריה מדברת״ נספרת בלי שיש
לה כאן תיקייה.

**המספרים האלה מתיישנים.** מקור האמת הוא `.claude/qa/stages.json`,
ו-`node .claude/qa/stage.js` אוכף אותו; המניין כאן הוא נוחות קריאה
בלבד, ומי שמוצא פער — `stage.js` צודק.

**כשמוסיפים אפליקציה** צריך לגעת בשבעה מקומות, ולא באחד: תיקייה עם
`index.html`, `manifest.json` ו-`sw.js` (שם המטמון בו הוא ייחודי
לאפליקציה); אייקונים ב-`img/`; רשומה ב-`DATA.APPS`; סמל ב-`ICONS`
שבדף הבית; ארבע מחרוזות ה-`badge`; ארבע מחרוזות ה-`lead` שמונות
את התחומים; ומפתח הקאש של דף הבית עצמו, שהשתנה.

## מה מצדיק אפליקציה — הקריטריון, ולמה הוא לא תחום הדעת

השאלה חוזרת בכל נושא חדש, ויש לה תשובה מדידה בקוד. נמדד ב-4.9.2026:

| | `ri(`/`rnd(` | `{he:"` |
|---|---|---|
| math-teen, math-uni, math-uni2, math-uni3 | 101–104 | 3–7 |
| math-app | 10 | 14 |
| english | 1 | 826 |
| history | 1 | 651 |
| ulpan | 1 | 489 |

שתי משפחות, ואין ביניהן רצף. במשפחת המתמטיקה השאלה **מחושבת**
בזמן ריצה ו-`{he:"` הן מחרוזות ממשק בלבד; בשלוש האחרות אין חישוב
כלל והתוכן **כתוב ביד**.

**אפליקציה מוצדקת רק בקוד שאין למנוע.** שתי צורות בלבד:

1. **מחולל** — התשובה מחושבת ולא מאוחסנת, והווריאציה אינסופית.
   זו כל משפחת המתמטיקה.
2. **אינטראקציה שאין במנוע** — ניקוד ב-`ulpan`, קטעי קריאה עם
   הקראה למילה ב-`english`, KaTeX וסימולציית בחינה מתוזמנת
   ב-`bagrut-806`, הדבק-והקרא ב-`reader`.

**כל השאר הוא נושא ב-`lomda`.** תוכן שנכתב כפריט, כמושג או כקטע,
ונענה בבחירה מארבע — אין בו שורת מנוע אחת חדשה.

**המבחן הזה מפליל את `history`.** `ri(`=1, אפס `PARA(`, אפס ניקוד —
היא נכשלת בשני התנאים, ולפי הכלל הייתה צריכה להיות נושא ב-`lomda`.
היא אפליקציה מפני שנבנתה לפני ש-`lomda` הייתה קיימת. **זו אינה
סיבה להזיז אותה** — היא עובדת, ומחיקת תוכן אסורה — אבל היא הסיבה
שאפשר לסמוך על הכלל: הוא נגזר מהקוד, ולכן הוא מפליל גם את מה
שכבר נבנה.

**ולמה זה חשוב יותר ממניין:** כל אפליקציה חדשה היא עותק נוסף של
המנוע. בסשן אחד ב-4.9 נמצאו שתי משפחות באגים שנולדו בדיוק מזה —
`<span class="k">` בלי ה-CSS שלו, שהציג לתלמיד "118" במקום 18,
ו-`.spread` שנפל ל-block בחמש אפליקציות. עותק עשירי אינו מוסיף
תוכן פי עשר; הוא מוסיף משטח באגים.

לכן: **המניין אינו יעד.** הקריטריון מחליט, והוא מייצר מעט
אפליקציות והרבה נושאים ב-`lomda`.

## `lomda` — נושא חדש בקובץ אחד, ולא באפליקציה חדשה

**לפני ששוקלים אפליקציה שתים־עשרה־ואחת: אם התוכן נכתב כפריט
מתוארך, כמושג או כקטע קצר עם שאלות — הוא לא צריך אפליקציה.**

**וזה כבר נבנה פעם אחת כאפליקציה, וזה היה מיותר.** הענף
`claude/new-exam-app-xkohvq` בנה את ״אזרח״ כאפליקציה שלוש־עשרה
מלאה — `index.html`, `manifest.json`, `sw.js`, ארבעה אייקונים
ורשומה ב-`DATA.APPS` שדחקה את `lomda` החוצה. למחרת `main` הכריע
הפוך, ו-`lomda/data/civics.js` מכסה היום את אותו תוכן בקובץ אחד.

**הענף מיועד למחיקה, ואין לבנות את ״אזרח״ מחדש.** מיזוג שלו
היה נותן אזרחות פעמיים, שלוש־עשרה תיקיות, ומניין שבור מול ארבע
מחרוזות ה-`badge`. אזרחות היא נושא ב-`lomda`, ונקודה.

העותק המקומי נמחק ב-6.9.2026; הרפרנס ב-GitHub עדיין קיים, מפני
ש**סוכן בסביבה הזאת אינו יכול למחוק רפרנס מרוחק** — `git push
--delete` מקבל 403 מהפרוקסי, בשתי התחביריות ובחמישה ניסיונות,
ואין לזה כלי ב-MCP. המחיקה ברשת היא פעולה של הבעלים.

**מה שהיה בו ושווה משהו כבר כאן.** 72 ההגדרות שנכתבו שם — ההסבר
שנפתח אחרי כל מענה — עברו ל-`lomda/data/civics.js` כארבעה נושאים
שמתחילים ב-`ez-`. ההעברה אומתה מחרוזת־מחרוזת לפני המחיקה: 288
מתוך 288 (72 פריטים × ארבע שפות) נמצאו בקובץ מילה במילה. גם
תיקון ה-`pure` שנולד שם נמצא ב-`main` בארבע האפליקציות.

מה שלא עבר ואינו קיים עוד: 72 נוסחי השאלות, 288 האפשרויות
הכתובות, 72 הרמזים, ושלד האפליקציה עם המבחן המדומה ובנק הטעויות.
זו הייתה החלטה — המנוע כאן בונה שאלה ומסיחים בעצמו — ולא תאונה.
קצה הענף היה `c07f253`, ומי שיצטרך אותו יחפש אותו שם.

`lomda/` היא אותו מנוע של משפחת החידון, אבל התוכן שלה אינו בתוך
`index.html`: הוא יושב ב-`lomda/data/*.js`, קובץ אחד לכל תחום,
ונטען בתגיות `<script>` רגילות — הדגם של `bagrut-806`.

הסכימה מוגדרת ב-`lomda/data/schema.js`: `E(שנה, כותרת, מה, סיבה,
תוצאה)` לפריט מתוארך, `C(מושג, הגדרה)`, `Sc(שורות, שאלות)` לקטע,
`Q(שאלה, תשובה, [מסיחים], [למה])` לשאלה, `TOPIC(...)` ו-
`BANK({track, topics})`. קובץ תוכן אינו מכיר את המנוע ואינו נוגע בו.

נושא חדש = שלוש נגיעות, ואפס שורות מנוע:

1. קובץ ב-`lomda/data/` שקורא ל-`BANK(...)`
2. תגית `<script src="data/<שם>.js">` ב-`index.html` — **לפני
   הסקריפט של המנוע**, אחרת סעיף 7 רץ כשעדיין אין `window.BANKS`,
   `TRACKS` ו-`TOPICS` יוצאים ריקים, ו**אין שגיאה**: המסך פשוט
   ריק מנושאים. זה נשבר בפועל בבנייה הראשונה.
3. אותו נתיב ב-`PRE` שב-`lomda/sw.js`, אחרת הנושא לא יעבוד אופליין

ואחריהן מעלים את הגרסה בשני המקומות, כמו בכל שינוי.
`node .claude/qa/banks.js` בודק את שלושתן, ואת שלמות ארבע השפות
בכל שדה של כל פריט.

**נגיעה רביעית, ורק אם רוצים:** הטקסט בדף הבית שמונה את התחומים.
**שם הכרטיס כבר אינו מונה אותם** — הוא ״לומדה — לימוד לפי נושא״,
וזה נקבע כשהנושא הרביעי נכנס: שם שמונה תחומים מתיישן בכל נושא,
ושלושה נושאים הספיקו כדי להראות את זה. מה שעדיין מונה הוא תיאור
הכרטיס וארבע מחרוזות ה-`lead`, ועדכון שלהם מחייב גם את מפתח הקאש
של דף הבית. הנושא עצמו עובד גם בלעדיהם — זה טקסט שיווקי, לא חיווט.

**ה-`sw.js` של `lomda` שונה מעשר האחרות בכוונה** — ה-`PRE` שלה
מונה גם את קובצי התוכן. לכן היא אינה נכללת בהשוואת ה-`sw.js`
ב-`engine.js`, בדיוק כמו `bagrut-806` שמצרף KaTeX. שלושת בלוקי
ה-`index.html` שם — האונבורדינג, המבחן הכיתתי ושער התנאים —
נבדקים בה כן, והם זהים.


## ג׳וש — מוח אחד, ואין שני

״עזרה מהמורה״ יושב ב-**`tutor-api/worker.js`** — האישיות, שלושה־עשר
התפקידים לפי אפליקציה, ארבע השפות, הבדיקה שהתשובה אינה מסגירה את
הפתרון (`revealsAnswer`), בדיקת המשוואות (`badEquation`) והתקרות
(`LIM`). `tutor/tutor.js` הוא הממשק שכבר מחווט בכל האפליקציות,
ו-`tutor-api/local/server.js` מריץ את ה-Worker מקומית — **מייבא
אותו, לא מעתיק אותו.** אין תלות ואין `npm install`.

**רפו שני לג׳וש נבנה, ונסגר ב-10.9.2026 בהחלטת הבעלים.**
`lagstein1-png/josh-server` החזיק `server.js` עצמאי עם אישיות משלו,
`express`+`cors`+`dotenv`, ומזהי אפליקציה אחרים (`math_806` מול
`math-app`). שני ג׳ושים אינם שני שרתים אלא שתי אישיויות שמתפצלות:
תיקון באחד אינו מגיע לשני, ו-`tutor/tutor.js` מצביע ממילא לכתובת
אחת. **אין לבנות אותו מחדש, ואין להעתיק את `worker.js` לרפו אחר.**
צריך פריסה — Cloudflare Workers, `tutor-api/README.md`.

**והרפו עצמו נמחק** — הבעלים הורה ב-10.9.2026 (״תמחק אותו, אני
לא צריך אותו״) וביצע. אין לסוכן בסביבה הזאת כלי למחוק רפו מרוחק:
נבדק ב-MCP של GitHub, ויש שם `create_repository`, `fork_repository`
ו-`delete_file` — אין `delete_repository`. המחיקה היא פעולת הבעלים
ב-GitHub Settings, כמו הרפרנס של ״אזרח״.

**המחיקה אומתה בשתי מדידות ב-10.9.2026**, ולא נלקחה כהצהרה:
`git ls-remote` על הכתובת נדחה בבקשת אימות, אחרי שקלונינג אנונימי
של אותה כתובת הצליח באותו יום; ו-`search_repositories` על
`user:lagstein1-png josh` החזיר `total_count: 0`.

**מה שאבד איתו, וטוב שכך:** `server.js` שלא רץ בכלל —
`node --check` החזיר `SyntaxError: Unexpected token '.'`, מפני
שהשורה הראשונה הייתה `.require('dotenv').config();`.

זה בדיוק התקדים של ״אזרח״ שכתוב סעיף אחד למעלה, ולכן הוא נרשם
כאן ולא רק ב-`FINDINGS.md`: מי שפותח את הקובץ הזה לא ידע עד היום
שג׳וש בכלל קיים, וזו הייתה הסיבה שנבנה שני.

**מה שנמדד בסגירה, על `b23a01c`:** `node tutor-api/local/server.js`
עולה בלי מפתח ואומר זאת, `/health` מחזיר את שלוש־עשרה האפליקציות
ואת התקרות, `/josh` מחזיר 200, ו-`/tutor/tutor.js` מוגש עם הכתובת
מוזרקת בזמן ההגשה בלבד — הקובץ שעל הדיסק נשאר עם `API = ""`, ולכן
`node .claude/qa/tutor.js` ממשיך לראות את ג׳וש כבוי, כפי שצריך.

## אפליקציית התאוריה

**״תאוריה מדברת״** מופיעה בדף הבית ומצביעה לריפו נפרד תחת אותו
חשבון. התיקייה אינה נמצאת בריפו הזה, וזה תקין. המזהה שלה
ב-`DATA.APPS` הוא `theory`, וכך גם מפתח ה-`ICONS` ורשימת
`EXTERNAL` ב-`apps.js`.

**השם האנגלי הישן אסור, ונמחק מהמאגר** — חשש לזכויות יוצרים
וסימן מסחר (3.9.2026), וההחלטה הורחבה למחיקה מלאה ב-4.9.2026.
אין שם חלופי ואין להמציא אחד.

**נשאר מופע חוקי אחד:** השדה `u` ברשומת האפליקציה שב-`DATA.APPS`,
שהוא **נתיב ולא שם**. הכרטיס בונה את הקישור כ-`a.u || "/"+a.id+"/"`,
וכתובת הפרסום נקבעת בשם הריפו הנפרד — GitHub Pages מפרסם פרויקט
תחת שם הריפו — ולכן היא אינה ניתנת לשינוי מכאן. שינוי שם הריפו
ל-`theory` יאפשר למחוק גם את השדה הזה.

`node .claude/qa/naming.js` אוכף את הכלל על כל המאגר, בכל אות.
הפירוט המלא נמצא ב-`NAMING.md`.
