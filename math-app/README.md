# חשבון ליסודי — ארבע פעולות החשבון

חיבור, חיסור, כפל וחילוק לכיתות א׳–ד׳, צעד אחר צעד, בלי לחץ של
זמן. שני רמזים לפני התשובה והסבר אחריה — הלומד שקורא לאט מקבל
דרך פנימה במקום ניחוש.

**מזהה:** `math-app` · **שלב:** `public` · **קובץ:** `index.html`, 4,368 שורות

## התוכן, כפי שנמדד

| | |
|---|---|
| שאלות שנבדקו | 3,600 |
| נושאים × רמות | 4 × 6 = 24 תאים |
| שפות | עברית, ערבית, רוסית, אנגלית |
| פסק דין | **PASS** — 0 חוסם, 0 לבדיקה |

מקור: `.claude/qa/reports/math-app.json`.

## איך נבנית שאלה — מחולל, לא מאגר

השאלה **מחושבת בזמן ריצה** מפרמטרים שהוגרלו, ולכן הווריאציה
אינסופית ואין תשובה שמאוחסנת. זה מה שמצדיק אפליקציה נפרדת ולא
נושא ב-`lomda` — הקריטריון המלא ב-`CLAUDE.md`, ״מה מצדיק אפליקציה״.

**הסכימה כאן שונה משאר משפחת המתמטיקה:** `hint1` / `hint2` /
`explain`, עם מצייני מיקום `{a}`, `{b}`, `{ans}` וסיומת לכל פעולה
(`hint1Add`, `hint2Sub`). אין כאן `steps` ואין `wrong` — פותחים
את הקובץ ומחקים את המבנה שלו בדיוק.

## מצב מורה

מבחן כיתתי שנארז כ**זרע בקישור**, לא כעשרים שאלות. התלמיד בונה
מהזרע את אותן שאלות אצלו, ולכן כל דבר שנכנס לבחירת השאלה חייב
להיות זהה בשני הצדדים.

## מפתחות `localStorage`

| מפתח | מה |
|---|---|
| `mlh-state-v2` | התקדמות הלומד |
| `shlav-exams-math-app-v1` | מבחנים שנבנו כאן |
| `shlav-exams-v1` | הישן, לקריאה בלבד |
| `mathapp-teacher` | מצב מורה |

**כל אפליקציה ומפתח משלה.** עד 31.8 כולן חלקו את `shlav-exams-v1`,
ומבחן שנבנה באחת הופיע באחרת. `node .claude/qa/storage.js` תופס
העתקה כזאת.

## בדיקות שנוגעות לה

    node .claude/qa/serve.js          # שרת מקומי, 127.0.0.1:8099
    node .claude/qa/smoke.js math-app    # דפדפן אמיתי — שגיאות JS
    node .claude/qa/cache.js          # גרסה מול מפתח קאש
    node .claude/qa/storage.js        # מפתח localStorage אחד לאפליקציה
    node .claude/qa/all.js            # הכול, ופסק דין אחד

## PWA — מה שהופך אותה לאפליקציה

`manifest.json` (‎3‎ אייקונים, `display: standalone`, `lang: he`, `dir: rtl`)
ו-`sw.js` שמצרף מראש **5 נכסים**. אחרי טעינה ראשונה היא עובדת
בלי אינטרנט, ואפשר להתקין אותה על מסך הבית.

**המטמון שלה הוא `math-app-` ושלה בלבד.** שנים־עשר קובצי ה-`sw.js`
מצרפים את אותם `/legal/terms.js` ו-`/legal/protect.js`, כל אחד
למטמון משלו — ולכן נגיעה ב-`legal/` מחייבת להעלות את **כל**
המפתחות יחד. ההסבר המלא ב-`CLAUDE.md`, ״מלכודת הקאש של `legal/`״.

## גרסה — שתי מחרוזות, לא אחת

    var BUILD="b69 · 2026-09-10";
    navigator.serviceWorker.register("sw.js?v=b69-pwa1");

`sw.js` גוזר את שם המטמון מ-`?v=` שבכתובת הרישום, **לא** מ-`BUILD`.
עדכון של אחד בלי השני משאיר את הלומד עם הקוד הישן.
`node .claude/qa/cache.js` נופל על אי־התאמה, וגם על מפתח שכבר
קיים ב-`origin/main` עם תוכן אחר.

## ״עזרה מהמורה״ (ג׳וש)

מחווטת בארבע נקודות: תגית `<script src="/tutor/tutor.js">`,
`TUTOR.mount(...)`, קריאת `TUTOR.open()` מהנתב, והנתיב ב-`PRE`
שב-`sw.js`. **הכפתור אינו מוצג** כל עוד `API` ריק ב-`tutor/tutor.js`;
ההפעלה היא `node .claude/qa/enable-tutor.js <כתובת>`. הצד השרתי
כולו ב-`tutor-api/worker.js`, והתפקיד של האפליקציה הזאת נמצא שם
ב-`ROLE`.

## מה אין כאן

אין `npm install`, אין build step, אין framework ואין תלות חיצונית.


---

*נוצר מקריאה בקוד ובדוחות ה-QA, 10.9.2026. כל מספר כאן נמדד בפקודה — אין הערכות.*
