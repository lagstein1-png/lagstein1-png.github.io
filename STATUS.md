# תמונת מצב — אפליקציות

הקובץ הזה נקרא בתחילת כל סשן, לפני שמתחילים עבודה. הוא קיים כדי
ששני סשנים לא יבנו את אותו דבר פעמיים, וכדי שלא יישאלו שוב שאלות
שהתשובה להן כבר נמדדה.

**כל מספר כאן נקרא מקובץ או מפלט של פקודה.** בסוף הקובץ יש נספח
עם הפקודות המדויקות ותאריך ההרצה. מספר בלי מקור אינו נכנס לכאן.

---

## מצב האישור — עדיין לא מוגדר

יש מצב בשם **APPROVED** שאליו כל האפליקציות אמורות להגיע.
**ההגדרה שלו אינה קיימת בריפו הזה, ואינה מומצאת כאן.**

עד שההגדרה תגיע, כל שורה בטבלה נושאת מצב מתוך שלושת אלה בלבד:

| מצב | פירושו |
|---|---|
| `נמדד` | הבדיקות הקיימות רצו עליה והתוצאה רשומה למטה |
| `לא נמדד` | היא בריפו, ואין לה כיסוי בכלי הבדיקה הקיימים |
| `מחוץ להישג` | אינה בריפו הזה, ואי אפשר לבדוק אותה מכאן |

**אף אפליקציה אינה מסומנת `APPROVED`, וגם לא תסומן** עד שיוגדר מה
בדיוק נדרש כדי לעבור. `נמדד` אינו `APPROVED`: הוא אומר רק שהבדיקות
שיש רצו ולא מצאו כלום, ולא שהבדיקות שיש מספיקות.

כשההגדרה תגיע — היא נכתבת כאן, בסעיף הזה, כרשימת תנאים שאפשר
לבדוק אותם. אז ורק אז נוסף עמוד `APPROVED` לטבלה.

---

## אחת־עשרה האפליקציות שנמצאות בפועל

המקור למניין הוא `DATA.APPS` שב-`index.html` שבשורש — אחד־עשר
מזהים, ולהם אחד־עשר סמלים תואמים ב-`ICONS`. כל כרטיס בונה את
הקישור שלו כ-`href="/"+a.id+"/"`.

`voice/` ו-`legal/` אינן ברשימה הזאת ואינן אפליקציות לימוד: הראשונה
היא דף אבחון של מנוע ההקראה (`<title>בדיקת קול</title>`), השנייה
היא עמוד תנאי השימוש.

### 1. מתמטיקה לחכמים שמתקשים

- **נתיב:** `/math-app/` · `math-app/index.html` (4,263 שורות)
- **מצב טכני:** `נמדד`. `BUILD="b40 · 2026-09-02"`, מפתח מטמון
  `?v=b40-pwa1` — תואמים. ארבע שפות ממשק: `he/en/ru/ar`.
  מצב מורה: קיים.
- **מצב תוכן:** 4 נושאים × 6 רמות, נוצרים במחולל.
- **בדיקות שיש לה:** `parse`, `cache`, `smoke`, `entropy`,
  `options`, `exam`, `eslint`.
- **תוצאה:** smoke נקי (18 פקדים). entropy: 12,000 שאלות,
  `noAns=0 multi=0 dupe=0 nan=0`, אין רמה בעלת תשובה קבועה.
  options: 14,400 שאלות, `under4=0`. exam ✓.
- **בעיות פתוחות שנמצאו:** אין.
- **מה חסר:** לא נמצא פער מול שאר האפליקציות. eslint מדווח שלושה
  ממצאים (`tDesc`, `id`, `nm`) — כולם ברשימת קו הבסיס
  ב-`.claude/qa/README.md` ומתועדים שם כשפירים.

### 2. שלב — מתמטיקה לתיכון

- **נתיב:** `/math-teen/` · `math-teen/index.html` (6,260 שורות)
- **מצב טכני:** `נמדד`. `BUILD="t40 · 2026-09-02"` = `?v=t40-pwa1`.
  **שתי שפות ממשק בלבד: `he/ar`.** מצב מורה: קיים.
- **מצב תוכן:** 18 נושאים × 4 רמות, במחולל.
- **בדיקות שיש לה:** כמו 1.
- **תוצאה:** smoke נקי (18 פקדים). entropy: 36,000 שאלות, הכול אפס.
  options: 43,200 שאלות, `under4=0`. exam ✓.
- **בעיות פתוחות שנמצאו:** אין.
- **מה חסר:** רוסית ואנגלית אינן בה. `marketing/facts.md` מתאר
  את זה כמצב מוכר ולא כתקלה, ולכן זה **פער מתועד ולא באג**.
  eslint: שישה ממצאים, כולם בקו הבסיס.

### 3. אקסיומה — תואר, שנה א׳

- **נתיב:** `/math-uni/` · `math-uni/index.html` (13,723 שורות —
  הקובץ הגדול בריפו)
- **מצב טכני:** `נמדד`. `BUILD="u38 · 2026-09-02"` = `?v=u38-pwa1`.
  ארבע שפות. מצב מורה: קיים.
- **מצב תוכן:** 23 נושאים × 4 רמות, במחולל.
- **בדיקות שיש לה:** כמו 1.
- **תוצאה:** smoke נקי. entropy: 46,000 שאלות, הכול אפס.
  options: 55,200 שאלות, `under4=1200 (2.2%)` — שתי שורות:
  `lineq L2` ו-`logic L1`, שתיהן 100%. exam ✓.
- **בעיות פתוחות שנמצאו:** אין. שתי השורות האלה **מתועדות
  כשפירות** ב-`.claude/qa/README.md`: שתיהן שאלות שיש להן שלוש
  תשובות אפשריות בסך הכול. השורה השלישית שברשימה, `defint L2`,
  לא הופיעה בהרצה הזאת — גם זה מתועד שם כתקין, כי היא נדירה
  מכדי להופיע בכל הגרלה.
- **מה חסר:** לא נמצא פער. eslint: `qq` ו-`id`, שניהם בקו הבסיס.

### 4. אקסיומה ב׳ — שנה שנייה

- **נתיב:** `/math-uni2/` · `math-uni2/index.html` (11,937 שורות)
- **מצב טכני:** `נמדד`. `BUILD="v37 · 2026-09-02"` = `?v=v37-pwa1`.
  ארבע שפות. מצב מורה: קיים.
- **מצב תוכן:** 18 נושאים × 4 רמות.
- **תוצאה:** smoke נקי. entropy: 36,000, הכול אפס.
  options: 43,200, `under4=0`. exam ✓.
- **בעיות פתוחות שנמצאו:** אין.
- **מה חסר:** לא נמצא פער. eslint: `id` בלבד, בקו הבסיס.

### 5. אקסיומה ג׳ — שנה שלישית

- **נתיב:** `/math-uni3/` · `math-uni3/index.html` (11,126 שורות)
- **מצב טכני:** `נמדד`. `BUILD="g36 · 2026-09-02"` = `?v=g36-pwa1`.
  ארבע שפות. מצב מורה: קיים.
- **מצב תוכן:** 16 נושאים × 4 רמות.
- **תוצאה:** smoke נקי. entropy: 32,000, הכול אפס.
  options: 38,400, `under4=0`. exam ✓.
- **בעיות פתוחות שנמצאו:** אין.
- **מה חסר:** לא נמצא פער. eslint: `id` בלבד, בקו הבסיס.

### 6. 806 — בגרות במתמטיקה, 5 יח״ל

- **נתיב:** `/bagrut-806/`. **היחידה שאינה קובץ אחד:**
  `index.html` (473 שורות), `app.js` (1,066), `speech.js`,
  `data/exams.js`, ו-`vendor/katex/` מקומי.
- **מצב טכני:** `נמדד`. `BUILD = "x8 · 2026-09-03"` יושב
  ב-`app.js:16`, ומפתח המטמון `?v=x8-pwa1` — תואמים.
  **אין `var LANGS`: עברית בלבד.** מצב מורה: אין.
- **מצב תוכן:** נכתב ביד ולא במחולל. שתי בחינות, שש שאלות,
  שנים־עשר סעיפים.
- **בדיקות שיש לה:** `parse`, `cache`, `smoke`, `exam806`.
  **`entropy`, `options` ו-`exam` אינן חלות עליה** — אין מחולל
  ואין בונה מבחן.
- **תוצאה:** smoke נקי (4 פקדים). exam806: 2 בחינות, 0 ממצאים.
- **בעיות פתוחות שנמצאו:** אין באג. אבל **שתי הבחינות מסומנות
  `season:"הדגמה"`** (`data/exams.js:64` ו-`:237`), 60 ו-64
  נקודות — כלומר אין בה תוכן שמתיימר להיות שאלון אמיתי, וזו
  החלטה מכוונת שמתועדת ב-`CHANGELOG.md`.
- **מה חסר:** תוכן מעבר לשתי בחינות ההדגמה. כמה, ומאיזה מקור —
  **החלטה שאינה בריפו.**

### 7. אולפן — עברית מעשית לעולים חדשים

- **נתיב:** `/ulpan/` · `ulpan/index.html` (4,592 שורות)
- **מצב טכני:** `נמדד`. `BUILD="a4 · 2026-09-02"` = `?v=a4-pwa1`.
  ארבע שפות. מצב מורה: קיים. **החדשה בריפו** — מספר גרסה `a4`
  מול `b40`/`t40` באחרות.
- **מצב תוכן:** 12 נושאים × 3 רמות.
- **בדיקות שיש לה:** כמו 1.
- **תוצאה:** smoke נקי (16 פקדים). entropy: 18,000, הכול אפס.
  options: 21,600, `under4=0`. exam ✓.
- **בעיות פתוחות שנמצאו:** **זניח** — eslint מדווח `'id' is
  already defined` (`ulpan/index.html`, בבלוק בונה המבחן), וזה
  בדיוק אותו דפוס שמתועד כשפיר בשבע אפליקציות אחרות, אבל
  **אולפן אינה ברשימה של אותה שורה** ב-`.claude/qa/README.md`.
  קו הבסיס המתועד מפגר אחרי הקוד, לא הקוד אחרי הבדיקה.
- **מה חסר:** לא נמצא פער תוכן.

### 8. ניב — אנגלית מדוברת וספרותית

- **נתיב:** `/english/` · `english/index.html` (4,880 שורות)
- **מצב טכני:** `נמדד`. `BUILD="n34 · 2026-09-02"` = `?v=n34-pwa1`.
  ארבע שפות. מצב מורה: קיים.
- **מצב תוכן:** 15 נושאים × 3 רמות.
- **תוצאה:** smoke נקי. entropy: 22,500, הכול אפס.
  options: 27,000, `under4=0`. exam ✓.
- **בעיות פתוחות שנמצאו:** אין. **eslint מחזיר אפס ממצאים** —
  שני הממצאים שקו הבסיס מייחס לה (`id`, `lg`) אינם קיימים עוד.
- **מה חסר:** לא נמצא פער.

### 9. מפנה — היסטוריה לחטיבה

- **נתיב:** `/history/` · `history/index.html` (5,818 שורות)
- **מצב טכני:** `נמדד`. `BUILD="m36 · 2026-09-02"` = `?v=m36-pwa1`.
  ארבע שפות. מצב מורה: קיים.
- **מצב תוכן:** 12 נושאים × 3 רמות.
- **תוצאה:** smoke נקי (16 פקדים). entropy: 18,000, הכול אפס.
  options: 21,600, `under4=0`. exam ✓.
- **בעיות פתוחות שנמצאו:** אין. **eslint מחזיר אפס ממצאים**,
  כמו ב״ניב״ — קו הבסיס מייחס לה `id` ו-`lg` שאינם שם עוד.
- **מה חסר:** לא נמצא פער.

### 10. מקריא קולי

- **נתיב:** `/reader/` · `reader/index.html` (1,709 שורות)
- **מצב טכני:** `נמדד`. **אין `BUILD`** — נושא מפתח מטמון בלבד,
  `?v=19-pwa1`. זה תקין ומתועד: אין לו פוטר שמציג גרסה, והמנגנון
  תלוי אך ורק ב-`?v=`. שתי שפות: `he/ar`. מצב מורה: אין.
- **מצב תוכן:** אין מאגר שאלות. כלי: מדביקים טקסט והוא נקרא בקול.
- **בדיקות שיש לה:** `parse`, `cache`, `smoke`, `eslint`.
  **`entropy`, `options`, `exam` ו-`exam806` אינן חלות** — אין
  תוכן להגריל.
- **תוצאה:** smoke נקי (5 פקדים).
- **בעיות פתוחות שנמצאו:** אין. eslint: `stopSpeak`, בקו הבסיס.
- **מה חסר:** לא נמצא פער. **אבל שים לב:** מה שבודק אותו בפועל
  הוא smoke בלבד, ואיכות ההקראה עצמה אינה נמדדת בשום כלי.

### 11. תאוריה מדברת

- **נתיב:** `/drivewise/` — הכרטיס בדף הבית בונה אותו מהמזהה.
- **מצב:** `מחוץ להישג`. **התיקייה אינה בריפו הזה.** התוכן
  מתפרסם מריפו נפרד, `lagstein1-png/drivewise`, שאינו בהיקף
  הגישה של הסשן. הרשת החיצונית חסומה בסביבת הפיתוח, ולכן גם
  לא ניתן לפתוח את הכתובת ולראות אם היא נטענת.
- **בדיקות שיש לה כאן:** אין. אף כלי בדיקה בריפו הזה אינו נוגע בה.
- **בעיות פתוחות:** אין לי גישה לנתון הזה.
- **מה חסר:** גישה לריפו `lagstein1-png/drivewise`.
  **״DriveWise״ אסור כשם מותג** (3.9.2026); המילה באות קטנה
  שנשארת ב-`id` וב-`/drivewise/` היא נתיב ולא שם. הפירוט
  ב-`NAMING.md`.

---

## סיכום הבעיות הפתוחות שנמצאו בפועל

| חומרה | ממצא | איפה |
|---|---|---|
| קריטי | — לא נמצא — | |
| חשוב | תוכן 806 כולו `"הדגמה"`, שתי בחינות | `bagrut-806/data/exams.js` |
| חשוב | ״תאוריה מדברת״ אינה ניתנת לבדיקה מכאן | ריפו נפרד |
| זניח | קו הבסיס של eslint מפגר: english ו-history נקיים היום, ואולפן חסרה בשורת `id` | `.claude/qa/README.md` |
| זניח | `extract.js` מחזיר קובץ ריק ל-806, ולכן מסלול ה-eslint המתועד מדלג עליה. לינט ישיר על `app.js` — נקי | `.claude/qa/extract.js` |

**קו הבסיס של כל הכלים הוא אפס, וכולם החזירו אפס.** `cache.js`:
11 קובצי `sw.js`, 0 ממצאים. `parse.js`: עובר. `smoke.js`: 13 דפים,
כולם נקיים. אין אף באג פתוח שנמצא במדידה.

---

## מקום לעשר האפליקציות הנוספות

היעד הוא **21** אפליקציות. אחת־עשרה מהן מתועדות למעלה; **עשר
חסרות, ואינן מומצאות כאן.** השורות שמורות ריקות בכוונה:

| # | שם | נתיב | מצב |
|---|---|---|---|
| 12 | — ממתין לרשימה — | | |
| 13 | — ממתין לרשימה — | | |
| 14 | — ממתין לרשימה — | | |
| 15 | — ממתין לרשימה — | | |
| 16 | — ממתין לרשימה — | | |
| 17 | — ממתין לרשימה — | | |
| 18 | — ממתין לרשימה — | | |
| 19 | — ממתין לרשימה — | | |
| 20 | — ממתין לרשימה — | | |
| 21 | — ממתין לרשימה — | | |

**כשאפליקציה נכנסת לשורה כזאת** — היא אינה קובץ אחד. `CLAUDE.md`
מונה שבעה מקומות שצריך לגעת בהם, ובכללם ארבע מחרוזות ה-`badge`
שסופרות את האפליקציות **כמילה ולא כספרה**. היום כתוב שם
״אחת־עשרה / أحد عشر / Одиннадцать / Eleven״, וזה אינו נגזר
מ-`DATA.APPS`.

---

## נספח: מאיפה כל מספר כאן

הכול נמדד ב-3.9.2026, על `claude/app-status-assessment-tb53zj`
שהיה באותו רגע **זהה ל-`origin/main`**
(`git rev-list --left-right --count origin/main...HEAD` → `0 0`),
כלומר על הקוד שב-`main`.

| מה | מאיפה |
|---|---|
| מניין האפליקציות והמזהים | `grep` על `DATA.APPS` ועל `ICONS` ב-`index.html` |
| `BUILD` ומפתחות המטמון | `node .claude/qa/cache.js` |
| מספר השורות בכל קובץ | `wc -l` |
| שפות ממשק | `Object.keys(LANGS)` בכרומיום מול השרת המקומי |
| מספר נושאים ורמות | `TOPICS.length` ו-`LVL.length`, באותה הרצה |
| מצב מורה | `grep -c "מצב מורה\|teacherMode\|TEACHER"` |
| שאלות שנבדקו, `noAns/multi/dupe/nan` | `node .claude/qa/entropy.js <app…>` |
| `under4` | `node .claude/qa/options.js <app…>` |
| בונה המבחן | `node .claude/qa/exam.js` |
| תוכן 806 | `node .claude/qa/exam806.js` |
| שגיאות JS ופקדים | `node .claude/qa/smoke.js <page…>` |
| ממצאי eslint | `extract.js` ואז `npx eslint`, ועל `app.js` של 806 — לינט ישיר |

**מה שלא נמדד, ואסור להציג כאילו כן:** אף בדיקה כאן לא רצה מול
`lagstein1-png.github.io` — הרשת החיצונית חסומה בסביבת הפיתוח.
הכול רץ מול `127.0.0.1:8099`. בדיקה מקומית אינה בדיקה חיה.
