# תמונת מצב — אפליקציות

הקובץ הזה נקרא בתחילת סשן, כדי ששני סשנים לא יבנו את אותו דבר
פעמיים. זה קרה בפועל יותר מפעם אחת, והפירוט ב-`CLAUDE.md`.

**המספרים כאן אינם נכתבים ביד.** גרסה ל-2.9 נכתבה ביד, ותוך יומיים
כל שבעת מספרי הגרסה שבה היו שגויים — האפליקציות זזו בין ארבע לשבע
גרסאות. מסמך שסשן מונחה לסמוך עליו, ושכל מספר בו לא נכון, גרוע
ממסמך שאינו קיים. לכן המדידה עברה למחולל, והמסמך מחזיק רק את מה
שאינו נמדד.

## המצב הנמדד — מרעננים לפני שקוראים

    node .claude/qa/status.js          גרסה ומפתח קאש לכל אפליקציה
    node .claude/qa/all.js             כל הבדיקות, פסק דין אחד
    node .claude/qa/apps.js            שבעת המקומות של אפליקציה חדשה

הטבלה למטה היא פלט של `node .claude/qa/status.js --md`. **אם עברו
שעות מאז — הריצו אותו שוב במקום להאמין לה.**

| אפליקציה | BUILD | מפתח קאש | תואם |
|---|---|---|---|
| `math-app` | b75 · 2026-09-12 | b75-pwa1 | תואם |
| `math-teen` | t75 · 2026-09-12 | t75-pwa1 | תואם |
| `math-uni` | u76 · 2026-09-12 | u76-pwa1 | תואם |
| `math-uni2` | v74 · 2026-09-12 | v74-pwa1 | תואם |
| `math-uni3` | g74 · 2026-09-12 | g74-pwa1 | תואם |
| `bagrut-806` | x36 · 2026-09-11 | x36-pwa1 | תואם |
| `ulpan` | a36 · 2026-09-12 | a36-pwa1 | תואם |
| `english` | n67 · 2026-09-12 | n67-pwa1 | תואם |
| `history` | m72 · 2026-09-12 | m72-pwa1 | תואם |
| `lomda` | l55 · 2026-09-12 | l55-pwa1 | תואם |
| `reader` | — | 44-pwa1 | מפתח בלבד |
| `kotvim` | k11 · 2026-09-12 | k11-pwa1 | תואם |
| `theory` | — | — | אינה בריפו הזה |

נוצר ב-`node .claude/qa/status.js --md`, 2026-09-12.


## מסירה — סוף סשן 10.9.2026

**המצב בטוח:** `main` והענף זהים, עץ העבודה נקי.
מספרי הבדיקות למטה הם פלט של `all.js` בסשן הזה, לא זיכרון.

### מה הושלם היום

- **GoatCounter הוסר מכל הדפים** — החלטת הבעלים, וההחרגה
  ב-`CLAUDE.md` בוטלה איתה. `grep -rl 'zgo.at' --include="*.html"`:
  15 לפני, אפס אחרי. ירדו גם המשפט ״האתר סופר כניסות״
  מ-`legal/terms.js` וההחרגה עצמה. שלושה־עשר מפתחות קאש עלו יחד,
  כי `legal/` השתנה. אומת בדפדפן: אפס בקשות יוצאות ל-`zgo.at`.
- **שער סיום** — `.claude/hooks/qa-gate.sh` כ-`Stop` hook, מריץ
  `all.js --static` וחוסם סגירת סשן על בדיקה שנפלה. `--static`
  הוא דגל ב-`all.js` עצמו, ולכן אינו מתיישן כשנוספת בדיקה.
- **שני הוקי סחיפה** — `main-drift.sh` בפתיחת סשן ולפני כל
  `git commit`. **אינם חוסמים.** נעילת קבצים חוסמת נשקלה ונפסלה.
- **הבאג ב-`math-uni`** — `x − undefined` בשני צעדי פתרון,
  ב-211 מתוך 400 הגרלות ב-`logs` רמה 4. ואיתו `nanTxt`
  ב-`entropy.js`, הפרצה שהסתירה אותו.
- **סכימת התוכן בתיעוד תוקנה** — `CLAUDE.md` ו-`maayan.md` לימדו
  `{q, a, options, hint}`, שאינה קיימת בקוד. הכתיבה היא `Q(...)`.
- **O-28 נסגר בטבלה** — הוא היה סגור בקוד ופתוח ברשימה.

### מה נשאר לעשות

1. **`theory/buy.html:56`** — `FORM_URL=""` עדיין ריק, וסוגר את
   O-10. **חסום על הבעלים:** דרושה כתובת הטופס מ-Google Forms
   כולל `?embedded=true`. כל עוד היא ריקה הדף מציג ״בקרוב״ ושום
   דבר לא נשבר — נבדק, לא מנחש.
2. **חמישה תאים ב-`math-uni`** שהאורך עדיין נושא בהם מידע, כולם
   מתחת לסף של `guess.js` ולכן הוא עובר עליהם. הם לא נבדקו
   ונמצאו תקינים — הם פשוט לא טופלו.
3. **`kotvim` בשלב `internal`** — `stages.json` מציין `bank: none`
   ו-`contentQA: not-run`. כדי לצאת נדרשים `entropy`, `options`
   ו-`exam`, ואין בה `buildQ`. ייתכן שהשלב צריך הגדרה לאפליקציה
   בלי בנק שאלות.
4. **19 ממצאים פתוחים ב-`FINDINGS.md`**, ובראשם O-22 (32 הגדרות
   מדעיות שנוסחו בידי כותבת ולא בידי מדען) ו-O-32 (65 אפשרויות
   ארוכות מדי בלומדה בשלוש השפות הזרות).

### בעיות ידועות

- **הרשת חסומה בסביבה.** אי אפשר לבדוק את האתר החי. ״`pages build
  and deployment` הצליח״ אינו ״האתר עובד״.
- **דוחות `qa/reports/` זזים בכל ריצה** — `content.js` מגריל, ולכן
  האחוזים משתנים והדיף אינו ריק. מערך הממצאים יציב; האחוזים לא.
- **שני `Stop` hooks רצים בשרשור** — זה של הסביבה (דורש עץ נקי
  ודחוף) וזה של הפרויקט. שער ה-QA נועד להיכשל מהר ולומר בדיוק מה
  נשבר, כי שני חוסמים ששותקים הם הדרך לתקוע סשן. עקיפה: `QA_GATE=0`.

### המלצות לסשן הבא

1. **קרא את `FINDINGS.md` לפני שאתה נוגע במשהו.** רשומת 10.9
   מתעדת באג שתלמידים ראו, ובדיקה שהחזירה אפס בזמן שהמסך היה שבור.
2. **`git fetch origin main` לפני שמתחילים, ושוב לפני ה-commit.**
   `main` זז 204 commits בין שני חלקי הסשן הזה, ופעמיים תוך כדי
   עבודה. `main-drift.sh` מריץ את זה בשבילך עכשיו.
3. **קובץ אחד, סוכן אחד, ברגע אחד.** בסשן הזה `git add` על קובץ
   שסוכן עוד ערך סחף את עבודתו לקומיט זר. השינוי שרד, אבל הוא
   יושב תחת כותרת של נושא אחר.
4. **בדיקה חדשה — הוכח שהיא נופלת לפני שאתה סומך עליה.** `nanTxt`
   הוכח כך: עם התיקון מוסר הוא מחזיר 268, ואיתו 0.

## מה המחולל לא יודע

שלושה דברים אינם נמדדים בשום כלי, וזה מה שהמסמך הזה קיים בשבילו:

- **`reader` ו-`theory` אינן חריגות.** ל-`reader` אין `BUILD` מפני
  שאין לה פוטר שמציג גרסה, והיא נושאת מפתח קאש בלבד. `theory`
  ("תאוריה מדברת") אינה בריפו הזה כלל — היא ריפו נפרד, ונספרת
  בשתים־עשרה. שתיהן תקינות, ואין מה "לתקן" בהן.
- **איכות ההקראה אינה נמדדת.** `smoke` בודק שאין שגיאת JS, לא
  שהעברית נשמעת נכון. מי שנוגע בהקראה — בודק באוזן.
- **תוכן שנכתב ביד אינו נבדק על נכונות עובדתית.** `entropy`
  ו-`options` בודקים מבנה: שהתשובה אינה קבועה, שיש ארבע אפשרויות,
  שאין שתי תשובות נכונות. שתאריך היסטורי נכון — לא.

## לפני שמתחילים משהו גדול

`git fetch origin main` ולהסתכל ב-log — **וגם שוב לפני ה-commit.**
חלון הבנייה הוא החלון המסוכן ולא הרגע שלפניו; ב-2.9 סשן שכן בדק
בהתחלה איבד את עבודתו לסשן שדחף שלושים וחמש דקות אחריו.

ומספר הגרסה מוקצה **במיזוג**, לא בבנייה: כל סשן מתפצל מ-main, רואה
את אותו מספר, ומסמן את עצמו אחד מעליו. מי שממזג בודק את main ומקצה
מספר טרי.
