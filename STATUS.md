# תמונת מצב — אפליקציות

הקובץ הזה נקרא בתחילת סשן, כדי ששני סשנים לא יבנו את אותו דבר
פעמיים. זה קרה בפועל יותר מפעם אחת, והפירוט ב-`CLAUDE.md`.

**המספרים כאן אינם נכתבים ביד.** גרסה ל-2.9 נכתבה ביד, ותוך יומיים
כל שבעת מספרי הגרסה שבה היו שגויים — האפליקציות זזו בין ארבע לשבע
גרסאות. מסמך שסשן מונחה לסמוך עליו, ושכל מספר בו לא נכון, גרוע
ממסמך שאינו קיים. לכן המדידה עברה למחולל, והמסמך מחזיק רק את מה
שאינו נמדד.

## המצב הנמדד — מרעננים לפני שקוראים

    node .claude/qa/status.js          גרסה ומפתח קאש לכל אפליקציה
    node .claude/qa/all.js             כל הבדיקות, פסק דין אחד
    node .claude/qa/apps.js            שבעת המקומות של אפליקציה חדשה

הטבלה למטה היא פלט של `node .claude/qa/status.js --md`. **אם עברו
שעות מאז — הריצו אותו שוב במקום להאמין לה.**

| אפליקציה | BUILD | מפתח קאש | תואם |
|---|---|---|---|
| `math-app` | b66 · 2026-09-10 | b66-pwa1 | תואם |
| `math-teen` | t66 · 2026-09-10 | t66-pwa1 | תואם |
| `math-uni` | u67 · 2026-09-10 | u67-pwa1 | תואם |
| `math-uni2` | v65 · 2026-09-10 | v65-pwa1 | תואם |
| `math-uni3` | g65 · 2026-09-10 | g65-pwa1 | תואם |
| `bagrut-806` | x31 · 2026-09-10 | x31-pwa1 | תואם |
| `ulpan` | a31 · 2026-09-10 | a31-pwa1 | תואם |
| `english` | n61 · 2026-09-10 | n61-pwa1 | תואם |
| `history` | m66 · 2026-09-10 | m66-pwa1 | תואם |
| `lomda` | l47 · 2026-09-10 | l47-pwa1 | תואם |
| `reader` | — | 37-pwa1 | מפתח בלבד |
| `theory` | — | — | אינה בריפו הזה |

נוצר ב-`node .claude/qa/status.js --md`, 2026-09-10.

## מסירה — סוף סשן 9.9.2026

הבעלים פרש לכמה שעות וביקש עצירה בנקודה בטוחה. **המצב בטוח:**
`main` ו-הענף זהים, עץ העבודה נקי, ו-`all.js` מחזיר 28 בדיקות
כולן עוברות.

### מה הושלם היום

- **״כותבים ביחד״ (`kotvim`)** — אפליקציה חדשה בשלב `internal`.
  תשעה נושאים, שלושה לכל סוג טקסט, 111 קטעים בארבע שפות. תוקנו
  בה ההקראה (`plainOf` חסרה), קוד מת שקרא לסמלים שנמחקו, הדגמה
  שהייתה של אולפן, ו-`TUTOR.q` ששלח סוג ונושא ריקים.
- **`saysym.js`** — בדיקה חדשה ב-`all.js`, ואיתה שלושה באגי הקראה
  אמיתיים בשלוש האוניברסיטאיות: `x → 0⁺` נשמע ״אפס ועוד״, `∫₀^∞`
  איבד את הגבולות, `A₄` הקריא את התו עצמו.
- **`guess.js`** — בדיקה חדשה ב-`all.js`, ואיתה **שמונה תאים
  שאפשר היה לפתור בלי לדעת את החומר** בכך שבוחרים תמיד את
  התשובה הארוכה או הקצרה ביותר. שלושה מהם היו ב-100%. אחרי:
  316 תאים, אין תא שאורך התשובה או מיקומה מסגירים אותה.
- **`content.js` הורחב** — הוא בדק את `q.ask` בלבד וברמה 1
  בלבד, ולכן 21 מחרוזות `why` חדשות נפלו לעברית בערבית, ברוסית
  ובאנגלית בלי שהדוח יאמר מילה. סורק עכשיו שאלה, אפשרות ונימוק,
  בכל ארבע הרמות.
- **`kotvim.js`** — התוכן של ״כותבים ביחד״ נמדד לראשונה.
- **O-18 אומת סגור** — `leaks.js` מחזיר 0 דליפות ב-645 פריטי `C`.
- **ROADMAP** — ״מפתח״ ו״מרשם״ נסגרו בהחלטת הבעלים. לא נמחק דבר:
  שתיהן היו הצעות שמעולם לא נבנו.

### מה נשאר לעשות

1. **`theory/buy.html:56`** — `FORM_URL=""`. שורה אחת, וסוגרת את
   O-10. דרושה כתובת הטופס המלאה מ-Google Forms, כולל
   `?embedded=true`. **חסום על הבעלים.**
2. **O-28** — `clicks.js` אינו מכסה את `reader`, `bagrut-806`
   ו-`kotvim`.
3. **חמישה תאים ב-`math-uni` שהאורך עדיין נושא בהם מידע**, כולם
   **מתחת** לסף 70% ולכן `guess.js` עובר עליהם — אבל הם לא
   נבדקו ונמצאו תקינים, הם פשוט לא טופלו (N=500): `matops L2`
   58%, `prodquo L3` 52%, `prodquo L2` 50%, `inverse L4` 48%,
   `complex L1` 44% (הקצרה).
4. **`kotvim` בשלב `internal`** — כדי לצאת נדרשים `entropy`,
   `options` ו-`exam`, ואין בה בנק שאלות. ייתכן שהשלב צריך
   הגדרה משלו לאפליקציה בלי `buildQ`.

### בעיות ידועות

- **הרשת חסומה בסביבה.** אי אפשר לבדוק את האתר החי, אי אפשר
  להגיע ל-GoatCounter, ואין כלי ל-GitHub Traffic. ״`pages build
  and deployment` הצליח״ אינו ״האתר עובד״ — שני דברים שונים.
- **`content.js` מודד שדה, לא הקראה.** הוא החזיר `PASS` על
  `math-uni2` בזמן שהיא הקריאה משפט מתמטי שגוי. `saysym.js` נועד
  בדיוק לפער הזה, אבל הוא מכסה רק את משפחת האוניברסיטה.
- **`TR_KEYS` ממופה לפי אינדקס, לא לפי מפתח** (`trAt(lang,start,arr)`).
  הוספת מחרוזת באמצע המערך מזיזה כל תרגום שאחריה בשלוש השפות,
  בלי שגיאה ובלי שדבר ייראה שבור — המסך מציג את התרגום של השאלה
  השכנה. **מוסיפים בסוף בלבד.**
- **`say.js math-uni` נופל על מדגם ריק** — הוא מדווח ״אין
  newQuestion/P״, סופר 0 שאלות ו-0 דליפות, ונופל בכל זאת
  (״1 מתוך 1 מסגירות״). אומת על עץ נקי: קדם לעבודה של היום.
- **דוחות `qa/reports/` זזים בכל ריצה** — `content.js` מגריל 150
  שאלות לתא, ולכן האחוזים משתנים והדיף אינו ריק. מערך הממצאים
  יציב; האחוזים לא.

### המלצות לסשן הבא

1. **קרא את `FINDINGS.md` לפני שאתה נוגע במשהו**, ובפרט את שלוש
   הרשומות מ-9.9. שתיים מהן מתעדות בדיקות שנמצאו פגומות.
2. **`git fetch origin main` לפני שמתחילים, ושוב לפני ה-commit.**
   `main` זז פעמיים היום בזמן עבודה.
3. **בדיקה שמחזירה ״0 ממצאים״ — ודא שהיא בנתה משהו.** זה קרה
   פעמיים היום, ובשתיהן הפלט נראה כמו הצלחה.
4. **בקשה למחוק — אמת קודם שהדבר קיים.** בקשה אחת היום הייתה
   מוחקת תוכן חי באפליקציה שבייצור.

## מה המחולל לא יודע

שלושה דברים אינם נמדדים בשום כלי, וזה מה שהמסמך הזה קיים בשבילו:

- **`reader` ו-`theory` אינן חריגות.** ל-`reader` אין `BUILD` מפני
  שאין לה פוטר שמציג גרסה, והיא נושאת מפתח קאש בלבד. `theory`
  ("תאוריה מדברת") אינה בריפו הזה כלל — היא ריפו נפרד, ונספרת
  בשתים־עשרה. שתיהן תקינות, ואין מה "לתקן" בהן.
- **איכות ההקראה אינה נמדדת.** `smoke` בודק שאין שגיאת JS, לא
  שהעברית נשמעת נכון. מי שנוגע בהקראה — בודק באוזן.
- **תוכן שנכתב ביד אינו נבדק על נכונות עובדתית.** `entropy`
  ו-`options` בודקים מבנה: שהתשובה אינה קבועה, שיש ארבע אפשרויות,
  שאין שתי תשובות נכונות. שתאריך היסטורי נכון — לא.

## לפני שמתחילים משהו גדול

`git fetch origin main` ולהסתכל ב-log — **וגם שוב לפני ה-commit.**
חלון הבנייה הוא החלון המסוכן ולא הרגע שלפניו; ב-2.9 סשן שכן בדק
בהתחלה איבד את עבודתו לסשן שדחף שלושים וחמש דקות אחריו.

ומספר הגרסה מוקצה **במיזוג**, לא בבנייה: כל סשן מתפצל מ-main, רואה
את אותו מספר, ומסמן את עצמו אחד מעליו. מי שממזג בודק את main ומקצה
מספר טרי.
