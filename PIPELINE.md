# מ-BUILD ל-PUBLIC

חמישה שלבים, ואפליקציה עולה אחד בכל פעם. הכלל היחיד שאין ממנו
חריגה: **אפליקציה מגיעה ללומד אמיתי רק אחרי `approved`.**

    build → internal → content-qa → approved → public

מקור האמת הוא `.claude/qa/stages.json`, ו-`node .claude/qa/stage.js`
אוכף. הוא נופל אם מה שכתוב ברישום, מה שכתוב בדף הבית ומה שכתוב
בקובץ האפליקציה אינם מסכימים — שלושה מקומות שנכתבים ביד בשלושה
רגעים שונים, ולכן נפרדים.

---

## השלבים

### 1 · `build`

נבנית. אין דרישה שתעבוד.

**כדי לצאת:** `parse.js` נקי, ו-`smoke.js` טוען את הדף בלי
שגיאת JS.

### 2 · `internal`

נטענת ורצה, אבל התוכן שבה עוד לא נבדק.

- **אינה ב-`DATA.APPS`** שבדף הבית. לא בגריד, לא בחיפוש, ולא
  במניין. `stage.js` נופל אם היא שם.
- **נושאת את השער הפנימי** מ-`.claude/qa/internal-gate.html`.
  מי שמגיע לכתובת רואה "עדיין לא פורסמה" והאפליקציה אינה
  נטענת. פתיחה: `?internal=<המפתח>`, פעם אחת למכשיר.

השער אינו סוד. האתר הוא HTML יחיד ב-GitHub Pages, אין שרת, ומי
שפותח את מקור הדף רואה את השאלות. הוא מונע מלומד להיתקל
באפליקציה לא בשלה — לא יותר מזה, ואומרים את זה במפורש.

**כדי לצאת:** `cache.js` נקי (BUILD ומפתח הקאש תואמים),
`entropy.js` באפס, `options.js` נבדק, `exam.js` עובר.

### 3 · `content-qa`

    node .claude/qa/serve.js &
    node .claude/qa/content.js <app>

הסורק מגריל שאלות מכל נושא ומכל רמה ומדווח בתשע משפחות. הדוח
נכתב ל-`.claude/qa/reports/<app>.md`.

**כדי לצאת:** `FAIL` באפס. כל `REVIEW` נקרא והוכרע — תוקן, או
נדחה במודע.

### 4 · `approved`

הבעלים עבר על הדוח ואישר.

כאן, ורק כאן, **מסירים את השער הפנימי** ומעלים את מפתח הקאש.
עדיין לא בדף הבית.

`stage.js` נופל אם השער עוד בקובץ בשלב הזה — שער ששכחו נועל
בדיוק את מי שמותר לו להיכנס.

### 5 · `public`

נכנסת לדף הבית. **שבעה מקומות, ולא אחד:**

1. תיקייה עם `index.html`, `manifest.json` ו-`sw.js` (שם מטמון ייחודי)
2. אייקונים ב-`img/` — `node .claude/qa/icon.js`
3. רשומה ב-`DATA.APPS`
4. סמל ב-`ICONS` שבדף הבית
5. ארבע מחרוזות ה-`badge` — **כמילה, לא כספרה**
6. ארבע מחרוזות ה-`lead` שמונות את התחומים
7. מפתח הקאש של דף הבית עצמו

`stage.js` בודק את 3 ואת 5, ומשווה את מניין ה-public לרישום
ולמספר שב-`badge`. אחד־עשר במקום שנים־עשר הוא FAIL, לא הערה.

---

## מה הסורק מוצא, ומה לא

תשע המשפחות:

| # | משפחה | דוגמאות לממצאים |
|---|---|---|
| 1 | אין תשובה תקינה | `no-answer` `empty-answer` `broken-answer` |
| 2 | כפולות או סותרות | `multi-answer` `dup-option` `equivalent-distractor` |
| 3 | שאלות כפולות | `contradiction` `one-question` `low-variety` |
| 4 | שדות חסרים | `no-hint` `no-steps` `no-why` |
| 5 | ניסוח וכתיב | `placeholder-left` `doubled-word` `mojibake` |
| 6 | תרגום | `translation-missing` `lang-broken` `lang-untranslated` |
| 7 | הקראה | `answer-in-say` `latex-in-say` `symbol-in-say` `html-in-say` |
| 8 | חשוד | `longest-answer` `position-bias` `few-options` |
| 9 | מבנה | `build-throw` `too-many-options` `option-shape` |

**מה הוא אינו עושה:** הוא אינו יודע אם 7×8=56, אינו קורא
היסטוריה ואינו שופט אם ניסוח מובן. הוא מודד רק את מה שאפשר
למדוד בלי לדעת את החומר. לכן `REVIEW` הוא שאלה שמופנית אליך —
לא קביעה שיש באג. נכונות מתמטית היא של `euclid`, ותוכן של
`maayan`.

**ולמה `PASS` נדיר.** מדד שמחזיר PASS על הכול אינו מודד. שמונה
האפליקציות חזרו כולן `REVIEW` בסריקה הראשונה, ובתוכה נמצאו שני
דברים שאיש לא ידע עליהם — ראו `CHANGELOG.md`.

---

## להוסיף בדיקה חדשה

בתוך `page.evaluate` שב-`content.js`:

    if(<תנאי>) add('שם-הממצא','REVIEW','מה נמצא',where,<מבחין>);

ואז שורה ב-`CAT` שאומרת לאיזו משפחה הוא שייך. זה הכול.

שלושה כללים שנקנו ביוקר בסריקה הראשונה, וכדאי לקרוא אותם לפני
שכותבים בדיקה:

- **ספירה אינה פסק דין.** עובדה אחת על האפליקציה — "אף מסיח
  אינו נושא הסבר" — נכתבה 1,440 פעם. ממצא נבדל אחד עם אחוז
  לצדו הוא מה שאפשר לפעול לפיו.
- **זהות השאלה אינה הנוסח שלה.** "מה קרה קודם?" הוא נוסח קבוע
  שכל תוכנו באפשרויות. בדיקה שמזהה שאלה לפי נוסח בלבד הפיקה 55
  ממצאי FAIL כוזבים ב״מפנה״, 162 ב״ניב״ ו-147 ב״אולפן״ — ובאקסיומה
  ב׳ גם רמה שנראתה כמייצרת שאלה אחת בלבד.
- **בדיקת פרוזה אינה בדיקת נוסחה.** `[−1 , 5)` ו-`18 : 15` אינם
  שגיאות פיסוק, והם החזירו 9,657 ממצאים באפליקציה אחת. בדיקות
  ניסוח רצות על `ask` ועל `hint` בלבד.

וכלל רביעי, על העיתוי: **בודקים במקום שבו הלומד רואה ושומע.**
ב״שלב״ התרגום קורה ב-`trHTML` ברגע הציור ולא ב-`buildQ`, ולכן
בדיקה על פלט `buildQ` דיווחה 72 מתוך 72 שאלות כ"לא מתורגמות"
כשכולן תקינות. ובהקראה: אפליקציה שיש בה `sayAsk`, `sitSay` או
`questionSay` כבר בוחרת בעצמה מה להשמיע לפני התשובה, ומדידה על
השדה הגולמי `say` מדווחת על דליפה שנסגרה.

וכלל חמישי, על מה שחייב לקרות: **בדיקה שמדווחת על התנהגות
תקינה נלמדת להתעלם ממנה.** הקראה שמונה את כל האפשרויות מכילה
את התשובה בהכרח — זה תפקידה, ומי שאינו קורא חייב לשמוע גם את
המסיחים. לכן דליפה מוגדרת כא־סימטריה ולא כנוכחות. וכשמכיילים
בדיקה, בודקים אחר כך שהיא עדיין תופסת את הבאג שבגללו נכתבה.
