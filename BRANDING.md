# הזהות החזותית

מסמך ההחלטה, אחות ל-`NAMING.md`. עד עכשיו האייקונים והצבעים נקבעו
אפליקציה־אפליקציה, והתוצאה הייתה שני אייקונים לשש אפליקציות: `history`
הציג את האות N של `english`, ושלוש האקסיומות חלקו קובץ אחד. הכלל כאן
הוא מקור האמת; כשמוסיפים אפליקציה, ממלאים לפיו.

## הכלל: דקדוק אחד, משתנים גוון וסימן

**כל האייקונים נבנים מאותו תבנית** — ריבוע 512 עם פינה 116, גרדיאנט
אנכי מהגוון אל הגוון הכהה שלו, וסימן לבן במרכז באותו משקל קו (36,
קצוות ופינות מעוגלים). זה מה שהופך עשרה ציורים למשפחה אחת. מה שמשתנה
בין אפליקציה לאפליקציה הוא **הגוון** ו**הסימן**, ושום דבר אחר.

**הסימן מספר מה יש באפליקציה, ואינו אות.** קובץ אייקון אחד משרת את
ארבע שפות הממשק, ולכן אות עברית או לטינית משרתת חלק מהקהל בלבד. שלוש
שנות התואר מסומנות בשלושה אופרטורים — `∫ ∂ ∑` — שנקראים זהה בכל שפה,
ותג הספרה עליהן הוא רומי (`I II III`) מאותה סיבה.

**התג קריא בגודל מלא בלבד.** במסך הבית הוא נקודה לבנה, וההבחנה בין
שלוש האקסיומות נשענת שם על הסימן והגוון. זה מכוון: שלושה סימנים
שונים עובדים בגודל קטן טוב יותר משלוש ספרות.

## הטבלה

| תיקייה | שם | סימן | גוון | כהה |
|---|---|---|---|---|
| *(שורש)* | דף הבית | מקבץ תשע צורות | `#174c7e` | `#0e3358` |
| `math-app` | מתמטיקה לחכמים שמתקשים | `÷` | `#0e9c8d` | `#0a6f64` |
| `math-teen` | שלב | מדרגות עולות על ציר | `#4f46e5` | `#3a2fb8` |
| `math-uni` | אקסיומה | `∫` + תג `I` | `#6d28d9` | `#4c1d95` |
| `math-uni2` | אקסיומה ב׳ | `∂` + תג `II` | `#9333ea` | `#6b21a8` |
| `math-uni3` | אקסיומה ג׳ | `∑` + תג `III` | `#b021b8` | `#7e1a86` |
| `english` | ניב | בועת דיבור עם גל קול | `#0f766e` | `#0a544e` |
| `history` | מפנה | ציר זמן שפונה למעלה | `#9a5b0f` | `#6d400a` |
| `reader` | מקריא קולי | שורות טקסט וגלי קול | `#5aa9e6` | `#2f7cb8` |
| `drivewise` | תאוריה מדברת | הגה | `#1e7a48` | `#125733` |

## איפה גוון אחד חי — רשימת בדיקה

גוון אחד מופיע בחמישה מקומות, וכולם חייבים להסכים:

1. `brand/marks.js` — `c` ו-`d` של האפליקציה. זה המקור; כל השאר נגזר.
2. הגרדיאנט של ארבעת קובצי ה-PNG ב-`<app>/img/`
3. `theme_color` ב-`<app>/manifest.json`
4. `--accent` ו-`--accent-soft` ב-`<app>/index.html`, בבהיר ובכהה
5. `BRAND` ב-`index.html` של דף הבית — צבע הכרטיס והסימן שבתוכו

הפער הנפוץ הוא בין 3 ל-4: המניפסט נערך בנפרד מהעמוד ומתפצל ממנו
בשקט. כך קרה ש-`reader` הכריז `#5b3f8a` סגול במניפסט בזמן שהממשק שלו
היה `#5aa9e6` תכלת. אחרי כל שינוי גוון, להריץ `node brand/build.js`
ולגרוף את המאגר על ההקס הישן.

## איך בונים מחדש

```
node brand/build.js
```

מייצר את `brand/svg/` ואת כל ה-PNG בתיקיות האפליקציות. וניל node,
בלי חבילות ובלי רשת; הרינדור נעשה בכרומיום המקומי (`CHROME=` כדי
להצביע על בינארי אחר). **לערוך את `brand/marks.js`, לא את ה-PNG** —
עד המסמך הזה לא היה מקור לאייקונים, רק בינאריים, ותיקון של אחד מהם
דרש ציור מאפס.

ארבעה קבצים לכל אפליקציה: `icon-512`, `icon-192`, `apple-touch-icon`
(180) ו-`icon-maskable-512`. האחרון בפריסה אחרת — רקע במלוא הריבוע
והסימן ב-72 אחוז — כי אנדרואיד חותך אותו לעיגול. אחרי שינוי סימן,
לוודא שהוא שורד את החיתוך.

## שלוש הערות שהן החלטות ולא פערים

**`:root.hc` אינו מותג.** בלוק הניגודיות הגבוהה מחזיק `--accent`
משלו (`#00308f` באפליקציות המתמטיקה, `#00434b` בניב ובמפנה) שנבחר
בשביל יחס ניגודיות ולא בשביל זהות. הוא אינו נגזר מהגוון ואין להחליף
אותו כשמחליפים מיתוג.

**`--c-base` / `--c-alg` / `--c-geo` / `--c-data` אינם מותג.** אלה
צבעי תחומי הלימוד בתוך האפליקציה, והם משותפים בכוונה לכל אפליקציות
המתמטיקה: הם עונים על ״באיזה נושא אני״, לא על ״באיזו אפליקציה אני״.

**`math-app` ו-`reader` אינן ב-`ACCENT`.** אין להן `--accent` — יש
להן פלטה משלהן (טורקיז־אמבר־קורל לילדים; ממשק כהה למקריא), ורק
האייקון והמניפסט שלהן נגזרים מ-`c`.

## ידוע ופתוח

**`math-app` ו-`english` באותה משפחת גוון** — `#0e9c8d` ו-`#0f766e` —
למרות שהן בקטגוריות שונות. בגודל אמיתי הן נבדלות (בהיר מול כהה,
`÷` מול בועה), אבל זו הנקודה החלשה במערכת. שתיהן נשארו כי הפנים של
שתיהן בנוי על הגוון הזה; שינוי דורש לגרור איתו את הממשק.

**ל-`drivewise` אין קובצי אייקון כאן.** היא מופיעה בדף הבית ומקשרת
ל-`/drivewise/`, אבל אין תיקייה במאגר הזה. הגוון והסימן מוגדרים
ב-`marks.js` עם `noFiles:true`, משמשים את הכרטיס בדף הבית, ומחכים
למי שיבנה שם את התיקייה.

**ל-`voice` אין מיתוג, וזה מכוון** — כלי בדיקה פנימי, כמו שכתוב
ב-`NAMING.md`.

## מפתח המטמון

האייקונים נמצאים ב-`PRE` של ה-service worker. **החלפת אייקון בלי
העלאת גרסה לא מגיעה למי שכבר התקין** — הוא נשאר עם הקובץ הישן.
כל שינוי כאן גורר בליץ גרסה ומפתח SW, בדיוק כמו שינוי קוד.
