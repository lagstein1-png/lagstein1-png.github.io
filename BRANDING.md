# צבע לכל אפליקציה

מסמך ההחלטה על הצבע, אחות ל-`NAMING.md`. הסמלים עצמם מתועדים
ב-`ICONS` שבדף הבית וב-`.claude/qa/icon.js`; כאן רק הצבע.

עד 10.9.2026 שלוש האקסיומות היו זהות לחלוטין — `theme_color`,
`--accent` ורקע האייקון — וכך גם ניב ומפנה. הסמלים כבר נבדלו, אבל
הצבע שמסביבם היה משוכפל, ולכן שורת הכתובת, מסך הפתיחה של ה-PWA
וכל מראה הממשק היו אותו דבר בשלוש אפליקציות שונות.

## הכלל: הצבע מזהה אפליקציה — בשלושה מקומות

לכל אפליקציה **צבע אחד**, והוא מופיע בשלושה מקומות שחייבים להסכים:

1. **רקע האייקון** — `linear-gradient(180deg, <הצבע>, <הכהה>)`
2. **`theme_color`** ב-`manifest.json` — שורת הכתובת ומסך הפתיחה
3. **`--accent`** ב-`index.html`, בבלוק הבהיר

הכהה בגרדיאנט הוא אותו גוון בערך 28% פחות בהירות, וקיים רק כדי
לתת לאייקון עומק. `--accent` במצב כהה הוא גוון בהיר יותר של אותו
צבע, כי הוא צריך להיקרא על רקע כהה.

## דף הבית הוא היוצא מן הכלל, וזה מכוון

בכרטיסי דף הבית הצבע מציין **תחום** ולא אפליקציה — `--c-math`,
`--c-lang`, `--c-life` — והסמל הוא שמבדיל בין אפליקציות. זו החלטה
של המסך ההוא: משטח עיון שבו קיבוץ עוזר, ואריח מרומז ב-13% ולא תג
מותג. אין לגזור ממנו את צבע האפליקציה ואין לגזור ממנו הפוך.

## הטבלה

| תיקייה | שם | צבע | כהה שבגרדיאנט | ‎--accent כהה |
|---|---|---|---|---|
| *(שורש)* | הפורטל | `#2f5fd8` | — | — |
| `math-app` | מתמטיקה לחכמים שמתקשים | `#0e9c8d` | `#0b7d71` | אין `--accent` |
| `math-teen` | שלב | `#4f46e5` | `#3b34c4` | `#818cf8` |
| `math-uni` | אקסיומה | `#6d28d9` | `#4c1d95` | `#a78bfa` |
| `math-uni2` | אקסיומה ב׳ | `#a21caf` | `#6b1275` | `#e879f9` |
| `math-uni3` | אקסיומה ג׳ | `#c026d3` | `#86198f` | `#f0abfc` |
| `english` | ניב | `#0f766e` | `#0a544e` | `#5eead4` |
| `history` | מפנה | `#9a5b0f` | `#6d400a` | `#e8b45c` |
| `reader` | מקריא קולי | `#2f7cb8` | `#1f5c8a` | ממשק כהה בלבד |
| `ulpan` | אולפן | `#8a5a2b` | שטוח | `#e8c9a0` |
| `lomda` | לומדה | `#4338ca` | שטוח | `#a5b4fc` |
| `bagrut-806` | בגרות 806 | `#1e40af` | שטוח | `#93b4fd` |
| *(ריפו נפרד)* | תאוריה מדברת | נקבע שם | — | — |

## איך מחליפים צבע

שלוש הנגיעות למעלה, ואז בליץ גרסה — האייקונים יושבים ב-`PRE` של
ה-service worker, ובלי מפתח טרי מי שכבר התקין נשאר עם הישן.

האייקון מצויר מחדש בכלי שכבר קיים, מאותו `path` שבדף הבית:

    node .claude/qa/icon.js <תיקייה> \
      'linear-gradient(180deg,<צבע>,<כהה>)' '#ffffff' '<path…>'

**להוציא את ה-`path` מ-`ICONS` שבדף הבית ולא לצייר חדש.** זה מה
ששומר על הסמל בכרטיס ועל הסמל במסך הבית כציור אחד.

## שני דברים שאינם מותג

**`:root.hc`** — ה-`--accent` בבלוק הניגודיות הגבוהה (`#00308f`
באפליקציות המתמטיקה, `#00434b` בניב ובמפנה) נבחר בשביל יחס
ניגודיות ולא בשביל זהות. הוא אינו נגזר מצבע האפליקציה ואין להחליף
אותו כשמחליפים צבע. סקריפט החלפה שאינו מבחין בין הבלוקים ידרוס
אותו בשקט — זה קרה, ונתפס לפני שנדחף.

**`--c-base` / `--c-alg` / `--c-geo` / `--c-data`** — צבעי תחומי
הלימוד בתוך האפליקציה משותפים בכוונה לכל אפליקציות המתמטיקה. הם
עונים על ״באיזה נושא אני״, לא על ״באיזו אפליקציה אני״.

## כמה רחוק זה מספיק רחוק

הצבעים נבדקו במרחק ΔE76 במרחב לאב, ולא בעין. הזוג הקרוב ביותר בכל
התיק הוא `math-teen` ו-`lomda` — ΔE 8.4, שניהם אינדיגו. הבא אחריו
`math-uni` ו-`math-uni2` — ΔE 14.4, אחרי שהורחקו: בגרסה הראשונה של
המסמך הזה הם היו 9.6, וזה נתפס במדידה ולא במבט.

הרף המעשי בתיק הזה הוא בערך **ΔE 15**: זה המרחק בין `math-app`
ל-`english` ובין `history` ל-`ulpan`, שני זוגות שחיים זה לצד זה
בלי בלבול. מתחת ל-10 שני צבעים נקראים כאותו גוון.

## ניגודיות

הגליף באייקון לבן, ולכן הצבע חייב להחזיק לבן ביחס 4.5:1 לפחות.
זו הסיבה ש-`reader` קיבל `#2f7cb8` (4.5:1) ולא את התכלת `#5aa9e6`
של הממשק שלו, שנותנת 2.5:1 — התכלת נשארה כמבטא בממשק הכהה, שם
היא נקראת על רקע כהה.

## ידוע ופתוח

**שני סגנונות רקע חיים במקביל.** שלוש האפליקציות החדשות — אולפן,
לומדה ובגרות 806 — נושאות רקע שטוח, והשמונה הוותיקות גרדיאנט.
לא אוחד: איחוד פירושו לצייר מחדש שמונה אייקונים בלי באג שמצדיק זאת.

**שלושה אייקונים ותיקים נושאים שוליים שקופים של 16 פיקסל** —
`math-app`, `math-teen` ו-`english` — משארית של המחולל שקדם
ל-`icon.js`. השאר במלוא הריבוע. ניכר רק בהשוואה צמודה.

**ובאותן שלוש, האייקון והסמל בכרטיס אינם אותו ציור.** `math-teen`
נושא במסך הבית את האות ״ש״ ו-`english` את האות ״N״, בזמן שב-`ICONS`
כתובים להן מדרגות ובועת דיבור. `math-app` נושא `÷` ובכרטיס שלו
פעולות חשבון. זה בדיוק מה ש-`icon.js` נועד למנוע, ובשמונה האחרות
זה כבר מוחזק. לא שונה כאן כי אין כאן באג אלא הכרעה: האם פניה של
ניב הן המונוגרם N או בועת הדיבור. מי שמכריע מריץ `icon.js` ומעלה
גרסה.

**`lomda` (`#4338ca`) ו-`math-teen` (`#4f46e5`) — ΔE 8.4, הזוג
הקרוב ביותר בתיק ומתחת לרף.** שניהם אינדיגו ונבדלים בעיקר בכהות.
בדף הבית הם בכרטיסים מרוחקים, אבל במסך הבית של הטלפון הם זה לצד
זה. לא שונה כאן: שתיהן קדמו למסמך, ואף אחת מהן אינה שגויה בפני
עצמה — זו החלטה איזו מהשתיים זזה, ולמי יש מספיק סיבה לבלוץ גרסה.
