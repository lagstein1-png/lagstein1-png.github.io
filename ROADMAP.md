# מפת הדרכים — 21 מועמדות, והפסק ההנדסי

**מי כותב:** מהנדס התוכנה ומנהל הפרויקט. **מתי:** 5.9.2026,
מול `main` בגרסה `bf57ef9`.

הרשימה של 21 המועמדות היא החלטת מוצר, והיא לא נשפטת כאן. מה
שנשפט כאן הוא **מה כל שורה עולה בקוד**, וזה נמדד מול הריפו
ולא מוערך.

הפסק נשען על נוהל שכבר קיים במאגר —
`.claude/skills/create-learning-app/SKILL.md`, שלב 0.5:

> רוב הנושאים אינם צריכים אפליקציה, אלא קובץ. `lomda/` היא מנוע
> שהתוכן שלו חיצוני. אם התוכן נכתב כפריט מתוארך, כמושג עם הגדרה
> או כקטע קצר עם שאלות — הוא נכנס לשם, ואין כאן שכפול בכלל.

נושא חדש בלומדה הוא **שלוש נגיעות** — קובץ ב-`lomda/data/`, תגית
`<script>` לפני המנוע, ואותו נתיב ב-`PRE` שב-`sw.js`. אפליקציה
חדשה היא **שבע נקודות חיווט ואחת־עשרה מפתחות מטמון**. היחס בין
השניים הוא היחס שקובע את הסדר.

## התוצאה במשפט אחד

מ-21 המועמדות, **תשע** הן אפליקציה. **שלוש כבר קיימות ופועלות
בייצור**, חמש הן קובץ תוכן, שתיים נכנסות לאפליקציה קיימת, ושתיים
חסומות עד החלטה. שלב א׳ שלך — "הכי קל, הכי קרוב לקיים" — קרוב
לקיים מפני שהוא **כבר נבנה**.

| פסק | כמה | מה זה אומר |
|---|---|---|
| כבר קיים בייצור | 3 | יורד מהרשימה |
| נכנס לאפליקציה קיימת | 2 | מאגר או נושא, לא אפליקציה |
| קובץ ב-`lomda/data/` | 5 | שלוש נגיעות כל אחד |
| אפליקציה חדשה | 9 | שבע נקודות + 11 מפתחות מטמון |
| חסום עד החלטה | 2 | שלב ה׳ שלך, ואני מסכים |

---

## שלוש שכבר קיימות — יורדות מהרשימה

| # | במפה | מה יש בפועל |
|---|---|---|
| 2 | אולפן — עברית מעשית לעולים | **אותה אפליקציה, אותה שורת תיאור.** `<title>` ב-`ulpan/index.html` הוא "אולפן — עברית מעשית לעולים חדשים". זהה מילה במילה |
| 4 | שקל — אוריינות פיננסית | `lomda/data/money.js` קיים ונטען. `banks.js`: 4 מאגרים, 7 נושאים, 91 פריטים |
| 12 | חיבור — אוריינות דיגיטלית | `lomda/data/digital.js` קיים ונטען |

התכוונת ל**עוד תוכן** בשלושתן, לא לאפליקציה חדשה — זה נושא נוסף
בקובץ קיים, וזה זול. אפליקציה חדשה באותו שם היא פיצול של קהל
בין שני מוצרים שאיש לא ידע להפנות ביניהם.

## שתיים שנכנסות לאפליקציה קיימת

**1 · נתיב — רישיון נהיגה מקצועי.** מאגר שני בתוך "תאוריה מדברת",
לא אפליקציה. מילות המפתח שלה בדף הבית הן כבר `תאוריה נהיגה רישיון`,
המנוע קיים, וההקראה שם **מוקלטת** ולא סינתטית — נכס שאי אפשר
לשחזר בזול באפליקציה חדשה. ובנוסף: השם "נתיב" מתנגש ב"ניב" הקיימת.
בתעתיק לארבע שפות, `Nativ` ו-`Niv` הם שני מותגים שנשמעים אחד.

**14 · מדגם — סטטיסטיקה לסטודנטים.** נושא במשפחת "אקסיומה".
הסתברות היא כבר אחד משלושת התחומים ש"אקסיומה" מכריזה עליהם
בדף הבית, והמחולל שם.

## חמש שהן קובץ תוכן, לא אפליקציה

התוכן שלהן הוא מושג עם הגדרה או קטע קצר עם שאלות — בדיוק מה
שסכימת `lomda/data/schema.js` מתארת.

| # | שם | הקובץ |
|---|---|---|
| 3 | זכות — מיצוי זכויות | `lomda/data/rights.js` |
| 8 | מפה — גיאוגרפיה לחטיבה | `lomda/data/geo.js` — הטבלה בנוהל מזכירה גאוגרפיה בשמה |
| 13 | קורות — קורות חיים וראיון | `lomda/data/work.js` |
| 18 | תווית — בטיחות בעבודה | `lomda/data/safety.js` |
| 19 | שער — מבחן התאזרחות | נושא ב-`civics.js` הקיים |

## תשע אפליקציות, ומאיזו משפחה כל אחת מועתקת

ההחלטה על המשפחה היא, לפי הנוהל, "ההחלטה היחידה שאי אפשר לתקן
אחר כך בזול". לכן היא נקבעת כאן ולא בזמן הבנייה.

| # | שם | משפחה | למה |
|---|---|---|---|
| 5 | צורה — לשון לבגרות | `ulpan` | שאלות כתובות עם מסיחים |
| 6 | אמנה — אזרחות לבגרות | `bagrut-806` | מבחן עם תתי־סעיפים |
| 7 | מפתן — מיצ״ב וחטיבה | `ulpan` | חידון, ארבע שפות |
| 9 | תא — ביולוגיה לבגרות | `bagrut-806` | תוכן ארוך כתוב ביד |
| 10 | יסוד — חשבון למבוגרים | `math-uni3` | תרגילים שנוצרים בקוד |
| 11 | אות — קריאה וכתיבה | `ulpan` | ניקוד והקראה, כמו אולפן |
| 15 | מסלול — מה״ט והנדסאים | `math-uni3` | מחולל |
| 16 | קוד — יסודות תכנות | `ulpan` | החלק הנבחן הוא חידון |
| 17 | גשר — ערבית מדוברת | `ulpan` | תמונת מראה של אולפן |

**אף אחת מהן לא מועתקת מ-`math-app`.** הנוהל אוסר את זה במפורש:
"הוא קורא לכל דבר בשם אחר… הוא לא תבנית — הוא ניב." כל שורה
שתועתק ממנו תיראה נכונה ולא תתפוס.

`10 · יסוד` דורש הערה: `math-app` הוא כבר ארבע פעולות החשבון
צעד אחר צעד למי שמתקשה. ההבדל היחיד הוא הטון — חיית המחמד שם
נכונה לילד ושגויה למבוגר. זו אפליקציה בגלל הטון, לא בגלל התוכן,
וזה נימוק תקף אבל חלש — ולכן היא אחרונה בשלב ג׳.

## שמות שחייבים להשתנות לפני בנייה

`NAMING.md` הוא הפוסק, והכלל הוא שם מותג מתועתק בארבע שפות. ארבעה
שמות במפה אינם עומדים בו:

| שם | הבעיה |
|---|---|
| נתיב | `Nativ` מול `Niv` הקיימת — מותג אחד בשתי אפליקציות |
| מפתן, מפה, מפתח | שלושה שמות ב-מפ־ ליד "מפנה" שכבר בייצור. בחיפוש ובהקראה זו מקשה אחת. לכל היותר אחד מהם שורד |
| תא, אות, קוד | שתי אותיות או מילה כללית. אי אפשר למצוא אותן בחיפוש ואי אפשר לתעתק אותן לשם שנשמע כמו מותג |

## שתיים חסומות — ואני מסכים איתך

**20 · מפתח — פסיכומטרי.** הסיכון שסימנת אמיתי, וזה אותו סוג
סיכון שהוביל למחיקת שם באוגוסט. פריטי מבחן רשמיים מוגנים. הדרך
היחידה היא בנק מקורי לחלוטין, ואז אסור להבטיח בשיווק שהוא "כמו
המבחן" — `marketing/facts.md` אוסר הבטחה שאין לה מקור.

**21 · מרשם — מונחי רפואה.** ההמלצה שלי היא **לא לבנות**. הקהל
של הפורטפוליו הוא מי שמתקשה, והוא הקהל שהכי סביר שיסתמך על מה
שכתוב. הצהרת אחריות אינה הופכת את זה לבטוח. אם בכל זאת — מילון
מונחים בלבד, בלי שאלות, בלי "נכון/לא נכון", ובלי מצב בחינה.

## הסדר שאני קובע

1. **קודם כל, שום בנייה.** בדיקת הקבלה של 12 האפליקציות החיות
   (פקודה 2 ב-`CELINE.md`) נסגרת ראשונה. אפליקציה שלוש־עשרה
   על תשתית שלא נבדקה היא חוב, לא נכס.
2. חמשת קובצי התוכן. שלוש נגיעות כל אחד, `banks.js` מאמת.
3. שלוש שאלות שם: מפתן, תא, אות — לפני שנוגעים בקוד.
4. `17 · גשר` היא האפליקציה הראשונה שנבנית. תמונת מראה של אולפן,
   המשפחה הנקייה, והקהל הגדול ביותר שאינו מכוסה.
