# כלי הבדיקה

כולם וניל node, בלי התקנה. playwright ו-chromium כבר קיימים בסביבה
(`require('/opt/node22/lib/node_modules/playwright')`).

הרצה: קודם מרימים שרת, ואז כל השאר.

    node .claude/qa/serve.js &          # 127.0.0.1:8099, שורש = הריפו

| כלי | מה הוא עונה עליו |
|---|---|
| `parse.js <file...>` | האם כל בלוק סקריפט בקובץ בכלל נפרס. אחרי כל עריכה. |
| `extract.js <outdir> <file...>` | מחלץ את הסקריפטים לקבצי js, כדי להריץ עליהם eslint |
| `entropy.js <app...>` | **האם יש רמה שהתשובה הנכונה בה קבועה** |
| `options.js <app...>` | **כמה אפשרויות התלמיד באמת רואה** — ומה שיעור השאלות שמציגות פחות מארבע |
| `smoke.js <page...>` | טוען כל דף, עובר שער תנאים ואונבורדינג, לוחץ על כל כפתור, אוסף שגיאות JS |
| `exam.js [app...]` | **האם מה שהמורה רואה בבונה המבחן הוא מה שהתלמיד מקבל** — וגם שמבחן ישן עדיין נבנה כפי שנבנה |
| `cache.js [app...]` | **האם `BUILD` ומפתח ה-`?v=` תואמים בכל אחד־עשר ה-sw.js** — וגם שאין `caches.match` גלובלי |
| `content.js [app...]` | **האם השאלה עצמה תקינה** — תשע משפחות ממצאים, ודוח PASS/REVIEW/FAIL לכל אפליקציה |
| `stage.js` | **האם משהו שלא אושר נגיש ללומד** — הרישום מול דף הבית מול השער הפנימי |

eslint:

    node .claude/qa/extract.js /tmp/js english/index.html math-uni/index.html
    cp .claude/qa/eslint.config.mjs /tmp/js/
    cd /tmp/js && npx --no-install eslint --config eslint.config.mjs .

**חסימת רשת חיצונית חובה** בכל הרצת דפדפן, אחרת כל דף נתקע על
הגופנים של גוגל:

    await page.route('**/*', r =>
      r.request().url().startsWith('http://127.0.0.1:8099') ? r.continue() : r.abort());

## למה `options.js` קיים בנפרד

`entropy.js` בודק שהתשובה נכונה ואינה קבועה, ולא ראה שנים־עשר אלף
שאלות ב״ניב״ — 48% מהן — הציגו שלוש אפשרויות במקום ארבע. שאלה כזאת
תקינה לפי כל מדד אחר, והתלמיד פשוט מנחש אחד משלושה. שתי הבדיקות
משלימות זו את זו ואף אחת אינה מספיקה לבדה.

שורה שמציגה שלוש **אינה בהכרח באג**: שאלה שיש לה שלוש תשובות אפשריות
בסך הכול, או שאלת הבנת נקרא שנכתבה ידנית עם שני מסיחים, היא תקינה.
ההבחנה היא לפי מקור האפשרויות — כתובות מראש בתוכן, או נבנות בזמן
ריצה — ולא לפי האחוז.

## קו הבסיס של `entropy.js` ו-`smoke.js` הוא אפס

היה זמן ש-`dupe` ב-math-uni לא היה אפס ותועד כאן כ"כוזב". הוא באמת
היה כוזב, אבל הסיבה לא היתה ב-math-uni אלא במדד: ההשוואה שיטחה את
האפשרויות ל-`textContent`, ולכן השבר `−5/x` והמכפלה `−5x` נראו זהים,
וכך גם שתי מטריצות שנבדלות רק בסידור העמודות (`−4 11 1 −24` מול
`−4 1 11 −24` מצטרפות לאותה מחרוזת).

`entropy.js` משווה היום במפתח מבני שבו כל גבול אלמנט הוא מפריד, ולכן
שתי אפשרויות נחשבות כפולות רק אם הן זהות גם על המסך. **`dupe` שאינו
אפס הוא באג אמיתי.** כך גם `noAns`, `multi`, `nan` ו-
`constant-answer levels`.

באותו אופן `smoke.js` היה מדווח על כל דף שטוען גופן מגוגל כ"שגיאה",
כי חסימת הרשת החיצונית — שהיא שלנו — נרשמת בכרום כשגיאת קונסולה
`net::ERR_FAILED`. הוא מסנן אותה היום ומדווח כמה בקשות חיצוניות נחסמו,
ולכן **`ERRORS` ב-smoke הוא באג אמיתי**, ומספר ה-controls שנלחצו נראה
גם כשהדף נקי.

קו הבסיס של **eslint** אינו אפס, אבל הוא רשום כאן במלואו. כל שורה
שאינה ברשימה הזאת היא ממצא חדש שצריך להסתכל עליו:

| קובץ | ממצא | למה הוא שפיר |
|---|---|---|
| english, history, math-app, math-teen, math-uni, math-uni2, math-uni3 | `'id' is already defined` | `var id` חוזר באותו scope |
| english, history | `'lg' is already defined` | אותו דבר |
| math-app | `'nm' is already defined` | שני `var nm` עוקבים באותה פונקציה |
| math-app | `'tDesc' is not defined` | `typeof tDesc==="function"` — שמירה על תכונה אופציונלית |
| math-teen | `'VOICE_SAMPLE' is not defined` ×5 | מוגדר ב-`Object.defineProperty(window,...)` כדי להחזיר עברית או ערבית לפי `LG`. eslint אינו רואה גלובל שנוצר כך |
| index (השורש), reader | `'stopSpeak' is not defined` | `typeof stopSpeak==="function"` ב-`demoQuiet()` — שמירה על תכונה אופציונלית, כמו `tDesc`. שני הדפים היחידים שאין בהם `function stopSpeak` |

`no-redeclare` שאינו ברשימה הזאת אינו בהכרח שפיר: כך נמצא ב-english
ש-`function P(lines,words,qs)` הבונה קטעי קריאה נדרסה בזמן ריצה על ידי
`var P={q:...}` של מצב המשחק. הבנאי נקרא היום רק בטעינה, לפני הדריסה,
ולכן זה לא התפוצץ — אבל נושא קריאה שייבנה בעצלתיים היה מקבל
`P is not a function`. הבנאי הוא `PARA` מאז.

## למה `cache.js` קיים

שני הבאגים שהוא מחפש אינם נכתבים — הם מועתקים. אפליקציה חדשה נולדת
מהעתקה של `sw.js` ושל בלוק הרישום מאפליקציה קיימת, ולכן היא יורשת גם
`caches.match` גלובלי שכבר הוסר במקום אחר, וגם את ההרגל לעדכן `BUILD`
בלי מפתח הקאש. אף אחד מהשניים אינו מתפוצץ במסך: הראשון מגיש קובץ ישן
ששמר worker של אפליקציה אחרת, והשני פשוט לא מגיע למי שכבר התקין.

לכן הבדיקה רצה על כל אחד־עשר הקבצים יחד ולא על מה שהשתנה. **קו הבסיס
שלה הוא אפס.**

`caches.match` בתוך הערה אינו ממצא — ההערות מוסרות לפני החיפוש, כי
ההערה שמסבירה למה לא משתמשים בקריאה הזאת נמצאת בכל הקבצים בכוונה.
לדף הבית ול-`reader` אין `BUILD` כלל, ולכן הם מדווחים ב-`·` ולא
ב-`✗`: אין להם פוטר שמציג גרסה, והמנגנון תלוי רק ב-`?v=`.

## למה `exam.js` קיים

בונה המבחן אורז את השאלות לתוך הקישור, והתלמיד בונה אותן מחדש
מהזרעים שבו. שני הצדדים הם קוד נפרד, ואין שום דבר שיצעק אם הם
ייפרדו: המורה יראה מסך תקין, התלמיד יקבל מבחן תקין, ורק הציונים
יהיו של שאלות אחרות. הכלי משווה את שתי הרשימות תו־בתו, ובודק
בנוסף שהחלפה של שאלה אחת אינה נוגעת באף אחת מהשאר ושמבחן ישן —
כזה שאין לו זרע לכל משבצת — עדיין נבנה בדיוק כפי שנבנה ביום
שנשלח.

## אייקונים לאפליקציה חדשה

    node .claude/qa/icon.js <תיקייה> <רקע> <צבע-קו> '<path d=…>'

מריצים ביד בלבד, כשמוסיפים אפליקציה. **זה אינו שלב בנייה** ואינו רץ
בפריסה: האתר נשאר קובץ HTML יחיד לכל אפליקציה. הסקריפט מצייר
בכרומיום שכבר מותקן לבדיקות, ומקבל את אותו `path` שנכתב ביד בתוך
`ICONS` שבדף הבית — כדי שהסמל בכרטיס והסמל במסך הבית לא יהיו שני
ציורים שונים שנפרדו עם הזמן. נוצרים ארבעה קבצים: 192, 512,
maskable-512 (מוקטן ל-60% כי אנדרואיד חותך ממנו עיגול) ו-apple-touch.


## `content.js` ו-`stage.js` — קו הבסיס שלהם אינו אפס, והוא לא אמור להיות

השניים האלה נוספו יחד עם `PIPELINE.md`, ושם כתוב התהליך במלואו.
כאן רק מה שנוגע להרצה.

**`stage.js` — קו הבסיס הוא אפס FAIL.** הוא אינו מריץ דפדפן
ורץ בשבריר שנייה, ולכן אפשר להריץ אותו אחרי כל שינוי. שלושת
המקורות שהוא משווה — `.claude/qa/stages.json`, `DATA.APPS`
שבדף הבית, והשער הפנימי שבקובץ האפליקציה — נכתבים ביד בשלושה
רגעים שונים, וזו בדיוק הסיבה שהם נפרדים בשקט.

האזהרות שהוא מדפיס אינן כשל: `legacy-published` פירושה
אפליקציה שפורסמה לפני שהשער נבנה. היא נשארת כך עד שהבעלים
עובר על ממצאי ה-REVIEW שלה.

**`content.js` — קו הבסיס הוא אפס FAIL, ולא אפס REVIEW.**
מדד שמחזיר PASS על הכול אינו מודד דבר. שמונה האפליקציות חזרו
כולן REVIEW בסריקה הראשונה, וזה המצב התקין.

    node .claude/qa/serve.js &
    node .claude/qa/content.js            # שמונה בעלי buildQ
    QA_N=400 node .claude/qa/content.js math-uni

`QA_N` הוא גודל המדגם לכל נושא×רמה, וברירת המחדל 150. בשמונה
האפליקציות יחד זה 66,150 שאלות וכמה דקות ריצה.

**`answer-in-say` — למה הוא מודד מה שהוא מודד.** דליפה היא
**א־סימטרית**: התשובה נאמרת ואף מסיח לא. ארבעה דברים אינם
דליפה, וכולם דיווחו ככזאת עד שכוילו — תשובה שנמצאת ב-`expr`
ומוצגת על המסך; הקראה שמונה את כל האפשרויות, שזה בדיוק תפקידה;
מסיח שנכתב `−37` ונאמר "מינוס 37", ולכן לא נמצא בהשוואה
מילולית; ואפליקציה שיש בה `sitSay` או `questionSay` ובוחרת
בעצמה מה להשמיע — היא נמדדת לפי הבחירה שלה. הניקוי מסימנים
משמש **רק** לספירת המסיחים; בדיקת התשובה נשארת מילולית, כדי
שההרפיה לא תבלע דליפה. נבדק שהכלל המכויל עדיין תופס את הבאג
המקורי: 540 מתוך 540 ב״מפנה״ על השדה הגולמי.

**שלוש בדיקות שהיו כוזבות ותוקנו, כדי שלא ייכתבו שוב:**

| הבדיקה | מה היא ספרה | למה זה היה כוזב |
|---|---|---|
| `contradiction` לפי נוסח בלבד | 55 ב״מפנה״, 162 ב״ניב״, 147 ב״אולפן״, ועוד `one-question` באקסיומה ב׳ | "מה קרה קודם?" הוא נוסח קבוע שכל תוכנו באפשרויות. זהות שאלה = נוסח + ביטוי + אפשרויות, ובשאלת האזנה גם התשובה |
| `space-before-punct` על `expr` | 9,657 ב-math-uni2 | `[−1 , 5)` ו-`18 : 15` הם טיפוגרפיה של מתמטיקה. בדיקות ניסוח על `ask` ו-`hint` בלבד |
| `lang-untranslated` על פלט `buildQ` | 72 מתוך 72 ב-math-teen | שם התרגום קורה ב-`trHTML` ברגע הציור. בודקים אחרי `trHTML`, וב-`ulpan` (`LEARN="he"`) לא בודקים כלל — העברית היא התוכן |

הדוחות ב-`.claude/qa/reports/`: `<app>.md` לקריאה, `<app>.json`
למכונה, ו-`summary.json` לכולם יחד. `stage.js` קורא את ה-json
כשאפליקציה מבקשת לעלות שלב.
