# נגישות, RTL וארבע שפות — הדרישות, ומה שכבר אכוף

**קהל היעד קובע את הרף.** דיסלקציה, ADHD, עולים חדשים ונהגים
מבוגרים — אלה לא משתמשי קצה תיאורטיים, הם הרוב כאן. מתג נגישות
שבור אינו פגם קוסמטי; הוא אפליקציה שלא עובדת בדיוק אצל מי שהיא
נבנתה בשבילו.

**כל מספר במסמך הזה נמדד ב-11.9.2026 ונרשם עם הפקודה שהפיקה
אותו.** מספר בלי מקור הוא שקר — ראו `CLAUDE.md`.

---

## 1 · ARIA

### מה כבר יש, ומה זה אומר

`grep -ho 'aria-[a-z]*' */index.html | sort | uniq -c` — **251** מופעים,
בשמונה תכונות:

| תכונה | מופעים | תפקיד |
|---|---|---|
| `aria-label` | 65 | שם לכפתור שהוא אייקון בלבד |
| `aria-pressed` | 63 | מתג דו־מצבי |
| `aria-hidden` | 42 | קישוט שאסור שיוקרא |
| `aria-checked` | 34 | `role="switch"` |
| `aria-live` | 31 | הודעה שמשתנה בלי ניווט |
| `aria-modal` | 11 | חלון שחוסם את הרקע |
| `aria-current` | 4 | הפריט הנוכחי ברשימה |
| `aria-labelledby` | 1 | כותרת של דיאלוג |

ו-`role` — 65 מופעים: `alert` 21, `switch` 19, `button` 12,
`group` 11, `status` 1, `dialog` 1.

### הכללים

1. **כפתור שהוא אייקון בלבד חייב `aria-label`.** SVG בלי טקסט הוא
   כפתור אילם לקורא מסך. ה-SVG עצמו מקבל `aria-hidden="true"` —
   הוא ציור, לא תוכן.
2. **מתג דו־מצבי הוא `role="switch"` עם `aria-checked`, או כפתור
   עם `aria-pressed`.** לבחור אחד ולדבוק בו בתוך אותה אפליקציה.
   **התכונה חייבת להתעדכן בקוד כשהמצב משתנה** — `aria-pressed`
   שנכתב ב-HTML ואינו זז הוא שקר לקורא המסך, וגרוע מכלום.
3. **כל הודעה שמופיעה בלי ניווט עוטפת ב-`aria-live`.** ״נכון״,
   ״טעות״, ״נשמר״, תשובת ג׳וש. `polite` לרוב; `assertive` רק
   כשהמשתמש חייב לעצור.
4. **`aria-hidden` על קישוט בלבד.** לעולם לא על משהו שאפשר להתמקד
   בו — מיקוד לתוך אזור מוסתר הוא מלכודת שקטה.
5. **דיאלוג:** `role="dialog"` + `aria-modal="true"` +
   `aria-labelledby` שמצביע לכותרת. בלי שלושתם זה `div` שנראה
   כמו חלון.
6. **אל תוסיף `role` למה שכבר יש לו אחד.** `<button role="button">`
   הוא רעש. ARIA נכנס רק למה ש-HTML לא נותן.

---

## 2 · מקלדת

### מה כבר יש

`grep -l 'keydown' */index.html` — **11 קבצים**. המקשים שנתפסים
בפועל (`grep -ho 'e.key==="[^"]*"'`):

    Escape 22 · Enter 21 · רווח 13 · s/S 8 · h/H 8 · ארבעת החיצים 4

`tabindex` — 12 מופעים של `0` ו-10 של `-1`.
`:focus-visible` — קיים ב-**14 מתוך 15** קובצי `index.html`
(`voice/` הוא היחיד בלי, והוא דף אבחון ולא אפליקציית לימוד).

### הכללים

1. **כל פעולה נגישה במקלדת.** אין פעולה שרק עכבר מגיע אליה.
2. **`Enter` ורווח מפעילים כל מה שמתנהג ככפתור.** אם זה `<button>`
   — הדפדפן עושה את זה לבד, וזו הסיבה להעדיף `<button>` על `<div>`.
3. **`Escape` סוגר כל שכבה** — דיאלוג, פאנל, תפריט. 22 המופעים
   שנמדדו הם בדיוק זה.
4. **`:focus-visible` חייב להיות נראה, ולא `outline: none`.**
   המשתמש שמנווט במקלדת צריך לדעת איפה הוא. **זו הדרישה היחידה
   כאן שקל להפר בטעות** — `outline: none` נכתב כדי ״לנקות״ ומוחק
   את הניווט כולו.
5. **`tabindex` חיובי אסור.** `0` להכניס לסדר הטבעי, `-1` למיקוד
   תוכניתי בלבד. `tabindex="3"` שובר את הסדר בכל הדף.
6. **דיאלוג פתוח כולא את המיקוד** ומחזיר אותו למקום שממנו נפתח
   כשהוא נסגר.
7. **קיצור מקש אינו תחליף לכפתור.** `s` להקראה ו-`h` לרמז הם
   תוספת; מי שאינו יודע עליהם חייב למצוא כפתור.
8. **רווח או `Enter` על כפתור ממוקד אינם קיצור.** הדפדפן כבר יורה
   `click`, וקיצור שיורה גם הוא מפעיל שתי פעולות מלחיצה אחת. מי
   שלא החריג את זה גילה שרמקול של תא תשובה שהגיעו אליו ב-Tab
   הקריא את השאלה במקום את התא, ו-`Enter` אחרי המענה דילג לשאלה
   הבאה במקום להקריא. השורה שמונעת זאת:

       if((e.key===" "||e.key==="Enter")&&e.target&&
          e.target.closest("button,[role='button'],a[href]"))return;

9. **קיצור אינו נורה בתוך שדה קלט** — `input`, `textarea`,
   `select`, `[contenteditable]` — ולא יחד עם Ctrl / Alt / Meta.
   בלי זה מי שמקליד ״4״ בתיבת טקסט עונה על השאלה.

**ואזור המגע, למי שמגיע באצבע ולא במקלדת:** `min-height:44px`
מופיע 88 פעמים בשנים־עשר קובצי `*/index.html`, ו-`min-height:48px`
עוד 30 (`grep -o`, 11.9.2026). כפתור חדש נכנס לאותן מידות ולא קטן
מהן — יד רועדת וזקן בן שבעים הם קהל היעד, לא מקרה קצה.

---

## 3 · הקראה — Web Speech API

**הפירוט המלא של המנוע נמצא ב-`CLAUDE.md`, בסעיף ״מנוע ההקראה —
ארבעה מנגנונים״.** כאן רק מה שנוגע לנגישות, ומה שאסור לשבור.

### ארבעת המנגנונים אינם קישוט

כולם מכסים כשל **שקט** של Web Speech, שנראה למשתמש בדיוק אותו
דבר — הקול מפסיק באמצע:

1. **שומר-ער** — Chrome ו-Edge בשולחן העבודה חותכים אחרי כ-15
   שניות. **רק בשולחן העבודה**: במובייל אין את הבאג, והפעולה
   מקרטעת שם.
2. **`_activeU`** — הפניה חיה ל-utterance, אחרת Chrome אוסף אותו
   ואת `onend` איתו.
3. **`ttsWatchdog`** — יש מכשירים שבהם `onend` לא נורה כלל.
4. **נפילה מקול רשת** — קול נוירלי שותק בלי אינטרנט.

`node .claude/qa/voice.js` בודק את ארבעתם מול `speechSynthesis`
מזויף. **נגעת בהקראה — הרץ אותו.**

### החוזה מול ה-API — שבעה כללים שהפרתם משתיקה את הקול בשקט

ארבעת המנגנונים למעלה מכסים כשלים של המנוע. אלה מכסים את מה
שהדפדפן דורש מ**הקוד**, וכל אחד מהם נלמד מבאג שקרה:

1. **בודקים שהוא קיים.** `if(!("speechSynthesis" in window))return`
   — ארבע פעמים בכל אחת מעשר האפליקציות המדברות, פעמיים
   ב-`reader` (`grep -c`, 11.9.2026). דפדפן בלי המנוע נשאר
   אפליקציה שעובדת, לא מסך שנשבר.
2. **האמירה יוצאת בתוך המגע עצמו.** קודם היא נעטפה ב-`voicesReady`
   שממתין לרשימת הקולות עד ארבע שניות — ואמירה שיוצאת ארבע שניות
   אחרי המגע נזרקת בשקט באנדרואיד ובספארי. **הבטחה שוברת את
   שרשרת המגע.** רשימת קולות ריקה אינה סיבה לשתוק: מדברים עם
   `u.lang` בלבד, וקול ברירת המחדל בשפה הנכונה עדיף על שתיקה.
3. **iOS דורש שחרור.** אמירה ריקה אחת מתוך `pointerdown` עם
   `{once:true}`, בקריאה ישירה בלי `await`. בלעדיה כל הקראה
   אוטומטית נבלעת — בלי חריגה, בלי לוג, בלי דרך לדעת.
4. **`cancel()` לפני כל אמירה, ומונה דורות אחריה.** `stopSpeak`
   מעלה את `_sayGen`, מנקה את `_activeU` וקורא ל-`cancel`; כל
   קריאה חוזרת בודקת `my===_sayGen`. בלי המונה, תשובה שחוזרת
   מהרשת אחרי שהמשתמש עצר מושמעת בכל זאת.
5. **עוזבים את הדף — הקול נעצר.** `visibilitychange` → `stopSpeak`,
   באחת־עשרה האפליקציות המדברות. בלעדיו הקול ממשיך ברקע אחרי
   מעבר לטאב אחר, ולפעמים נתקע עד סגירת הדפדפן.
6. **מה שנשלח הוא טקסט, לא HTML.** `speak` מריץ `plainOf` על כל
   מחרוזת; בלעדיו הסטודנט שמע ״span class frac״ באמצע השאלה.
7. **קוד השפה מגיע מ-`LANGS[lang].tts`** ולא מניחוש הדפדפן, והקצב
   נחתך ב-`clamp(...,0.55,1.4)` — קצב מחוץ לתחום מתקבל אצל חלק
   מהמנועים כשתיקה.

### הכללים

1. **קול לפי שפת הטקסט, לא לפי שפת הממשק.** ארבעת הקודים נמדדו
   במאגר: `he-IL` 24, `en-US` 18, `ar-SA` 15, `ru-RU` 15.
   בלימודי שפה המודל עוטף את המילה הנלמדת ב-`« »`, וכל קטע כזה
   מוקרא בקול של אותה שפה.
2. **אין קול בשפה הזאת במכשיר — אומרים.** לא כפתור שלוחצים עליו
   ולא קורה כלום. הנוסח קיים בארבע השפות ב-`tutor/tutor.js`.
3. **ההקראה לא מסגירה את התשובה.** `node .claude/qa/say.js`
   אוכף, ו-`leaks.js` אוכף את אותו דבר על הגדרות בלומדה.
4. **מה שנאמר אינו מה שכתוב.** `^`, `_`, `±`, אינדקסים ותגיות
   HTML חייבים לעבור המרה לפני ההקראה — אחרת המכשיר מקריא
   ״קארט״. `node .claude/qa/saysym.js` אוכף.
5. **כל לחיצה מושמעת** — כפתור שנלחץ מוקרא בשמו.
   `node .claude/qa/clicks.js` אוכף בשתים־עשרה האפליקציות.
6. **מתג להשתקה.** מי שההקראה מפריעה לו חייב לכבות אותה.

---

## 4 · RTL וארבע שפות

### מה כבר יש

`grep -ho '<html[^>]*>' */index.html` — **חמישה־עשר מתוך חמישה־עשר**
נושאים `lang="he" dir="rtl"`. שלושה־עשר קבצים מעדכנים `dir`
בזמן ריצה בהחלפת שפה.

### הכללים

1. **`dir` ו-`lang` משתנים יחד עם השפה.** עברית וערבית `rtl`,
   רוסית ואנגלית `ltr`. `lang` שגוי גורם לקורא המסך להגות בשפה
   הלא נכונה.
2. **מאפייני CSS לוגיים ולא פיזיים** — `margin-inline-start`
   ולא `margin-left`. פיזי נשבר בהיפוך כיוון, ותמיד רק בשפה אחת,
   ותמיד אצל מי שאינו בודק אותה. **קו הבסיס היום הוא אפס:** נמדד
   11.9.2026 על עשר האפליקציות, על `reader` ועל דף הבית — אפס
   `margin-left`, `margin-right`, `padding-left` ו-`padding-right`
   בשנים־עשר הקבצים. אין כאן בדיקה שתתפוס, ויש מה לשבור.
3. **טקסט לטיני או מספר בתוך משפט עברי** — לעטוף ב-`<span dir="ltr">`,
   אחרת הסדר על המסך מתהפך. `d(x , y)` ו-`t = 3` הם המקרים
   הנפוצים כאן. המחלקה שעושה זאת היא
   `.en{direction:ltr;unicode-bidi:isolate}` — **בלי `isolate`**
   המשפט הלטיני גורר אחריו את סימני הפיסוק של המשפט העברי, והנקודה
   קופצת לצד הלא נכון.
4. **גופן שיש בו את הכתב.** ערבית ורוסית דורשות משפחה שתומכת בהן,
   אחרת המשתמש רואה ריבועים. `node .claude/qa/fonts.js` אוכף —
   **וזו בדיקה שנולדה מכישלון אמיתי:** גרסתה הראשונה עברה גם
   אחרי שהמשפחה הוסרה מבקשת הגופנים.
5. **כפתור השפה נכתב בשפה שאליה הוא מוביל.** ״العربية״ גם כשהממשק
   עברי — מי שאינו קורא עברית צריך למצוא אותו.
   `node .claude/qa/langbtn.js` אוכף.
6. **ארבע השפות בכל פריט תוכן.** `{he, ar, ru, en}`, ולא שלוש.
   `banks.js` ו-`content.js` אוכפים.

### המלכודת שחוזרת: המחרוזת העברית היא המפתח

באפליקציות האוניברסיטה כל מחרוזת עטופה ב-`_()`, וב-`math-teen`
המנגנון הוא `trHTML` עם `TR_AR`. **בשני המקרים המחרוזת העברית
עצמה היא המפתח במילון.** שינוי מילה בעברית בלי לעדכן את המפתח
**מוחק את התרגום בשקט, בלי שגיאה**.

ו-`TR_KEYS` הוא מערך **ממופה־אינדקס**: הוספת מפתח באמצע מזיזה כל
תרגום שאחריו בשלוש השפות. **מוסיפים בסוף בלבד**, ומעגנים ב-
`TR_KEYS.indexOf(<המפתח הראשון בבלוק>)`.

### ומה שאינו עובר ב-`render` נשאר עברי בכל שפה

כותרת תחתונה, קישור התנאים, ״כל האפליקציות״ וקישור הדילוג הם HTML
סטטי שאינו עובר דרך `render`. **`lgChrome()` הוא מה שמתרגם אותם** —
`LGFOOT`, `LGTERMS`, `LGHOME`, `LGSKIP`. אלמנט חדש שנכתב ישירות
ב-HTML ולא נרשם שם יישאר עברי בערבית, ברוסית ובאנגלית, **בלי שום
שגיאה** — ואף בדיקה אינה מודדת את זה.

**וזה כבר קרה בכיוון ההפוך, וזהו ממצא פתוח (`O-46`):** עשר
אפליקציות מתרגמות קישור דילוג, ולשש מהן אין אותו. `lgChrome` מחפש
`a.skip[href='#main']`, והאנקור עצמו כתוב רק ב-`math-teen`,
`math-uni`, `math-uni2` ו-`math-uni3`; ב-`english`, `history`,
`ulpan`, `lomda`, `math-app` ו-`kotvim` ה-`querySelector` מחזיר
`null`, ההשמה מדולגת בשקט, ולמי שמנווט במקלדת אין דרך לדלג על
הסרגל העליון. נמדד 11.9.2026: `grep -l 'href="#main"' */index.html`
= 4 מול `grep -l LGSKIP */index.html` = 10. דף הבית ו-`pricing/`
נושאים קישור משלהם (`#apps`, `#plans`).

התיקון הוא **רוחבי מוצהר** — אנקור ו-`id="main"` בשש אפליקציות,
ואיתם שישה מפתחות קאש — ולכן הוא ממצא ולא שורה שמתקנים בדרך.

---

## 5 · ששת מצבי הנגישות

`node .claude/qa/a11y.js` מאמת בתשע אפליקציות את השרשרת המלאה —
`applyModes` מדליק מחלקה **ו**ל-CSS יש כלל שמשתמש בה:

| יכולת | שמות שראינו בפועל |
|---|---|
| פונט קריא | `clear-font` · `clearfont` · `clear` |
| ניגודיות | `hi-contrast` · `hc` |
| תנועה מופחתת | `reduce-motion` · `rm` |
| ריווח | `spaced` |
| גודל טקסט | `ts2` · `ts3` |
| ערכת צבע | `theme` |

**שלוש המשפחות קוראות לאותו מצב בשלושה שמות**, ולכן `grep` על שם
אחד מדווח ״חסר״ על אפליקציה שהמצב בה עובד. זה קרה: שלוש קביעות
שגויות נכנסו ל-`ARCHITECTURE.md` בדיוק כך. **הבדיקה שואלת על
יכולת, לא על מחרוזת** — ושם חדש מתווסף לטבלה שבראש `a11y.js`,
לא לקוד.

**חוליה שבורה באחד משני השלבים = מתג שהמשתמש מפעיל ולא קורה
כלום.** בלי שגיאה, בלי אזהרה.

---

## 6 · כללי איכות קוד

אלה חלים על כל שינוי כאן, ולא רק על נגישות.

1. **Vanilla JS בלבד.** אין React, אין Vue, אין npm, אין build
   step, אין TypeScript. **אין תלות חיצונית חדשה, בלי חריגים** —
   גם לא CDN של CSS או של גופנים.
2. **בין פתרון חכם לקריא — קריא.** מי שיקרא את זה בעוד חודש הוא
   סשן אחר בלי ההקשר שלך.
3. **השינוי הקטן ביותר שפותר את הבעיה.** קוראים את הקוד לפני
   שמשערים.
4. **`BUILD` ומפתח הקאש עולים יחד.** שתי מחרוזות באותו קובץ,
   ואינן נגזרות זו מזו. עדכון של אחת בלבד = המשתמש לא יראה את
   התיקון לעולם. **המספר מוקצה במיזוג, לא בבנייה.**
5. **`localStorage` משותף לכל האפליקציות** — אותו מקור. מפתח
   שנולד בהעתקה הוא מגירה ששתיים כותבות אליה. `storage.js` תופס.
6. **אין מחיקת תוכן קיים** — שאלה, רמז, מסיח, תרגום. ניסוח מחדש
   מותר.
7. **אין מפתח API בריפו.** לעולם.
8. **בדיקה חדשה חייבת הוכחת נפילה לפני מיזוג:** שבור → אדום,
   תוקן → ירוק, ושני הפלטים נרשמים ב-`FINDINGS.md`. **בדיקה
   שמעולם לא נראתה אדומה אינה בודקת כלום.**
9. **הרץ `node .claude/qa/all.js` לפני commit.** ושער הסיום
   מריץ את הגרסה הסטטית בעצמו.
10. **קוד הריצה של האפליקציה הוא ES5.** נמדד 11.9.2026 על
    אחת־עשרה האפליקציות ועל `bagrut-806/app.js` ו-`speech.js`:
    **אפס** `const`, **אפס** פונקציות חץ, ו-`let` בודדים (0–6
    לקובץ). `var` בלבד. היוצא מן הכלל הוא `sw.js`, שרץ רק בדפדפן
    מודרני ולכן כתוב ב-`const` ובחצים — וכך גם כלי ה-QA, שהם Node.
    זו אינה נוסטלגיה: הקהל כאן כולל מכשירים ישנים, ואין build step
    שיתרגם.
11. **כל מחרוזת שנכנסת ל-HTML עוברת `esc()`.** תוכן שנכתב ביד אינו
    ״בטוח״ — גרש בודד בשאלה שובר את התגית שמסביבה, והשאלה נעלמת
    מהמסך בלי שגיאה בקונסולה.
12. **הערה אומרת *למה*, לא *מה*.** ״מוסיף אחד ל-i״ אינו הערה;
    ״בלי זה Chrome אוסף את ה-utterance וההקראה נתקעת בלי שגיאה״
    כן. מי שמוחק הערה כזאת מוחק את הסיבה שהשורה קיימת.

---

## 7 · מה אוכף מה

| דרישה | כלי |
|---|---|
| ששת מצבי הנגישות מחווטים | `a11y.js` |
| ארבעת מנגנוני ההקראה | `voice.js` |
| ההקראה לא מסגירה את התשובה | `say.js` · `leaks.js` |
| אין סימן גולמי בהקראה | `saysym.js` |
| כל לחיצה מושמעת, ויש מתג להשתקה | `clicks.js` |
| גופן שיש בו את הכתב | `fonts.js` |
| כפתור השפה בשפה שאליה הוא מוביל | `langbtn.js` |
| ארבע שפות בכל פריט תוכן | `banks.js` · `content.js` |
| `BUILD` מול מפתח הקאש | `cache.js` · `status.js --check` |
| מפתח `localStorage` לאפליקציה | `storage.js` |
| הכול, ופסק דין אחד | `all.js` |

**ומה שאין עליו בדיקה, ולכן נופל על העין:** `aria-pressed` שאינו
מתעדכן בקוד, `outline: none` שמוחק מיקוד, `tabindex` חיובי,
מאפיין CSS פיזי במקום לוגי, קישור דילוג חסר, ומחרוזת סטטית שלא
נרשמה ב-`lgChrome` ולכן נשארת עברית בכל שפה. ששתם עוברים את כל
החבילה בשקט.

**וכך נמצא ליקוי אמיתי בזמן כתיבת המסמך הזה.** סריקה של
`outline: none` החזירה **12 מופעים בְּ-11 קבצים**. אחד עשר מהם
יושבים על מצב המנוחה ויש להם כלל `:focus` שמסמן — זה הדגם הנכון.
ה-12 היה `textarea:focus{outline:none}` ב-`reader`, שהוא **תוך
המיקוד עצמו** וגובר בסגוליות על `:focus-visible` הגלובלי שבאותו
קובץ. תוקן. `tabindex` חיובי נמדד באותה סריקה: **אפס**.
