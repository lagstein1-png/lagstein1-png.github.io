# מנוע ברק — יומן החלטות

כל הכרעה בין שתי דרכים טובות נרשמת כאן, עם הכלל מסעיף 9 של המנדט
שהכריע אותה. ״ממתין לאישור יהושע״ מסומן במפורש.

| # | ההחלטה | החלופה שנפסלה | הכלל שהכריע |
|---|---|---|---|
| D-1 | **`docs/barak-lesson-engine-spec.md` אינו קיים** — רשם הפעולות נבנה פעם אחת ב-`tutor/barak-core.js`, בלי לבנות מנגנון מקביל | להמציא ״שלב A״ לפי הזיכרון | ״קוראים את הקוד לפני שמשערים״; אין מקור — אין המצאה |
| D-2 | הפאנל נשאר `tutor/tutor.js`; `barak-core.js` נטען אחריו ומחליף את **הקריאה לשרת** בלבד (`send()` מאציל ל-`BARAK.ask`) | לשכתב את הפאנל | ״השינוי הקטן שעובד עדיף על שכתוב״; שבע בדיקות QA כבר מחזיקות את הפאנל |
| D-3 | `POST /ask` הוא הנתיב החדש; `/` ממשיך לקבל את הגוף הישן (`q`, `messages`) ומנורמל לאותו מנוע | לשבור את הלקוחות הישנים | ״תאוריה מדברת״ החיה ואפליקציה במטמון ישן עדיין שולחות את הגוף הישן |
| D-4 | התשובה נושאת `say` **וגם** `text` (אותו ערך) | `say` בלבד | `search.js` ו-`tutor.js` הישנים בודקים `text`; שני שדות זולים משבירה |
| D-5 | שרשרת המודלים: רשימת `models` נמשכת מה-API פעם ל-isolate, ומסוננת ל-Flash ואז Flash-Lite שתומכים ב-`generateContent`; נופלים לשם קבוע רק כשהרשימה לא נענתה | לקבע שמות מהזיכרון | המנדט: ״לא לקבע שמות מהזיכרון״; `gemini-2.5-flash` כבר נפל על 404 ב-16.9 |
| D-6 | מונה גלובלי: צובר בזיכרון ה-isolate ונכתב ל-KV לכל היותר פעם ב-10 דקות → ≤144 כתיבות ליום ל-isolate; מונה לכל IP בזיכרון בלבד | KV בכל בקשה (1,000+/יום) | Cloudflare חינמי: יעד <700 כתיבות |
| D-7 | פעולה שהמודל מחזיר ואינה ברשימה שהאפליקציה שלחה — **נזרקת**, והתשובה ממשיכה עם `action:null` | לזרוק את כל התשובה | ילד לא צריך לסבול מטעות של המודל; הטקסט עדיין עוזר |
| D-8 | טקסט על פעולה מוצג **רק אחרי** שהפעולה החזירה `true`; נכשלה — נוסח קבוע בארבע שפות (״לא הצלחתי לעשות את זה עכשיו״) במקום `say` | להציג את `say` ולנסות | ״ברק לעולם לא מכריז על פעולה שלא בוצעה״ |
| D-9 | פעולות משותפות עם אימות: `next` מאומת בשינוי `id` של המסך לפני/אחרי; `hint` באורך ה-DOM; אחרות בקיום כפתור פעיל שנלחץ | להאמין ללחיצה | יושרת פעולות מדידה, לא הצהרה |
| D-10 | **הריפו הנפרד של ״תאוריה מדברת״**: `barak-core.js` מועתק לריפו (`barak-core.js` בשורש הריפו הנפרד) כפי שהמנדט מבקש, עם כותרת ״מקור האמת בריפו הראשי״; ארבעת קובצי `/tutor/` האחרים נטענים בנתיב מוחלט מאותו מקור, כמו `legal/` וכמו `theory.patch` שנבדק בדפדפן | להעתיק את כל חמשת הקבצים | המנדט מפורש על ״עותק מקומי של קובץ הלקוח״; חמישה עותקים = חמש סחיפות |
| D-11 | `tutor-staging` = `[env.staging]` באותו `wrangler.toml`, KV נפרד, וורקפלו `deploy-tutor-staging.yml` שכותב את אותו `GEMINI_API_KEY` מ-Secrets | Worker בקובץ נפרד | קובץ אחד, מוח אחד; ההבדל הוא שם ו-KV בלבד |
| D-12 | `josh-face.js` שומר על שמו; **אין** שינוי בעשרת האירועים | שינוי שם | המנדט: ״החלפת השם היא החלטה נפרדת שממתינה ליהושע״ — **ממתין לאישור יהושע** |
| D-13 | הבדיקות החיות (מודל אמיתי) רק מרנר של GitHub, עם תקרה קשיחה של 60 קריאות לריצה ומונה בקובץ | להריץ מכאן | הרשת חסומה כאן; המנדט: ≤60 ביום |
| D-14 | דף `/barak/` נרשם ב-`stages.json` תחת `notApps` — הוא דף תצוגה, לא אפליקציית לימוד, ואינו נמנה ב-`badge` | להוסיף ל-`DATA.APPS` | ״המניין אינו יעד״; שלוש־עשרה נשארות שלוש־עשרה |
| D-15 | הבחירה החזותית בדף `/barak/`: השמרנית ביותר — אותו פאנל, אותם צבעים, אותו דיוקן | עיצוב חדש | סעיף 9: ״החלטה ויזואלית שלא הוכרעה — השמרנית״ — **ממתין לאישור יהושע** |
| D-16 | `joshAsk()` אינה קיימת במאגר (`grep -rn joshAsk` ריק), ולכן אין כינוי לאחור; `barakAsk` הוא `BARAK.ask` | ליצור `joshAsk` יש מאין | אין למה להיות תואם לאחור |
| D-17 | **הפאנל אינו מסתיר את התרגיל:** גיליון תחתון (≤46dvh, בלי הכהיה, המסך מעליו לחיץ; **וכשהמקלדת פתוחה** — 17.9.2026 — הפאנל ממלא את `visualViewport` במצב קומפקטי: דיוקן 56px, ▾/✕ אייקונים, בועת שאלה בשורה אחת, `.claude/qa/keyboard.js`) בטלפון; עמודה צדית 400px במסך ≥900px; כפתור קיפול `#tu-min`; לחיצה מחוץ לקופסה אינה סוגרת | להשאיר חלון במרכז מעל וילון | המנדט: ״לא מסתיר שאלה, תשובות או כפתורים״ — החלטה ויזואלית, **ממתין לאישור יהושע** |
| D-18 | **אין הקראה אוטומטית של כל תשובה.** ▶ נשאר על כל הודעה; ברק מדבר רק בלחיצה | להקריא כל תשובה מיד | קהל ADHD/דיסלקציה — קול שקופץ בלי בקשה מפריע; שכבת ה-TTS הקיימת לא הוחלפה |
| D-19 | **בבחינה של ״תאוריה מדברת״ אין ברק:** אין כפתור ואין הקשר במסך `exam` | להשאיר אותו גם שם | ״המבחן אינו מביט בלומד״; המסך מסתיר רמזים בכוונה |
| D-20 | `kotvim` בלי `next_question` (אין כפתור ״הבא״ ישר עד שכל ארבעת החלקים נבחרו); `bagrut-806` בלי `highlight_option` (תשובות מוקלדות) ובלי `formula_sheet` (אין דף כזה) | לרשום פעולה שתיכשל | ״ברק לעולם לא מכריז על פעולה שלא בוצעה״ — פעולה שאין לה דרך כנה אינה נרשמת |
| D-21 | `theory.patch` נוצר מחדש מה-`git diff` של הריפו הנפרד, ו-`qa/tutor.js` 4ב מקבל `./barak-core.js` מקומי כמכסה את `/tutor/barak-core.js` | לצרף גם את הנתיב המשותף ל-PRECACHE שם | D-10: העותק המקומי הוא ההחלטה, והבדיקה הולכת אחריה |
| D-22 | ההיסטוריה לשרת: 8 הודעות (ארבעה חילופים) ב-`/ask`; הגוף הישן שומר `LIM.msgs=14` | לקצץ גם את הישן | הפרטיות במנדט חלה על המנוע החדש; הלקוח הישן לא נשבר |

