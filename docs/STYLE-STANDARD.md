# תקן עיצוב משותף — כפתורי ההקראה והעזר (26.9.2026)

מקור האמת הוא בלוק ה-CSS ב-`math-teen/index.html` (אחרי `<body>`), ומועתק
מילה במילה לכל אפליקציה. הסריקה הרוחבית בודקת שהצבעים זהים בכל האפליקציות.

## הצבעים — אותם בדיוק בכל מקום

| כפתור | `data-a` | רקע | מסגרת | טקסט |
|---|---|---|---|---|
| הקראה | `read`, `readsteps` | `#0e9c8d` | `#0b7c71` | לבן מודגש |
| עצירה | `stop`, `hush` | `#bf4426` | `#a03a20` | לבן מודגש |
| רמז | `hint` | `#b45309` | `#92400e` | לבן מודגש |
| לימור (המורה) | `tutor` | `#6d28d9` | `#5b21b6` | לבן מודגש |
| פתרון מודרך | `guide` | `#1d4ed8` | `#1e40af` | לבן מודגש |
| נוסחה | `rule` | `#334155` | `#1e293b` | לבן מודגש |

- גובה מינימלי `46px`, `font-weight:700`, טקסט לבן — גם בריחוף (hover).
- כפתורי הקראה גדולים (`.spk:not(.sm)`): ירוק `#0e9c8d` כמו כפתור ההקראה.
- עצירת הקראה (`.spkStop`): אדום `#bf4426`.

## רמקול לכל תשובה (`.optspk`)

- כל אפשרות בתרגול עטופה ב-`.orow` (flex) עם כפתור `.optspk` לידה:
  `min-width/min-height:46px`, רקע `#0e9c8d`, מסגרת `#0b7c71`, רדיוס `12px`.
- הכפתור מוצג רק כשההקראה דולקת (`state.settings.tts`), והלחיצה קוראת
  `speak()` על טקסט האפשרות בלבד.
- התווית לקורא מסך: "השמעת התשובה" — מתורגמת לכל ארבע השפות
  (TR_KEYS + trAt לערבית/רוסית/אנגלית; ב-math-app מפתח `sayOpt` ב-STR).
- הרמקול הקטן (`.spk.sm`) באפליקציות משפחת civics: אותו ירוק `#0e9c8d`
  עם מסגרת `#0b7c71` ואייקון לבן.

## אפליקציות עם עיצוב משלהן

- **math-app** (עיצוב ילדים): ה-`.hintpill` שומר על הצורה אבל בצבע הרמז
  התקני `#b45309`; כפתור לימור סגול דרך הבלוק; רמקול לכל תשובה ב-`.optwrap`.
- **reader**: כפתור הניגון הראשי ירוק `#0e9c8d`, מצב עצירה אדום `#bf4426`,
  כפתור לימור סגול.
- **barak** (דמו לימור): "שאלו את לימור" סגול, הרמז כתום.

## כפתורי שפה — צבע אחד בכל האפליקציות (26.9.2026)

התלונה: "השפות צבעים שונים". מעכשיו אין קשר לצבע המבטא של האפליקציה —
כפתורי השפה זהים בכל מקום, בגוון הכחול של דף הבית (`--brand`).

- **לא פעיל:** רקע שקוף, מסגרת `#d4dbe7`, טקסט `#475569`.
- **פעיל (`.on`):** רקע `#2f5fd8`, מסגרת `#2f5fd8`, טקסט לבן מודגש.
- גיאומטריה (ללא שינוי): רדיוס `10px`, ריפוד `3px 9px`, גובה מינימלי `36px`,
  גופן `.82rem`.
- **math-app:** `.oblang.on` באותו כחול `#2f5fd8` עם טקסט לבן.
- **reader** (`#langbar`, יש מצב כהה): הפעיל באותו כחול בשני המצבים; הלא־פעיל
  נשאר במשתני הנושא כדי לא להישבר בכהה.

## כללים נלווים

- **גרסאות:** כל שינוי באפליקציה מעלה גם את `BUILD` וגם את מחרוזת ה-`?v=`
  ברישום ה-service worker — אחד בלי השני לא מנקה מטמון אצל מתקינים.
- **ארבע שפות:** כל מחרוזת חדשה שמוצגת למשתמש נכנסת למילונים של עברית,
  ערבית, רוסית ואנגלית.
- **שער פרסום:** אפליקציה חבויה (INTERNAL) לא כותבת את `document.title`
  שלה — הכותרת נשארת "לא פורסם".
