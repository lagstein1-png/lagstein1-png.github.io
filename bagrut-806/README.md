# 806 — בגרות במתמטיקה, 3–4 ו-5 יח״ל

בנק שאלות **כתוב ביד** לפי שאלון 806, עם נוסחאות מוקלדות
ב-KaTeX וסימולציית בחינה מתוזמנת.

**מזהה:** `bagrut-806` · **שלב:** `public` · **קובץ:** `index.html`, 492 שורות + `app.js` (1,216 שורות)

## היא היחידה שאינה קובץ אחד — וזו הארכיטקטורה שאליה רוצים להגיע

| קובץ | מה |
|---|---|
| `index.html` | 492 שורות, כמעט ריק מקוד |
| `app.js` | המנוע — **וכאן יושבים `BUILD` וקריאת הרישום**, לא ב-`index.html` |
| `data/exams.js` | התוכן, כתוב ביד |
| `speech.js` | מנוע ההקראה בסגנון המודול שלה |
| `vendor/katex/` | ספרייה חיצונית **שהוטמעה כקבצים מקומיים** — אינה נטענת מהרשת |

**אין להוסיף עוד `vendor/`.** זה חריג מתועד, לא תקדים.

## התוכן, כפי שנמדד

| | |
|---|---|
| אוספים | 8 |
| סעיפים | 114 |
| תאים | 48 |
| פסק דין | **PASS** — 0 חוסם, 0 לבדיקה |

מקור: `.claude/qa/reports/bagrut-806.json`. **`content.js` אינו
סורק אותה** — הוא מגריל מ-`buildQ`, ולה אין. הסורק שלה הוא
`node .claude/qa/content806.js`, שעובר על הבנק **במלואו ולא במדגם**.

## עברית בלבד, ובכוונה

היא מסומנת `pickLang: true` ואינה נושאת ארבע שפות כשאר האפליקציות.
זו החלטה מתועדת ולא פער — `node .claude/qa/tutor.js` בודק אותה
במפורש.

## מפתח `localStorage`

`bagrut806-v1` — התקדמות הלומד. אחד, ושלה בלבד.

## בדיקות שנוגעות לה

    node .claude/qa/serve.js
    node .claude/qa/exam806.js        # הסכימה: speech מול latex, tolerance, reject
    node .claude/qa/content806.js     # הבנק הסטטי, במלואו
    node .claude/qa/smoke.js bagrut-806
    node .claude/qa/all.js

## PWA — מה שהופך אותה לאפליקציה

`manifest.json` (‎3‎ אייקונים, `display: standalone`, `lang: he`, `dir: rtl`)
ו-`sw.js` שמצרף מראש **14 נכסים**. אחרי טעינה ראשונה היא עובדת
בלי אינטרנט, ואפשר להתקין אותה על מסך הבית.

**המטמון שלה הוא `bagrut806-` ושלה בלבד.** שנים־עשר קובצי ה-`sw.js`
מצרפים את אותם `/legal/terms.js` ו-`/legal/protect.js`, כל אחד
למטמון משלו — ולכן נגיעה ב-`legal/` מחייבת להעלות את **כל**
המפתחות יחד. ההסבר המלא ב-`CLAUDE.md`, ״מלכודת הקאש של `legal/`״.

## גרסה — שתי מחרוזות, לא אחת

    const BUILD = "x34 · 2026-09-10";  // ב-app.js
    navigator.serviceWorker.register("sw.js?v=x34-pwa1");

`sw.js` גוזר את שם המטמון מ-`?v=` שבכתובת הרישום, **לא** מ-`BUILD`.
עדכון של אחד בלי השני משאיר את הלומד עם הקוד הישן.
`node .claude/qa/cache.js` נופל על אי־התאמה, וגם על מפתח שכבר
קיים ב-`origin/main` עם תוכן אחר.

## ״עזרה מהמורה״ (ג׳וש)

מחווטת בארבע נקודות: תגית `<script src="/tutor/tutor.js">`,
`TUTOR.mount(...)`, קריאת `TUTOR.open()` מהנתב, והנתיב ב-`PRE`
שב-`sw.js`. **הכפתור אינו מוצג** כל עוד `API` ריק ב-`tutor/tutor.js`;
ההפעלה היא `node .claude/qa/enable-tutor.js <כתובת>`. הצד השרתי
כולו ב-`tutor-api/worker.js`, והתפקיד של האפליקציה הזאת נמצא שם
ב-`ROLE`.

## מה אין כאן

אין `npm install`, אין build step, אין framework ואין תלות חיצונית.

`vendor/katex/` הוא היוצא מן הכלל היחיד, והוא מקומי ולא מהרשת.

---

*נוצר מקריאה בקוד ובדוחות ה-QA, 10.9.2026. כל מספר כאן נמדד בפקודה — אין הערכות.*
