# יומן שינויים

סימן הגרסה שמוצג בכל אפליקציה משמש גם כמפתח המטמון של ה-service
worker. שינוי הגרסה הוא מה שמביא את הקוד החדש למי שכבר התקין את
האפליקציה — ולכן שני המספרים זזים תמיד ביחד.

## `bagrut-806` מעדיף קול נשי, כמו אחת־עשרה האחרות · x27

**9.9.2026.** ב-`speech.js` הכריעה האיכות לבדה, ו-״Microsoft Asaf״
ניצח ״Carmit״ שהותקנה לידו. עכשיו אותו סדר כמו ב-english: קול שלא
יעבוד לתחתית, אחריו המגדר (נשי → לא מזוהה → גברי), ובתוך הקבוצה
האיכות. הוכחה במנוע דיבור מזויף: Asaf לפני, Carmit אחרי; עם Hila
Online ברשימה — Hila. הפירוט ב-`FINDINGS.md`.

## ״בטיחות״ — המסלול התשעה־עשר בלומדה · l39

**9.9.2026.** נושא חדש נכנס כקובץ תוכן, לא כאפליקציה שלוש־עשרה.
`lomda/data/safety.js` — שלושה נושאים (הבית, הדרך, הסימן
הכתוב), 45 פריטים בשלוש רמות, ארבע שפות מלאות, ובכל רמה שלישית
קטע `Sc` עם שאלות שהתשובה להן נמצאת בתוך הטקסט.

**שלוש נגיעות ואפס שורות מנוע,** כמו שכתוב ב-`CLAUDE.md`: הקובץ,
תגית `<script>` לפני המנוע, ושורה ב-`PRE` שב-`lomda/sw.js`.

**התוכן הוא מנגנון ולא הוראה,** באותה זהירות של `money.js` ומאותה
סיבה: אדם עלול לפעול לפיו במציאות. אין כאן הנחיה רפואית, אין מספר
חירום כתשובה נכונה, ואין חוק או תקן עם מספר — חוק משתנה, והמאגר
אינו יודע מתי הוא נקרא. מי שיודע **למה** העשן מסוכן יבין גם הנחיה
שישמע במקום אחר.

**נמדד:** `banks.js` — 19 מאגרים, 45 נושאים, 862 פריטים, 0 ממצאים
(לפני: 18 / 42 / 817). `options.js` על לומדה — 81,000 צירופי שאלה,
`under4=0`, `multiCorrect=0`, `noCorrect=0` (לפני: 75,600).
בכרומיום מול השרת המקומי, בארבע השפות: המסלול נטען, שלושת הנושאים
מופיעים, אפס שגיאות JS.

**ותיאור הכרטיס עודכן בארבע שפות** — ״שמונה־עשר תחומים״ היה נכון
אתמול והיום הוא לא. שתי מחרוזות בכל שפה (כרטיס ו-`lead`) וגם
`marketing/wix-content.md`, ולכן מפתח הקאש של דף הבית עלה ל-43.

**אגב כך, שני שדות מתים ב-`heat.js`:** `icon:"flask"` אינו
מפתח ב-`ICONS` — אבל השדה `icon` נקרא, נשמר ב-`TRACKS` ואינו
מצויר לעולם, בכל 19 המאגרים; ו-`cls:"t-sci"` אינו קיים בגיליון
הסגנון, ולכן הכרטיס נפל ל-`--accent` הכללי של `.trk` במקום לצבע
מסלול. שניהם הוחלפו בערכים שקיימים. אף אחד מהם לא שבר דבר.



## כפתור השפה הערבי והרוסי — בגופן שיש בו את הכתב

**ענף `claude/language-bar-netiv-fix-rtuxi6`, 9.9.2026.**
`b58 t58 u56 v54 g53 x25 a21 n52 m60 l34 · reader 32 · הפורטל 42` —
כל שנים־עשר המפתחות, מפני ש-`legal/protect.js` השתנה.

הכלל `html[lang="ar"]` חל רק כשהדף **כולו** בערבית, וסרגל השפות הוא
בדיוק המקום שבו זה לא מספיק: הכפתור "العربية" מוצג גם כשהממשק עברי —
זה כל תפקידו — ואז הוא יורש את גופן הכותרת. Heebo ו-Assistant אינם
מכילים לא ערבית ולא קירילית, ולכן "العربية" ו-"Русский" נפלו לגופן
ברירת המחדל של המערכת, ושני כפתורים מתוך ארבעה נראו בגודל אחר.

הטקסט נשאר קריא — הדפדפן משלים גליף חסר מהגופן הבא בשרשרת — ולכן
זה לא נראה כמו באג, ואיש לא דיווח. נמדד בכרום דרך השרת המקומי:

    דף הבית  ru = Heebo           (ar כבר היה מתוקן, ru לא)
    pricing  ar = Heebo, ru = Heebo
    reader   ar = Assistant, ru = Assistant
    שער התנאים ar = Heebo, ru = Heebo — בכל שתים־עשרה האפליקציות

ארבעה תיקונים, שני כללי CSS כל אחד, לפי `lang` של הכפתור עצמו —
תכונה שכבר הייתה בסימון בכל ארבעת המקומות:
`index.html`, `pricing/index.html`, `reader/index.html`
ו-`legal/protect.js`, שהוא השער שכל שתים־עשרה מזריקות והמסך הראשון
שלומד חדש רואה. אחריהם: `14 כפתורים נמדדו, 0 ממצאים`.

**פונקציית החלפת השפה לא נגעה בה איש.** אומת בכרום ב-360px
וב-1200px, בארבע השפות, בדף הבית וב-pricing: `lang` ו-`dir`
מתחלפים, `aria-pressed` עובר, כל כפתור לפחות 44×44, וגלישה
אופקית אפס בשני הרוחבים.

ונוספה `node .claude/qa/langbtn.js`, שמודדת בדפדפן את מה שהכפתור
**קיבל** ולא את מה שה-CSS אומר. `fonts.js` היה ירוק לגמרי עם הבאג
בפנים, מפני שהוא שואל על הדף ולא על הכפתור.



## הבנק הסטטי נסרק — `content806.js`

**`bagrut-806` הייתה האפליקציה היחידה בריפו בלי דוח תוכן.**
`content.js` מגריל שאלות מ-`buildQ`; ל-806 אין מחולל, התוכן שלה
כתוב ביד ב-`data/exams.js`, והיא נפלה בין הכיסאות. `stage.js`
הדפיס עליה ״לא נסרקה״, ולכן היא גם לא יכלה לעלות שלב.

`content806.js` סורק אותה **במלואה ולא במדגם** — 8 אוספים, 48
שאלות, 114 סעיפים — וכותב את אותו `reports/<app>.json` ש-`stage.js`
קורא. אין שכפול של `exam806.js`: שם הסכימה, `speech` מול `latex`,
`tolerance` ו-`reject`. כאן רק מה שרואים כשכל המאגר פתוח יחד —
כפילות בין אוספים, ניסוח, מה שההקראה תיתקל בו, ואיזון הנושאים.

**הוכחת נפילה על עותק שבור, בלי לגעת בתוכן האמיתי** (`QA806_DATA`):
FAIL עם 2 חוסמים ויציאה 1, ועל התוכן האמיתי PASS ויציאה 0.

**ושתי בדיקות שלי נדלקו על תוכן תקין, ותוקנו לפני המיזוג:**

- **כפילות שאינה כפילות.** ההשוואה הייתה על `text` בלבד, ו״נתונה
  הפונקציה שלהלן.״ הוא פתיח גנרי בשמונה שאלות — כל מה שמבדיל
  ביניהן ב-`latex`. שמונה שאלות שונות לגמרי סומנו ככפולות.
  המפתח כולל היום גם את הנוסחה, ולסעיף גם את נוסחת השאלה־האם.
- **״תשובה שאפשר לנחש״ ירדה כליל.** היא דיווחה שהתשובה 2 חוזרת
  ב-10 מתוך 114 סעיפים. ב-`content.js` המשפחה נכונה כי בוחרים
  מארבע; כאן התשובה נכתבת בשדה חופשי, ו-8.8 אחוזים הם התפלגות
  טבעית של מספרים קטנים — לא אקספלויט.

בדיקה שנדלקת על תוכן תקין מאמנת להתעלם ממנה, וזה גרוע מבדיקה
שאינה קיימת.

וגם: משפחת ממצאים ששמרה את החומרה **הראשונה** שנוספה לה בלעה
ממצא חוסם שהגיע אחריה, והפסק דין יצא REVIEW במקום FAIL. החומרה
היום היא החמורה שנמצאה.

`stage.js` מדפיס היום ״סריקה PASS 0/0״ במקום ״לא נסרקה״, והאזהרה
על 806 ירדה. נשארה אזהרה אחת — ״תאוריה מדברת״, שאינה בריפו הזה.

22 בדיקות, כולן עברו. אף אפליקציה לא נגעה, ולכן אין העלאת גרסה.
## הקמת הבוט — משבעה צעדים לשלושה

**ענף `claude/math-tutor-bot-elementary-cq348c`, 9.9.2026.** תיעוד
וכלי בדיקה; אף מפתח קאש אינו עולה.

`ALLOW_ORIGIN` הוסר מההוראות — ברירת המחדל בקוד נכונה. ה-KV
הוחלף בהצהרה אחת, מפני שהחסם החזק הוא אשראי מראש בחשבון
Anthropic ולא הקוד הזה. ונוסף `.claude/qa/enable-tutor.js`,
שעושה בפקודה אחת את ארבעה־עשר השינויים שחייבים לקרות יחד
בהפעלה — כתובת, גרסת תנאים, שנים־עשר מפתחות ו-STATUS.

## `theory/buy.html` — הטופס הוא קבוע אחד, `FORM_URL`

**9.9.2026.** במקום מציין מקום בהערה, שורה אחת: `var FORM_URL="";`.
כשהיא מלאה הדף בונה `<iframe>` עם `title` בעברית; כשהיא ריקה נשארת
הודעת ״בקרוב״. נבדק בכרומיום בשני המצבים, אפס שגיאות. לדף אין מפתח
קאש משלו — השומר ב-`sw.js` של השורש אינו מגיש את `theory/`.

## חסם העלות נכשל־סגור

**ענף `claude/math-tutor-bot-elementary-cq348c`, 9.9.2026.** שרת
בלבד; אף מפתח קאש אינו עולה.

`overLimit` החזיר `false` כשאין KV — כלומר בלי הקישור לא הייתה
תקרה יומית כלל, וה-README סימן את הצעד ״מומלץ״. עכשיו השירות
מסרב לעבוד בלעדיו ומחזיר 503 עם ההסבר, ומי שרוצה אחרת מצהיר
`ALLOW_NO_RATE_LIMIT=yes`.

## המאה, בארבע השפות · lomda l33 · history m59

**״century XIX״ אינו אנגלית.** הרמז וכרטיס המשוב הציגו את המאה של
האירוע, והמילה תורגמה לארבע שפות — אבל הסדר ואופן כתיבת המספר לא.
הקוד בנה תמיד ״מילה ואחריה ספרה רומית״:

| | היה | עכשיו |
|---|---|---|
| אנגלית | `century XIX` | `19th century` |
| רוסית | `век XIX` | `XIX век` |
| עברית | `המאה XIX` | `המאה ה-19` |
| ערבית | `القرن XIX` | `القرن 19` |

**הממצא היה רשום ב-FINDINGS כ״זניח״ וכ״החלטה של הבעלים״, ושניהם
היו שגויים.** הטענה הייתה שאין תשובה נכונה בקוד כי לאנגלית סדר
מילים אחר — אבל לכל שפה יש בדיוק תשובה אחת, ורק הסדר ואופן המספר
משתנים. ברוסית הספרה הרומית היא הכתיב המקובל ולכן היא נשארת;
בעברית ובערבית אין משתמשים בה כלל, ולקהל שקורא לאט ספרה ברורה יותר.

**`century.js` נכתב לפני התיקון, והורץ אדום.** 22 ממצאים בשתי
האפליקציות ובארבע השפות, ואחרי התיקון — נקי. הבודק מחלץ את
`centuryStr`, `centuryOf`, `_rom` ואת ארבעת מילוני `T` מתוך
ה-`index.html` ומריץ אותם, ולכן הוא בודק את הקוד שרץ אצל הלומד
ולא העתק שלו. נוסף ל-`all.js` ולטבלה ב-README.

**ההקראה לא נשברה.** `saySpell` ממיר ספרה רומית עומדת בפני עצמה
לספרה רגילה, וזה נחוץ עכשיו ברוסית בלבד: `XIX век` נאמר `19 век`.
שאר הצורות אינן מכילות רומית. נמדד על הפונקציה עצמה, וגם בדפדפן
אמיתי בשתי האפליקציות ובארבע השפות — כולל לפני הספירה.

`centuryStr` מזין רמז וכרטיס משוב בלבד, ולעולם לא תשובה או מסיח,
ולכן שינוי הצורה אינו נוגע בבדיקת התשובות.
## `deployed.js` — מסלול הלומד על הבתים שנפרסו, ומניין התחומים ב-CI

**ענף `claude/happy-johnson-usjuav`, 9.9.2026.** כלים בלבד. אף קובץ
שמוגש ללומד לא השתנה, ולכן אין העלאת גרסה ואין מפתח קאש חדש.

**`.claude/qa/deployed.js` — חדש, ורץ ביד בלבד.** מוציא worktree על
קומיט, מגיש אותו בשרת המקומי כפי ש-GitHub Pages מגיש, ומעביר דפדפן
אמיתי במסלול של לומד — שער השפה, האונבורדינג, המסך — בארבע שפות
ובכל אפליקציה ציבורית. בודק שגיאות JS, שמחרוזת ה-`BUILD` שבקובץ
באמת מופיעה על המסך, ושהגרסה ומפתח המטמון הם אותה גרסה. מדפיס md5
מול `git show <sha>:index.html` כהוכחה שאלה אכן הבתים של הקומיט.
**אינו בדיקה חיה** — הרשת חסומה כאן, ו-DNS, CDN ותעודה אינם נבדקים.

אינו ב-`all.js`: דורש רשת, יוצר worktree, ולוקח `1m52s` בריצה מלאה.

**`serve.js` מקבל `QA_PORT`** — שורה אחת, כדי שתמונת מצב תוגש במקביל
לשרת הרגיל.

**`apps.js` סעיף 6.5 — מניין התחומים של הלומדה.** מונה את
`lomda/data/*.js` ומשווה למילה שבכרטיס ובפסקת הפתיחה, בארבע שפות.
זו הבדיקה שהייתה תופסת את הבאג של אתמול לפני הדחיפה, והיא רצה
ב-CI. הוכחת הנפילה נעשתה על `e83db88` עצמו — 8 ממצאים שם, 0
ב-`main`.

## `leaks.js` — תיאור של פריט בלומדה אינו מצטט את כותרתו, על כל פריט ובארבע השפות

**ענף `claude/launch-popup-free-tags-akwn5i` (סבב שני), 8.9.2026.** ה-QA
ב-`main` על קומיט המיזוג `9b7aa6c` יצא אדום ב-`say`: ״האוניברסיטה
העברית נפתחה״ ← ״…האוניברסיטה העברית נפתחה בטקס גדול…״. מקומית אותו
קומיט עבר — `say.js` דוגם 60 שאלות באקראי, והמאגר גדל ב-104 פריטים.

**התוכן תוקן ב-`main` קודם.** סשן מקביל דחף את `5db8f63` (״ארבעה
פריטים שהתיאור שלהם חזר על הכותרת״) ואת l31 בזמן שכאן נוסחו מחדש
שבעה. מי שהגיע ל-`main` ראשון נשאר; הניסוחים מכאן נזרקו, ומפתח
לומדה נשאר l31.

**מה נלקח מהבנייה שנזרקה: `leaks.js`, ב-`all.js`.** עובר על כל פריט
`E` בכל 18 המאגרים, בארבע השפות; תיאור שמכיל את הכותרת מילה במילה
הוא ממצא. על התוכן שלפני שני התיקונים: 7 (`edu.js` ×6, `geo.js` ×1).
על `main` אחרי `5db8f63`: 0 מתוך 232. `say.js` נשאר — הוא בודק את
המנוע, וזה בודק את התוכן, בלי מזל.

## ארבעה פריטים בלומדה הקריאו את התשובה בתוך השאלה · l31

**`say` תפס דליפה:** בשאלת ״על איזה אירוע מדובר?״ ההקראה קוראת את
תיאור האירוע, ובארבעה פריטים שנחתו ב-8.9 התיאור חזר על הכותרת
מילה במילה — ״הכנסת חוקקה את חוק זכויות התלמיד…״ כשהתשובה היא
״חוק זכויות התלמיד״. הבדיקה דוגמת 60 שאלות אקראיות, ולכן ריצה
אחת עברה וריצה שנייה נפלה.

**נמדד בסקריפט על כל `lomda/data/*.js`, בארבע השפות:** שלושה
פריטים ב-`edu.js` בעברית (האוניברסיטה העברית, חוק חינוך מיוחד,
חוק זכויות התלמיד) ואחד ב-`geo.js` בערבית בלבד (ייבוש אגם החולה).
התיאור נוסח מחדש כך שאינו מכיל את הכותרת, בכל שפה שבה חזר עליה.
שום תוכן לא נמחק: הכותרת, הסיבה והתוצאה ללא שינוי.

`node .claude/qa/say.js lomda --n 200`: 0 דליפות. `banks.js`: 0 ממצאים.

## תמונת שיתוף לכל דף · b57 t57 u54 v52 g51 x24 a20 n51 m58 l31 · reader 31 · הפורטל 40

**קישור בוואטסאפ הציג את האייקון הקטן, ובפייסבוק כלום.** תגיות
השיתוף מ-6.9 הצביעו על `icon-512.png` — ריבוע 512 — עד שתהיה
תמונה בגודל שהרשתות מצפות לו (משימה E1 ב-`CELINE.md`).

**עכשיו לכל דף `img/og.png` של 1200×630**, 12 קבצים בין 35KB
ל-46KB (`ls -l`): רקע בצבע `theme_color` מה-`manifest.json` של
אותה אפליקציה, האייקון שלה, השם העברי מ-`DATA.APPS` באות גדולה,
ושורת ״למידה שנשמעת״. בלי טקסט שיווקי. ניגודיות הלבן על הרקע
חושבה בסקריפט לכל צבע; `math-app` לבדה הוכהתה ב-15% כדי לעבור
4.5:1, שאר האחת־עשרה עוברות כפי שהן. `pricing/` מצביע על תמונת
הבית. נוספו `og:image:width` ו-`og:image:height`, ו-`twitter:card`
עלה ל-`summary_large_image` — זה מה שגורם לכרטיס הרחב במקום
לתמונה הממוזערת. התמונות אינן ב-`PRE` של ה-`sw.js`: הן נקראות
על ידי השרתים של הרשתות, לא על ידי המשתמש, ואין מה לשמור אופליין.

**12 מפתחות עולים**, כי 12 קובצי `index.html` השתנו.
## ארבעה קטעי קריאה ל״חימום וקירור״, והמניין בדף הבית · l32 · 41

**ענף `claude/happy-johnson-usjuav`, 8.9.2026.** תוכן ומספר, ולא מנוע.

הבעלים ביקש שוב את ״מדעים מדברים״ ב-React Native, ובזמן שהענף
הזה בנה מסלול `heat` משלו — סשן אחר הקדים ודחף ל-`main` את
`l30` שלמעלה. **מי שהגיע ראשון נשאר**, ומהבנייה שנזרקה נלקח רק
מה שאין בה: המסלול שנחת בנוי כולו מפריטי `C`, ואין בו אף
`Sc` — קטע ושאלות עליו.

לכן נוספו כאן **ארבעה קטעים ברמה 3**: שלוש כפות באותה כוס
(הולכה) ורדיאטור מול מזגן באותו חדר (הסעה) ב-`heat-ways`; כוס
תה שמתקררת ב-`heat-temp`; ובדיקת הסוודר עם שני מדחומים
ב-`heat-ideas`. כל נימוק מפנה לשורה בקטע ולא לידע חיצוני — מי
שקרא, יכול לענות. ארבע שפות, בלי אות לטינית אחת בשדות `ar`
ו-`ru`.

**ודף הבית אמר ״שבעה־עשר תחומים״ כשכבר היו שמונה־עשר.** המסלול
החדש נכנס ל-`lomda` בלי שהמניין בדף הבית זז, בארבע שפות ובשני
מקומות בכל שפה — כרטיס הלומדה ופסקת הפתיחה. תוקן, ומפתח הקאש של
דף הבית עלה ל-41. `wix-content.md` ו-`STATUS.md` רועננו מהכלים
שלהם.

## תקרת עלות, מודל זול, ודליפת הקראה שנחשפה · m58 l31

**ענף `claude/math-tutor-bot-elementary-cq348c`, 8.9.2026.**

**עלות.** המודל ל-`claude-haiku-4-5` והתקרה היומית מ-3000 ל-100,
לבקשת הבעלים. החלפת המודל לבדה הייתה מפילה כל פנייה ב-400:
`effort` מחזיר שגיאה ב-haiku ו-`fallbacks` שייך למשפחת opus.
נוספה טבלת `CAP` ושמונה בדיקות עליה.

**הקראה.** `formName` הקריא את שדה `w`, ופריט שתיאורו פותח
בכותרת הסגיר בכך את התשובה — ״האוניברסיטה העברית נפתחה״
ב-`edu.js`. מי שלוחץ רווח כדי לשמוע את השאלה שמע את הפתרון.
הצורה נופלת עכשיו לצורה אחרת, ב-`lomda` וב-`history` יחד.

## ״חימום וקירור״ — נושא מדעי חדש בלומדה · l30

**ענף `claude/math-tutor-bot-elementary-cq348c`, 8.9.2026.** תוכן
בלבד; מפתח אחד עולה.

הבעלים ביקש אפליקציה שלוש־עשרה בשם ״מדעים מדברים״, ב-React
Native, עם מסך התחברות. שלושה דברים מנעו את זה, וכולם כללים
כתובים: React ו-npm אסורים במאגר הזה ו-GitHub Pages אינו יכול
להגיש אפליקציית טלפון כלל; ״אין חשבון, אין הרשמה״ כתוב בתנאי
השימוש; והקריטריון ב-`CLAUDE.md` קובע שתוכן שנכתב כמושג ונענה
בבחירה מארבע הוא **נושא ב-lomda** ולא אפליקציה.

לכן אותו תוכן נבנה כמסלול `heat`: שלושה נושאים, 45 פריטים,
ארבע שפות. דרכי מעבר החום; טמפרטורה מול חום מול אנרגיה תרמית;
ותפיסות חלופיות לצד ההבחנה בין שפה יומיומית לשפה מדעית.

התוכן **נכתב מאפס** ואינו העתקה מחומרי לימוד של אף גוף.

## מבצע השקה: חלון קופץ, תגיות חינם, theory/buy.html, פס בעמוד מחירים, אירועי GoatCounter · 39

**ענף `claude/launch-popup-free-tags-akwn5i`, 7.9.2026.** אין עדיין גבייה,
ולכן דף הבית אומר זאת ומודד. שכבה מעל הכרטיסים, לא שינוי בהם: `DATA.APPS`,
המחירים והטקסטים לא נגעו.

**דף הבית (39 — 37 ו-38 נלקחו ב-`main` בזמן הבנייה).** `launch.js` ו-`launch.css` חדשים בשורש, נטענים
ב-`?v=1` ומצורפים מראש ב-`sw.js`. חלון קופץ ״🎉 מבצע השקה״ 500ms אחרי
הטעינה, פעם ב-7 ימים (`localStorage: launch-popup-seen`, חותמת זמן),
`role="dialog"`, Escape ורקע סוגרים, ״התחילו ללמוד״ גולל אל `#grid`.
כל תגית `.t.price` מקבלת ״חינם בתקופת ההשקה״ עם המחיר חוצה; ״חינם״
מקבלת ״חינם לתמיד״; `data-launch-done="1"` אומר ״לא לגעת״ וגם ״כבר
טופל״. ״תאוריה מדברת״ נשארת בתשלום: `120 ₪` חוצה, ״מבצע השקה: 79 ₪״,
וכפתור ״לרכישה״ אל `theory/buy.html`. הכרטיס נושא `data-app` — התוספת
היחידה ב-`draw()`. ארבע שפות לכל מחרוזת.

**החלון ממתין לשער התנאים.** בביקור ראשון `legal/protect.js` פותח את
`.lg-wrap` מעל הכול; החלון נפתח רק כשאין שער על המסך. נמצא בדפדפן.

**אירועי GoatCounter,** רק אם `window.goatcounter` קיים: `launch-popup-shown`,
`launch-popup-start`, `launch-popup-dismiss`, `theory-buy-click`.

**`theory/buy.html`** — עמוד RTL: ״ההרשמה לרכישה נפתחת בקרוב״, מקום
לטופס (`<!-- GOOGLE_FORM_HERE -->`), ותגית GoatCounter כמו בשאר הדפים.

**`/pricing/`** — פס צהוב כאלמנט הראשון ב-`body`: ״מבצע השקה: כל
המסלולים פתוחים כעת ללא חיוב״. לדף אין `sw.js` משלו, ואין מפתח להעלות.

**`tests/launch.test.html`** — שבע בדיקות בדפדפן; `?without=1` מריץ
אותן בלי `launch.js` להוכחת נפילה. הפלטים ב-`FINDINGS.md`.

## תיקון: הבדיקה החשבונית פסלה תשובות נכונות

**ענף `claude/math-tutor-bot-elementary-cq348c`, 8.9.2026.** תיקון
בשרת בלבד; אף מפתח קאש אינו עולה.

`badEquation` קרא מספרים שלמים בלבד, ולכן ״1/2 = 0.5״ נקרא
כ-״= 0״ ונפסל: המורה אמר דבר נכון והשומר זרק אותו. עכשיו שלושה
שלמים נבדקים בדיוק, ועשרוני מקבל סבילות של 0.011 — עיגול לשתי
ספרות. שבע בדיקות חדשות, עם הוכחת נפילה.


## עוד 104 פריטי E בשישה מאגרים — המאגרים החדשים השוו למנוסים · l27

**ענף `claude/21-apps-unified-format-isge0e`, 8.9.2026.** תוכן בלבד;
מפתח אחד עולה.

**המשך ישיר של הסבב הקודם.** אחרי `med`, `eng` ו-`geo` נשארו 34 תאים
ב-`low-variety` שבדוח `content.js`, כולם רמות 1–2 של מאגרים שכולם `C`.
הסבב הזה כיסה שישה מהם: `bio`, `chem`, `phys`, `media`, `edu`
(16 פריטים כל אחד) ו-`tec-ai` (8; `tec-invent` כבר היה מלא ב-`E`).

**כלי חדש שנולד מהסבב הקודם:** רשימת **144 האירועים התפוסים** בכל
שבעה־עשר המאגרים, שנגזרה מהקוד ונמסרה לכל הכותבות. כותבת הרפואה
גילתה לבד ש-`nearEvents` נופלת חזרה על `allEvents()` הגלובלי, ולכן
אירוע שנכתב פעמיים הוא תוכן כפול; מהסבב הזה זה נאכף מראש.

**התוצאה, נמדדה בדפדפן ב-300 בנייות לכל רמה:**

    ותיקים (5 מאגרים):        248 פריטים → 449 שאלות שונות · 1.81 לפריט
    חדשים עם E (9 מאגרים):    424 פריטים → 748 שאלות שונות · 1.76 לפריט
    חדשים C בלבד (3 מאגרים):   96 פריטים → 102 שאלות שונות · 1.06 לפריט

**תשעת המאגרים שקיבלו `E` עומדים היום באותה רמה כמו הוותיקים.**
`low-variety` ירד מ-34 תאים ל-12.

**שלושה מאגרים נשארים `C` בלבד, וזו החלטה ולא פער:** `bible`,
`lit` ו-`lashon`. בתנ״ך, בספרות ובלשון אין אירוע מתוארך שהוא התשובה
לשאלה ״מתי״, ולכפות עליהם `E` פירושו להמציא שנים — הדבר האחד
שאסור. **מי שיראה אותם ב-`low-variety` בעתיד: זה מכוון.**

**104 השנים נבדקו אחת־אחת.** הכותבות ויתרו על אירועים שלא היו
בטוחות בשנתם — גלי הרדיו של הרץ (1886/1887/1888), הפסטור של פסטר,
הכלבים של פבלוב, שלבי פיאז׳ה, טיקטוק — ועל אירועים ששנתם צמודה
לשנה אחרת באותה רמה, מפני ששתי שנים צמודות הופכות את השאלה לניחוש.

`node .claude/qa/all.js` → `18 בדיקות, כולן עברו`.
## ״תאוריה מדברת״ — תפקיד בשרת, וחיווט מוכן להחלה

**ענף `claude/math-tutor-bot-elementary-cq348c`, 8.9.2026.** אין
כאן שינוי באפליקציות, ולכן אף מפתח אינו עולה.

האפליקציה השתים־עשרה יושבת בריפו נפרד, אבל **באותו מקור** — ולכן
היא טוענת את `/tutor/tutor.js` מכאן, בדיוק כמו `/legal/`. נוספה
`ROLE.theory` ב-`worker.js`; בלעדיה `readBody` דוחה אותה ב-400.

החיווט עצמו נבנה ונבדק בדפדפן אמיתי בארבע השפות, ונשמר
כ-`tutor-api/theory.patch` — לסוכן כאן יש גישת קריאה בלבד לריפו
ההוא. הפירוט וההוכחות ב-`tutor-api/THEORY.md`.

## `bagrut-806` מדברת ארבע שפות · b56 t56 u53 v51 g50 l28 n50 m57 a19 x23 30

**ענף `claude/math-tutor-bot-elementary-cq348c`, 8.9.2026.**

האפליקציה עברית בלבד ואין בה בורר שפה. במקום לתרגם אותה — עבודה
שאינה המשימה — נוסף דגל `pickLang` לתשתית: אפליקציה בלי בורר
מקבלת בורר בתוך הפאנל, ושפת הבוט מנותקת משפת האפליקציה.

הכפתור עבר משורת הרמזים לשורת ״בדקו״: שורת הרמזים נבנית רק כשיש
`steps`, וסעיף בלי רמזים היה נשאר בלי מורה.

`tutor/tutor.js` משותף כמו `legal/` — עותק ישן יושב במטמון של כל
אפליקציה עד שהמפתח שלה עולה — ולכן אחד עשר המפתחות עלו יחד.

## ״עזרה מהמורה״ בכל אחת עשרה האפליקציות · b55 t55 u52 v50 g49 l27 n49 m56 a18 x22 29

**ענף `claude/math-tutor-bot-elementary-cq348c`, 8.9.2026.** התשתית
עברה מהאפליקציה אל קובץ משותף, ואחת עשרה האפליקציות מחווטות אליו.
`legal/` לא השתנה מבנית הפעם, ולכן דף הבית נשאר `38`.

### תשתית אחת, לא אחת עשרה העתקות

`tutor/tutor.js` — פאנל, הקראה, ארבע שפות וכיווני כתיבה, קובץ
אחד שכל האפליקציות טוענות בדיוק כמו `/legal/`. הקוד שהיה בתוך
`math-app` נמחק ממנה והוחלף בקריאה ל-`TUTOR.mount(...)`.

הפאנל נתלה על `document.body` ולא בתוך `#app`, מפני שכל האפליקציות
בונות את `#app` מחדש בכל `render()` — פאנל שיושב בפנים נמחק
באמצע הקלדה.

**תווית הכפתור באה מהתשתית** ולא ממילון האפליקציה. במילונים האלה
המחרוזת העברית היא המפתח, והוספת מחרוזת שם היא נגיעה במנגנון
התרגום.

### תפקיד לכל אפליקציה

בשרת: `CORE` — אחת עשרה שורות התנהגות זהות לכולן, ו-`ROLE` —
תפקיד לכל אפליקציה לפי המקצוע ורמת הלימוד. אפליקציה שאינה
ברשימה נדחית ב-400.

### ארבע שפות, והקראה

שפת הבוט הולכת אחרי בורר השפה הקיים; החלפת שפה באמצע שיחה מאפסת
אותה, אחרת הבוט היה ממשיך לענות בשפה הקודמת. כיוון הכתיבה נקבע
לפי שפת הבוט ולא לפי האפליקציה.

לכל תשובה כפתורי **הקראה** ו**עצירה** ובורר **מהירות** (0.7 עד
1.15), והטקסט **נשאר על המסך** כל זמן ההקראה. אין מיקרופון —
ולא בטעות: `node .claude/qa/tutor.js` נופל אם מישהו יוסיף אחד.

**בלימודי שפה** (`english`, `ulpan`) שפת ההסבר אינה השפה הנלמדת:
המודל עוטף כל מילה בשפה הנלמדת ב-`« »`, והדפדפן מקריא כל קטע כזה
בקול של אותה שפה.

### שלושה באגים שנמצאו בדפדפן ותוקנו

1. **רשימת הקולות מגיעה אחרי הציור הראשון** — `getVoices()` החזיר
   0 בפתיחה, והפאנל הציג ״אין קול בשפה הזאת״ ונשאר כך. נוספה
   האזנה ל-`voiceschanged`.
2. **החלפת שפה באמצע שיחה** — הפאנל התחלף, הבוט המשיך בשפה
   הישנה. השיחה מתאפסת עכשיו גם על שינוי שפה.
3. **מיפוי השאלה היה של בונה המבחן ולא של התרגול** — נשלח
   `expr` ריק בארבע אפליקציות החידון, ו-`[object Object]`
   ב-`english`. השדות נמדדו בדפדפן והמיפוי אוחד לשתי המשפחות.

## ״חשבון ליסודי״ — שם חדש, ובוט לימודי · 38 b54 t54 u51 v49 g48 a17 n48 m55 l26 x21 28

**ענף `claude/math-tutor-bot-elementary-cq348c`, 8.9.2026.** נגענו
ב-`legal/`, ולכן **כל שנים־עשר המפתחות** עולים יחד ולא תשעה.

### השם

״מתמטיקה לחכמים שמתקשים״ → **״חשבון ליסודי״**, בארבע השפות. השם
הקודם אמר למי האפליקציה מיועדת, והוא גם תייג את הילד שמחזיק את
המכשיר. השם החדש אומר את התחום ואת שכבת הגיל.

    الحساب للمرحلة الابتدائية
    Арифметика для начальной школы
    Arithmetic for Primary School

עודכן בכל אחד מחמשת המקומות שב-`NAMING.md`, ובנוסף בדף הבית,
בעמוד המחירים ובחומרי השיווק. `CHANGELOG.md` הוא היסטוריה ולכן
המופעים בו נשארו.

### ״עזרה מהמורה״ — בוט לימודי

שכבת עזרה שלישית ב-`math-app`, אחרי שלושת הרמזים ואחרי הלמידה
האיטית — לא במקומם.

**הוראות המערכת יושבות בשרת ולא בדפדפן.** `tutor-api/worker.js`,
במשתנה `SYSTEM`. דפדפן אפשר לערוך, ולכן מורה שמוגדר בדפדפן אינו
מורה אלא הצעה.

**האתר סטטי, ולכן יש שרת.** GitHub Pages אינו יכול להחזיק מפתח
API — מפתח בקובץ סטטי הוא מפתח פומבי. פונקציית Cloudflare מחזיקה
אותו כ-Secret; אין ולא יהיה מפתח במאגר.

**ברירת המחדל: כבוי.** `TUTOR_API` ריק, ואז הכפתור אינו נבנה
כלל. מי שלא פרס שרת אינו רואה שינוי.

**מה נשלח:** התרגיל שעל המסך, שפת הממשק, ומה שהילד כתב. לא שם,
לא התקדמות, לא הגדרות. השיחה יושבת בזיכרון בלבד ונמחקת עם השאלה
הבאה.

**שתי בדיקות מכניות על כל תשובה, בשרת:** חשיפת הפתרון אסורה
בשני התורים הראשונים, וכל ״א פעולה ב = ג״ שבטקסט מחושב מחדש —
`8 + 7 = 16` נפסל. נפסל → ניסיון שני אחד → שאלה מנחה קבועה.
`node .claude/qa/tutor.js`, 22 בדיקות.

**תקרות עלות:** 60 פניות ליום ל-IP, 3,000 ליום לשירות כולו,
700 טוקנים לתשובה, `effort: "low"`, ותקרה מקומית של 30 במכשיר.

### תנאי השימוש

המשפט ״כלום לא נשלח לשום שרת״ סויג ל״ההתקדמות שלכם אינה נשלחת
לשום שרת״, ונוסף סעיף ״עזרה מהמורה״ בארבע השפות. `LEGAL.version`
נשאר `1.0` **כל עוד השירות כבוי** — ו-`tutor.js` נופל ביום
ש-`TUTOR_API` יתמלא בלי שהגרסה עלתה.

## פריטי E ברפואה, הנדסה וארץ ועולם — מגוון השאלות הוכפל · l26

**ענף `claude/21-apps-unified-format-isge0e`, 8.9.2026.** תוכן בלבד;
מפתח אחד עולה.

**מה הניע את זה — מדידה.** שנים־עשר המאגרים החדשים נכתבו כמעט כולם
כפריטי `C`, מפני שהכותבים הונחו לא להמציא שנים. נמדד בדפדפן, 300
בנייות לכל רמה:

    מאגרים ותיקים:  248 פריטים → 449 שאלות שונות  ·  1.81 שאלות לפריט
    מאגרים חדשים:   384 פריטים → 425 שאלות שונות  ·  1.11

הסיבה: `buildQuestion` בונה מפריט `E` שלוש צורות ברמה 1, ארבע ברמה 2
וחמש ברמה 3; מפריט `C` הוא בונה אחת.

**מה נעשה: הוספה, לא המרה.** 48 פריטי `E` נוספו ל-`med`, `eng`
ו-`geo` — ארבעה לרמה 1 וארבעה לרמה 2 בכל אחד משני הנושאים שבכל קובץ.
**אף פריט `C` קיים לא נמחק, לא נוסח מחדש ולא הוזז בין רמות.** הרמות
1 ו-2 הן כעת `c×4 + e×4`, וזה בטוח: `nearEvents` בונה מסיחי שנה
מפריטי `e` בלבד, ו-`formTerm` בונה מסיחי הגדרה מפריטי `c` בלבד.

**התוצאה, אותה מדידה:**

    כל שנים־עשר החדשים: 432 פריטים → 575 שאלות שונות · 1.33 לפריט
    ובששת הנושאים שנגענו בהם, שאלות שונות ברמות 1/2:  4/4  →  16/17

**48 השנים נבדקו אחת־אחת.** הכותבות ויתרו על אירועים שלא היו בטוחות
בשנתם (הקמת מקורות, מתקן שורק, שנת הסיום של ייבוש החולה) ועל אירועים
שכבר קיימים במאגרים אחרים — לינד ב-`health`, ג׳נר, רנטגן, לנדשטיינר
והאבעבועות ב-`science` — מפני ש-`nearEvents` נופל חזרה על כל האירועים
בכל המאגרים.

**וזה נבדק ולא הונח:** 2,400 שאלות נבנו סביב ארבעת האירועים שכן
מופיעים בשני מאגרים (פניצילין, הטלפון), ובאף אחת מהן לא הופיעו שתי
אפשרויות שמתארות את אותו אירוע.

`node .claude/qa/all.js` → `18 בדיקות, כולן עברו`.

## בונה המבחן הכיתתי — המסלולים היו 3/4/5 יח״ל · b53 t53 u50 v48 g47 a16 n47 m54 l25

**ענף `claude/21-apps-unified-format-isge0e`, 8.9.2026.** תיקון רוחבי,
ולכן תשעה מפתחות עולים יחד. `legal/` לא נגעו בו, ולכן שלושת הנותרים
(`bagrut-806`, `reader`, השורש) נשארים.

**הבאג.** שורת ״יחידות״ בבונה המבחן הגיעה עם המודול ממשפחת math-uni
ורשימתה הייתה קשיחה — `[3,4,5]`. בארבע אפליקציות החידון המסלולים אינם
יחידות לימוד אלא מסלולי תוכן (`nature`, `anc`, `sound`…), ולכן לחיצה
על ״4 יח״ל״ כתבה `state.track="4"` — מזהה שאינו קיים — ו-`topicsForTrack`
החזיר **אפס נושאים**. בלי שגיאה, בלי הודעה: המורה ראה רשימת נושאים ריקה.
ובלי ללחוץ בכלל, הבונה הציג רק את נושאי המסלול הראשון.

**נמדד בדפדפן אמיתי, לפני:**

    lomda    39 נושאים · 3 נגישים · אחרי לחיצה: 0 (track="4")
    history  16 נושאים · 4 נגישים · אחרי לחיצה: 0
    english  15 נושאים · 3 נגישים · אחרי לחיצה: 0
    ulpan    12 נושאים · 3 נגישים · אחרי לחיצה: 0

**אחרי:** הכפתורים נגזרים מ-`TRACKS` ונושאים את שמות המסלולים
האמיתיים; ב-lomda שבעה־עשר כפתורים, ולחיצה על ״אזרחות״ נותנת 6 נושאים.

**שלוש נגיעות נוספות שהתיקון חשף:**
- `trackSel()` ו-`topicsForTrack` נופלים חזרה למסלול הראשון כשהמזהה
  אינו קיים — מכשיר שכבר שמר `state.track:"4"` היה נשאר ריק גם אחרי התיקון.
- מעבר מסלול מנקה נושאים שנבחרו במסלול הקודם (לא הוצגו, ולכן גם אי
  אפשר היה לבטלם) ומאפס את `EB.q` — בלעדיו מבחן שנבנה אחרי החלפת מסלול
  נארז מהשאלות של המסלול הקודם. נמדד: מבחן ב״כימיה״ יצא עם
  `["matter","body","earth"]`.
- `exSlots` נמנע מכפילות: מבחן כימיה בן עשר שאלות יצא עם ״חמצן״ ארבע
  פעמים, ״אלקטרון״ פעמיים ו״גרעין האטום״ פעמיים — **חמש שאלות שונות
  מתוך עשר.** אחרי: 8 מתוך 10 בשלוש הרצות, וזה המרב האפשרי (שני נושאים
  × ארבעה פריטים ברמה). התיקון הוחל גם בחמש אפליקציות המתמטיקה, מפני
  ש-`exSlots` יושבת בתוך בלוק המנוע ש-`engine.js` דורש שיהיה זהה בתשע.

`node .claude/qa/all.js` → `18 בדיקות, כולן עברו`.

## שנים־עשר תחומים חדשים בלומדה — 17 מסלולים, 39 נושאים · 37 · l24

**ענף `claude/21-apps-unified-format-isge0e`, 8.9.2026.** כל מספר כאן
מפלט של פקודה שהורצה בסשן הזה.

**הבקשה הייתה רשימה של 21 אפליקציות.** תשעה מתוכם כבר קיימים
(`DATA.APPS`): אולפן, ניב, מפנה, לומדה, אנגלית=ניב, היסטוריה=מפנה,
מתמטיקה (שש אפליקציות), חשבון=`math-app`, ומדעים=מסלול `nature`.
שנים־עשר הנותרים נכנסו **כמאגרי תוכן בלומדה ולא כתיקיות**, לפי
הקריטריון: תוכן שנכתב ביד ונענה בבחירה מארבע אינו מצדיק עותק
נוסף של המנוע.

**שנים־עשר קבצים חדשים ב-`lomda/data/`** — `bible`, `lashon`, `lit`,
`geo`, `phys`, `chem`, `bio`, `med`, `eng`, `tech`, `media`, `edu`.
כל אחד מסלול משלו, שני נושאים, שלוש רמות, 32 פריטים, ארבע שפות
בכל שדה. `node .claude/qa/banks.js` אחרי:
`✓ lomda 17 מאגרים · 39 נושאים · 632 פריטים · 18 קובצי data`,
`1 אפליקציות נבדקו, 0 ממצאים`. לפני: 5 מאגרים, 15 נושאים.

**אפס שורות מנוע.** שלוש הנגיעות של נושא חדש בלבד — תגית
`<script>` לפני המנוע, נתיב ב-`PRE` שב-`sw.js`, וגרסה. ארבע מחלקות
הצבע חולקו מחזורית בין שבעה־עשר המסלולים, כפי שההערה על
`TRACK_CLS` צופה.

**הוכחת נפילה למלכודת הסדר.** התגית של `data/tech.js` הועברה אחרי
המנוע: `banks.js` → `1 ממצאים`, ובדפדפן אמיתי מול השרת המקומי
`TRACKS` ירד ל-16 ו-`TOPICS` ל-37 — **בלי שום שגיאת JS**, בדיוק
הכישלון השקט שהבדיקה קיימת בשבילו. שוחזר → `0 ממצאים`, 17 ו-39.

**`cache.js` נתן אדום־ואז־ירוק משלו:** בזמן שהחיווט היה קיים ושנים־עשר
הקבצים עוד לא נכתבו — `✗ lomda/sw.js: PRE מצרף נתיב שאיננו קיים`
על כל שנים־עשר. אחרי הכתיבה: `12 sw.js נבדקו, 0 ממצאים`.

**דף הבית (37).** כרטיס הלומדה וארבע מחרוזות ה-`lead` מונים עכשיו
שבעה־עשר תחומים; מילות החיפוש של הכרטיס הורחבו; `wix-content.md`
סונכרן מ-`DATA.APPS`. **מניין האפליקציות לא זז** — ארבע מחרוזות
ה-`badge` נשארו ״שתים־עשרה״, מפני שלא נולדה אפליקציה.

`node .claude/qa/all.js` → `18 בדיקות, כולן עברו`.
## מנוע ההקראה של "תאוריה מדברת" בכל אחת־עשרה האפליקציות · b53 t53 u50 v48 g47 x21 a16 n47 m54 l24 · reader 28

**ענף `claude/build-sw-cache-busting-pcjd34`, 7.9.2026.**
כל הממצאים כאן מפלט של פקודות שהורצו בסשן הזה — `.claude/qa/all.js`,
`.claude/qa/voice.js` ו-`.claude/qa/cache.js`.

הבקשה הייתה להעתיק את הקול של "תאוריה מדברת" לכל האפליקציות. מה
שנמצא בקריאת שני הצדדים: את שכבת ההגייה שם אי אפשר להעתיק — היא
תוכן של תאוריה בנהיגה (`speech-rules.json`, מילון `KTIV` של תמרורים
ורמזורים, קריוקי, קבצי הקלטה). מה שכן עובר, ומה שבאמת מפריד בין
הקול שם לקול כאן, הם ארבעה מנגנונים שמכסים כשלים שקטים של Web
Speech. אף אחד מהם לא היה בעשר מאחת־עשרה האפליקציות.

**1 · שומר-ער.** Chrome ו-Edge בשולחן העבודה חותכים הקראה אחרי כ-15
שניות באמצע מילה. `pause`+`resume` כל תשע שניות מחזיק את המנוע ער,
ורק היכן שיש את הבאג — במובייל הפעולה הזאת דווקא מקרטעת. היה קיים
רק ב-bagrut-806, ושם רץ גם במובייל; עכשיו מוגבל לשולחן העבודה.

**2 · הפניה חיה ל-utterance.** Chrome אוסף אובייקט שאין אליו הפניה,
ואיתו נעלמים `onend` ו-`onboundary`: ההקראה נתקעת באמצע, ההדגשה
קופאת על מילה אחת, והמקטע הבא לא יוצא לעולם. לא היה באף אפליקציה
(ב-bagrut-806 `current` שימש לכך במקרה).

**3 · שומר זמן.** יש מכשירים שבהם `onend` פשוט לא נורה, והתור נשאר
תקוע עם כפתור שמראה "עצירה". ארבע שניות שבהן המנוע לא מדבר ולא
ממתין נחשבות סוף. לא היה באף אפליקציה.

**4 · נפילה מקול רשת לקול מקומי.** הקולות הנוירליים של Edge הם קולות
רשת, והם שותקים כשהאינטרנט נופל. עד כה זה סיים את ההקראה בשקט.
עכשיו: הדגל נכבה, נבחר קול מקומי, ואותו טקסט נאמר שוב — פעם אחת.

**דירוג הקולות.** `math-teen`, `math-uni`, `math-uni2`, `math-uni3`
ו-`reader` לא דירגו קולות כלל: הם לקחו את הראשון ברשימה בתוך קבוצת
המגדר, כלומר את מי שהמערכת התקינה קודם — לרוב הרובוטי שבהם. נוסף
להם `voiceQuality` בניקוד של "תאוריה מדברת", והמגדר נשאר המיון
הראשון כפי שהוחלט כאן קודם. רשימת הקולות שבמסך ההגדרות מסודרת
עכשיו באותו סדר, כדי שהיא תראה את מה שהאפליקציה באמת בוחרת.

**`.claude/qa/voice.js` — בדיקה חדשה, 19 בדיקות בסך הכול.** מחליפה את
`speechSynthesis` במנוע מזויף שאפשר לומר לו להיכשל, ובודקת בכל
אחת־עשרה האפליקציות את ארבעת המנגנונים. היא החזירה שני ממצאים
אמיתיים ביום שנכתבה:

· הקנס שקול רשת שנכשל קיבל היה קטן מדי מכדי להתגבר על הבונוס שקול
  נוירלי של Microsoft מקבל על שם היצרן (30) ועל "Natural" (22).
  כלומר הנפילה לא עבדה: אותו קול מת נבחר שוב מיד אחרי שנכשל.
  התיקון הוא `voiceUsable` — מיון קודם ולא עוד סעיף בניקוד.
· הבדיקה עצמה עברה בשקט על שלוש אפליקציות מפני שקול הרשת לא נבחר
  בהן מלכתחילה. בדיקה ריקה שמדווחת ✓ גרועה מבדיקה שנופלת, והיא
  אומרת עכשיו במפורש כשלא היה מה לבדוק.

`voice/` לא נגעה: היא דף אבחון של המנוע ואין בה תור הקראה. `legal/`
לא נגעה, ולכן אין כאן העלאה של אחד־עשר המפתחות יחד.

**QA:** 18 מתוך 18 עברו, ו-`voice.js` — 11 מתוך 11.

## מחירים, ספירה אנונימית, גופן רוסי — כל שנים־עשר המפתחות · 36 · b52 t52 u49 v47 g46 x20 a15 n46 m53 l23 · reader 27

**ענף `claude/homepage-pricing-analytics-update-7qxk3w`, 7.9.2026.**
כל המספרים כאן מהקבצים או מפקודות שהורצו; מקור המחירים הוא הודעת
הבעלים מאותו יום.

**דף הבית (36).** ״4 חינם לתמיד, 8 בתשלום״ במקום ״חינם לגמרי״, בארבע
שפות; תיבת הסטטיסטיקה מונה את החינמיות מתוך שדה `price` חדש בכל
רשומה ב-`DATA.APPS`; תגית מחיר בכל כרטיס (`חינם` / `39 ₪` / `120 ₪
חד־פעמי`); ״מחירים״ בתפריט; שלב 3 של ״איך זה עובד״ אומר שבתשלום נשמר
בשרת רק מצב המנוי; סעיף ״עליי״ בנוסח החדש.

**`/pricing/` — דף חדש, ארבע שפות.** Starter מ-39, Pro 99, Family 149
לחודש; טבלת שתים־עשרה האפליקציות; מורה 29, בית ספר 990; שש שאלות
נפוצות. ״התחל״ פותח מייל, עד שיש תשלום. רשום ב-`stages.json` כ-`notApps`.

**GoatCounter בשלושה־עשר דפים ומשפט בתנאים** — ״האתר סופר כניסות
באופן אנונימי, ללא עוגיות״ בארבע שפות, `updated` 2026-09-07. קוד האתר
`yehoshua-apps` נמסר על ידי הבעלים באותו יום; הספירה לפי נתיב, אפליקציה
אפליקציה. **זו הסיבה שכל שנים־עשר המפתחות עולים:** `legal/` מצורף
מראש בכולן.

**גופן רוסי באחד־עשר דפים.** Heebo ו-Assistant אינם מכילים קירילית,
והממשק הרוסי נפל לגופן המערכת — בדיוק מה שקרה לערבית ב-3.9. Noto Sans
נטען מאותה בקשת גופנים ומוחל על `lang="ru"` (`--ru`, כמו `--ar`).
`arabic.js` הפך ל-`fonts.js` ובודק שני כתבים: 11 ממצאים לפני, אפס
אחרי, שתי מוטציות נתפסות. הפירוט ב-`FINDINGS.md`.

**math-uni (u49) — defint רמה 2.** `a=ri(2,4)`, `hi=ri(3,5)` ושומר על
`hi=2a`: 8 מתוך 16 הצירופים הישנים ייצרו מסיח שווה לתשובה או שני
מסיחים שווים, ו-`buildQ` הגריל מחדש עד 12 פעמים. עכשיו אפס מתוך תשעה.

**m53 ו-l23 ולא m52 ו-l22:** בזמן הבנייה `main` קיבל את `fbaff22` עם
אותם שני מספרים לתוכן אחר, ובדיקה 6 של `cache.js` תפסה את זה אחרי
`git fetch` — שני ממצאים, בדיוק השתיים. הוקצו מחדש במיזוג.

**תשתית.** `FINDINGS.md` בשורש ושלושה כללים ב-`CLAUDE.md`;
`.github/workflows/qa.yml` מריץ את `all.js` בכל push ו-PR;
`cache.js` בדיקה 6 — מפתח שקיים ב-`origin/main` עם תוכן אחר נופל
(11 ממצאים לפני ההקצאה כאן, אפס אחריה); `pw.js` מוצא את Playwright
גם מחוץ לסביבת הפיתוח.

## 806 — המאגר מאוזן: שש שאלות בכל אחד משמונת הנושאים · bagrut-806 x19

**זו הייתה המלצה בסוף הדוח הקודם, והכלל שנוסף ל-CLAUDE.md אומר
שהמלצה כזאת אינה נשאלת אלא מבוצעת.** הסבב הקודם הוסיף שתיים בכל
נושא, אבל השאיר את הפער: בעיות קיצון עמדו על שלוש וגאומטריה
אנליטית על ארבע, מול שש בשלושה נושאים אחרים. תלמיד שדוח הנושאים
החלשים סימן לו ״בעיות קיצון״ קיבל חצי ממה שקיבל מי שנחלש בחשבון
דיפרנציאלי.

`806-demo-8` נבנה לפי הפער ולא לפי תבנית: שלוש שאלות בבעיות קיצון,
שתיים בגאומטריה אנליטית, ואחת בכל אחד משלושת הנושאים שעמדו על חמש.
**אחריו כל שמונת הנושאים על שש בדיוק** — נמדד: מינימום 6, מקסימום 6.

מ-40 שאלות ו-98 סעיפים ל-48 ו-114.

מה יש באוסף: מזעור שטח פנים של פחית גליל בנפח נתון — ומתגלה שהגובה
יוצא בדיוק כקוטר; מלבן חסום בפרבולה; מזעור סכום בהינתן מכפלה; משיק
למעגל דרך הניצבות לרדיוס; שטח משולש ושיפוע תיכון; התפלגות בינומית
על חמש הטלות מטבע; סדרה חשבונית שנתונים בה שני איברים לא סמוכים;
וקירור אקספוננציאלי עם טמפרטורת חדר שאינה אפס.

**36 מסלולי `reject` — כולם טעויות שחושבו, לא מספרים שנראים סבירים.**
בפחית: מי שהניח שהגובה שווה לרדיוס. במלבן: הצבת שיעור האיקס במקום
בפונקציית השטח. במשיק: שיפוע הרדיוס עצמו, ומי שחתך את ציר האיקס
במקום את ציר הוואי. בבינומי: מי שוויתר על המקדם, ומי שענה ״בדיוק
ארבעה״ על ״לפחות ארבעה״. בסדרה: חלוקת ההפרש במספר הלא נכון של
קפיצות. בקירור: מי ששכח שטמפרטורת החדר אינה חלק מהדעיכה.

אומת בשלוש שכבות: כל מספר חושב לפני שנכתב; 36 מסלולי ה-`reject`
נבדקו עצמאית מול לוגיקת `checkAnswer` וכולם נדחים; ו-333 טענות
בבדיקת דפדפן על כל 57 הסעיפים בשמונת האוספים, כל אחד פעמיים.
אפס שגיאות JS, ואף אמירה בהקראה אינה מכילה לוכסן. חבילת QA מלאה:
17 בדיקות, כולן עברו.

**ליקוי בבדיקה שלי, לא בתוכן:** הבדיקה הניחה כפתור נושא לכל שאלה,
ונפלה כשאוסף נשא שלוש שאלות באותו נושא. האפליקציה צודקת — היא
מאחדת נושא לכפתור אחד — והטענה תוקנה להשוות מול נושאים ייחודיים.

`BUILD` ומפתח המטמון עלו יחד ל-x19, ו-STATUS.md עודכן.

## הענף של ״אזרח״ — אומת שאין בו מה להציל, ונמחק מקומית

`claude/new-exam-app-xkohvq` בנה אזרחות כאפליקציה נפרדת, `main`
הכריע שזה נושא בלומדה, והענף נשאר עומד כדי שהעבודה לא תיזרק.
היום היא כבר לא בו: הניסוחים עברו ל-`lomda/data/civics.js`,
ותיקון ה-`pure` שנולד בו נמצא ב-`main` בארבע האפליקציות.

**האימות קדם למחיקה, לא להפך.** סקריפט השווה כל אחת מ-288
המחרוזות (72 פריטים × ארבע שפות) מול הקובץ שב-`main`: **288 מתוך
288 נמצאו מילה במילה, אפס חסרות.** רק אז נמחק העותק המקומי.

**הרפרנס ב-GitHub לא נמחק מכאן, ואי אפשר.** `git push --delete`
ו-`git push origin :branch` מחזירים שניהם **HTTP 403** מהפרוקסי,
בחמישה ניסיונות עם השהיה עולה, ואין כלי מחיקת רפרנס ב-MCP של
GitHub. מחיקת הרפרנס היא פעולה של הבעלים.

**מה שנמחק ואינו קיים עוד**, וזה כתוב כאן כדי שלא יחפשו: 72 נוסחי
השאלות, 288 האפשרויות הכתובות, 72 הרמזים, ושלד האפליקציה — מסך
המבחן המדומה עם השעון, ובנק הטעויות. קצה הענף היה `c07f253`.

שלושה מסמכים שתיארו את הענף כקיים יושרו: `CLAUDE.md` בסעיף
`lomda`, `CELINE.md` בסעיף 3ג, וההערה בראש `lomda/data/civics.js`.
הערות בלבד, בלי שינוי התנהגות, ולכן בלי העלאת גרסה.

## 32 אפשרויות שאפשר היה למחוק ביום השני · math-uni u48 · math-uni2 v46 · math-uni3 g45 · math-teen t51

**בארבע אפליקציות המתמטיקה היו 32 מסיחים שהתלמיד יכול היה למחוק
בלי לדעת מתמטיקה.** אפשרות מתה היא טקסט שמופיע ב-98% מההגרלות
ואינו התשובה הנכונה אף פעם — התלמיד רואה אותה שוב ושוב, לומד
שהיא אף פעם לא נבחרת, ומוחק אותה. שתיים כאלה באותה שאלה מעלות
את הניחוש מ-25% ל-50%. זה קרה ב-31 רמות.

**מה נעשה בהן.** 12 הוחיו: הן נכונות לפעמים, והמחולל פשוט לא
הגיע למקרה שבו הן התשובה — החישוב תוקן, ועכשיו הן נבחרות כשהן
נכונות. 20 סובבו: הן נשארו מסיחים, אבל מופיעות ב-34%–72%
מההגרלות במקום ב-98%+, ולכן היעדרותן כבר אינה סימן.

לא נמחקה אף אפשרות ואף שאלה. מספר מחרוזות העברית עלה בארבע
האפליקציות, והמחוללים והנושאים זהים.

**ובדרך נתפס באג נכונות.** ב-`math-uni2`, בשאלת `extrema2`,
כשהדיסקרימיננטה מתאפסת הפונקציה היא `f = a(x + by/2a)²` — ולכן
"מינימום" נכון בדיוק כמו "המבחן אינו מכריע". שתי תשובות נכונות
ב-2.8% מההגרלות, והתלמיד שבחר נכון סומן כטועה. המקרה נחסם
במחולל. על 158 ביטויים שהוערכו בחישוב — אפס מקרי D=0 נותרו.

**`GENV` עלה מ-5 ל-6 בארבע האפליקציות, וקישור מבחן ישן ייפסל.**
זו התנהגות מכוונת ולא תקלה: הקישור נושא זרע, והזרע נבנה לשאלה
דרך מיפוי שהשתנה עכשיו. קישור שנוצר לפני השינוי היה מייצר אצל
התלמיד שאלות אחרות מאלה שהמורה ראה — עדיף שייפסל בגלוי מאשר
שיעבוד לא נכון בשקט. מבחן חדש נבנה כרגיל.

בדיקות: `deadsweep` 32→0 אפשרויות מתות ו-31→0 רמות ·
`node .claude/qa/all.js` 17 מתוך 17 · `entropy`
noAns/multi/dupe/nan = 0 בארבעתן · `options` multiCorrect=0
ו-noCorrect=0, ו-under4 זהה לקו הבסיס המתועד · `trMissing`
ar/ru/en = 0 בשלוש אפליקציות האוניברסיטה.

## 806 — שתי שאלות נוספות בכל אחד משמונת הנושאים · bagrut-806 x18

**המאגר היה לא מאוזן, וזה נמדד ולא הוערך.** לפני השינוי היו
בעיות קיצון עם שאלה אחת בלבד וגאומטריה אנליטית עם שתיים, בזמן
שלחשבון דיפרנציאלי, אינטגרלי וטריגונומטריה היו ארבע כל אחד.
תלמיד שהדוח סימן לו ״בעיות קיצון״ כנושא חלש קיבל שאלה אחת לתרגל
בה, ואחריה לא נשאר לו דבר.

שני אוספים חדשים, שמונה שאלות בכל אחד — **אחת בכל נושא**, ולכן
כל נושא עלה בדיוק בשתיים:

| נושא | היה | עכשיו |
|---|---|---|
| חשבון דיפרנציאלי | 4 | 6 |
| חשבון אינטגרלי | 4 | 6 |
| טריגונומטריה | 4 | 6 |
| הסתברות | 3 | 5 |
| סדרות | 3 | 5 |
| גדילה ודעיכה | 3 | 5 |
| גאומטריה אנליטית | 2 | 4 |
| בעיות קיצון | 1 | 3 |

סך הכול: מ-24 שאלות ו-66 סעיפים ל-40 שאלות ו-98 סעיפים.

`806-demo-6` הוא 3–4 יח״ל. `806-demo-7` הוא 5 יח״ל, וכל שאלה בו
דורשת צעד שאינו הצבה: נוסחת ההסתברות השלמה ובייס, סכום סדרה
הנדסית אינסופית, נגזרת של מנה אחרי פישוט, שטח בין שני גרפים,
משוואה טריגונומטרית עם שני פתרונות בתחום, השלמה לריבוע, ופונקציית
נפח שאחד משורשיה מנוון.

**כל מספר בתוכן חושב, ואף אחד לא הוערך.** כולל מסלולי ה-`reject`:
כל אחד מהם הוא טעות שרצה בפועל — עם החזרה במקום בלי, חזקה 5 במקום
4, קוסינוס במקום סינוס, ריבית פשוטה במקום דריבית, סימן הפוך
בהשלמה לריבוע, ו-r בריבוע במקום r.

**שני ליקויים שלי נתפסו לפני שנכתבו.** בשאלת האינטגרל תכננתי
גבולות שבהם הגבול התחתון תורם אפס — ולכן מי ש״שכח את הגבול
התחתון״ היה מקבל את התשובה הנכונה. זו בדיוק התאונה ש-`reject`
קיים בשבילה, והפונקציה הוחלפה כך שהגבול התחתון תורם מינוס אחת.
ובשאלת הריבית `tolerance` של שקל שלם קיבלה גם 13032; היא הודקה
לחצי שקל, שהוא בדיוק ״מעגל לאותו שקל״, ונבדק שהמסלול השגוי —
ריבית פשוטה, 12000 — רחוק ממנה ב-1031.

284 טענות עוברות בבדיקת הדפדפן: כל 49 הסעיפים בשבעת האוספים, כל
אחד פעמיים — התשובה הנכונה מתקבלת והשגויה נדחית — ואף אמירה
בהקראה אינה מכילה לוכסן. חבילת ה-QA המלאה: 17 בדיקות, כולן עברו.

`BUILD` ומפתח המטמון עלו יחד ל-x18, כי `data/exams.js` מצורף מראש
ב-`sw.js`. הטבלה ב-STATUS.md עודכנה בהתאם.
## אזרחות: הניסוחים מ״אזרח״ עברו ללומדה · lomda l19

**72 הגדרות, בארבע שפות, מועתקות מילה במילה.** הענף
`claude/new-exam-app-xkohvq` בנה אזרחות כאפליקציה נפרדת, ו-`main`
הכריע שזה נושא בלומדה. הענף סגור ואינו נמזג — אבל התוכן שנכתב בו
לא נזרק, וזה מה שעבר.

**מה עבר.** ההסבר שנפתח שם אחרי כל מענה — הפסקה שאומרת למה זו
התשובה — הוא בדיוק מה שהסכימה כאן קוראת לו הגדרה. 72 מהם נכנסו
כ-`C(מושג, הגדרה)`, וכל שם מושג נכתב בארבע שפות. ההעתקה נעשתה
מהדפדפן ולא ביד: הענף נטען, `TOPICS` נשלף ל-JSON, והמחולל בנה את
הבלוקים — אפס שגיאות תעתיק.

**מה לא עבר, וזו החלטה ולא פער.** ארבע האפשרויות הכתובות לכל
שאלה, והרמזים. המנוע כאן בונה את השאלה ואת המסיחים בעצמו מתוך
ההגדרות של הפריטים השכנים, ולכן למסיח כתוב אין לאן להיכנס; והרמז
שם נגזר מנוסח שאלה שכבר אינו קיים.

**שמונה נושאים אוחדו לארבעה**, לפי ארבעת המסלולים שהיו להם בענף —
יסודות הדמוקרטיה, זכויות ואזרחות, רשויות השלטון, ומדינת ישראל
וחברה. הסיבה מדידה: `banks.js` דורש לפחות ארבעה פריטים בכל רמה
כדי שיהיה מסיח רביעי אמיתי, ושלושה לכל רמה נכשלו בה ב-24 ממצאים.
אחרי האיחוד — שישה בכל רמה, ואפס ממצאים.

מאגר האזרחות של הלומדה גדל משני נושאים לשישה. נמדד:
`banks.js` — 15 נושאים ו-215 פריטים; `entropy` על `lomda` עלה
מ-19,800 הגרלות ל-22,500 ו-`options` מ-19,800 ל-27,000, כולם
נקיים — כלומר הנושאים החדשים באמת נבחנים.

תיאור כרטיס המסלול הורחב בארבע שפות: הוא מנה את הכנסת, החוקים
ובית המשפט, ומעכשיו גם זכויות וחברה. `all.js`: 17 מתוך 17.
## דף הבית מסביר למה Edge — סעיף ״הקראה״ עם קישור · דף הבית 35

**השורה בהירו הספיקה למי שכבר ידע, ולא למי ששואל למה.** היא אומרת
שב-Edge יש קולות טבעיים ונותנת קישור, אבל לא מסבירה שההקראה בכל
האפליקציות היא קול המערכת ושהאפליקציה אינה מביאה קול משלה — ולכן
בכרום על ווינדוס יש קול אחד, גברי, ולא משנה מה עושים בהגדרות.

**נוסף סעיף `#voices` אחרי ״הוספה למסך הבית״**, באותו מבנה: כותרת,
שורת הסבר ושלושה כרטיסים — ווינדוס (כרום מול Edge, Asaf מול Hila
ו-Avri), אנדרואיד ואפל, עם מסלולי ההגדרות שכבר כתובים ב-`femNote`
של האפליקציות ולא מסלולים חדשים. מתחתיו: בווינדוס מחוץ ל-Edge לחצן
`microsoft-edge:` שפותח את האתר ב-Edge; בתוך Edge משפט ״אתם כבר
ב-Edge״; בשאר המערכות הפסקה נסתרת וההסבר לבדו נשאר, כי הקישור לא
פותח שם כלום. השורה בהירו קיבלה ״למה?״ שמוביל לסעיף. ארבע שפות.

**באג שנתפס בצילום מסך לפני שנדחף:** הלחצן ישב ב-`<p class="prose">`,
והכלל `.prose a` צבע את הטקסט בצבע המותג — לבן על כחול הפך לכחול
על כחול, לחצן ריק. עבר ל-`<div class="cta">` כמו בהירו; נמדד
`color: rgb(255,255,255)` אחרי התיקון.

**אומת בדפדפן אמיתי** בשלושה UA: כרום־על־ווינדוס — לחצן עם
`href="microsoft-edge:<הכתובת>"`, בלי ה-`#`; Edge־על־ווינדוס —
המשפט בלי קישור; אנדרואיד — `display:none`. שלושה כרטיסים בכל
אחד, אפס גלילה אופקית ב-390 וב-1100, אפס שגיאות JS. `all.js` —
17 בדיקות, כולן עברו.

**מפתח הקאש הוקצה במיזוג: `35-pwa1`.** בזמן הבנייה הענף נשא 33,
אבל `main` כבר הגיע ל-34 (`454f1e0`) — אותו מפתח פעמיים היה משאיר
את מי שקיבל את 34 בלי הסעיף הזה לעולם. מילון `DATA` מוזג מפתח־מפתח
מול הבסיס המשותף: `main` שינה שם רק את `en.h1a` ו-`en.h1b`, והענף
הוסיף שבעה מפתחות בכל שפה — אפס מפתחות ששני הצדדים שינו. שתי
שורות התיעוד (`CLAUDE.md`, `ARCHITECTURE.md`) עודכנו ל-35 ו-26.

## המבחן הכיתתי שאושר אינו המבחן שנשלח · history m51 · english n45 · ulpan a14 · lomda l18

**מורה בנה מבחן, ראה את השאלות, אישר ושלח — והכיתה נבחנה בשאלות
אחרות.** הבאג חי בארבע האפליקציות שיש בהן בונה מבחן, ואף אחת
מהבדיקות לא תפסה אותו.

**למה זה קרה.** הקישור שנשלח בוואטסאפ נושא זרע, לא שאלות. התלמיד
בונה מהזרע את אותן שאלות אצלו — וזה עובד רק אם כל מה שנכנס לבחירה
זהה בשני הצדדים. `chooseIndex`, החזרה המרווחת, שוקלת כל פריט לפי
`state.item`: פריט שלא נראה מקבל משקל 6, פריט שנכשל 1–5, פריט
שנשלט 1. המשקלים האלה הם היסטוריית הלמידה של מי שיושב מול המסך,
והם היו בנתיב של המבחן. אצל המורה, שכבר תרגל, מערך המועמדים אחר
מאשר אצל התלמיד, ולכן אותו זרע בדיוק הצביע על פריט אחר.

**נמדד, לא נאמד.** דפדפן אמיתי מול `origin/main` ב-`a9c2c00`: אותו
זרע, שתי היסטוריות למידה — אחת ריקה ואחת שבה מחצית הפריטים נכשלו —
חמש הרצות על ארבע האפליקציות, עשר שאלות בכל הרצה. **39 מתוך 200
שאלות יצאו שונות**, ו-19 מתוך 20 ההרצות היו שונות מאפס. הפיזור:
`ulpan` עד 5 מתוך 10 בהרצה אחת, `english` עד 4, `history` עד 3,
`lomda` עד 2.

שני המסכים תקינים לגמרי, ולכן אין מה שיצעק: המורה רואה מבחן, התלמיד
נבחן במבחן, ורק הציונים הם של שאלות אחרות.

**התיקון — פרמטר רביעי.** `buildQuestion(tid, lv, avoid, pure)`.
`pure=true` בוחר אחיד ואינו נוגע ב-`state` כלל, ו-`buildQ` — הנתיב
היחיד שהמבחן עובר בו — מעביר אותו תמיד. התרגול לא השתנה: החזרה
המרווחת ממשיכה לשקול בדיוק כמו קודם. אחרי התיקון, באותן חמש הרצות:
**0 מתוך 200**.

**למה `exam.js` לא תפס.** הוא משווה את מה שהמורה רואה למה שהתלמיד
מקבל — אבל בתוך אותו דפדפן ובאותו טאב, ולכן `state.item` זהה בשני
הצדדים בהגדרה. הבדיקה שנדרשה היא הזרקת היסטוריית למידה שונה בין
שתי הבנייות, וזה מה שנמדד כאן.

אפליקציות המתמטיקה מחוללות שאלה מפרמטרים ואינן קוראות ל-`state`
בדרך הזאת — לא נגעתי בהן.

`node .claude/qa/all.js`: 17 מתוך 17. eslint על קו הבסיס בלבד
(`id` ו-`lg`). `STATUS.md` רוענן ב-`status.js --md`.
## סבב של עשר וסיכום בסופו · ulpan a13 · english n44 · history m50 · lomda l17

**בארבע אפליקציות החידון התרגול לא נגמר אף פעם.** `grep -c 'סבב'` על
`ulpan`, `english`, `history` ו-`lomda` החזיר 0 בכולן: שאלה אחרי שאלה,
בלי רגע שבו רואים מה יצא, בלי מה לקחת מהתרגול ובלי סיבה לחזור מחר.
במשפחת המתמטיקה זה כבר קיים — `viewSummary` ב-`math-teen` — והסיכום
כאן מועתק ברוחו משם, בלי שעון ובלי דירוג.

**עכשיו עשר שאלות הן סבב.** בכרטיס השאלה נוסף צ׳יפ ״3 מתוך 10״
(המילה מ-`ofN` הקיים, ולכן בארבע השפות), ואחרי העשירית ״הבא״ מוביל
למסך סיכום במקום לשאלה אחת־עשרה: משפט פותח לפי הדיוק (ארבע דרגות,
והנמוכה אומרת ״סבב קשה — וזה בסדר״), אריחי נכון לבד / נכון עם רמז /
לא נכון / דיוק, ואריח הרמז מופיע רק במפנה ובלומדה, שיש בהן רמז.
ארבעה כפתורים: ״סבב נוסף״, ״לרמה הבאה״ (רק בדרגות 1–2 ומתחת לרמה 3),
״רמה אחת למטה״ (רק בדרגה 4 ומעל רמה 1), ״שליחה בוואטסאפ״ דרך
`waLink` שכבר שימש את מצב המורה, וחזרה לנושאים. הסיכום נקרא בקול
בשפת הממשק, ורצף הימים הקיים (`state.streak`) מוצג כשהוא גדול מאחד.

**מה לא השתנה:** המחוללים, הרישום ל-`state.stats`, מצב המורה, השער
והאונבורדינג. `P.rres` הוא מצב של הסשן ואינו נשמר; מעבר נושא או
רמה מתחיל סבב חדש. 18 מפתחות נוספו ל-`T` בכל אחת מארבע השפות,
ו-`he`/`ar`/`ru`/`en` נשארו באותו גודל (117→135 באולפן ובניב,
133→151 במפנה, 125→143 בלומדה; נמדד בסקריפט שמשווה מפתחות).

**אומת בדפדפן אמיתי**, 16 ריצות — ארבע אפליקציות בארבע שפות: עשר
תשובות דרך הממשק (כולל תרגיל סידור באולפן ורמז במפנה ובלומדה),
הסיכום מופיע אחרי העשירית בדיוק, שלושה או ארבעה אריחים לפי
האפליקציה, קישור הוואטסאפ נושא את כתובת האפליקציה, ״סבב נוסף״ מחזיר
ל-״1 מתוך 10״, וכפתורי הרמה מופיעים רק בדרגות ובמצבים שנקבעו. אפס
שגיאות JS. `node .claude/qa/all.js`: 15 בדיקות, כולן עברו.

## תגיות שיתוף ב-12 הדפים · b51 t50 u47 v45 g44 x17 a13 n44 m50 l17 · reader 26 · הפורטל 33

**כל קישור שנשלח בוואטסאפ או בפייסבוק הגיע בלי כותרת ובלי תמונה.**
`grep -c 'og:image'` על כל קובצי `index.html` החזיר 0 — וכל תוכנית
ההפצה (`marketing/plan-weekly.md`) היא קישורים בוואטסאפ ובקבוצות.
מצב המורה שולח קישור מבחן לכיתה שלמה בוואטסאפ, וגם הוא הגיע עירום.

**עכשיו בכל אחד מ-12 הדפים** (הבית ואחת־עשרה האפליקציות שבריפו)
שש תגיות אחרי תיאור הדף: `og:type`, `og:title` מה-`<title>`,
`og:description` מ-`<meta name="description">`, `og:url`, `og:image`
(האייקון `img/icon-512.png` של אותה אפליקציה, עד שתהיה תמונת
1200×630 — משימה E1 ב-`CELINE.md`), ו-`twitter:card`. שום ניסוח
חדש: הטקסט הוא זה שכבר היה בדף.

**הכתובת בתגיות מוחלטת**, כי וואטסאפ ופייסבוק אינם פותרים כתובת
יחסית. זה סותר את מה ש-`launch.md` ציין כיתרון (״אין כתובת מוחלטת
בקוד״), ולכן `launch.md` מתעד עכשיו את פקודת ההחלפה למעבר מארח.

**תיאור דף הבית אמר ״תשע אפליקציות לימוד חינמיות״.** `DATA.APPS`
מונה שתים־עשרה, וההחלטה מ-5.9 היא שהרוב יהיה בתשלום. עכשיו:
״שתים־עשרה אפליקציות לימוד״. שאר התיאור לא השתנה.

**מפתחות הקאש של 12 הדפים הועלו במיזוג**, לפי `CLAUDE.md` (המספר
מוקצה במיזוג ולא בבנייה). תצוגה מקדימה נקראת מהשרת ולא מהמטמון,
ולכן השיתוף עובד גם למי שטרם עדכן.

**ומפתח דף הבית הוקצה פעמיים.** הענף סימן את דף הבית `32` מול
`main` שראה `31`; בזמן הבנייה `main` הקצה `32` בעצמו (הצעת Edge,
`bf8bd43`). שתי גרסאות שונות עם אותו מפתח היו משאירות את מי שקיבל
את הראשונה בלי השנייה לעולם, ולכן במיזוג השני דף הבית קיבל `33`.
בדיוק המקרה ש-`CLAUDE.md` מתאר, ובדיוק מה שהכלל שם נועד לתפוס.

`node .claude/qa/all.js` אחרי השינוי: 15 בדיקות, כולן עברו.
## דף הבית מציע לעבור ל-Edge, כמו כל אפליקציה · דף הבית 32

**דף הבית היה המקום היחיד בלי ההצעה.** אחת עשרה האפליקציות מציעות
בווינדוס־שאינו־Edge קישור `microsoft-edge:` שפותח את אותה כתובת
ב-Edge בלחיצה, כי כרום על ווינדוס רואה בעברית קול מערכת אחד בלבד
ו-Edge נושא קולות טבעיים משלו. מי שנכנס מדף הבית בכרום גילה את זה
רק בתוך האפליקציה, אחרי שכבר נכנס. נספר בקוד:
`grep -c microsoft-edge: index.html` החזיר 0.

**עכשיו השורה יושבת מתחת ל״ללא הרשמה״ בהירו**, באותו תנאי
(`isWinNotEdge()`), באותו ניסוח ובאותו קישור. מחוץ לווינדוס,
ובתוך Edge, הפסקה נשארת `hidden`. ארבע השפות — המחרוזות של
`reader`, עם ״האתר״ במקום ״המקריא״.

**אומת בדפדפן אמיתי** בשלושה UA: כרום־על־ווינדוס מציג את השורה
עם `href="microsoft-edge:<הכתובת>"` בארבע השפות; Edge־על־ווינדוס
ואנדרואיד — `display:none`. אפס שגיאות JS. `all.js` — 17 בדיקות,
כולן עברו.

**מפתח הקאש של דף הבית עלה ל-`32-pwa1`**, ושתי שורות התיעוד
שמצטטות אותו (`CLAUDE.md`, `ARCHITECTURE.md`) עודכנו איתו.

## 806 — שני שערים אחרונים, ורשת שתתפוס את הבא · bagrut-806 x16

**איבר הקוסינוס היה בלתי נראה גם באוסף השני.** `2cos(60°)=1`, ולכן
`2·8·6·cos60°` שווה `8·6` בדיוק ומשפט הקוסינוסים מתכווץ ל-`b²+c²−bc`.
תלמיד שכתב `64+36−48` בלי לדעת מה הקוסינוס עושה שם קיבל `√52 = 7.21`,
שהיא התשובה הנכונה במלואה. הזווית עברה ל-50 מעלות — חדה ולא מיוחדת,
ולכן הקוסינוס נדרש באמת ומחשבון הכרחי. הצלע יוצאת 6.19 והשטח 18.39,
ושישה מסלולים שגויים נופלים רחוק. האוסף השלישי כבר תוקן ל-120 מעלות,
שם הקוסינוס שלילי; שניהם יחד מלמדים את שני הכיוונים.

**ושתי שאלות היו אותה שאלה.** `x³−6x²+9x+1` ו-`x³−6x²+9x+2` נבדלות
רק במקדם החופשי, ולכן הנגזרת זהה ונקודות הקיצון זהות — 1 ו-3. השאלה
באוסף הראשון והשאלה באוסף השלישי ביקשו את אותו שיעור x וקיבלו את
אותה תשובה. הפונקציה באוסף הראשון עברה ל-`x³−9x²+24x+1`, קיצון ב-2
וב-4, ואומתה גם בגזירה נומרית שסרקה את שינויי הסימן ומצאה בדיוק
שתיים.

**שני אלה נמצאו ביד, ולא על ידי אף בדיקה — וזה מה שתוקן באמת.**
`reject` הוא שדה רשות חדש בסכימת התוכן: רשימת התשובות השגויות שחייבות
להידחות, רשומה ליד התשובה הנכונה. `exam806.js` מריץ עליהן את
`checkAnswer` של האפליקציה ונופל אם אחת מהן מתקבלת, ולכן `tolerance`
שיתרחב מתישהו ויבלע את הטעות ייתפס מיד ולא בקריאה ידנית שנה אחר כך.
שמונה סעיפים נושאים אותו היום, ובהם ארבעת הליקויים שתוקנו בסבבים
הקודמים — השטח שהיה שווה לאינטגרל, הנושא שהיה עביר בלי לוגריתם,
המקדם החופשי שהיה גם התשובה, והקוסינוס.

**כל ערך ב-`reject` חייב להיות מסלול שחושב.** מספר אחד נכתב שם בלי
מקור ונמחק: `12.81` לא היה שום מסלול שגוי, רק מספר שנראה סביר.
הכלל כתוב עכשיו בסכימה שבראש הקובץ.

`exam806.js` מקבל גם נתיב תוכן כארגומנט, וכך נבדקה הבדיקה עצמה:
מקרה מבחן שבו `tolerance: 0.5` בולע את `"2"` הפיל אותה ויצא בקוד 1.

## ״תאוריה מדברת״ מסומנת ב-4 שפות · הפורטל 31

**המשתמש: הכרטיס של ״תאוריה מדברת״ לא מציין שהיא זמינה בארבע
שפות.** נבדק בכל מקום שהכרטיס מוצג: `DATA.APPS` שבדף הבית,
`marketing/wix-content.md`, ה-`badge` וה-`lead`. עשרה מתוך שנים־עשר
הכרטיסים נושאים את התווית `tLangs` (״4 שפות״ בארבע השפות); ״תאוריה
מדברת״ נשאה ״הקראה״ בלבד. הכרטיס האחר בלי התווית הוא 806, וזה נכון —
`facts.md` מסמנת אותה ״בעברית בלבד״. העובדה עצמה כבר כתובה
ב-`facts.md`: 1,273 שאלות התאוריה בארבע שפות.

**התיקון: התווית הקיימת, לא טקסט חדש.** `"tLangs"` נוסף לרשומת
`theory`, ושורת התוויות של הכרטיס ב-`wix-content.md` עודכנה בהתאם.
התיאור בארבע השפות לא שונה. אומת בדפדפן ב-390: בעברית ״4 שפות״,
בערבית ״٤ لغات״, ברוסית ״4 языка״, באנגלית ״4 languages״ — בתוך
הכרטיס, בלי גלילה אופקית, אפס שגיאות JS. `all.js` — 17 בדיקות עברו.
## `exam.js` נפל פעם במאה ריצות בלי באג

בריצה מלאה של `all.js` נפלה `exam` פעם אחת ועברה בשתיים שאחריה.
שוחזר עם פלט שמור, 8 ריצות של `exam.js`: השמינית נפלה על math-uni2
עם `roll→[]` — החלפת שאלה 3 קיבלה זרע חדש, והמחולל הפיק ממנו את
אותה שאלה בדיוק. נמדד בדפדפן, 300 החלפות לאפליקציה: 1 ב-math-uni2
(`improper` L2), 1 ב-math-uni3 (`unif` L2), 1 ב-lomda (`body` L2),
0 בשש האחרות. הדרישה "הטקסט של שאלה 3 השתנה" אינה נכונה למחולל
אקראי; הבדיקה דורשת עכשיו שהזרע השתנה ושאף שאלה אחרת לא. הכוונה
המקורית — החלפה נוגעת בשאלה אחת בלבד — נשמרת במלואה. 3 ריצות אחרי
התיקון: 27 מתוך 27.

## נקודה בתוך מספר אינה סוף משפט · reader 25

**ה-audit של מנוע ההקראה בארבע השפות מצא:** `buildPlain` ו-`build`
העלו את מונה המשפטים על כל נקודה, בלי לבדוק אם היא בין ספרות.
"ב-14.5, הוקמה המדינה" נקרא כשני משפטים — "ב-14." ואז "5, הוקמה
המדינה" — עם הפסקה באמצע המספר וסמן משפט שקופץ. כך גם "1,250.75".
שאר האפליקציות מגנות על זה ב-`insideNumber`; ל-reader לא הייתה.

**`insideNumber(prev, p, next)`**: נקודה או פסיק שמשני צידיהם ספרות
הם חלק מהמספר, בשני המסלולים — טקסט גולמי ואסימוני הניקוד. אותיות
אינן נוגעות בזה: "ד.ר" נשאר כפי שהיה. אומת בדפדפן: המשפט "בשנת
1948, ב-14.5, הוקמה המדינה." הוא משפט אחד בשני המסלולים (7 → 5
משפטים במבחן הקבוע), האמירה נושאת אותו בשלמותו ב-he-IL, וההדגשה
מילה־מילה פוגעת 4 מתוך 4.

## פסיק אחרי מספר נשמע · math-app b50

**מאותו audit:** ב-`toSpoken` הביטוי `/\d[\d,]*/` תפס גם פסיק שאחרי
מספר, ולכן "מ־5, והגענו" נאמר בעברית בלי ההפסקה — ב-3 מחרוזות של
`STR.he` שיש בהן פסיק אחרי מציין מקום. עכשיו `/\d(?:[\d,]*\d)?/`:
פסיק נכנס למספר רק בין ספרות, "1,250" נשאר מספר אחד והפסיק שאחריו
נשאר פיסוק. אומת: "מ־חָמֵשׁ, והגענו", "אֶלֶף מָאתַיִם חֲמִשִּׁים",
יישור ההדגשה מלא ב-17 מתוך 17 מחרוזות ההסבר והרמז ובארבע השפות.

## רווח ו-Enter על רמקול ממוקד · lomda l16 · history m49

**הממצא השני מביקורת הקוד על הרמקולים.** המטפל הגלובלי של המקלדת
במסך התרגול תפס רווח ו-Enter גם כשהמיקוד היה על כפתור: רווח על
רמקול של תא תשובה הקריא את השאלה במקום את התא, ואחרי שעונים Enter
עליו דילג לשאלה הבאה במקום להקריא. זה היה נכון לכל כפתורי ההשמעה
במפנה ובלומדה גם לפני הרמקולים החדשים, אבל ארבעה רמקולים בכל
שאלה הפכו את זה לנתיב הראשי של מי שעובד במקלדת. באולפן היה כבר
שומר כזה לרווח.

**שורה אחת, זהה בשני הקבצים:** רווח או Enter על כפתור, `role=button`
או קישור ממוקד יוצאים מהמטפל, והדפדפן מפעיל את הכפתור בעצמו. רווח
ו-Enter על תא תשובה ממוקד בוחרים בו, על "הבא" עוברים לשאלה הבאה
פעם אחת, ובלי מיקוד על כפתור רווח מקריא את השאלה ו-Enter עובר
לשאלה הבאה, כמו קודם. מקשי 1–4 ללא שינוי.

אומת במקשים אמיתיים בדפדפן, בשתי האפליקציות: רווח ו-Enter על רמקול
לפני התשובה ואחריה מקריאים את התא שלו ואינם מקדמים שאלה; רווח
ו-Enter על תא בוחרים בו; Enter על "הבא" קורא ל-`newQuestion` פעם
אחת; 0 שגיאות JS.

## שורה רוסית ב-320 · lomda l15 · history m48

**ביקורת קוד על הרמקולים בתאי התשובה מצאה רגרסיה:** ברוסית, ברוחב
320, שורת התא והרמקול גלשו מחוץ למסך — לומדה 6 מתוך 30 שאלות,
מפנה 6 מתוך 30 (אחת מהן הייתה קיימת גם לפני הרמקולים). בעברית,
ערבית ואנגלית 0. הסיבה: `.orow` הוא פריט grid, ופריט grid נולד עם
`min-width:auto`, ולכן מילה רוסית ארוכה הרחיבה את השורה מעבר למכל.

**שתי שורות CSS, זהות בשני הקבצים.** `.orow{min-width:0}` עוצר את
השורה — ולבדו הוא רק מזיז את הבעיה: הטקסט חרג מגבול התא ב-21 מתוך
120 תאים (לומדה) ו-25 (מפנה) ונגע ברמקול ב-3 ו-2. לכן גם
`.opt span:last-child{overflow-wrap:anywhere}`: מילה שרחבה מהשורה
נשברת בתוכה. `anywhere` שובר רק כשאין ברירה — נמדד שכל שבירה שנוספה
היא מילה שרוחבה עולה על רוחב השורה הזמינה (130px ב-320), ובעברית
ובערבית אף שורה לא השתנתה.

**אחרי, 30 שאלות × 4 שפות × 320/390/1024 × שתי אפליקציות:** 0
גלישות, 0 תאים שהטקסט חורג מהם, 0 נגיעות ברמקול, הרמקול בתוך השורה
בכל 120 התאים, ובצד הנכון ב-RTL וב-LTR. `all.js --fast` עבר,
`engine.js` מאשר שהעותקים זהים.

## תא התשובה מדבר · lomda l14 · history m47

**המשתמש: "לומדה לא קורא תא התשובות בעברית."** נמדד בדפדפן: במשפחת
החידון של מפנה ולומדה נקראו רק השאלה (רווח, כפתור ההשמעה שבכרטיס)
והכרטיס המלא אחרי התשובה. תאי התשובה עצמם היו אילמים — אין בהם
רמקול ואין מחווה. במפנה זה נסבל, כי האפשרויות קצרות (שנה, שם);
בלומדה השאלה "מה קרה?" מציגה ארבעה משפטים שלמים, ומי שקורא בקושי
לא יכול לבחור ביניהם בלי לשמוע אותם.

**עכשיו לכל תא רמקול משלו**, מחוץ לכפתור התשובה (כפתור בתוך כפתור
אינו HTML תקין, ואחרי שעונים התא נעול והרמקול היה נעלם איתו).
הרמקול מקריא את התא הזה בלבד, בשפת הממשק, דרך אותו מטפל `say`
שמשמש את שאר הכפתורים. אותו קוד בשתי האפליקציות, כדי שלא ייווצר
פער בין העותקים. `hearOpt` נוסף ל-T בארבע השפות כתווית נגישות.

אומת בדפדפן: ארבעה רמקולים לארבעה תאים, כל אחד מקריא בדיוק את
הטקסט של התא שלו ב-`he`, לפני התשובה ואחריה; הבחירה בתא עובדת;
אפס גלילה אופקית ב-320 וב-390; `say.js` — הרווח עדיין אינו מסגיר
את התשובה; `engine.js` — העותקים זהים; smoke נקי.

## הדרך לקול נשי, בחמש האפליקציות שלא הציעו אותה · t49 · u46 · v44 · g43 · reader 24

**"אין קול נשי" הוא נכון, והוא אינו באג באפליקציה.** כרום על ווינדוס
אינו נושא קולות משלו — הוא רואה רק את קולות המערכת, ובעברית המערכת
מציעה **אחד בלבד: Asaf הגברי**. "הוספת קולות" בהגדרות ווינדוס לא
תביא קול נשי בעברית, כי כזה אין למיקרוסופט להתקנה. Edge לעומתו נושא
קולות טבעיים משלו, נשיים וגבריים — Hila ו-Avri בעברית — ולכן שם הקול
הנשי עובד. **אף שורת קוד לא יכולה לייצר קול שאינו מותקן במכשיר.**

**מה שכן היה חסר: ההצעה לעבור.** הסכימה `microsoft-edge:` פותחת את
אותה כתובת ב-Edge מתוך כל דפדפן בווינדוס — לחיצה אחת במקום מסלול
הגדרות שאינו מוביל לשום מקום. היא הייתה קיימת בחמש אפליקציות בלבד
(`math-app`, `english`, `history`, `ulpan`, `lomda`), ו**חסרה בדיוק
באלה שהתלונה נוגעת בהן**: `math-teen`, שלוש האקסיומות ו-`reader`.
נספר בקוד: `grep -c 'microsoft-edge:'` החזיר 0 בחמשתן.

מי שנתקל בהן קיבל את `femNote` — הוראות התקנה שבווינדוס מובילות
למבוי סתום — ולא קיבל את הדבר היחיד שבאמת עובד.

**עכשיו חמשתן מציעות אותה**, ליד אותה הודעה ובאותו תנאי:
`isWinNotEdge()`, כלומר ווינדוס שאינו Edge בלבד.

**התרגומים לא הוזזו.** באקסיומות המילון בנוי משני מערכים מקבילים,
`TR_KEYS` ומנות לכל שפה, ומפתח שנדחף באמצע היה מסיט את כל מה
שאחריו. שתי המחרוזות נוספו **בסוף** `TR_KEYS`, והמנות החדשות נרשמו
ב-`trAt` עם המקום המדויק שלהן — 1707 ב-math-uni, 1409 ב-math-uni2,
1278 ב-math-uni3. ב-`math-teen` וב-`reader` המילון הוא מפתח־ערך,
ושם ההוספה בטוחה מלכתחילה.

**`reader` עבר מ-`textContent` ל-`innerHTML`** בשורה הזאת בלבד, כי
ההצעה היא קישור. שני חלקיה עוברים `esc`.

**אומת בדפדפן אמיתי** עם UA של כרום־על־ווינדוס: בכל חמש האפליקציות
`isWinNotEdge()` מחזיר true, `edgeNote()` מכיל את הקישור, ואפס
שגיאות JS. באקסיומות נבדק גם שהמפתחות נחתו במקומות שיועדו להם, ששלוש
השפות מחזירות את התרגום — ושמספר המפתחות החסרים נשאר **0 בכל אחת
משלוש השפות**, כלומר אף תרגום קיים לא הוסט.

**מה שלא נעשה:** ל-`bagrut-806` אין העדפת מגדר כלל — לא רשימת שמות,
לא זיהוי ולא ההודעה. זו האפליקציה היחידה שנשארה בלי, והיא צריכה את
המנגנון כולו ולא הוספה של הצעה.
## ההקראה אמרה את התשובה לפני שעונים · n43 · a12 · m46 · l13

**המספרים הוקצו במיזוג ל-`main`**, מול n42, a11, m45, l12 שהיו שם.

**מקש הרווח בתרגול הקריא את `P.q.say` בלי לבדוק אם כבר ענו**, בעוד
שמקשי 1–4 שלידו מוגנים ב-`!P.done`. וב-`say` יושבת התשובה: במפנה
ובלומדה כרטיס הפריט המלא — הכותרת, השנה והסיפור — ובניב ובאולפן
המילה או המשפט שמחפשים. נמדד בדפדפן אמיתי, 60 שאלות לכל אפליקציה,
בלי שאלות האזנה: מפנה 60, לומדה 54, ניב 21, אולפן 15. מי שקורא לאט
לחץ רווח כדי לשמוע את השאלה, ושמע את הפתרון.

**התיקון הזה כבר נכתב פעם אחת** — `e28a56f`, 2.9, בענף
`claude/8-apps-automated-qa-j18kf9` — ולא מוזג. `lomda` נולדה אחריו
מהעתקה של `history` וירשה את הבאג. הועבר לארבע: `sayAsk` אומר לפני
שעונים את מה שעל המסך (שאלת האזנה — השמע הוא השאלה ונשאר; השלמה —
המשפט עם החסר כשהות), ואחרי שעונים `say` המלא כפי שהיה. במבחן
הכיתתי `sitSay` בודק שמה שנאמר אינו מכיל את התשובה — לאולפן היה
`speak(q2.say)` חשוף, ולמפנה וללומדה הלוגיקה ישבה בתוך המטפל.

**אחרי:** אותה מדידה, 0 דליפות בארבע. `node .claude/qa/say.js` הוא
המדידה הזאת כבדיקה קבועה ב-`all.js`, ונופל על הקוד הישן (4 מתוך 4).

## מבחן שנבנה בלומדה הופיע במפנה · lomda l13

`lomda/index.html` נולד מהעתקה של `history` ונשא את המפתחות שלה
מילה במילה: `EKEY="shlav-exams-history-v1"` — כולל ההערה "מפתח
לאפליקציה הזאת בלבד" — ו-`"mifne-teacher"` לקוד המורה. שתיהן באותו
מקור, ולכן מבחן כיתתי שמורה בנה באחת הופיע בשנייה, וקוד מורה שנקבע
באחת נעל את השנייה. זה הבאג שתוקן ב-31.8 לכל האפליקציות
(`735bf22`), והוא חזר בהעתקה.

עכשיו `lomda-exams-v1` ו-`lomda-teacher`. המפתח הישן נשאר כ-`EKEY_OLD`
לקריאה בלבד, ו-`exAdoptLegacy` מעתיק ממנו רק מבחנים שכל נושאיהם
מוכרים בלומדה — נמדד: 16 נושאים במפנה, 11 בלומדה, אפס משותפים.
קוד המורה נקרא מהמפתח הישן כשאין חדש, כדי שמורה לא יינעל בחוץ.
אומת בדפדפן: מפתח משותף עם מבחן של מפנה ומבחן של לומדה — לומדה
מאמצת רק את שלה. `node .claude/qa/storage.js` תופס מפתח חי שמופיע
בשתי אפליקציות, ונופל על הקוד הישן (2 ממצאים).

## מסך ההתקדמות ספר לחיצות, לא סעיפים · bagrut-806 x15

הועבר מ-`9e998ae` (ענף `claude/hebrew-apps-mapping-xbwdum`, 4.9, לא
מוזג). כל לחיצה על "בדקו" הוסיפה שורה ל-`weak`, ולכן תלמיד שניסה
שלוש פעמים ופתר ברביעית נמדד "1 מתוך 4" והנושא נצבע אדום. `weak`
נבנה עכשיו מ-`att` — רשומה לסעיף — וסעיף נספר פעם אחת; לחיצה על
שדה ריק וסעיף שלא הגיעו אליו בסימולציה אינם כישלון. `rebuildWeak`
רץ גם בטעינה. אומת בדפדפן: ריק + שלוש שגויות + נכונה →
`{"הסתברות":{"ok":1,"no":0,"tries":4}}`.

## שתי בדיקות חדשות, ותיעוד שהתיישן

`storage.js` ו-`say.js` נכנסו ל-`all.js` (17 בדיקות). טבלת קו הבסיס
של eslint קיבלה את `lomda` ו-`ulpan` בשורות `id` ו-`lg`;
`ARCHITECTURE.md` §9 אומר ש-`learning-core/` (48 בדיקות עוברות)
אינה נטענת באף אפליקציה; `CELINE.md` D1 סגור — שלוש האפשרויות
ב-`lineq L2` ו-`logic L1` הן קו בסיס מתועד ולא באג.

## הקצב חזר ללומד · math-app b49

**תשובה נכונה קפצה לשאלה הבאה מעצמה אחרי שנייה אחת.** `onAns`
הריץ `setTimeout(advance,1000)`, ולכן ילד שקורא לאט לא הספיק
לקרוא את "מעולה! ההתמדה שלכם השתלמה", ההקראה נקטעה באמצע המשפט
על ידי התרגיל הבא, ולא היה שום דבר ללחוץ עליו כדי להמשיך בקצב
שלו. הכפתור "ממשיכים הלאה" היה קיים בקוד — אבל רק אחרי **שתי
טעויות**, כלומר רק למי שנכשל.

**math-app הייתה היחידה בפורטפוליו שעשתה את זה.** נמדד: שמונה
האפליקציות האחרות (`math-teen`, שלוש האקסיומות, `english`,
`history`, `ulpan`, `lomda`) כולן נכנסות ל-`phase="reveal"` גם
בתשובה נכונה ועוצרות על כפתור, ולאף אחת מהן אין
`setTimeout(advance)` ולו פעם אחת. וזה סתר את מה שדף הבית מבטיח
בארבע שפות: **"קצב שנקבע על ידי הלומד"**.

**עכשיו גם תשובה נכונה עוצרת על הכפתור.** אותו מסך משמש לשתי
התוצאות, ולכן הוא נשען על התוצאה האחרונה במקום להניח כישלון:
בתשובה נכונה כרטיס ירוק עם המחמאה, **בלי לגלות את התשובה** — מי
שענה נכון אינו צריך שיגלו לו; בשתי טעויות הכרטיס האדום עם
"התשובה היא", ההסבר, ואותו כפתור. בלי ההפרדה הזאת תלמיד שענה
נכון היה מקבל מסך אדום שאומר לו שטעה.

**אומת בדפדפן אמיתי**, בשני המסלולים: אחרי תשובה נכונה, 2.5
שניות — יותר מפי שתיים מההשהיה שהייתה — `qi` נשאר 0, `phase`
הוא `reveal`, והכפתור "ממשיכים הלאה" על המסך; לחיצה עליו מקדמת
ל-`qi=1`. בשתי טעויות הכרטיס מכיל "התשובה היא 9" ואת ההסבר.
אפס שגיאות JS בשני המסלולים. `all.js` — 13 בדיקות, כולן עברו.

## שתי בדיקות ב-`exam806.js` שלא יכלו להיכשל

**בלי שינוי גרסה: לא נגעתי בקוד שמוגש למשתמש.** קובץ בדיקה אחד,
ולכן אין מה למסור למי שכבר התקין.

**`accept` על `type:"number"` אינו נקרא כלל.** `checkAnswer` יוצא
בענף המספרי אחרי `parseNum`, לפני שהוא מגיע ל-`accept`. מי שכתב
שם ניסוח חלופי האמין שהרחיב את מה שמתקבל, וקיבל שדה מת — נמדד
בפועל מול הקוד: `accept:["חמש"]` על `value: 5` מחזיר `false`.
הסובלנות במספרים היא `tolerance`, ו-`parseNum` כבר מקבל שבר,
אחוז ופסיק עשרוני. הבודק אומר את זה עכשיו במפורש.

**והבדיקה שכן עמדה על `accept` לא יכלה להיכשל לעולם.** היא הזינה
כל ניסוח חזרה ל-`checkAnswer` וציפתה שיתקבל — אבל `checkAnswer`
בונה את רשימת המותרים כ-`[value].concat(accept)`, ולכן כל איבר
ברשימה מתקבל מעצם היותו בה. בדיקה שהתשובה עליה תמיד "כן" נראית
כמו רשת ביטחון ואינה אחת.

במקומה נבדק מה שכן יכול להישבר: ניסוח שמנורמל בדיוק כמו קודמו.
**נמצאו ארבעה כאלה בתוכן הקיים** — ב-`806-demo-3` שאלה 3 סעיף א
רשומים תשעה ניסוחים, ו-`3x²-12x+9`, `3*x^2-12*x+9`, `3x^2−12x+9`
ו-`3x²−12x+9` מנורמלים כולם ל-`3x^2-12x+9`, שהוא ה-`value` עצמו:
`normExpr` כבר מטפל בחזקה עילית, בכוכבית ובמקף היוניקודי. חמשת
הנותרים אמיתיים.

**זו הערה ולא ממצא, בכוונה.** התלמיד שכותב את הניסוח הזה כן
מתקבל — דרך ה-`value`. מה שנשבר הוא רק האמונה של מי שספר תשעה
ניסוחים וקיבל חמישה. דבר שאינו פוגע בתלמיד אינו מצדיק חבילת
בדיקות אדומה, ולכן `all.js` נשאר ירוק.

**אומת:** `node .claude/qa/all.js` — 13 בדיקות, כולן עברו. שתי
הבדיקות החדשות נבדקו בשתי מוטציות מכוונות בקובץ התוכן ונדלקו
בשתיהן.

## התקנה נפרדת לכל אפליקציה — הפורטל 27 · b48 · t48 · u45 · v43 · g42 · n42 · m45 · a11 · l12 · x14 · reader 23

**דף הבית כבר ידע להתקין את עצמו. עכשיו הוא יודע להתקין כל אחת
מהאפליקציות בנפרד.** לכל אחת מ-12 האפליקציות יש `manifest.json` עם
`id` ו-`scope` משלה ו-`sw.js` משלה, ולכן מערכת ההפעלה כבר ראתה בהן
אפליקציות נפרדות — אבל כפתור ההתקנה הופיע רק בתוך האפליקציה ואחרי
אישור התנאים. מי שרצה שלוש היה צריך להיכנס לשלוש, לחכות לפס בכל
אחת, ולדעת מראש שהוא קיים.

לכל כרטיס יש היום **התקנה** לצד **כניסה**. הוא מוביל לאפליקציה עם
`?install=1`, והיא מציגה את הכפתור מיד. לכל אחד שם נגיש נפרד
("התקנת ניב כאפליקציה נפרדת"), כי "התקנה" לבדו חוזר זהה בשתים־עשרה
כרטיסים ולקורא מסך זו רשימת מילים זהות.

**ובדרך התברר שהמנגנון עמד בדיוק מול הבקשה.** `lg-install-off` היה
מחרוזת אחת ב-`localStorage`, ושתים־עשרה האפליקציות ודף הבית חולקים
מקור אחד. סגירת הפס פעם אחת בכל מקום — או **התקנה מוצלחת של
אפליקציה אחת**, שגם היא כותבת את המפתח — השתיקה את ההצעה בכל השאר
לתמיד. מי שהתקין את "שלב" לא היה מקבל הצעה להתקין את "ניב" לעולם.
המפתח פר-אפליקציה מהיום, והישן נקרא כברירת מחדל כדי לכבד ביטול
קודם.

נמדד בדפדפן: 12 כרטיסים, 12 כפתורים, 12 תוויות ייחודיות, כולם
בגובה 44px; `?install=1` גובר על ביטול קודם ובלעדיו הביטול מכובד;
ביטול ב-math-app כותב `lg-install-off:math-app` בלבד ו-english
עדיין מציעה; אפס שגיאות JS.

`legal/protect.js` השתנה, ולכן כל שנים־עשר מפתחות הקאש עלו יחד.

## ״שלב״ בארבע שפות · math-teen t47 · הפורטל 25

משימה 4ב ב-`CELINE.md`. ל״שלב״ היו עברית וערבית בלבד, והמילון
הערבי הוא הגדול בפורטפוליו. נוספו **רוסית ואנגלית**: `TR_RU`
ו-`TR_EN`, כל אחד 1,925 מפתחות (מפלט סקריפט האיחוד) — 1,875
המפתחות האפקטיביים של `TR.ar` כפי שנקראו מהדף הרץ (`TR_AR`,
`TR_AR_XQ` וההשמות הנפרדות יחד), ועוד 50 מילות ההקראה של
`SAY_MAP` ו-`PROSE_MAP`. 191 מהם הועתקו כמות שהם ממילוני
`math-uni`, כדי שמסכי ההגדרות ובונה המבחן יאמרו את אותו הדבר
בשתי האפליקציות; השאר תורגמו לפי הסכימה, ובודק אוטומטי אכף
שוויון מפתחות, שימור מספרים, סימנים מתמטיים, משתנים, מצייני
מיקום, תגיות ורווחים בקצוות של קטעי משפט.

**ההנדסה שהתרגום לבדו לא היה מכסה:** בחירת הקול נגזרת עכשיו
מקוד ה-TTS שב-`LANGS` ולא מ-`LG==="ar"`; טבלאות ההקראה עוברות
ברוסית ובאנגלית דרך המילון (`sayWord`), כמו ב-`math-uni`; דוגמת
הקול, הכותרת התחתונה, קישור התנאים, אות הלוגו ואותיות ימי השבוע
בארבע שפות; זיהוי שפת המכשיר בכניסה הראשונה מכיר גם רוסית;
ושורת הכיוון בשער התנאים — שה-README של ה-QA סימן כבאג עתידי
בדיוק ליום הזה — היא `var dirv=LANGS[LG].dir` ולא `"rtl"` קבוע.

**תיקון מדידה:** `_()` רשם ב-`trTodo()` גם ביטויים מתמטיים בלי
אות עברית אחת (`(1)/(4)`), שעברו דרכו בדרך להקראה. עכשיו הוא
רושם רק מחרוזת שיש בה עברית. ונמצא מפתח שהיה חסר גם בערבית —
שורת נוסחה בדף הנוסחאות — והושלם בשלוש השפות.

**נבדק בכרומיום, סיור מלא בכל שפה:** 18 נושאים × 3 רמות, תשובה
שגויה, רמז ופתרון מודרך עד סופו בכל שאלה, וכל המסכים. 432 שאלות
ו-1,018 צעדים בכל שפה, `trTodo()` ריק ברוסית ובאנגלית, אפס עברית
על המסך ובטקסט ההקראה, אפס שגיאות JS. הפריסה ב-LTR נבדקה בצילום
במסך השאלה, בדף הנוסחאות ובבונה המבחן. `all.js`: 13 בדיקות, כולן
עוברות.

דף הבית: תג ״4 שפות״ לכרטיס ״שלב״. `marketing/facts.md`:
אחת־עשרה מתוך שתים־עשרה — נותר 806 בעברית בלבד.

## המקריא הקולי בארבע שפות · reader 22 · הפורטל 24

עד היום היה למקריא הקולי ממשק בעברית ובערבית בלבד. נוספו **רוסית
ואנגלית** — משימה 4א ב-`CELINE.md`: `var LANGS` נושא עכשיו ארבע
שפות, ול-`TR` נוספו מילוני `ru` ו-`en` עם בדיוק אותם 102 מפתחות
של `ar` (המפתח הוא המחרוזת העברית, אות באות; `node` שבדק שוויון
מפתחות מול `ar` הוא המקור למספר). הטקסט שהמשתמש מדביק נשאר עברי
— זו האפליקציה — ומה שמתחלף הוא רק הממשק שמסביב.

שתי השפות החדשות הן משמאל לימין, ולכן `dir` של המסמך מתחלף
והפריסה מתהפכת. ההערה שטענה "אין כאן היפוך פריסה" תוקנה, ושני
אזורי הטקסט של המשתמש קיבלו כיוון משלהם: `#text` הוא `rtl` קבוע,
ותיבת ההדבקה מתיישרת לימין ברגע שיש בה אות עברית (`syncInputDir`)
וכשהיא ריקה — לפי שפת הממשק, כדי שה-placeholder המתורגם יתיישר
לפי שפתו. נמדד בכרומיום: `direction` של התיבה `ltr` כשהיא ריקה
באנגלית ו-`rtl` אחרי הדבקת עברית.

**שני באגים שהיו שם גם בערבית**, ונמצאו רק מפני שהבדיקה ספרה
מחרוזות עבריות בממשק: (1) ה-`placeholder` וה-`aria-label` של תיבת
ההדבקה נשארו בעברית בכל שפה — `trSkip`, שנועד לתוכן של המשתמש,
פסל את כל ה-`textarea` ובכלל זה את מאפייניו; לולאת המאפיינים
מדלגת עכשיו רק על שער התנאים. (2) ההערה "· בלי ניקוד" במסך
הקריאה לא התחלפה בהחלפת שפה, כי היא נכתבת מ-JS אחרי הצילום —
נוספה ל-`refreshDynamic`.

בדף הבית הכרטיס של המקריא נושא עכשיו את תג ״4 שפות״ (`tLangs`),
ו-`marketing/facts.md` אומר "עשר מתוך שתים־עשרה" במקום תשע —
נותרו ״שלב״ בשתי שפות ו-806 בעברית בלבד.

נבדק בכרומיום בארבע השפות: מחוץ לאזור הטקסט של המשתמש אין
מחרוזת עברית אחת ברוסית ובאנגלית (מלבד כפתור "עברית" בבורר
השפה, שהוא שמה של השפה), אפס שגיאות JS, ומסך הקריאה נפתח
באנגלית עם "Sentence 1 of 2". `arabic.js reader` עובר, ו-`all.js`:
13 בדיקות, כולן עוברות.

## שני נושאים ל-lomda: בחירות וכדור הארץ · lomda l11

`civics` היה המאגר היחיד עם נושא אחד; לכל השאר היו שניים. נוסף לו
**״בחירות ורשות מקומית״** — השיטה היחסית, אחוז החסימה, הבחירה
הישירה של ראש הרשות ושל ראש הממשלה וביטולה. נבדק שאינו חופף
ל-`money/wage`, שכבר מכסה ביטוח לאומי ודיני עבודה.

ל-`nature` נוסף **״כדור הארץ והסביבה״** — טמבורה וקרקטאו, נדידת
היבשות של וגנר והמנגנון שהיה חסר לה, ארהניוס וקילינג, וחור האוזון
שנמצא במדידה ונסגר בהסכם. `matter` ו-`body` מכסות פיזיקה, כימיה
ורפואה; מדעי כדור הארץ היו החסר.

שני הנושאים נכנסו לקבצי מאגר **קיימים**, ולכן אפס שינויי חיווט:
אין תגית `<script>` חדשה, אין נתיב חדש ב-`PRE`, ואין כרטיס מסלול
חדש. רק תוכן, והעלאת גרסה.

`banks.js`: 5 מאגרים · 11 נושאים · 143 פריטים, אפס ממצאים — ובכללם
שלמות ארבע השפות בכל שדה. בכרומיום נבנו 2,400 שאלות בשש
צמדי נושא־רמה: אפס בלי תשובה, אפס עם שתי תשובות נכונות, אפס
כפילויות, וכולן בארבע אפשרויות. 13 בדיקות, כולן עוברות.

## הכרטיס של ״תאוריה מדברת״ הוביל ל-404 · הפורטל 22

**🔴 קריטי, והיה חי.** ב-4.9 נמחק שדה הנתיב (`u`) מהכרטיס בדף הבית,
בהנחה שהריפו הנפרד כבר שונה ל-`theory`. **ההנחה לא נכונה.** הכרטיס
בונה את הקישור כ-`a.u || "/" + a.id + "/"`, ולכן כל מי שלחץ על
הכרטיס הגיע לכתובת `/theory/` — שאינה קיימת.

**נמדד, ולא שוער:** רשימת הריפוזיטוריז של החשבון מחזירה **שניים
בלבד** — `lagstein1-png.github.io`, והריפו שנושא את השם האנגלי הישן.
ריפו בשם `theory` אינו קיים. GitHub Pages מפרסם פרויקט תחת שם הריפו,
ולכן הכתובת החיה היא זו של הריפו שקיים.

שני הבודקים סתרו זה את זה, וזה מה שאיפשר לזה לעבור: `apps.js` דורש
נתיב מפורש לאפליקציה שאין לה תיקייה כאן (סעיף 4.5, נוסף באותו יום),
ו-`naming.js` אוסר את השם הישן בכל קובץ. `main` נשאר אדום ב-`apps.js`
מרגע המחיקה.

**הפתרון: החרגה של מחרוזת, לא של קובץ.** השדה הוחזר, ו-`naming.js`
מדלג על שדה `u` שערכו הנתיב הזה, בתוך `index.html` בלבד — לא על
הקובץ, ולא על אותה מחרוזת עם רווח אחר.
אומת בפועל: הוספת המילה בהערה בראש אותו קובץ הפילה את הבדיקה,
והסרתה החזירה אותה לאפס. גם הודעת ההצלחה של הבודק תוקנה, כי היא
הצהירה שהשם ״אינו מופיע בשום קובץ״ — וזה כבר לא נכון.

**מה שסוגר את זה סופית אינו כאן:** שינוי שם הריפו הנפרד. ביום שזה
קורה נמחקים שלושה דברים יחד — השדה, ההחרגה ב-`naming.js` וסעיף 4.5
ב-`apps.js`. עד אז השדה הוא נתיב טכני ולא שם, וכך זה מנוסח
ב-`NAMING.md`.

## הנושא החמישי בלומדה — בריאות ותזונה · lomda l9

שני נושאים. **מה יש באוכל** — הניסוי של לינד בצפדינה (1747), המונח
״ויטמין״ (1912), ויטמין D (1922), מלח מיודד (1924), קמח מועשר
(1941), ההנחיות התזונתיות הראשונות (1980), המלצת ארגון הבריאות על
סוכר חופשי (2015) והסימון בחזית האריזה בישראל (2020). **עישון,
תנועה ושינה** — מחקר פרמינגהם (1948), מחקר הרופאים הבריטים (1954),
הדוח האמריקאי על עישון (1964), איסור העישון בישראל (1983), הדוח על
פעילות גופנית (1996), אמנת הטבק (2003), הרחבת האיסור (2007) ותוכנית
הפעילות העולמית (2018).

המושגים: קלוריה, סוכר חופשי, סיבים תזונתיים, נתרן, גורם סיכון,
פעילות אירובית, לחץ דם והתמכרות לניקוטין. שני קטעי המקור הם שני
הטורים בטבלה שעל האריזה — ל-100 גרם מול ״למנה״, כשגודל המנה נקבע
בידי היצרן — ומאה וחמישים הדקות בשבוע, שנמדדות כסכום ולא כאורך כל
פעם.

**הזהירות כאן גדולה מזו שבכלכלת הבית, ומאותה סיבה מוגברת:** אנשים
עלולים לפעול לפי זה בגוף שלהם. לכן כל פריט הוא גילוי, חוק או מנגנון
ולא הוראה מה לאכול; אין מינונים, אין המלצה אישית ואין אבחון;
וההמלצות היחידות שמופיעות מיוחסות במפורש לגוף שפרסם אותן.

הלומדה: **חמישה מאגרים, תשעה נושאים, 117 פריטים**, ארבע שפות בכל
שדה (`node .claude/qa/banks.js`). `STATUS.md` רוענן מהמחולל.

**מפתח דף הבית הוקצה מחדש במיזוג.** הענף לקח 21, ובאותה שעה `main`
לקח 21 לתיקון ההדר — שתי גרסאות שונות עם אותו מפתח. שני השינויים
נוחתים יחד תחת **22**, ו-21 של `main` נשאר שלו.

`node .claude/qa/all.js` — שלוש־עשרה בדיקות, כולן עוברות.

## הנושא הרביעי בלומדה — כלכלת הבית · lomda l8 · הפורטל 19

**משכורת, מסים, מחירים והלוואות.** שני נושאים: *משכורת וזכויות* —
שעות עבודה ומנוחה (1951), הגנת השכר (1958), פיצויי פיטורים (1963),
שכר מינימום (1987), עבודת נשים (1954), דמי מחלה (1976), שוויון
הזדמנויות בעבודה (1988) ופנסיה חובה (2008); ו*מחירים והלוואות* —
חוק מע״מ (1975), השקל שהחליף את הלירה (1980), הגנת הצרכן (1981),
תוכנית הייצוב והשקל החדש (1985), הסדרת הלוואות חוץ־בנקאיות (1993),
תקנות ביטול עסקה (2010), חוק נתוני אשראי (2016) וחוק צמצום השימוש
במזומן (2018).

המושגים הם תלוש שכר, ברוטו ונטו, ביטוח לאומי, פנסיה, ריבית, מע״מ,
סך ההחזר והצמדה למדד. שני קטעי המקור הם קריאת תלוש, והמודעה
״הלוואה של 10,000 ₪, רק 220 ₪ לחודש״ — שחסר בה מספר התשלומים, ובלעדיו
אי אפשר לדעת שסך ההחזר הוא 13,200 ₪ ושההלוואה עולה 3,200 ₪.

**מה שלא נכתב כאן, בכוונה:** אין סכומים עדכניים, אין אחוזי מס ואין
שיעורי ריבית. הם משתנים, והמאגר אינו יודע מתי הוא נקרא — מספר
שמשתנה כל שנה אינו נכתב כתשובה נכונה. כל פריט הוא חוק או מנגנון,
ולא עצה מה לעשות בכסף.

הלומדה: **ארבעה מאגרים, שבעה נושאים, 91 פריטים**, ארבע שפות בכל
שדה (`node .claude/qa/banks.js`).

**שם הכרטיס בדף הבית הפסיק למנות תחומים.** הוא היה ״לומדה — מדע,
אזרחות ורשת״, והיה מתיישן בכל נושא; עכשיו הוא ״לומדה — לימוד לפי
נושא״, כמו התגית של האפליקציה עצמה. מה שעדיין מונה תחומים הוא תיאור
הכרטיס וארבע מחרוזות ה-`lead`, וזה מתועד ב-`CLAUDE.md` כנגיעה
רביעית שהיא רשות.

**ותוקן ממצא שלא נולד כאן:** `naming.js` נכשל על `CHANGELOG.md`
עצמו. הרשומה שהודיעה על מחיקת השם האנגלי הישן כתבה אותו פעמיים —
פעם בתוך `"id":"…"` ופעם בתוך תבנית החיפוש של הבודק. הבדיקה נכשלה
על `main` לפני שנגעתי בו, ואומת: `git stash` והרצה על העץ הנקי
החזירו את אותם שני הממצאים. שתי השורות נוסחו מחדש בלי לכתוב את
השם, והבדיקה חזרה לאפס.

`node .claude/qa/all.js` — שלוש־עשרה בדיקות, כולן עוברות.

## השם האנגלי הישן נמחק מהמאגר · הפורטל 18

**הוחלט למחוק את השם האנגלי של ״תאוריה מדברת״ כליל** — לא רק כשם
מותג באות גדולה, כפי שנקבע אתמול, אלא בכל אות ובכל מקום: מזהים,
מפתחות, הערות קוד ומסמכים.

מה שהשתנה:

- שדה ה-`id` ברשומת האפליקציה ב-`DATA.APPS` נושא היום את הערך
  `"theory"` במקום השם האנגלי הישן, וכך גם מפתח ה-`ICONS` ורשימת
  `EXTERNAL` ב-`apps.js`.
- שמונה־עשר אזכורים ב-`ARCHITECTURE.md`, ב-`learning-core/`,
  ב-`marketing/`, בקובץ הסוכן `bezalel` וב-README של הבדיקות —
  כולם אומרים היום ״תאוריה מדברת״.
- `naming.js` מחפש היום את השם הישן בכל צורת אות, ולא רק את צורת
  המותג באות גדולה — כלומר הוא נכשל גם על אותיות קטנות.

**נשאר מופע חוקי אחד, והוא נתיב ולא שם:** השדה `u` ברשומת
האפליקציה. הכרטיס בונה את הקישור כ-`a.u || "/" + a.id + "/"`,
ולכן נתיב מפורש גובר על המזהה. הוא נשאר מפני שכתובת הפרסום נקבעת
בשם הריפו הנפרד — GitHub Pages מפרסם פרויקט תמיד תחת שם הריפו —
ושינוי הנתיב כאן בלי שינוי שם הריפו נותן קישור ל-404. הבדיקה
מנכה את השדה הזה מהשורה לפני החיפוש, ולכן **מופע נוסף באותה שורה
עדיין נתפס** — נבדק במוטציה.

כדי למחוק גם אותו צריך לשנות את שם הריפו הנפרד ל-`theory`; אז
נמחק השדה, ונמחקות איתו שלוש השורות שמחריגות אותו.

**מה שנבדק.** שתים־עשרה הכרטיסים בדף הבית נטענו בכרומיום: כל
קישור מצביע ליעד הנכון, האייקון של התאוריה נשמר, ואפס שגיאות JS.
שלוש מוטציות על הבודק — השם חוזר באות קטנה לקובץ שיווקי, המזהה
מוחזר לקדמותו, ומופע נוסף באותה שורה של שדה `u` — ושלושתן נתפסו.
`all.js`: 13 בדיקות, כולן עברו.

מפתח הקאש של דף הבית עלה ל-18, כי `index.html` השתנה.

## הנושא השלישי בלומדה — מחשב ורשת · lomda l7 · הפורטל 17

**זה המבחן של המנגנון, והוא עבר:** נושא שלם נוסף בלי שורת מנוע אחת.
`lomda/data/digital.js` נכתב, תגית `<script>` נוספה לפני המנוע, ונתיב
נוסף ל-`PRE` שב-`sw.js`. זהו.

שני נושאים במסלול חדש: **האינטרנט — מאיפה הוא הגיע** (ARPANET 1969,
מעבר ל-TCP/IP ב-1983, האתר הראשון ב-1991, גוגל 1998, מוזאיק,
ויקיפדיה, יוטיוב והאייפון), ו**בטיחות ברשת** (חוק הגנת הפרטיות 1981,
תולעת מוריס 1988, ILOVEYOU בשנת 2000, תקנות הפרטיות האירופיות 2018,
חוק הספאם הישראלי 2008, סטוקסנט 2010, WannaCry 2017, וכופרת צינור
הדלק ב-2021). המושגים הם פישינג, אימות דו־שלבי, כופרה וגיבוי,
ושני קטעי המקור הם איך כתובת הופכת למחשב, ואיך נראית הודעת מרמה
על חבילה שממתינה.

**למה דווקא זה:** קהל היעד כולל עולים חדשים ונהגים מבוגרים, ומי שאינו
יודע מה זה פישינג משלם על כך בכסף ולא בציון. המושגים נבחרו לפי מה
שמגן ולא לפי מה שמרשים.

המאגר מביא את הלומדה ל-**שלושה מאגרים, חמישה נושאים, 65 פריטים**,
כולם בארבע שפות (`node .claude/qa/banks.js`).

**מה כן היה צריך לגעת בו מחוץ ללומדה:** שם הכרטיס בדף הבית מונה את
התחומים, ולכן הוא מתיישן עם כל נושא. עודכן ל״לומדה — מדע, אזרחות
ורשת״, יחד עם ארבע מחרוזות ה-`lead`, ומפתח הקאש של דף הבית עלה
ל-`17-pwa1`. זה טקסט ולא חיווט — הנושא עובד גם בלי זה, וזה מתועד
עכשיו ב-`CLAUDE.md` כנגיעה רביעית שהיא רשות.

**המספרים הוקצו מחדש במיזוג, ולא בבנייה.** הענף נבנה על `l1`
וסימן את עצמו `l2`; בזמן שהוא נבנה נדחפו ל-`main` ארבעה שינויים
נוספים ללומדה, ו-`l2` כבר תפוס שם על ״ההתקדמות נמחקה בכל כניסה״.
`main` על `l6` ועל `16-pwa1`, ולכן כאן `l7` ו-`17-pwa1`. שתי גרסאות
שונות עם אותו מפתח פירושן שמי שקיבל אחת לא יקבל את השנייה לעולם —
וזה בדיוק מה שהמנגנון נועד למנוע.

**מיזוג ה-`lead` היה איחוד ולא בחירת צד:** `main` שינה את מניין
יחידות הבגרות (״3–4 ו-5 יח״ל״) באותה מחרוזת שבה אני הוספתי את
התחומים החדשים. הוחלה ההחלפה שלי על הטקסט של `main`, ושני השינויים
שרדו.

**ותוקנה שורה בהערת סעיף 7 שהייתה שקרית:** היא אמרה שהתגית נכנסת
״בסוף index.html״ — הניסוח שנשאר מהבנייה הראשונה, לפני שהתברר
שהתגית חייבת לבוא לפני המנוע. זה בדיוק הבאג שהסעיף מזהיר מפניו,
כתוב בתוך האזהרה עצמה.

`node .claude/qa/all.js` — שלוש־עשרה בדיקות, כולן עוברות.


## התלמיד שולח קישור ולא קוד, וקולות Gemini הוסרו · math-app b46 · english n39 · history m41 · lomda l5 · ulpan a8 · math-teen t45 · math-uni u43 · math-uni2 v41 · math-uni3 g40 · reader 21

**חשוב — המורה מקבל קישור, לוחץ, והתוצאה נקלטת.** הודעת הוואטסאפ של
התלמיד מסתיימת עכשיו בקישור לאפליקציה עם הקוד אחרי `#r=`, במקום
בקוד ערום להעתקה. פתיחת הקישור אצל המורה עוברת למסך הציונים, קולטת
את ההגשה ופותחת את המבחן; הכתובת מנוקה כדי שרענון לא יקלוט שוב.
תיבת ההדבקה מקבלת גם קישור שלם. הקוד עדיין מוצג על המסך להעתקה
ידנית. תשע אפליקציות, קוד זהה. אומת בדפדפן מקצה לקצה בארבע מהן:
בנייה, פתרון, שליחה, לחיצה, קליטה, והדבקת קישור שלם.

**קולות Gemini הוסרו משש אפליקציות.** math-app, math-teen, math-uni,
math-uni2, math-uni3 ו-reader. הקהל לא ישיג מפתח API, Edge נותן קול
נשי טבעי בחינם, והמסך בלבל. קולות המכשיר לא השתנו; שחרור ה-iOS של
speechSynthesis נשאר, ושחרור אלמנט השמע ששירת רק את Gemini הוסר.
שמירה ישנה עם מפתח או קול Gemini נטענת ונופלת לקול המכשיר בשקט,
נבדק בדפדפן. מילוני התרגום שמרו את הרשומות היתומות; המחרוזות
העבריות שנאלצו להשתנות קיבלו מפתחות חדשים עם ערבית, רוסית ואנגלית,
ודף הנחיתה של שלוש אפליקציות האוניברסיטה כבר לא מבטיח "קול טבעי".
`git diff --stat` לשש: 795 שורות נוספו, 1,927 נמחקו, לפני המיזוג.

**חשוב — "29 − 7" הוצג "7 − 29" במבחן.** רצף של שני מספרים וסימן
ניטרלי בפסקה עברית מתהפך. `.mline` ו-`.xq-e` לא הוגדרו מעולם בחמש
האפליקציות שהעתיקו את מודול המבחן, וב-math-app גם המשפט "כמה זה
29 − 7?" התהפך. עכשיו שניהם מבודדים שמאל־לימין, וב-math-app התרגיל
שבתוך המשפט עטוף בקטע LTR. נמדד לפי מיקום הספרות על המסך: לפני
התיקון "7−29", אחריו "29−7".

**math-app — שמירה ישנה בלי `petAuto` אופסה בטעינה.** `PETSUGG` הוגדר
אחרי `load()`, ו-`isSuggestedName` שנקראת מתוכה זרקה. אותה מחלקת
באג כמו `GEM_VOICES` ב-b42. המשתנה עבר לפני `load()`.

**זניח — שורת 3/4/5 יח״ל בבונה המבחן.** הגיעה עם המודול ממשפחת
math-uni; ב-math-app יש מסלול אחד ואין יחידות. מוצגת רק כשיש יותר
ממסלול אחד.

## התלמיד לא יכול היה לשלוח תשובות בוואטסאפ · math-app b45 · english n38 · history m40 · lomda l4 · ulpan a7 · math-teen t44 · math-uni u42 · math-uni2 v40 · math-uni3 g39

**קריטי — "לא ניתן לבדוק את מספר הטלפון 0508554380… חסרה קידומת חיוג".**
`waLink` בנה `wa.me/0508554380` מהמספר כפי שהמורה הקליד, ו-wa.me
מקבל רק מספר בינלאומי בלי אפס מוביל. כל תלמיד שלחץ "שליחה
בוואטסאפ" בסוף המבחן נתקע על ההודעה הזאת, והתשובות לא הגיעו.
עכשיו אפס מוביל מקבל 972, "00" בהתחלה נופל, ומספר שכבר מתחיל
בקידומת מדינה נשאר. גם מבחנים שכבר נבנו מתוקנים, כי ההמרה נעשית
בזמן בניית הקישור ולא בשמירה. הפונקציה יושבת בבלוק המבחן המשותף,
ולכן תוקנה בתשע האפליקציות יחד; `engine.js` מאשר שהן זהות. אומת
בבדיקת יחידה על שש צורות הקלדה, כולל מקף, פלוס ומספר זר.

## כפתורי המבחן הכיתתי היו שקופים בחמש אפליקציות · math-app b44 · english n37 · history m39 · lomda l3 · ulpan a6 · math-teen t43 · math-uni u41 · math-uni2 v39 · math-uni3 g38

**חשוב — "מבחן חדש" ו"צרו את המבחן" נראו כטקסט קטן עם פלוס.** מודול
המבחן הכיתתי הועתק ממשפחת math-uni, ושם המחלקות `.btn-p` ו-`.btn.w`
מוגדרות ב-CSS. ב-math-app, english, history, lomda ו-ulpan הן לא
הוגדרו מעולם, ולכן כל הכפתורים הראשיים של המורה — "מבחן חדש", "צרו
את המבחן", "התחלת מבחן", "סיום" וקישורי הוואטסאפ — קיבלו רק את
הבסיס של `.btn`: בלי רקע, בלי צבע, וברוחב הטקסט. עכשיו הן מוגדרות
בחמש האפליקציות, בכחול שמבדיל את כפתור המורה משאר המסך. "מבחן
חדש" גם הוגדל בכל תשע האפליקציות שיש בהן מסך ציונים, כדי שיהיה
הכפתור הראשי של המסך ולא שורה בין השורות. נבדק בצילום מסך
בדפדפן אמיתי, ו-`engine.js` מאשר שבלוק המבחן נשאר זהה בכולן.

**math-app היא b44 ולא b43, ו-math-uni היא u41 ולא u40.** בזמן
הבנייה `main` הקצה את `b43` ואת `u40` לתיקונים אחרים — תג המספר
במבחן, ו-math-uni. אותו מפתח עם תוכן שונה פירושו שמי שכבר קיבל
את b43 לא היה מקבל את הכפתורים לעולם, ולכן המספר הוקצה מחדש
במיזוג, כפי שהכלל דורש.

## ההתקדמות נמחקה בכל כניסה, והפתרון נחשף בטעות הראשונה · math-app b42 · english n36 · history m38 · lomda l2 · ulpan a5

**קריטי — math-app התחילה מהאונבורדינג בכל כניסה.** `load()` בדק את
`googleVoice` השמור מול `GEM_VOICES`, אבל המערך מוגדר רק אחרי
`var state=load()`, וב-`var` הוא עדיין `undefined` באותו רגע. הקריאה
`.some` עליו זרקה `TypeError`, ה-`catch` החזיר מצב ריק — והחיה, השם
וה-XP נעלמו, אף שהשמירה ישבה ב-`localStorage` שלמה. הבדיקה הוסרה:
`gvOf` כבר נופל לקול הראשון כשהמזהה אינו מוכר. אומת בדפדפן: אותה
שמירה שהחזירה `onboarding` בגרסת `main` מחזירה עכשיו `practice`.

**חשוב — טעות אחת חשפה מיד את הפתרון.** "מצב רגוע", שהוא ברירת
המחדל, נתן את התשובה אחרי לחיצה שגויה אחת, ותלמיד שטעה בטעות סתם
לא קיבל הזדמנות לחשוב. עכשיו תמיד יש ניסיון שני: במצב רגוע הטעות
הראשונה פותחת את הרמז הראשון מעצמה, ובלעדיו רק מעודדים; הפתרון
המלא מגיע אחרי הטעות השנייה. תיאור המצב בהגדרות עודכן בארבע שפות.

**חשוב — "אין קול נשי" בכרום על ווינדוס, וההודעה שלחה למקום הלא נכון.**
כרום על ווינדוס רואה רק את קולות המערכת: בעברית יש אחד, Asaf
הגברי, ובערבית — בלי חבילת שפה — אף אחד. ההודעה הפנתה ל"הוספת
קולות", שאינה מציעה קול נשי בעברית, ובערבית אמרה "פתחו ב-Edge או
Chrome" למי שכבר יושב בכרום. עכשיו, בווינדוס ומחוץ ל-Edge, שתי
ההודעות אומרות ש-Edge נושא קולות טבעיים בשפה הזאת, נשיים וגבריים,
ונותנות קישור `microsoft-edge:` שפותח את אותה כתובת ב-Edge בלחיצה.
חמש האפליקציות שיש בהן את ההודעה תוקנו יחד; ארבעת עותקי המנוע
נותרו זהים.

## math-teen t42 — הערבית שנכתבה ולא הוצגה

**26 תרגומים שישבו במילון כקוד מת.**
חלון קוד המורה נבנה דרך `teT()`, שהחזיר את המחרוזת העברית כמות
שהיא בלי לעבור בתרגום. התרגומים הערביים לכל 26 המחרוזות היו כתובים
במילון מזמן ופשוט לא נקראו מעולם. שורה אחת — להעביר את `teT` דרך
`_()` — הפעילה את כולם.

**ושלושה מקומות שהטקסט בהם נבנה סביב ערך משתנה** ולכן לא היה לו
מפתח יציב במילון: ״לא נמצאה נוסחה עם ⟨מה שחיפשת⟩״ עבר לתבנית עם
מציין מקום, וכך המשפט מתורגם והחיפוש נשאר כפי שהוקלד; וכן הודעת
האישור למחיקה ושורת ״נפל ב:״ במסך הציונים.

**והמקף לפני מספר.** ‎«و-8»‎ במקום ‎«و 8»‎. המקף ב״ו-8״ הוא כלל כתיב
עברי — תחילית בת אות אחת נצמדת למספר במקף — ואין לו מקבילה בערבית.
מנגנון פיצול המחרוזות בלע אותו יחד עם המספר ופלט אותו כמות שהוא,
ב-6.5% מהמחרוזות, במרחק אות אחת מהמינוס הטיפוגרפי שהאפליקציה
משתמשת בו.

מה שבא לפני המקף מכריע, וזה נמדד ולא נוחש: תחילית שנצמדת — ‎«و»‎ בת
האות האחת, או ‎«الـ»‎ ו-‎«بـ»‎ שנכתבו במילון עם תַטְוִיל שנועד בדיוק
לסמן שהן נדבקות להמשך — נדבקת למספר בלי כלום. מילה שלמה כמו ‎«على»‎
או ‎«إلى»‎ מקבלת רווח. עכשיו ‎«الـ5»‎ ו-‎«و8»‎ מול ‎«على 2»‎.

## 806 — אוסף חמישי, 5 יח״ל · bagrut-806 x13

אוסף ה׳ — שש שאלות, שמונה־עשר סעיפים, 180 נקודות — ממשיך בדיוק
במקום שבו אוסף ד׳ נעצר. שישה נושאים שארבעת האוספים הקודמים לא
נגעו בהם:

- **סדרה הנדסית** — שתי שאלות הסדרות שהיו כאן שתיהן חשבוניות.
  כאן האיבר הכללי, הסכום החלקי, והסכום האינסופי — כולל תנאי
  ההתכנסות ולמה המעריך בסעיף א הוא חמש ובסעיף ב שש
- **מעגל** — השלמה לריבוע מהצורה הכללית, ומציאת הנקודה הגבוהה
  ביותר בלי לפתור משוואה
- **הסתברות בלי החזרה** — מה משתנה במונה ובמכנה אחרי ההוצאה
  הראשונה, ושני מסלולים ל״אחד מכל צבע״
- **נגזרת של לוגריתם טבעי** — ותחום ההגדרה שפוסל את השורש השלילי
- **נפח גוף סיבוב** — ריבוע הפונקציה, מקדם פאי, וגבול תחתון שאינו
  אפס
- **משפט הסינוסים ושטח משולש** — שתי שאלות המשולש שהיו כאן שתיהן
  משפט הקוסינוסים

**מה אומת.** כל שמונה־עשר התשובות חושבו מחדש בסקריפט בלתי־תלוי
שקורא אותן מהקובץ; הסטייה הגדולה ביותר 0.0027, בעיגול לשתי ספרות.
ארבעים ושישה מסלולי פתרון שגויים אופייניים נבדקו מול טווחי
הסובלנות ואף אחד אינו נופל בתוך טווח — ההדוק, שטח משולש בלי
הסינוס, רחוק פי שלושה. גם סכום שלוש ההסתברויות בשאלת הכד אומת
ויצא אחת. האפליקציה הורצה בכרומיום: חמישה כרטיסים, KaTeX מרנדר,
אפס שגיאות JS.

**ובדרך תוקן ממצא כוזב ב-`exam806.js`.** הבדיקה ש״רמז אינו מכיל
את התשובה״ חיפשה תת־מחרוזת, והתשובה `80` יושבת בתוך `180` —
כלומר רמז שאומר ״סכום הזוויות במשולש הוא 180 מעלות״ נדלק כאילו
הוא פותר במקום התלמיד. ההתאמה נעשית היום בגבולות מספר: ספרה,
נקודה או פסיק משני צדי ההתאמה פוסלים אותה. נבדק בשתי מוטציות —
רמז שבאמת מסגיר את התשובה עדיין נתפס, ומספר שמכיל אותה כתת־מחרוזת
כבר לא.

## 806 — אוסף רביעי, 5 יח״ל · bagrut-806 x12

**אישור מפורש לכתוב שאלות מקוריות בסגנון הבחינה, מסומנות ככאלה.**
זו הייתה השאלה הפתוחה היחידה ב-806, והיא נסגרה. שלושת האוספים
שקדמו כבר נכתבו במצב הזה; מעכשיו זו החלטה מתועדת ולא ברירת מחדל.

אוסף ד׳ — שש שאלות, שמונה־עשר סעיפים, 180 נקודות — נכתב כדי לכסות
את מה שלא היה מכוסה בשלושת האוספים הקודמים:

- **התפלגות בינומית** — מקדם הבינום, המאורע המשלים, ותוחלת שאינה
  מספר שלם
- **פונקציה מעריכית** — קיצון ופיתול של `x·e^(−x)`, שבה הגורם
  המעריכי לעולם אינו מתאפס
- **שטח בין שתי פונקציות** — אינטגרל אחד על ההפרש, ולא הפרש של
  שני שטחים
- **בעיית קיצון** — גדר על שלוש צלעות, שבה יחס הצלעות יוצא קבוע
  ואינו תלוי באורך הגדר
- **חקירת פונקציה טריגונומטרית** — `2sin x + x`, שני פתרונות בתחום
- **גדילה רציפה** — זמן הכפלה בלוגריתם טבעי, והמלכודת של בסיס
  1.05 במקום `e`

`season:"הדגמה"` נשאר כפי שהוא, ולכן הכרטיס מציג ״בחינת הדגמה — לא
בחינה אמיתית״ בדיוק כמו שלושת קודמיו. תווית הרמה היא ״5 יח״ל״.

**מה אומת, ואיך.** כל שמונה־עשר התשובות חושבו מחדש בסקריפט
בלתי־תלוי שקורא אותן מהקובץ — הסטייה הגדולה ביותר היא 0.0043,
בעיגול של תשובה לשתי ספרות. ארבעים מסלולי פתרון שגויים אופייניים
נבדקו מול טווחי הסובלנות, ואף אחד אינו נופל בתוך טווח; ההדוק
מביניהם — ריבית בדידה `1.05¹⁰` במקום גדילה רציפה — רחוק פי שמונה
מהטווח. האפליקציה הורצה בכרומיום: ארבעה כרטיסים, KaTeX מרנדר,
אפס שגיאות JS.

**ובדרך נסגר באג בדוח הנושאים החלשים.** `d.weak` ממופתח במחרוזת
`q.topic`, ובתוכן היו שני שמות לאותו נושא — ״אינטגרל״ מול ״חשבון
אינטגרלי״, ו״חקירת פונקציה״ מול ״חשבון דיפרנציאלי״. תלמיד חלש
באינטגרלים התפצל לשתי שורות, וכל מחצית עלולה הייתה ליפול מתחת לסף
ולא להיות מסומנת כלל. השמות אוחדו, ו-`exam806.js` בודק מעכשיו
שכל `topic` נמצא באוצר מילים סגור — כדי שהסחיפה לא תחזור בשקט.

## 806 — ארבעה שערים שנסגרו, ותווית רמה · bagrut-806 x11 · הפורטל 16

**איבר הקוסינוס היה בלתי נראה.** בשתי השאלות הטריגונומטריות היחידות
באפליקציה הזווית היא 60 מעלות, ושם `2cos(60°)=1` — כלומר
`2·8·5·cos60°` שווה `8·5` בדיוק, ומשפט הקוסינוסים מתכווץ ל-`b²+c²−bc`.
תלמיד שכתב `64+25−40` בלי לדעת מה הקוסינוס עושה שם קיבל 7, שהיא
התשובה המלאה. שאלה 5 באוסף השלישי עברה ל-120 מעלות: שם הקוסינוס
שלילי והחיסור הופך לחיבור — הכיוון שלא הודגם באפליקציה מעולם. הצלע
יוצאת 11.36, ו-7 נדחה. אומת גם בגאומטריה אנליטית, שאינה משפט
הקוסינוסים. אותו ליקוי קיים באוסף השני ולא נגענו בו: הוא של סשן אחר
שהגיע ל-main ראשון.

**״השטח הכלוא״ לא הדגים מעולם ששטח אינו אינטגרל.** התחום היה כזה
שהפונקציה חיובית בכל אורכו, ולכן השטח יצא שווה לאינטגרל והכלל לא
נדרש. התחום עבר ל-`[-1,1]`: האינטגרל יוצא 2 עגול ומשכנע, והשטח 2.30.
`tolerance` ירד ל-0.05 — ב-0.5 ההפרש `8/27 = 0.296` היה נבלע בטווח,
ומי שלא פיצל היה נחשב צודק. זו הייתה אזהרה של בודקת הרמות, ואומתה
מול `checkAnswer` של האפליקציה.

**נושא חלש נמדד באחוזים, והאחוז מדד את מבנה הבחינה.** נושא עם שני
סעיפים נפל ל-50% על טעות אחת ונצבע אדום; נושא עם שלושה נשאר על 67%
וירוק. אותה ידיעה, שני צבעים. `isWeak` אינו תלוי במספר הסעיפים:
החלקה אחת אינה מסמנת נושא, שתי טעויות מסמנות, ונושא שכולו שגוי מסומן
תמיד. הוחל על דוח הסימולציה ועל מסך ההתקדמות יחד.

**והכותרת טענה חמש יחידות.** בדיקת הרמות עברה על 18 הסעיפים וקבעה,
לפי מה שכל סעיף דורש בפועל, ש-`806-demo-1` ו-`806-demo-2` הם 3–4
יחידות ו-`806-demo-3` הוא 3–5. האפליקציה מצהירה עכשיו ״3–4 ו-5 יח״ל״
בארבע השפות, וכל אוסף נושא תווית `level` משלו. **אין בריפו מקור מאומת
לסילבוס הרשמי של שאלון 806**, וההערה בקובץ התוכן אומרת זאת: הרמה
נגזרה מהדרישה ולא מטענה מה נמצא בשאלון.

`x=−1` נכתב במינוס U+2212 ולא במקף, מפני ש-`speech.js` ממיר אותו
ל״מינוס״ ומשאיר מקף רגיל כמו שהוא — עם מקף ההקראה הייתה אומרת ״איקס
שווה 1״, ההפך מהשאלה.

## מטמון דף הבית החזיק 5MB של אפליקציות אחרות · הפורטל 15

**ה-`sw.js` של דף הבית רשום ב-scope `/`, ולכן הוא מטפל גם בביקור
הראשון באפליקציה אחרת** — לפני שה-worker שלה נרשם. הוא שמר את
התשובה במטמון שלו, ומהרגע שה-worker של האפליקציה קיים הוא זה שמגיש
את הנתיב, והעותק ההוא לא נקרא עוד לעולם.

נמדד בכרום אחרי סיור בכל עשר האפליקציות: `site-13-pwa1` החזיק
5,356KB זרים ב-15 רשומות — math-uni ‏970KB, math-uni2 ‏886KB,
math-uni3 ‏773KB, math-teen ‏611KB, history ‏542KB, english ‏425KB,
ulpan ‏341KB, math-app ‏327KB, ‏katex.min.js ‏269KB ועוד — מול 172KB
משלו. כל אחת מהרשומות כפולה: ה-worker של אותה אפליקציה מחזיק עותק
משלו. על טלפון של מי שהאפליקציות האלה נבנו בשבילו, זה מקום שנלקח
לחינם.

**התיקון:** ה-worker מגיש רק מה ששייך לדף הבית — קובץ בשורש, או אחת
משלוש התיקיות שאין להן worker משלהן (`img`, `legal`, `voice`).
אפליקציה חדשה נופלת מהכלל החוצה מאליה, בלי לגעת בקובץ.

אחרי אותו סיור בדיוק: `site-15-pwa1` מחזיק 198KB, זרים 0KB, תשע
רשומות ואין בהן אף נתיב של אפליקציה אחרת. אופליין נבדק עם הרשת
מנותקת — דף הבית, `legal` ו-`voice` נטענים מהמטמון, ו-math-uni
ו-bagrut-806 נטענים מה-worker שלהן. הקוד הישן נמדד לפני השינוי
כבקרה שלילית והראה את 5,356KB.

**`cache.js` גדל משתי בדיקות לחמש.** שם המטמון מול התחילית
ש-`activate` מוחק (‏`caches.delete` אינו מוגבל ל-scope, ולכן תחילית
שאינה תואמת מוחקת מטמון של אפליקציה אחרת); נתיבי `PRE` שקיימים
בדיסק (ההתקנה עושה `catch(() => {})`, ולכן נתיב שגוי נבלע בשקט —
104 נתיבים נבדקו, כולם תקינים); ושומר הבעלות החדש, שחובה בשורש
ואסור בכל השאר, מפני שבתוך `sw.js` של אפליקציה הוא היה מבטל לה את
כל הקאשינג. חמישה באגים הוכנסו אחד־אחד כבקרה שלילית, וכל חמשת
הממצאים נתפסו. קו הבסיס אפס על שנים־עשר הקבצים.

**המספר 14 הוקצה מחדש.** `main` תפסה אותו ללומדה עם קוד אחר, ולכן
הפורטל כאן הוא 15 — שתי גרסאות שונות תחת אותו מפתח פירושן שמי
שקיבל אחת לא יקבל את השנייה לעולם.


## לומדה — נושא חדש הוא קובץ, לא אפליקציה · lomda l1 · הפורטל 14

**כל אפליקציה בפורטפוליו נולדה מהעתקה של קובץ בן אלפי שורות.**
נושא חדש פירושו היה לפתוח `index.html` חדש, לשכפל מנוע שלם, ולתחזק
מאותו רגע עותק נוסף של אותו קוד. `lomda/` שוברת את זה במקום אחד
בדיוק: **סעיף 7 — ״תוכן״ — הוחלף בטוען**, וכל השאר זהה לשאר המשפחה.

התוכן יושב ב-`lomda/data/*.js` ונטען בתגיות `<script>` רגילות, לפי
הדגם של `bagrut-806`. הסכימה — `E(שנה, כותרת, מה, סיבה, תוצאה)`,
`C(מושג, הגדרה)`, `Sc(שורות, שאלות)`, `TOPIC`, `BANK` — מוגדרת
ב-`data/schema.js`, וקובץ תוכן אינו מכיר את המנוע ואינו נוגע בו.

**נושא חדש = שלוש נגיעות ואפס שורות מנוע:** קובץ שקורא ל-`BANK(...)`,
תגית `<script>` לפני המנוע, ואותו נתיב ב-`PRE` שב-`sw.js`.

שני מאגרים נכנסים עם האפליקציה — **מדע וטבע** (חומר ואנרגיה, הגוף
והבריאות) ו**אזרחות** (איך המדינה עובדת). שלושה נושאים, תשע רמות,
**39 פריטים**, כולם בארבע שפות (`node .claude/qa/banks.js`).
כל פריט מתוארך נושא שנה של פרסום או של גילוי, ולא הערכה: פריט
שהתאריך שלו אינו חד־משמעי לא נכתב כפריט מתוארך, מפני שהשנה היא
התשובה לשאלה ״מתי זה קרה״.

### הבאג השקט שנתפס בבנייה, ובגללו נכתב בודק

שלוש תגיות ה-`<script>` הושמו בזנב הקובץ, ליד `/legal/` — והמנוע
יושב בסקריפט שנפתח מיד אחרי `<div id="app">`. כלומר סעיף 7 רץ לפני
שהתוכן נטען: `TRACKS` ו-`TOPICS` יצאו ריקים, המסך הראה אפס נושאים,
ו**שום שגיאה לא נזרקה**. `smoke.js` החזיר `clean`. מה שנפל בסוף היה
`exam.js`, על `spec` שהוא null, במקום אחר לגמרי בקוד.

**`.claude/qa/banks.js`** — בודק חדש, מחווט ל-`all.js`. הוא בודק
תגית לכל קובץ, סדר טעינה (התוכן לפני המנוע, ו-`schema.js` לפני
קובצי התוכן), נוכחות ב-`PRE`, ושלמות התוכן: ארבע שפות בכל שדה,
שנה בכל פריט מתוארך, שלושה מסיחים ושלושה נימוקים בכל שאלת קטע,
ולפחות ארבעה פריטים ברמה. **קו הבסיס אפס, ואומת מול שלושה מקרי
כשל** — תגית שהוזזה אחרי המנוע, נתיב שהוסר מ-`PRE`, ושפה שנמחקה
משדה. שלושתם נתפסו.

### מה שנגזר מזה בשאר הריפו

- **דף הבית:** רשומה ב-`DATA.APPS`, סמל שכבות ב-`ICONS`, המניין
  מ״אחת־עשרה״ ל״שתים־עשרה״ בארבע השפות, ארבע מחרוזות ה-`lead`
  שמונות את התחומים, ותווית התחום שנפתחה גם למדעים. מפתח הקאש
  של דף הבית עלה ל-`14-pwa1`.
- **`engine.js`** בודק עכשיו תשעה עותקי מנוע ולא שמונה. שלושת בלוקי
  ה-`index.html` — אונבורדינג, מבחן כיתתי ושער תנאים — זהים בייט־בייט
  גם ב-`lomda`. ה-`sw.js` שלה **אינו** בהשוואה, בכוונה: ה-`PRE` שלה
  מונה גם את קובצי התוכן, כמו `bagrut-806` שמצרף KaTeX.
- **`a11y.js`, `arabic.js`, `exam.js`, `all.js`** — `lomda` נוספה
  לרשימות שלהן, ולכן היא נבדקת ולא רק קיימת.
- **הערת ה-`legal/`** בשנים־עשר קובצי ה-`sw.js` אמרה ״תשע אפליקציות
  מקדימות-קאשינג״. עודכנה למספר הנכון בכולם.

`node .claude/qa/all.js` — שלוש־עשרה בדיקות, כולן עוברות.

## הממשק הערבי היה בגופן בלי אותיות ערביות — n35 · m37 · b41 · t41 · u39 · v38 · g37 · reader 20

**שמונה מעשרת הדפים הציגו ערבית בגופן שאין בו גליף ערבי אחד.**
Heebo, Fredoka ו-Assistant אינם מכילים ערבית כלל, והדפדפן אינו
מתלונן: הוא נופל בשקט לגופן ברירת המחדל של המערכת, גליף אחר גליף.
התוצאה תלויה במכשיר, ולעולם אינה מה שעוצב.

נמדד בדפדפן ולא נקרא בקוד: הוחלף הממשק לערבית בכל דף, ונבדק
`getComputedStyle(el).fontFamily` על טקסט ערבי גלוי. שבעה דפים
החזירו `Heebo`, `reader` החזיר `Assistant`, ורק `אולפן` ודף הבית
החזירו `Noto Sans Arabic`. אחרי התיקון — כל העשרה מחזירים אותה.

התקלה הזאת דווחה כאן פעמיים ולא תוקנה, וזה מובן: **מי שרואה אותה
אינו קורא עברית, וגם לא כותב באגים בעברית.** לכן היא לא תתגלה
בשימוש, ולכן היא צריכה בודק.

`Noto Sans Arabic` נוסף לאותה בקשת גופנים שכבר קיימת בכל דף —
אין כאן תלות חיצונית חדשה, וקובצי הגופן נמשכים רק כשמוצג טקסט
ערבי. הכלל מוחל על `lang="ar"` בלבד, ונבדק שהעברית בכל עשרת
הדפים נשארה בגופן שלה: Heebo, Fredoka ו-Assistant כמקודם.

**`.claude/qa/arabic.js`** — בדיקה חדשה, מחווטת ל-`all.js`. היא
שואלת שלוש שאלות: יש ממשק ערבי; המשפחה **נטענת** מבקשת הגופנים
או מ-`@font-face`; ויש כלל שמחיל אותה. שני ניסוחים תקינים
מתקבלים — שם מלא בתוך הכלל, או משתנה `--ar`.

הגרסה הראשונה של הבודק הייתה שקרית: היא חיפשה את שם המשפחה בכל
הקובץ, ולכן הצהרת `--ar` לבדה סיפקה אותה. הסרתי את המשפחה מבקשת
הגופנים — והבדיקה עברה. עכשיו היא בודקת את ה-`<link>` ואת
`@font-face` בלבד, ושני מקרי הכשל אומתו בפועל: בלי טעינה, ובלי
כלל. בודק שעובר תמיד אינו בודק.

## מסך התרגול הבטיח מה שכבר נבנה · bagrut-806 x10

**״דוח הנושאים החלשים ועבודה בלי אינטרנט נכנסים בשלב 4״** עמד במסך
התרגול המודרך אחרי ששלב 4 כבר נדחף. שניהם קיימים בקוד — הדוח נבנה
מ-`weak`, וה-service worker נרשם — והמשפט אמר לתלמיד שהם חסרים.
הערת הכותרת ב-`app.js` סימנה מאותה סיבה את שלב 3 כ״עד כאן״.

`index.html` נמצא ב-precache, ולכן מפתח המטמון עולה איתו.

## 806 — אוסף שלישי, ותשובות נכונות שנדחו · bagrut-806 x9

**שני סשנים בנו תוכן ל-806 באותו יום, ושניהם סימנו את עצמם `x8`.**
זו אינה תאונה אלא בדיוק מה שכתוב ב-`CLAUDE.md`: כל סשן מתפצל מ-`main`,
רואה `x7`, ומקצה לעצמו את הבא בתור. הפעם זה נתפס במיזוג — שתי גרסאות
שונות עם אותו מפתח קאש פירושן שמי שהתקין אחת לא יקבל את השנייה לעולם.
הוקצה `x9`.

**מי שהגיע ל-main ראשון נשאר.** שתי בחינות ההדגמה שנחתו קודם לא נגענו
בהן במבנה, ושש השאלות שנבנו במקביל נכנסו כ-`806-demo-3` — אוסף שלישי
ולא דריסה. גאומטריה אנליטית קיימת רק בו. הבודק `content806.js` שנבנה
כאן נמחק: `exam806.js` שהגיע ראשון הוא על-קבוצה שלו, והוא אפילו מריץ
את `checkAnswer` האמיתית מול כל תשובה רשומה.

**תשובה נכונה שנדחתה.** `normExpr` השווה מחרוזות אחרי הסרת רווחים
בלבד, ולכן `f'(x)=3x^2-12x+9` — הצורה שמורה מלמד לרשום — נפסלה, וכך גם
מינוס יוניקוד של מי שהעתיק מהמסך. היום יורדת קידומת של שם פונקציה, אבל
רק תבנית מוכרת ורק כשיש סימן שוויון אחד: בלעדי שני התנאים גם
`3x^2-12x+9=0` היה מתקבל, וזו טענה אחרת לגמרי. 38 מקרים נבדקו מול
`checkAnswer` עצמה, ובהם עשרה שחייבים להידחות.

**נוסחה שהדפיסה את מה שהשאלה בודקת.** שמונה מחרוזות `latex` הציגו ערך
מוצב במקום נוסחה כללית — `a_20 = a_1 + 19d` מעל רמז ששואל כמה קפיצות
יש, ו-`f'(x) = 3x^2-12x+9` מעל שאלה שמבקשת למצוא את הנגזרת. שש תוקנו
באוסף החדש, ושתיים בבחינות שכבר היו.

**ושני סעיפים שאפשר היה לעבור בלי לדעת:** ערך המינימום של
`x³−6x²+9x+2` הוא 2, שהוא גם המקדם החופשי וגם `f(0)` — שלושה מסלולים
שגויים לתשובה נכונה, והשאלה שונתה להפרש בין המקסימום למינימום. ובנושא
״גדילה ודעיכה״ כל היעדים היו חזקות של 2, ולכן אפשר היה להשלים 30 מתוך
30 בלי לוגריתם אחד.

**`parse.js` נתן מעבר ריק לקובץ js חיצוני** — הוא סרק תגיות `<script>`
בלבד, ולכן ארבעת קובצי ה-js של 806, האפליקציה הרב-קבצית היחידה, דיווחו
`0 parse failures` בלי שנפרסה בהם שורה. וכרטיסי ההדגמה, שלושתם, נשאו
כותרת זהה; נוסף להם סימן סידורי, ו-`season` נשאר ״הדגמה״ בדיוק מפני
שהוא מה שמדליק את תווית ״לא בחינה אמיתית״.

## 806 — מאגר התוכן: משאלה אחת לשש · bagrut-806 x8

**עד עכשיו היו באפליקציה שאלה אחת ושני סעיפים.** כל הקוד של ארבעת
השלבים — ההקראה, הרמזים, הסימולציה, דוח הנושאים החלשים — רץ מעולם
על נושא אחד. עכשיו יש שתי בחינות, שש שאלות, שנים־עשר סעיפים
ושלושים ושישה שלבי רמז, בשישה נושאים: הסתברות, סדרות, חקירת
פונקציה, גדילה ודעיכה, אינטגרל וטריגונומטריה.

**כל השאלות מסומנות `season:"הדגמה"`, וזה אינו קישוט.** הן נכתבו
לאפליקציה ולא נלקחו משאלון שניתן. כלל 2 בקובץ התוכן אומר בדיוק
למה: תלמיד שמתאמן על "קיץ 2023 מועד ב׳" ומגלה שזו שאלה מומצאת
מאבד אמון בכל השאר.

**כל תשובה אומתה בחישוב, לא בעין.** האיבר העשרים, סכום הסדרה,
נקודת המקסימום, ערכה, האוכלוסייה אחרי תשע שעות, הזמן ל-8000,
נקודת החיתוך, השטח — והאינטגרל נבדק גם נומרית מול הפונקציה
הקדומה. משפט הקוסינוסים ושטח המשולש חושבו לשש ספרות.

**ואז אומתה כל תשובה מול בודק התשובות של האפליקציה עצמה** — כי
תשובה נכונה שהאפליקציה דוחה היא באג שהתלמיד פוגש. שנים־עשר
סעיפים, כל אחד פעמיים: התשובה הנכונה מתקבלת, ותשובה שגויה נדחית.

**הפורמטים שתלמיד באמת כותב.** מי שחישב נכון אינו כותב את התשובה
כפי שהיא בקובץ: הוא כותב `7/12` ולא `0.5833`, `4/5` ולא `0.8`,
`0,8` בפסיק, `80%`, או `7.21110255` ישר מהמחשבון. כל אלה נבדקו
ומתקבלים.

**`tolerance` רחבה בכוונה, ונבדק שהיא בטוחה.** בשאלה שמבקשת עיגול
לשתי ספרות, `0.01` מקבלת גם את מי שעיגל לספרה אחת. נבדק במפורש
שאף מסלול פתרון שגוי אינו נופל בטווח: בטריגונומטריה, פיתגורס בלי
איבר הקוסינוס נותן 10, חיבור במקום חיסור 12.17, וסינוס במקום
קוסינוס 4.11 — כולם רחוקים. הטווח סולח רק על עיגול של תשובה
נכונה, וזו ההכרעה: מי שפתר נכון וכתב 7.2 לא טעה בטריגונומטריה.

**הסימולציה רצה מקצה לקצה בפעם הראשונה על יותר משאלה אחת.** שישה
סעיפים, שעון, בלי רמזים ובלי פתרונות; ענינו נכון על שני נושאים
ושגינו בשלישי, והדוח נתן 42 מתוך 64, פירק לפי נושא (2/2, 2/2,
0/2), ואמר "מה לחזור עליו קודם: טריגונומטריה".

63 טענות עוברות בשלוש חבילות בדיקה, אפס שגיאות JS. אף אמירה
בהקראה אינה מכילה לוכסן — נבדק על כל שש השאלות.

**`BUILD` ומפתח המטמון עלו יחד ל-x8.** `data/exams.js` מצורף מראש
ב-`sw.js`, ולכן בלי העלאה מי שכבר התקין היה נשאר עם שאלה אחת
לנצח — בדיוק הבאג שהמנגנון נועד למנוע.
## קבוצות — רמת בסיס שמגדירה · math-uni u38

**הנושא לימד פעולות על קבוצות ולא אמר מעולם מה קבוצה היא.** ארבע
הרמות פתחו ב"בקבוצה A יש 17 איברים" והמשיכו למשלים ולדה־מורגן;
מי שלא הביא את המושג מהבית קיבל נוסחאות לחשב בהן, בלי הדבר שהן
מדברות עליו.

רמה 1 היא היום רמה מגדירה, בשתי גרסאות מוגרלות:

- **חזרות וסדר** — `A = {1, 15, 1, 15, 20, 3, 20}`, וכמה איברים
  יש בה. שלושת המסיחים הם שלוש דרכים שונות לספור את אותה רשימה:
  כל הופעה, מחיקת חזרה אחת בלבד, ומספר המספרים שנכתבו פעמיים.
- **∈ מול ⊆ והקבוצה הריקה** — איזו קביעה נכונה. הקביעה הנכונה
  מוגרלת מארבע צורות שונות (`x ∈ A`, `{x} ⊆ A`, `∅ ⊆ A`, `A ⊆ A`),
  ולכן אי אפשר לזהות אותה לפי תבנית הכתיבה, והמסיחים הם בדיוק
  ארבע הטעויות שחוזרות: `{x} ∈ A`, `x ⊆ A`, `∅ ∈ A`, ומספר שאינו
  בקבוצה.

**שאלת ההכלה וההדחה לא נמחקה.** היא ירדה לרמה 2 וניצבת שם לצד
המשלים, ושתיהן מוגרלות — שתיהן ספירה לפי נוסחה, ולכן הן באמת
באותה רמה. רמות 3 ו-4 לא השתנו.

**הסוגריים המסולסלים נכנסו למילון ההקראה.** בלעדיהם `{5} ⊆ A`
ו-`5 ⊆ A` נשמעו אותו דבר בדיוק — וזו ההבחנה היחידה שהשאלה בודקת.
`⊆` נוסף גם למילון ההסברים, שבו הוא חסר מלכתחילה.

22 מחרוזות חדשות, כולן בערבית, ברוסית ובאנגלית: `trMissing` מחזיר
0 בשלוש השפות. `entropy.js` — 46,000 הגרלות, אפס ממצאים;
`options.js` — הנושא אינו מופיע ברשימת השאלות שמציגות פחות מארבע
אפשרויות; 4,000 הגרלות של רמה 1 אומתו בקוד אחת־אחת, וכל קביעה
נכונה יצאה נכונה וכל מסיח יצא שקרי.
## שער השלבים, וסריקת תוכן על 66,150 שאלות · ניב n36 · אולפן a5 · מפנה m37

**אפליקציה חדשה אינה מתפרסמת ביום שנבנתה, ומאגר שאלות נסרק
לפני שקוראים אותו ביד.** שני כלים חדשים ותיקון אחד שהם מצאו.

**`stage.js` — חמישה שלבים.** `build → internal → content-qa →
approved → public`, ואפליקציה מגיעה ללומד רק אחרי `approved`.
עד אז היא אינה ב-`DATA.APPS` ונושאת שער פנימי. הכלי משווה שלושה
מקורות שנכתבים ביד בשלושה רגעים שונים — הרישום, דף הבית, וקובץ
האפליקציה — ולכן נפרדים בשקט. הוא בודק גם את מניין ה-`badge`,
ארבע המחרוזות שאינן נגזרות מ-`DATA.APPS` ולכן נשכחות.
אחת־עשרה האפליקציות הקיימות רשומות `legacy-published`: הן
פורסמו לפני שהשער נבנה, וזו אזהרה ולא כשל.

**`content.js` — תשע משפחות ממצאים.** 66,150 שאלות משמונה
האפליקציות שיש להן `buildQ`, 150 הגרלות לכל נושא×רמה. אין כאן
"מאגר" לפתוח: השאלות נוצרות בזמן ריצה, ולכן הסורק מגריל אותן
בדפדפן אמיתי ומודד. הוא אינו יודע אם 7×8=56 — נכונות מתמטית
היא של אוקלידס — ולכן `REVIEW` הוא שאלה שמופנית לבעלים ולא
קביעה שיש באג.

**ומה שהוא מצא ביום הראשון.**

**תרגיל ההשלמה במבחן הכיתתי היה בלי המשפט.** `formGap` בונה את
המשפט עם ה-`_____` לתוך `gapHtml`; מסך התרגול מצייר אותו,
ו-`buildQ` — הנתיב של המבחן הכיתתי — מעולם לא צירף אותו. התלמיד
קיבל את הרמז בשפת האם וארבע מילים באנגלית, בלי המשפט ובלי לדעת
איפה החסר. ב-31% מההגרלות (484 מתוך 1,548 שנמדדו) שתי מילים
מהארבע נמצאות במשפט ושתיהן נכונות באותה מידה; בשאר פשוט אין על
מה לענות. התיקון הוא שורה אחת, ובאותו אופן שבו הבנת הנקרא כבר
מצרפת את הקטע שלה: `expr=q.gapHtml`. אותה שורה נכנסה גם לאולפן,
שם היא כרגע ריקה — נמדד ב-1,080 הגרלות שהבנק שלה אינו מייצר
`gap` כלל — מפני שהקובץ נולד מהעתקה של ניב, ותיקון שיושב רק
בעותק אחד חוזר כבאג בעותק הבא.

**וההקראה אמרה את התשובה לפני שעונים — תוקן.** ב״ניב״,
ב״מפנה״ וב״אולפן״ מקש הרווח קרא את `P.q.say` בלי לבדוק אם כבר
ענו, בעוד שמקשי 1–4 שלידו כן מוגנים ב-`!P.done`. ב״מפנה״ `say`
הוא כרטיס הפריט המלא ובו התאריך והסיפור — כלומר התשובה — ב-900
מתוך 900 השאלות שנדגמו; בניב 453 מתוך 1,031, ובאולפן 285 מתוך
849. מי שקורא לאט לוחץ רווח כדי לשמוע את השאלה, ושמע את הפתרון.

הכלל שנקבע: **לפני שעונים נאמר מה שמוצג על המסך; אחרי שעונים
נאמר `say` המלא, כפי שהיה.** שאלת האזנה יוצאת מן הכלל ואינה
משתנה — שם השמע *הוא* השאלה, והשתקתו הופכת אותה לבלתי פתירה.
בתרגיל ההשלמה נאמר המשפט עם החסר, וה-_____ נעשה שהות ולא מילה.
זה נכתב בפונקציה אחת בשם `sayAsk`, בכל אחת משלוש האפליקציות.

**ובמבחן הכיתתי היה חור שלישי.** לניב הייתה כבר `sitSay` שבודקת
אם מה שעומד להיאמר מכיל את התשובה; אולפן, שנולדה מהעתקה שלה,
קיבלה במקומה `speak(q2.say)` חשוף — וכפתור ההקראה במבחן הקריא
את הפתרון. ל״מפנה״ הייתה הלוגיקה הנכונה אבל בתוך המטפל של
הכפתור, ולכן אי אפשר היה למצוא אותה ולא לבדוק אותה. שתיהן נושאות
עכשיו `sitSay` בשם הזה. אחרי התיקון: אפס דליפות ב-1,755 שאלות
מבחן שנדגמו בשלוש האפליקציות.

**וארבע בדיקות של הכלי עצמו, על דליפת ההקראה, שהיו כוזבות.**
מדד שמדווח על מה שחייב לקרות הוא מדד שלומדים להתעלם ממנו:
תשובה שמופיעה ב-`expr` מוצגת על המסך ממילא; הקראה שמונה את כל
האפשרויות מכילה את התשובה בהכרח, וזה תפקידה — מי שאינו קורא
חייב לשמוע גם את המסיחים (3,262 ממצאים ב״שלב״); המסיח `−37`
נאמר "מינוס 37" ולכן השוואה מילולית לא מצאה אותו והמנייה נראתה
חד־צדדית (69 נוספים); ואפליקציה שכבר בוחרת בעצמה מה להשמיע
נמדדת לפי הבחירה שלה ולא לפי השדה הגולמי. דליפה היא היום
**א־סימטרית**: התשובה נאמרת ואף מסיח לא. נבדק שהכלל המכויל
עדיין תופס את הבאג המקורי — 540 מתוך 540 ב״מפנה״.

**ושלוש בדיקות נוספות שהיו כוזבות ותוקנו לפני שהדוח הוגש.** מדד שמדווח
רעש קובר את מה שכן חשוב, ולכן הן רשומות ב-`.claude/qa/README.md`
במלואן: זהות שאלה אינה הנוסח שלה — "מה קרה קודם?" הוא נוסח קבוע
שכל תוכנו באפשרויות, וזה החזיר 55 FAIL כוזבים ב״מפנה״, 162
ב״ניב״ ו-147 ב״אולפן״, ובאקסיומה ב׳ גם רמה שנראתה כמייצרת שאלה
אחת בלבד; בדיקת פרוזה אינה
בדיקת נוסחה (9,657 ממצאים על `[−1 , 5)` ו-`18 : 15`); ובודקים
במקום שבו הלומד רואה (72 מתוך 72 שאלות ב״שלב״ נספרו כלא
מתורגמות, בזמן שהתרגום שם קורה ב-`trHTML` ברגע הציור).

## 806 — האפליקציה האחת־עשרה · הפורטל 12 · bagrut-806 x5

**שאלון 806 בהקראה: סימולציית בחינה עם שעון, ותרגול מודרך שבו
הרמזים נחשפים אחד־אחד.** נבנתה בארבעה שלבים, וכל אחד מהם נבדק
בדפדפן אמיתי לפני שהבא התחיל.

**שלד וסכימה.** חמישה קבצים ולא אחד — `index.html`, `app.js`,
`speech.js`, `data/exams.js` ו-`sw.js` — בניגוד לשאר הריפו, ולפי
הספק. הבחינה שבתוכה מסומנת `season:"הדגמה"` ונושאת תווית "לא בחינה
אמיתית": שאלה שנכתבה כאן ומוצגת כ"קיץ 2023 מועד ב׳" שורפת את האמון
בכל מה שיבוא אחריה.

**הקראה בלי שירות ענן.** Web Speech API בלבד, ולכן היא עובדת גם
אופליין ואינה עולה דבר. ארבע התקלות שנלמדו באפליקציות האחרות
מטופלות במפורש: המתנה לרשימת הקולות, פיצול לאמירות קצרות, פיסוק
בין שתי ספרות שאינו נקודת חיתוך, ופינג נגד עצירת ההקראה בכרום.
**מה שנאמר על נוסחה הוא השדה `speech`, לעולם לא ה-LaTeX** — נבדק
שאף אמירה אינה מכילה תו לוכסן. המשפט הנקרא מסומן בצהוב, 13.86
בבהיר ו-9.20 בכהה.

**רמזים שאינם פתרון.** "בדקו" אינו חושף דבר, "רמז" מקדם שלב אחד
ואומר כמה נשארו, ו"הפתרון המלא" נפרד ודורש לחיצה משלו. תלמיד
שנתקע צריך דרך קדימה שאינה "תראה לי את התשובה".

**בדיקת התשובה סלחנית בכתיב ונוקשה במתמטיקה.** לסעיף שהתשובה בו
0.8 מתקבלים 0.8, 0,8, ‎.8‎, 80% ו-4/5; "בערך חצי" נדחה עם הסבר מה
כן אפשר לכתוב. פסיק מטופל לפי הקשר — 3,500 הוא אלפים, 0,58 הוא
עשרוני. מפענח המספרים עבר 15 מתוך 15 מקרים בחישוב.

**סימולציה, דוח, ואופליין מלא.** שעון שסופר לאחור ונשאר רץ גם
כשיוצאים מהמסך, בלי רמזים ובלי פתרונות עד ההגשה, ובסיום דוח לפי
נושא שאומר במילים מה לחזור עליו קודם — מי שקורא לאט אינו מפענח
גרף. מסך "התקדמות" צובר את אותו מידע לאורך זמן, מהתרגול ומהסימולציות
יחד. ה-service worker מצרף מראש 34 קבצים ובהם KaTeX והגופנים
שלו במלואם: בלעדיהם נוסחה נראית אופליין כשורת סימנים, וזה בדיוק
המסך שהאפליקציה קיימת כדי למנוע.

**שער התנאים נכנס גם לכאן.** אפליקציה שמתפרסמת בדף הבית בלי השער
שיש בעשר האחרות היא פער שהבעלים לא בחר בו. המשמעות: מלכודת הקאש
של `legal/` מונה עכשיו **אחד־עשר** מפתחות ולא עשרה.

**דף הבית:** מ"עשר" ל"אחת־עשרה" בארבע השפות, סמל דף עם עקומה,
ומפתח קאש 12.

שני דברים תוקנו בכלים תוך כדי: `serve.js` מפנה עכשיו `/dir` ל-`/dir/`
ב-301 כמו GitHub Pages — בלעדיו כל נתיב יחסי נפתר מול השורש, וכל
אפליקציה רב-קבצית נראתה שבורה בבדיקה בזמן שבאתר החי היא תקינה;
ו-`exam.js` בודק גם את `אולפן`, שירשה את בונה המבחן ולא נבדקה.

## 806 — להשהות, ולחזור משפט אחורה · bagrut-806 x3

תוספת מעל מנוע ההקראה של שלב 2, ולא מנוע שני. שני סשנים בנו את
שלב 2 במקביל באותה שעה; זה שהגיע ל-main ראשון נשאר, וכאן נוספו
עליו רק הדברים שלא היו בו. הבנייה המקבילה נזרקה — וזו בדיוק
התאונה ש-CLAUDE.md מזהיר מפניה, שקרתה שוב מפני שלא נבדק `main`
בזמן העבודה אלא רק בתחילתה.

**השהיה, ומשפט אחורה.** "עצרו" לבדו מחייב להתחיל את השאלה מהתחלה,
וזה בדיוק מה שגורם למי שאיבד את החוט לוותר. בסרגל יש עכשיו ⏮,
השהו/המשיכו, ו-⏭, וכולם עובדים ברזולוציה של משפט.

**שתי תקלות שהתוספת חשפה:**
- פינג ה-keepalive נגד עצירת ההקראה בכרום עושה `pause(); resume()`
  כל ארבע שניות. בלי חריגה הוא היה מבטל את ההשהיה של המשתמש תוך
  ארבע שניות — כלומר מבטל את הכפתור שהוא הרגע לחץ.
- אנדרואיד מתעלם מ-`pause()`. לכן יש גם דגל עצירה משלנו: מנוע
  שמכבד משתתק מיד, ומי שלא — מסיים את המקטע הנוכחי ועוצר.

**"השאלה כולה" — כפתור אחד לשאלה על סעיפיה.** קודם היה כפתור
לכל פסקה בנפרד, ומי שרצה לשמוע שאלה שלמה לחץ ארבע פעמים וחיכה
בין לחיצה ללחיצה. הסדר הוא נוסח השאלה, נוסחתה, ואז כל סעיף עם
נוסחתו.

**נוסחה בלי `speech` אומרת זאת עכשיו בקול רם.** היא מעולם לא
הוקראה — זה נכון ומכוון, כי המנוע לא ייגע ב-LaTeX גולמי — אבל
בשקט זה נראה ככפתור הקראה שבור. שורה אדומה פונה לכותב התוכן.

25 טענות עוברות מול מנוע דיבור מזויף שרושם כל אמירה, אפס שגיאות
JS, ואפס גלילה אופקית ב-320, ב-390 וב-768 פיקסלים. אומת במפורש
שאף אמירה של "השאלה כולה" אינה מכילה לוכסן, ושההשהיה שורדת חמש
שניות של פינג.

## הפורטל 12 · b40 · t40 · u36 · v36 · g35 · n33 · m36 · reader 19 · ulpan a2

**כפתור ההקראה במבחן הקריא לתלמיד את התשובה.**
השדה `say` נבנה במפורש כ"השאלה והתשובה עליה", וההערה בקוד אומרת
שהוא נועד להישמע *אחרי* המענה. במבחן הכיתתי אותו שדה מוזן לכפתור
ההקראה, שזמין *לפני*. מי שלחץ על התאמת הנגישות — כלומר בדיוק מי
שהיא נועדה לו — שמע את התשובה. במפנה 97 מתוך 97 שאלות "לפי המקור",
בניב 742 מתוך 1,665 שאלות מבחן. היום אפס בשתיהן: מוקרא מה שעל
המסך, והאפשרויות אינן חלק ממנו.

**התשובה הנכונה הייתה תמיד הארוכה ביותר.**
בכל 24 שאלות "לפי המקור" במפנה, בלי יוצא מן הכלל. מי שלחץ תמיד
על הארוכה קיבל 100% בלי לקרוא את המקור. בניב אותו ליקוי, ושם
חמור יותר: הבנת הנקרא הציגה שלוש אפשרויות בלבד, וברמה 3 הארוכה
נכונה ב-93%. אחרי התיקון: 25.0% ו-32.5%, מול בסיס אקראי של 25%.
ואיזון אורכים לבדו לא הספיק — כל עוד התשובה נשארה בראש הטווח
הוא הוריד רק ל-66%. מה שסגר הוא פיזור מפורש של דרגת האורך.

**ותיקון שיצר ליקוי משלו.** ההארכה נעשתה ב-18 מסיחים בעזרת מילה
מוחלטת — "כלל", "רק", "שום". התשובות הנכונות לא השתנו, ולכן
המגבירים התרכזו בצד אחד, ונחשן "הימנע מהאפשרות המוחלטת" עלה
מ-27.8% ל-40.3%. נמדד על ידי בודקת שלא כתבה את התיקון, וזו בדיוק
הסיבה שהיא קיימת. המסיחים הוארכו מחדש בפסוקית ניטרלית, בלי לקצר,
והנחשן חזר ל-24.0–28.1%.

**ארבע אפשרויות שרק שתיים מהן יכולות להיות נכונות — 19 רמות.**
שתי אפשרויות הופיעו בכל שאלה ולא היו התשובה לעולם. התלמיד מוחק
אותן ביום השני ומנחש אחד לשניים. הפתרון אינו מחיקה אלא החייאה:
להגריל גם את המקרה שבו המסיח נכון, כי זה מלמד את הצד השני של
הכלל. עשר רמות ירדו לאפס אפשרויות מתות.

**וארבעה באגי נכונות שנתפסו באימות עצמו.** החמור: ב-`residue` L3
הקוטב יכול היה לשבת *על* מסלול האינטגרציה, האינטגרל אינו מוגדר
כלל, והאפליקציה סימנה 0.

**204 מסיחים הוחזרו כשגויים בלי מילת הסבר** — 72 במפנה ו-132
בניב, בארבע שפות. היום לכל אחד יש נימוק.

**תוצאת בדיקת הקול דיווחה בעברית בכל שפה**, בשש אפליקציות. מי
שהקול לא עובד לו ואינו קורא עברית קיבל הודעת תקלה שאינו מבין.

**המילה האנגלית הנלמדת בניב לא סומנה כאנגלית**, ולכן קורא מסך על
דף שה-`lang` שלו עברית הקריא אותה בקול עברי — הכישלון שהאפליקציה
קיימת כדי למנוע. ותעתיק ה-IPA הוסתר ממנו: ההגייה מגיעה מכפתור הקול.


**כל תשעת ה-service worker חילקו ביניהם מטמון אחד — והוכח בדפדפן.**
`caches.match(req)` בלי שם מטמון סורק את **כל** המטמונים של המקור
לפי סדר, ומחזיר את ההתאמה הראשונה. תשע האפליקציות יושבות תחת אותו
`lagstein1-png.github.io`, ולכן קובץ בעל אותו נתיב שנשמר על ידי
אפליקציה אחת הוגש לאפליקציה אחרת — גם כשהיא כבר עודכנה. המדידה
בכרום: `english` קיבלה את `/legal/terms.js` מהמטמון של דף הבית.
אחרי התיקון — `caches.open(CACHE).then(c=>c.match(req))` — כל
אפליקציה קוראת רק מהמטמון שלה, והבדיקה עברה. יש בקרה שלילית: אותה
בדיקה בדיוק הורצה על הקוד הישן ונכשלה, כפי שנדרש.

במקביל: תשובת ניווט נשמרת עכשיו תחת `req` ולא תחת `"./"`, ורק
כאשר `status===200` — כדי שתשובת שגיאה לא תיכנס למטמון ותוגש שוב
ושוב. וההערה בראש הקובץ, שטענה שיש "מקור אמת אחד" לגרסה, תוקנה:
מחרוזת הרישום מקודדת קשיח ואינה נגזרת מ-`BUILD`. מי שמעדכן אחת
חייב לעדכן את השנייה, וההערה אומרת את זה עכשיו במפורש.

**חמישה באגי נכונות, כל אחד אומת בהגרלה של 5,000 שאלות.**
מסיח שהוא תשובה נכונה שנייה אינו טעות ניסוח — הוא שאלה שאי אפשר
לענות עליה נכון.

- `math-teen genSystem` רמה 3: מערכת המשוואות יצאה מנוונת ב-16.8%
  מההגרלות, ואז שלוש מארבע האפשרויות נכונות. נוסף `while(b1===a1)`.
- `math-uni genLimits` רמה 3: הגבול הוכרז `∞` בעוד שהוא `−∞`,
  ב-16.2% מההגרלות. תוקן גם מקור הטעות — הנוסחה בדף הנוסחאות
  שממנה נבע הכלל השגוי, בארבע השפות.
- `math-uni genExtrema` רמה 2: מסיח שהוא בדיוק אותה קבוצת פתרונות
  כמו התשובה, ב-15.8%.
- `math-uni2 genDouble` רמה 3: `π·4` ו-`2π·2` הוצעו יחד — אותו ערך
  בשתי כתיבות, ב-34.4% מההגרלות.
- ותיקוני תצוגה שקוראים שגוי: `x¹`, `1i`, `1e^x`, `1x`, ומקף ASCII
  שההקראה הגתה "שתיים".

**`GENV` עלה בארבע אפליקציות — וקישורי מבחן ישנים ייפסלו.**
`math-teen` מ-3 ל-4, ו-`math-uni`, `math-uni2`, `math-uni3` מ-2
ל-3. הזרע קובע את השאלה, והתיקונים שינו את המיפוי זרע←שאלה; אותו
זרע מייצר עכשיו שאלה אחרת. מבחן שנשלח לפני העדכון יידחה בכניסה
במקום להיפתח עם שאלות שאינן אלה שהמורה ראה. **זו התנהגות מכוונת
ולא תקלה** — זה בדיוק מה ש-`GENV` נועד לעשות. מורה שקישורו נפסל
בונה אותו מחדש, ומקבל את הגרסה המתוקנת.

**שתי הקראות דיברו זו על זו.** מונה הדורות `_sayGen`, שקיים
ב-`math-teen`, חסר ב-`english`, `history` ובשלוש האקסיומות: הקראה
ישנה שהתחילה המשיכה לדבר מעל החדשה. המונה הועתק לחמשתן. רמזים
בשלוש האקסיומות עוברים עכשיו דרך `speakProse`. וב-`math-app`:
כפתור שנשאר בלי תווית, `÷` שנקרא "לחלק בֵּית", `aria-label="X"`
חסר משמעות, אזור `aria-live` להודעות, טבעת פוקוס נראית, ועצירת
הקראה ב-`pagehide`.

**ארבע שפות.** תוצאת בדיקת הקול (`VCHKMSG`) הופיעה בעברית בלבד
בשש אפליקציות ותורגמה לארבע. תווית הזמן בבונה המבחן חזרה לעברית
בכל שפה בארבע אפליקציות. `" פחות "` נוסף ל-`TR_KEYS` ב-`math-uni`,
ו-`speakMath` ב-`math-teen` עובר דרך `_()`. הוסר הומוגליף לטיני
שהסתתר בתוך מילה רוסית, ותוקן ניקוד תועה ב-`math-teen` — בשני
מופעיו יחד, כי המחרוזת העברית היא המפתח ב-`TR_AR`.

**36 האייקונים ייחודיים.** `english` ו-`history` חלקו אייקון זהה
בייט-בייט, ושלוש האקסיומות חלקו אייקון אחד בין שלושתן. צוירו
מחדש — מ׳ / ב׳ / ג׳ — וכל המידות נבדקו. נוספו `theme-color` לבהיר
ולכהה, שופרה הניגודיות של `--faint` ו-`--coral-deep`, ותוקן
`background_color` של `reader`.

**הגשות תלמידים נדרסו בשקט** בשבע אפליקציות — תוקן.
## אולפן — האפליקציה העשירית · הפורטל 11 · ulpan a1

**עברית לעולים חדשים, בנויה סביב מה שעוצר את היום ולא סביב דקדוק.**
קהל היעד אינו "לא יודע עברית". הוא חי בעברית כל יום ונכשל בה כל
יום — בקופת חולים, מול פקיד, מול טופס שחוזר. לכן סדר הלימוד כאן
נקבע לפי מה שעוצר, לא לפי שכיחות בטקסט.

ארבעה מסלולים, שנים־עשר נושאים, שלוש רמות בכל נושא, 275 פריטים —
כל אחד בעברית ובשלוש שפות ההוראה:

- **אות וצליל** — ב׳ מול כ׳, ד׳ מול ר׳, אותיות סופיות; הניקוד
  שהופך ספר לספר ולמספרה; והצלילים ח׳, ע׳ ו-צ׳ שאינם קיימים
  ברוסית ואין להם אות באנגלית.
- **מילים** — מספרים ושעות, בית ומשפחה, גוף לרופא, ופעלים יומיים
  **בשתי הצורות יחד**: אישה שאומרת "אני רוצה" בצורת הזכר מובנת,
  אבל שומעת שתיקנו אותה ומפסיקה לדבר.
- **מדוברת** — "סליחה, לא הבנתי" ו"על מה אני חותם?"; קופת חולים;
  בנק, דואר ועירייה.
- **שלטים וטפסים** — מה שקוראים על הקיר ובשדה של טופס, ומכתבים
  אמיתיים באורך ארבע שורות.

**המסיח הוא תמיד המילה האמיתית שקל להחליף בה.** דָּג מול רָג אינו
תרגיל, כי רָג אינה מילה; דַּק מול רַק כן. אותו כלל בקטעי הקריאה:
כל שאלת הבנה קיבלה מסיח רביעי, ולכן `options.js` מדווח 0% שאלות
עם פחות מארבע אפשרויות — הבסיס ב-`english`, שממנו נבנה השלד, הוא
8.9%.

**הניקוד מוצג תמיד, והתעתיק הלטיני לצידו.** מי שקורא לאט קורא
אותיות ולא מילים, ובלי ניקוד הוא מנחש תנועה ומקבל מילה אחרת.
התעתיק נשאר LTR בגופן נפרד: בלעדיו רמה 1 חסומה בפני מי שעדיין
אינו מזהה אות עברית בכלל.

**`Noto Sans Arabic` נטען לצד Heebo.** Heebo אינו מכיל גליפים
ערביים, והממשק הערבי נפל עד היום לגופן ברירת המחדל של המערכת.
זו אותה בקשת גופנים ולא תלות חדשה. **התקלה הזאת קיימת גם בשמונה
האפליקציות האחרות ולא תוקנה כאן.**

השלד הוא זה של `english`, על כל מה שכבר עובד בו: הקראה, מצב מורה,
בונה מבחן כיתתי, ארבע שפות ממשק ונגישות. מה שהוחלף הוא שפת הלימוד
עצמה — כל מקום שבו נאמר "אנגלית" עבר ל-LEARN, שפת החומר הנלמד,
והחומר הנלמד נאמר עכשיו בקול עברי ולא באנגלי.

**דף הבית:** מ"תשע אפליקציות" ל"עשר", בארבע השפות. המספר כתוב
במילה ולא בספרה ואינו נגזר מ-`DATA.APPS` — מי שיוסיף את האחת־עשרה
צריך לדעת את זה, וזה נכתב עכשיו ב-`CLAUDE.md` יחד עם שבעת המקומות
שאפליקציה חדשה נוגעת בהם.

אומת מול השרת המקומי: פרסינג נקי; smoke בדפדפן אמיתי — אפס שגיאות
JS, 16 פקדים; entropy — 18,000 הגרלות, אפס תשובות קבועות, אפס
כפילויות; options — 21,600 שאלות, אפס עם פחות מארבע אפשרויות ואפס
עם שתי תשובות נכונות; 115 מפתחות ממשק בכל אחת מארבע השפות, אפס
דליפת עברית; 275 פריטי תוכן, לכולם ארבע שפות מלאות.

## בחירת שאלות למורה — b39 · t39 · u35 · v35 · g34 · n32 · m35

**המורה בונה מבחן, ורואה עכשיו את השאלות שבו לפני שהוא שולח אותו.**
בונה המבחן הכיתתי קיים בשבע האפליקציות, והמורה בחר בו נושאים, רמה
ומספר שאלות — אבל את השאלות עצמן הוא לא ראה מעולם. הן הוגרלו ברגע
הלחיצה על ״צרו את המבחן״, נארזו לתוך הקישור, והמורה פגש אותן
לראשונה כשהתלמידים כבר נבחנו. במסך הבנייה יש עכשיו רשימה של השאלות
עצמן, בסדר שבו התלמיד יקבל אותן, ולכל אחת ״החלפה״ ו״הסרה״; מתחת
לרשימה כפתור הוספה לכל נושא שנבחר, ומעליה ״הצגת התשובות״ ו״הגרלה
מחדש״. מי שרוצה להמשיך לסמוך על ההגרלה פשוט לא נוגע ברשימה — ברירת
המחדל לא השתנתה.

**זרע נפרד לכל שאלה, וזה מה שהופך ״החלפה״ לאפשרית.**
עד כאן כל שאלות המבחן נבנו מזרם אקראי אחד עם זרע אחד, ולכן שאלה
מספר שלוש הייתה תלויה בשתיים שלפניה. החלפה שלה הייתה משנה גם את
הרביעית ואת החמישית — כלומר לא החלפה אלא הגרלה מחדש של כל מה
שאחריה. כל משבצת נושאת עכשיו זרע משלה, והמבחן נארז כרשימת
`[נושא, רמה, זרע]`. הזרע הוא 24 ביט ולא 32 כי הוא נוסע בתוך הקישור
שנשלח בוואטסאפ; מבחן מלא של 25 שאלות מגיע ל-892 תווי קוד.

**מבחן שכבר נשלח נשאר בדיוק כפי שהיה.** מבחן ישן אין לו זרע
למשבצת, ולכן `examQuestions` מזהה אותו ובונה אותו במסלול הישן, מזרע
אחד, בדיוק כפי שנבנה ביום שנשלח — אחרת הציון של מי שכבר נבחן היה
משתנה למפרע. גם מזהה המבחן נשמר: הזרע נכנס לחישוב רק כשהוא קיים,
וכל מבחן ישן מקבל את המזהה שקיבל תמיד, כדי שההגשות שנקלטו אליו לא
יתייתמו. `GENV` לא זז, כי אף מחולל שאלות לא שונה.

**המבחן נשלח בסדר שהמורה אישר.** עד כאן `exbuild` ערבב את השאלות
בשליחה. עכשיו הערבוב קורה בבנייה של הרשימה, המורה רואה את הסדר
הסופי, והשליחה אורזת אותו כמות שהוא.

**המחוון ״מספר שאלות״ לא עשה כלום ב-math-app וב-english.** המורה
גרר אותו ל-20 ובנה מבחן של 10, בכל פעם. ב-english לא היה לו מטפל
כלל; ב-math-app הוא נפל למטפל הכללי של המחוונים ונשמר אל
`state.settings.exn` — שדה שאיש לא קורא — במקום אל `EB.n`. בשתיהן
יש עכשיו מטפל, וב-history וב-math-teen וב-שלוש האקסיומות, שבהן הוא
כבר עבד, נוספה רק בניית הרשימה מחדש בסוף הגרירה.

**עשר מחרוזות חדשות, בארבע השפות.** נוספו לבלוק נפרד ולא לשורות
המילון הענקיות, שכל שינוי בהן מתנגש במלואו. ב-math-teen, שבה
המחרוזת העברית היא המפתח, הן נוספו ל-`TR_AR` — ונבדק שהן באמת
עוברות דרך `trHTML` ולא רק יושבות במילון.

**`.claude/qa/exam.js`** — כלי חדש שמשווה תו־בתו את מה שהמורה רואה
מול מה שהתלמיד מקבל מהקישור, ובודק שהחלפת שאלה אחת אינה נוגעת
בשאר ושמבחן ישן עדיין נבנה כמו קודם. שבע האפליקציות עוברות אותו.

## הפורטל 10 — סמל לכל אפליקציה, גופן ערבי, ונגישות דף הבית

**תשעה כרטיסים זהים בלי שום עוגן לעין.**

דף הבית היה בנוי נכון — ארבע שפות, מצב כהה, יעדי מגע 44 פיקסלים,
פלטה שכולה עוברת AA — ובכל זאת נקרא כתבנית. שלוש סיבות, וכולן
ויזואליות ולא מבניות: הסמל היה האות ״י״ בתוך ריבוע כחול; רשימת
האפליקציות הייתה תשעה מלבנים של טקסט בלבד, בלי דבר שהעין נתפסת בו;
ורצועת המספרים הייתה ארבע קופסאות שמשכו יותר תשומת לב מהאפליקציות
עצמן.

מה השתנה: לכל אפליקציה יש עכשיו סמל SVG מצויר ידנית שנושא את צבע
התחום — ארבע פעולות החשבון, מדרגות ל״שלב״, השטח מתחת לעקומה, סוגרי
מטריצה, המישור המרוכב, בועת דיבור, חץ שפונה ל״מפנה״, טקסט עם גלי
קול, והגה. הסמלים `aria-hidden` וניטרליים לשפה, ולכן לא נוסף אף
מפתח תרגום. הסמל הראשי הוחלף בשלושה פסים עולים תחת קשת קול, ארבע
הקופסאות אוחדו ללוח אחד עם קווי הפרדה, ולכל כותרת מקטע נוסף פס קצר
שנותן לדף קצב אחיד.

ובדרך, שני דברים שלא היו קישוט: גוף הטקסט עלה מ-17 ל-18 פיקסלים
לפי כלל הפרויקט, ו-`Noto Sans Arabic` נטען לצד Heebo — Heebo אינו
מכיל גליפים ערביים כלל, והממשק הערבי נפל עד היום לגופן ברירת המחדל
של המערכת. קבצי הפונט נמשכים רק כשבאמת מוצג טקסט ערבי.

הסמל בכרטיס יושב בשורה משלו ולא לצד הכותרת: לצידה הוא גזל ממנה 58
פיקסלים ושבר שמות ארוכים לשתי שורות.

אומת: 8 צירופים של שפה ומצב תצוגה — 9 סמלים בכל אחד, אפס שגיאות JS,
אפס עברית מחוץ לממשק העברי; ניגודיות הסמל על הרקע שלו 4.56 בבהיר
ו-6.46 בכהה, והפלטה כולה בין 4.91 ל-17.79; smoke נקי, 9 פקדים
הופעלו; eslint אפס. הלוגיקה לא נגעה — CSS, סמלי SVG, וגרסת
ה-service worker בלבד.

**שער התנאים החליף שפה ודף הבית לא הלך אחריו.** השער נפתח לפני
הכול, ויש בו בורר שפה משלו, מפני שמי שאינו קורא עברית נתקל בו
כקיר עוד לפני שהגיע להגדרות. תשע האפליקציות מאזינות ל-`legal:lang`
ומחליפות את עצמן — דף הבית, שהוא הדף הראשון שכל אחד רואה, לא היה
ברשימה. מי שבחר בשער אנגלית ואישר, נחת על דף בית בעברית מלאה.
נוסף מאזין, והשפה גם נשמרת ל-`localStorage` כמו בבחירה רגילה.

**מונוגרמת הלוגו הייתה "י" בכל ארבע השפות** — גם לצד Joshua וגם
לצד Иехошуа, שם אות עברית בודדת נראית כתקלת קידוד ולא כמיתוג. היא
נגזרת עכשיו מהאות הראשונה של השם באותה שפה: י / ي / И / J.

**"9" במסך "במספרים" היה מוקלד ביד** בעוד המונה שמתחתיו נגזר
מ-`APPS.length`. הוספת אפליקציה עשירית הייתה נותנת "9 אפליקציות
לימוד" מעל "1 מתוך 10". שני המספרים נגזרים היום מאותו מקור, וכך
גם מספר השפות מ-`LANGS`.

לצד אלה: שם נגיש לשתי קבוצות הכפתורים בסרגל (בורר השפה וסינון
התחומים) בארבע השפות — קורא מסך הכריז עליהן "עב, ع, Ру, EN" בלי
לומר מה הן; כתובת המייל שהייתה יעד מגע בגובה 20 פיקסלים; ו-manifest
שנשא `theme_color` ‏`#174c7e` משארית פלטה ישנה בעוד המותג הוא
`#2f5fd8` והרקע `#f6f7fb`.

## הפורטל 9

**"דיסלקציה" חוזרת — כמילת חיפוש בלבד.**

המילה ירדה מכל האפליקציות ומהפורטל, וזה נכון: זה כלי לימוד ולא
טיפול, ואסור לטעון אחרת. אבל היא ירדה גם משדה `k` של ״מקריא קולי״ —
שדה מילות החיפוש, שאינו מוצג לאיש ואינו טענה. התוצאה הייתה שהורה
שמחפש "דיסלקציה" בדף הבית מצא אפס תוצאות, וזה בדיוק הקהל שהאפליקציות
נבנו בשבילו.

היום החיפוש מחזיר תוצאה אחת, והמילה אינה מופיעה באף שם, תיאור או
מחרוזת ממשק — נבדק במפורש. יש הבדל בין להימצא לבין להבטיח.


## מצב הדגמה — הפורטל 8 · b38 · t38 · u34 · v34 · g33 · n31 · m34 · reader 18

## הפורטל 7 · math-app b37 · math-teen t37 · אקסיומה u33/v33/g32

**המילה ״דיסלקציה״ הוסרה מכל האפליקציות, בכל שפה.** היו לה 17
מופעים: תיאור המנוע של ״לחכמים שמתקשים״, תג הפתיחה שלה בארבע
השפות, שני תיאורי הגדרות (פונט ברור, מרווחים נדיבים) בארבע השפות,
מילת חיפוש בכרטיס של ״מקריא קולי״ בדף הבית, והערה ב-CSS שחוזרת
בשלב ובשלוש האקסיומות. במקומה מתואר מה האפליקציה עושה ולמי היא
עוזרת — ״למי שקשה לו לקרוא״, ״נוח יותר לקריאה״, ״קל יותר לעקוב
אחרי השורה״ — בלי לנקוב באבחנה. גם ״ADHD״ ו-״СДВГ״ שהופיעו לצידה
באותה שורה בתג הפתיחה האנגלי והרוסי ירדו איתה, כדי שארבע השפות
יאמרו את אותו הדבר. בדיקה חוזרת על כל הריפו, בכל צורת כתיב ובכל
אחת מארבע השפות, מחזירה אפס מופעים.

## reader 17

**ההסבר שמגיע אחרי טעות נאמר עכשיו גם בערבית.**
מדידה מול המתרגם של האפליקציה עצמה, על 65,000 שאלות שנבנו, הראתה
תמונה חדה: טקסט השאלה, ארבע האפשרויות ו-3,324 צעדי הפתרון המודרך
כולם ערבית מלאה — ודווקא ההסבר שהתלמיד מקבל ברגע שבו טעה, 465
מחרוזות, נשאר עברית ב-99% מהמקרים. זה הטקסט היחיד באפליקציה
שנכתב כדי להסביר טעות, והוא הגיע בשפה שהתלמיד לא בחר. נוספו 223
מפתחות — 157 הסברים שלמים ו-64 קטעים שמרכיבים את המשפטים
המשתנים — וכעת אפס מתוך 465.

**מפתח קיים שלא הוצב: התרגום היה שם וההחלפה החטיאה אותו.**
`trSeg` ו-`trHTML` חיפשו בטקסט הגולמי את המחרוזת אחרי שנורמלה
(רצף רווחים כווץ לרווח יחיד). במחרוזת כמו ‎"a : b   ·   חלוקה"‎
המחרוזת המנורמלת אינה תת־מחרוזת של המקור כלל, `replace` לא מצא
דבר, והשורה נשארה עברית — בלי שאיש ידווח על מפתח חסר, שהרי הוא
היה קיים. חמש שורות בדף הנוסחאות נראו כך. עכשיו הרווחים שמסביב
נשמרים והגרעין מוחלף במלואו.

**‎"ו-3"‎ נקרא בערבית כמינוס שלוש.**
המקף שמחבר תחילית עברית למספר נבלע במפצל כסימן מינוס, ובערבית
יצא ‎"و-3"‎ — כלומר מינוס. 1,025 מחרוזות הושפעו. כשהתו שלפני
המקף הוא אות עברית, הוא שייך לכתיב העברי בלבד ונשמט. במקביל
יושר הרווח לפני מספר מוזרק: צורות החיבור ‎بـ‎ ו-‎الـ‎ נשארות
צמודות כנדרש, וכל שאר המילים קיבלו רווח.

**‎`trMissing()`‎ החזיר תמיד אפס — הבדיקה הייתה מתה.**
הוא ספר מול `TR_SRC`, אובייקט שהוצהר ריק ומעולם לא התמלא.
`.claude/agents/four-language-checker.md` מורה במפורש להריץ אותו
ולדווח לפיו, ולכן כל מחרוזת חסרה עברה כ״תקין״. עכשיו הוא סופר
מול מה שבאמת הופיע על המסך ולא נמצא לו תרגום.

**עוד עברית שדלפה מתחת לממשק ערבי.** ההכרזה לקורא מסך אחרי כל
תשובה (״נכון״ / ״לא נכון״ והסבר) נמסרה בעברית, וכך גם ההסבר
שהוקרא בקול. מסך התקלה הכללי היה עברית קשיחה. ההנחיה שנשלחת
למנוע ההקראה בענן אמרה ״קרא בעברית״ מעל טקסט ערבי. הודעת התקלה
של מנוע הדיבור נבחרה בשורה ‎`TTSMSG.he`‎ עם ההערה ״האפליקציה
הזאת בעברית בלבד״ — נכונה לפני שנוספה ערבית, ומאז התעלמה מ-ar,
ru ו-en שכבר יושבים בטבלה. אותה שורה בדיוק תוקנה גם ב״מקריא
קולי״.

**קול במכשיר שאינו מדווח על שפה.** ‎`hebVoices`‎ סינן לפי שדה
‎`lang`‎ בלבד, וקול שמחזיר שדה ריק נעלם מהרשימה — האפליקציה
הכריזה ״אין קול בשפה הזאת״ על מכשיר שיש בו אחד. נוסף רמז לפי שם
הקול, כמו ב״אקסיומה״.

**דף הנוסחאות: ‎`<`‎ ו-‎`>`‎ גולמיים בתוך טקסט.** בשורת
הדיסקרימיננטה הם נכתבו כתווים ולא כישויות. הדפדפן מתאושש, אבל
מנוע התרגום עובר על טקסט שבין תגיות ולכן הפסיק לראות את מה שבא
אחרי ה-‎`<`‎, והמשפט האחרון נשאר עברי. הוחלפו ב-‎`&lt;`‎
ו-‎`&gt;`‎.

**מקריא קולי:** תווית ‎`aria-label`‎ של בורר השפה (״שפת הממשק״)
לא הייתה במילון הערבי — התווית היחידה שקורא מסך מקריא, ודווקא
היא נשארה עברית.

לאחר התיקונים: מעבר על כל מסך, כל נושא וכל רמה בערבית מחזיר אפס
שורות עברית ואפס מפתחות חסרים; 49,266 מחרוזות נבדקו מול המתרגם —
אפס עברית, אפס רווח כפול, אפס הדבקה שאינה צורת חיבור תקינה.
`entropy` ו-`smoke` ללא שינוי מקו הבסיס.

## english n30 · history m33 · math-app b36 · math-teen t36 · math-uni u32 · math-uni2 v32 · math-uni3 g31

**מבחן של אפליקציה אחת נפתח באפליקציה אחרת, והמסך התרסק.**

כל שבע האפליקציות שמרו את המבחנים תחת אותו מפתח, `shlav-exams-v1`.
מורה שבנה מבחן ב״שלב״ ופתח את ״אקסיומה״ ראה שם את אותו מבחן, ואז
`buildQ` קיבל נושא שאינו מוכר לאפליקציה, החזיר `null`, והמסך נפל
על `q.topic` של null.

לכל אפליקציה יש היום מפתח משלה, והמפתח המשותף הישן נשאר לקריאה
בלבד ואינו נמחק — מבחן שנבנה לפני השינוי עדיין נפתח. בנוסף, נושא
שאינו מוכר לאפליקציה מסונן בשקט במקום להפיל את המסך.

נוספו חמישה סוכני משנה (`euclid`, `gilad`, `maayan`, `nimrod`,
`renana`) וסוכן `design`, בלי לגעת בארבעה הקיימים, ו-`CLAUDE.md`
שמרכז את כללי הפרויקט ומוסכמת התקשורת.

**מילון הערבית של math-teen מוזג מפתח־מפתח מול הבסיס המשותף:**
1,013 מפתחות בבסיס, 815 שנוספו בצד אחד, 2 בצד השני, ואפס מפתחות
ששני הצדדים שינו. בלי מיזוג כזה, בחירת צד אחת הייתה מוחקת 815
תרגומים או 2 — כאן לא נמחק אף אחד. 1,830 מפתחות בסך הכול.

**גרסאות.** הענף סימן t30, n28, m31 ו-v30 — כולם מספרים שכבר היו
על main עם תוכן אחר, כי הענף נבנה מ-main ישן יותר. הוקצו מספרים
טריים מעל main.

אומת: 246,300 שאלות בשבע אפליקציות, 0 noAns · 0 multi · 0 dupe ·
0 NaN; smoke נקי בשבעה דפים; 18 צירופי אפליקציה־שפה, אפס עברית.

## history m33

**מאגר התוכן הוכפל, וארבעה נושאים חדשים נוספו.**

לכל נושא היו שנים־עשר פריטים — ארבעה בכל רמה, ומהם שלושה אירועים
בלבד ברמה 1. תלמיד שתרגל נושא ברצף פגש את אותו אירוע שוב אחרי
שלוש שאלות. היום יש שמונה פריטים בכל רמה, והמאגר עלה מ-144 פריטים
ל-384: 224 אירועים, 128 מושגים ו-32 קטעי מקור.

ארבעה נושאים חדשים סוגרים פערים בתוכנית הלימודים, אחד בכל מסלול:
מצרים ומסופוטמיה (העת העתיקה), יהודי אירופה בימי הביניים (ימי
הביניים), אמנציפציה ואנטישמיות (העת החדשה), והמלחמה הקרה ושחרור
המושבות (המאה ה-20). כל אחד נכנס במקומו הכרונולוגי בתוך המסלול,
ולכן מצרים מופיעה לפני יוון ולא בסוף הרשימה.

ההרחבה נעשית ב-`more(id, lv, items)` ולא בעריכת הקריאות הקיימות
ל-`topic()`. הסיבה אינה סגנון: ההתקדמות של מי שכבר למד נשמרת לפי
אינדקס הפריט בתוך הרמה, ופריט שהיה נדחף לאמצע המאגר היה מזיז כל
מה שנלמד למקום אחר. `more()` דוחף לסוף בלבד.

`GENV` עלה מ-2 ל-3. תוכן חדש משנה מה שאלה מזורעת מייצרת, וקישור
מבחן שנוצר בגרסה הקודמת היה מגיש עשרים שאלות אחרות בשקט. היום הוא
נפסל בהודעה ברורה, וזה בדיוק מה ש-GENV קיים בשבילו.

התוכן כתוב בארבע השפות, ולא רק הממשק. נבדק מבנית לפני השחבור:
שנה ייחודית בכל מאגר (`formFirst` פוסל שנה חוזרת), הגדרת מושג
ייחודית בכל האפליקציה (`formTerm` שואב מסיחים מכולן), שלושה מסיחים
בכל שאלת מקור, ואפס דליפת שפה בין ארבעת המילונים.

תיאורי ארבעת המסלולים עודכנו בארבע השפות, כדי שלא יבטיחו פחות
ממה שיש בהם.

אומת: 28,800 שאלות — 0 שאלות עם פחות מארבע אפשרויות, 0 noAns ·
0 multi · 0 dupe · 0 NaN, אפס רמות בעלות תשובה קבועה; smoke נקי;
eslint ללא ממצא חדש מעבר לשניים הרשומים כשפירים; 48 מתוך 48
צירופי נושא־רמה מייצרים שאלה תקינה בכל אחת מארבע השפות.

## history m32 · english n29 · math-teen t35 · math-uni u31 · math-uni2 v31 · math-uni3 g30

**לשונית הדפדפן נשארה בעברית בכל השפות.**

שש מתוך שבע האפליקציות לא נגעו ב-`document.title` מעולם. החלפת שפה
עדכנה את `html lang`, את הכיווניות, את הכותרת שעל המסך ואת כל
התוכן — אבל שם הלשונית, הסימנייה, ושם האפליקציה כשמתקינים אותה
למסך הבית נשארו במחרוזת העברית הקשיחה שב-HTML. משתמש שלמד בערבית
התקין אפליקציה ששמה עברי.

`math-app` כבר עשתה את זה נכון (`document.title=T().appName` בתוך
applyModes), וזה הדפוס שהועתק לשאר. ב-history וב-english השם
והתיאור כבר היו מתורגמים ורק לא נכתבו לכותרת; בארבע אפליקציות
המתמטיקה נוסף מפתח תרגום אחד לכל אחת, בסוף המילון בלבד ועם אינדקס
לפי `TR_KEYS.indexOf` ולא לפי אורך המערך.

הכותרות היום, לדוגמה ב-history: `مفنيه — التاريخ للمرحلة الإعدادية`,
`Мифне — История для средней школы`, `Mifne — History for middle school`.

בדרך נבדק גם מה שלא היה שבור, כדי לא לנחש: ההקראה בערבית עוברת
נכון ל-`ar-SA` (וברוסית ל-`ru-RU`), הכיווניות `rtl` נכונה, ותוכן
השאלות מתורגם במלואו — 288 שאלות ב-history, אפס עברית.

אומת: 190,500 שאלות בשש אפליקציות, 0 noAns · 0 multi · 0 dupe ·
0 NaN; smoke נקי בשישה דפים; 15 מסכים בכל שפה בכל אפליקציה, אפס
עברית; 1,627 מפתחות תרגום, 0 חסרים.

## דף הבית 6 · history m31 · math-uni2 v30

**ובדרך: שני footers בדף הבית.** הדף הציג את שורת הקרדיט פעמיים —
אחת מתורגמת, ומתחתיה שריד HTML סטטי בעברית. בעברית זו הייתה כפילות,
ובערבית ברוסית ובאנגלית גם עברית מתחת לטקסט המתורגם. השריד הוסר;
ה-footer שמתמלא מהמילון נשאר.

**גרסאות.** הענף סימן את history כ-m30 ואת math-uni2 כ-v29 — שני
מספרים שכבר היו על main עם תוכן אחר. מפתח מטמון אחד לשתי גרסאות
שונות מונע מהמשתמש לקבל את השנייה, ולכן הם m31 ו-v30.

## math-app b35

**הקול הנשי לא היה נשי — ובחלק מהמכשירים לא היה שם בכלל.**
מי שבחר ״נשי רגוע״ או ״נשי רגיל״ ברוב מכשירי אנדרואיד שמע בדיוק
את אותו קול כמו קודם. הקול העברי היחיד שם נקרא ״Google עברית״ —
שם של יצרן ולא שם פרטי — והאפליקציה לא ידעה לומר עליו אם הוא נשי
או גברי, ולכן לא נגעה בו. עכשיו, כשמבקשים קול נשי ובמכשיר אין
אחד, גובה הקול מורם בכל מקרה: לא רק כשהקול מזוהה כגברי, אלא גם
כשאינו מזוהה — כי ״לא ידוע״ אינו ״נשי״.

**ובכיוון ההפוך: הפריסט ״גברי״ נשמע מורם.**
הרמת הגובה הופעלה בלי לבדוק מה בכלל ביקשו, והוכפלה בגובה של כל
פריסט. מי שבחר דווקא קול גברי קיבל אותו מורם ב-35 אחוז, וגם
״איטי וברור״ הורם. עכשיו ההרמה קורית רק כשמבקשים נשי, וכל שאר
הבחירות נשמעות כפי שהן כתובות.

**קול נשי שהיה במכשיר ולא הופיע ברשימה.**
רשימת הקולות הראתה שישה, בסדר שבו המכשיר החזיר אותם — ובמכשיר עם
הרבה קולות הקול הנשי היחיד נפל מחוץ לשישייה, ומי שחיפש אותו הסיק
שאין כזה. עכשיו הרשימה מסודרת בדיוק כמו שהאפליקציה בוחרת: הקול
המתאים לבחירה שלכם ראשון, ומתחתיה כפתור שפותח את כל השאר.

**קול שנבחר בשפה אחת גבר על הבחירה בשפה אחרת.**
קול שנעצתם נשמר גם אחרי החלפת שפה: מי שבחר קול עברי ועבר לאנגלית
קיבל קול עברי שמבטא אנגלית, ומאותו רגע גם בחירת ״נשי״ לא שינתה
כלום — הנעיצה קדמה לה. עכשיו נעיצה שאינה בשפה הנוכחית חוזרת
לבחירה האוטומטית, וחוזרת אליכם כשחוזרים לשפה שבה נבחרה.

**וההודעה על הרמת הגובה נאמרת רק כשהיא נכונה.**
היא הופיעה גם כשהפריסט היה גברי, כלומר הסבירה הרמה שלא קרתה;
וכפתור ״השמע״ השמיע דוגמה בלי ההרמה, כלומר משהו אחר ממה שההקראה
עצמה משמיעה. שניהם תואמים עכשיו למה שקורה באמת.

**למה b35 ולא b34.** הענף הזה נבנה במקביל לתיקון שם החיה, ושניהם
סימנו את עצמם b34. מפתח מטמון אחד לשתי גרסאות שונות פירושו שמי
שקיבל אחת מהן לא היה מקבל את השנייה לעולם — בדיוק הבאג שהמנגנון
הזה נועד למנוע. שני התיקונים נמצאים יחד ב-b35.

## math-teen t34

**שאלה שהציגה שלוש אפשרויות במקום ארבע, וההסבר שנשאר בעברית.**

בשאלת הדיסקרימיננטה — "כמה פתרונות יש למשוואה?" — רשימת המסיחים
נבנתה משלושה ביטויים מותנים שהכילו תמיד כפילות. הכפילות נבלעה
בסינון, והתלמיד ראה שלוש אפשרויות בכל 600 ההגרלות של הרמה: ניחוש
אחד משלושה במקום אחד מארבעה. לשאלה הזאת יש בדיוק שלוש תשובות
בעולם, ולכן התיקון אינו רק להסיר את הכפילות אלא להוסיף מסיח רביעי
שהוא טעות אמיתית: "אינסוף פתרונות". ריבועית שבה a אינו אפס מתאפסת
לכל היותר בשתי נקודות, ולכן המסיח הזה בודק בדיוק את ההבנה הזאת.

**ההסבר אחרי טעות הופיע בעברית גם בערבית.** הפאנל "מה קרה כאן"
הכניס את הכותרת ואת נוסח ההסבר למסך גולמיים, בלי לעבור דרך `_()`.
ההכרזה לקורא המסך כן תורגמה, ולכן הפער לא נראה בשום בדיקת מילון —
תלמיד שלמד בערבית קיבל את ההסבר לטעות שלו בעברית, בדיוק ברגע שבו
הוא הכי צריך אותו. שניהם עוברים היום דרך `_()`, ומחרוזת שאינה
במילון נופלת חזרה לעברית כמו קודם ולא נשברת.

אחרי התיקון: `under4` ב-math-teen ירד מ-1.4% לאפס, 43,200 שאלות
נבדקו, ו-15 מסכים בערבית בלי מילה אחת בעברית.

## english n28 · math-app b34

**שני מדדי בדיקה ששיקרו, ושני באגים שהם הסתירו.**

`entropy.js` השווה אפשרויות לפי `textContent`, שמשטח מבנה: השבר
`−5/x` והמכפלה `−5x` נותנים אותה מחרוזת, וכך גם שתי מטריצות
שנבדלות רק בסידור העמודות. לכן `dupe` ב-math-uni היה 243, תועד
כ"חיובי כוזב", ומצב שבו יש כפילות אמיתית לא היה נראה שונה. אחרי
מעבר להשוואה מבנית שבה כל גבול אלמנט הוא מפריד: 159,000 שאלות
בשבע אפליקציות, אפס כפילויות. **קו הבסיס של המדד הזה הוא אפס
מעכשיו.**

`smoke.js` ספר את חסימת הרשת של עצמו כשגיאה. כרום רושם בקשה
שבוטלה כ-`net::ERR_FAILED`, ולכן כל דף שטוען גופן מגוגל — תשעה
מתוך אחד־עשר — דיווח `ERRORS` תמיד, ומספר הכפתורים שנלחצו הוסתר
מאחורי הדיווח הזה. שגיאה אמיתית הייתה נבלעת ברעש.

**english n28 — הבנאי שנדרס.** `function P(lines,words,qs)` שבונה
את שישה־עשר קטעי הקריאה נדרס בזמן ריצה על ידי `var P={q:...}` של
מצב המשחק. זה לא התפוצץ רק משום שכל הקריאות ל-P קורות בטעינה,
לפני הדריסה; נושא קריאה שייבנה בעצלתיים היה מקבל
`P is not a function`. eslint דיווח על זה, וקו הבסיס פטר את זה
כ"no-redeclare שפיר". הבנאי הוא `PARA` מאז, וקו הבסיס של eslint
רשום היום ב-README במלואו כטבלה — כל שורה שאינה בה היא ממצא חדש.

**math-app b34 — שם החיה שקפא בעברית.** `state.petName` נשמר פעם
אחת באונבורדינג, כמחרוזת מוכנה בשפה שהייתה פעילה אז. משפחה
שהתחילה בעברית ועברה לערבית ראתה `ملصقات ג׳ינג׳י`, בועת פתיחה
בעברית מתחת למסך ערבי, ובאנר שגיאת הקראה בעברית. שם שהמשתמש
הקליד הוא שלו ואינו זז; שם שהאפליקציה הציעה נפתר מחדש בכל הצגה
לפי השפה שעל המסך. `petAuto` מבדיל ביניהם, ושמירות קיימות מהוגרות
לפי התאמה להצעה באחת מארבע השפות. שמונה־עשר צירופי אפליקציה־שפה,
אפס עברית על המסך.

**מספר הרמות נלקח מהאפליקציה.** `entropy.js` קיבע `[1,2,3]`,
ולכן רמה שנוספה לא נבדקה אפילו פעם אחת — וב-math-app, שיש לו
שש רמות, שלוש הרמות הגבוהות מעולם לא נבדקו. הכיסוי שם הוכפל
בבת אחת. 184,500 שאלות בשבע אפליקציות, כל המדדים אפס.

## math-teen t33

**מיזוג.** שתי מערכות שינויים נבנו במקביל על אותו קובץ: תיקוני
ההקראה מצד אחד, ורמה רביעית, השלמת המסיחים והתרגום לערבית מצד
שני. אף אחת מהן לא הכילה את השנייה, ולכן שני מספרי הגרסה שקדמו
למיזוג אינם מתארים את הקובץ שנוצר ממנו.

מילון התרגום מוזג מפתח־מפתח מול הבסיס המשותף: 1,010 מפתחות
משותפים, 812 שנוספו בצד אחד, 3 בצד השני, ואפס מפתחות ששני
הצדדים שינו — כלומר לא הייתה כאן הכרעה בין שתי גרסאות של אותו
תרגום. בשורה היחידה ששני הצדדים שינו באמת — ההודעה שנאמרת אחרי
טעות — נשמרו שניהם: ההכרזה לקורא המסך מתורגמת, וההקראה עוברת
דרך speakProse שקורא לתרגום בעצמו.

## math-uni u29 · math-uni2 v28 · math-uni3 g28

**עוד עשרים רמות שהתשובה בהן הייתה קבועה.**
מדידה של כל נושא בכל רמה — 500 הגרלות לכל צירוף, וספירת התשובות
הנכונות השונות — מצאה עשרים רמות נוספות שבהן התשובה זהה ב-500
מתוך 500. הבולטת שבהן: בנגזרת ברמה 3 הפונקציה הייתה תמיד
‎x·ln x + c‎, והקבוע נעלם בגזירה — כלומר הדבר היחיד שהשתנה בשאלה
היה הדבר היחיד שאינו משפיע על התשובה, והתשובה ‎ln x + 1‎ חזרה
בכל שאלה. באינטגרל המסוים תמיד פונקציה אי־זוגית על תחום סימטרי,
ולכן תמיד 0. ברדיוס ההתכנסות תמיד ‎R = ∞‎. בהתכנסות במידה שווה
תמיד ״לא״.

עכשיו שני הכיוונים של כל כלל מופיעים: באינטגרל המסוים גם פונקציות
זוגיות, ואפס נעשה מסיח אמיתי; ברדיוס ההתכנסות ‎R = 0‎, ‎R = ∞‎
ו-‎R‎ סופי — וזה בדיוק מה שהרמה נועדה ללמד, אילו גורמים מזיזים את
הרדיוס. בשאלת התכנסות הטור ״לא ניתן להכריע״ הפך לתשובה נכונה
כשמבחן המנה אינו מכריע, מה שקודם לא קרה אף פעם.

**שתי תשובות נכונות באותה שאלה.** בשאלת הפוכה לוגית הופיע מסיח
‎¬p ∨ q‎, שהוא שקול לוגית לתשובה הנכונה. הוחלף ב-‎¬q ⇒ p‎, שאינו
שקול, וההסבר אומר למה.

**מסיח שהיה נכון בסתר.** בשאלות הערכים העצמיים איברי האלכסון של
המטריצה יצאו לפעמים הערכים העצמיים עצמם, ואז המסיח ״לקחתם את
האלכסון״ היה התשובה. טווח ההגרלה הודק.

כל תשובה בעשרים הרמות אומתה מחדש בדרך שנייה, ובנוסף נבדק שבכל
שאלה יש בדיוק אפשרות נכונה אחת — מסיח שנכון בסתר גרוע מתשובה
קבועה.

**הוראות התקנת קול נשי** הושוו לנוסח שכבר בשש האפליקציות האחרות,
כולל מסלול אנדרואיד של גוגל, זיהוי אייפד ומסלול מק — בארבע השפות.

## math-teen t32

**התלמיד הערבי ראה עברית בדיוק ברגע שטעה.**
566 מחרוזות באפליקציה לא היו מתורגמות, ורובן הסברי המסיחים —
״חיברתם במקום לחסר״, ״שכחתם לחלק במאה״ — כלומר הטקסט שנפתח בדיוק
כשהתשובה הייתה שגויה. זה הרגע שבו התלמיד הכי צריך להבין, וזה הרגע
שבו הוא קיבל שפה שאינו קורא. אליהם נוספו טקסטי הצעדים בפתרון
המודרך.

361 מפתחות חדשים במילון. אימות: מעבר על 199 מסכים בערבית — כל מסך
עליון, ארבע הרמות, רמזים, כל צעדי הפתרון, ולחיצה מכוונת על מסיח
שגוי כדי להוציא את ההסבר — **אפס שברי עברית**. לפני כן היו כאלה בכל
רמה.

**מה שנשאר, ובמפורש.** חמישה מקומות אינם ניתנים לסגירה בתוספת
למילון כי הטקסט נבנה סביב ערך משתנה: הודעת ״לא נמצאה נוסחה עם…״
שמשלבת את מה שהמשתמש חיפש, חלון קוד המורה שאינו עובר דרך מנגנון
התרגום כלל, והודעות שגיאה שמשלבות טקסט מהדפדפן. כולם דורשים שינוי
קוד ולא תרגום.

**ועוד דבר שנמדד ולא תוקן.** 6.5% מהמחרוזות מציגות בערבית מקף לפני
מספר — ‎«و-8»‎ במקום ‎«و 8»‎ — כי המקף העברי ב״ו-8״ שורד את התרגום.
זו התנהגות קיימת מזמן, לא חדשה. היא אינה יוצרת אי־בהירות אמיתית,
כי כל מינוס באפליקציה נכתב בסימן הטיפוגרפי ‎−‎ ולא במקף, אבל השניים
נבדלים רק באורך. התיקון הנכון תלוי בכללי הכתיב הערבי — אם המקף
נמחק או מוחלף ברווח, ובאיזה הקשר — ולכן הוא נשאר להכרעה ולא נוחש.

## math-teen t31

**שאלות שהציגו שלוש אפשרויות במקום ארבע.**
1,993 מתוך 43,200 שאלות, ב-32 שורות של נושא ורמה. לכל מחולל הייתה
רשימת מסיחים באורך שלוש בדיוק, וכשהגרלה הביאה שתי טעויות שונות
שנותנות את אותו מספר — מקדם 1 במשוואה, חמישים אחוז באחוזים, שורש
שהוא הנגדי של השני — הכפילות נשמטה בשקט והתלמיד ניחש אחד משלושה.
עכשיו 600, וכולן `quad` רמה 3, שבה יש שלוש תשובות אפשריות בסך הכול
ואין רביעית.

**ושתי תשובות נכונות באותה שאלה.**
בריבועית רמה 2, ב-28% מהשאלות: כשמקדם המוביל הוא 1, המסיח ״חילקתם
ב-2 במקום ב-2a״ **הוא** הפתרון הנכון. הוא שרד את סינון הכפילות רק
מפני שסדר השורשים יצא הפוך. אותה תקלה בקנה מידה קטן ברמה 1.

וברמה 4, ב-2.7% מהשאלות, אותו דבר משורש אחר: כששני השורשים נגדיים
יוצא מקדם אפס, המשוואה נכתבת ‎x² = 0x + 36‎ במקום ‎x² = 36‎, והמסיח
שהופך את שני הסימנים הוא בדיוק אותה קבוצת פתרונות. ההגרלה המנוונת
הוצאה — וממילא זו שאלה אחרת, שאין בה מה להעביר אגף, וזה כל מה
שהרמה מלמדת.

כל מסיח חדש הוא טעות שתלמיד באמת עושה, עם הסבר שנותן בדיוק את
המספר שלידו: ״שכחתם לחלק במאה״, ״חיברתם במקום לחסר כשבידדתם את b״,
״שכחתם את π״. ושלוש הגרלות מנוונות הוצאו במקום להמציא להן מסיח:
שני שברים זהים, מלבן ששטחו שווה להיקפו, ו-‎x = 0‎ במשוואה לינארית.

חוב התרגום לערבית ירד תוך כדי מ-628 מחרוזות ל-566.

## english n27 · history m30

**חצי מהשאלות ב״ניב״ נתנו ניחוש של אחד משלושה.**
13,053 מתוך 27,000 שאלות הציגו פחות מארבע אפשרויות. וזו לא הייתה
התנגשות אקראית בין מסיחים אלא החלטה שנכתבה בקוד: בשאלות ההקשבה
ובשאלות השימוש נבנו שלושה מסיחים ואז נחתכו לשניים. כל שאלת הקשבה
באפליקציה, מאז ומתמיד, הייתה אחד משלושה.

עכשיו 8.9%, וכל מה שנשאר הוא ארבע שורות של הבנת הנקרא — שם שלוש
אפשרויות אינן באג אלא תוכן: 44 שאלות ההבנה נכתבו ידנית, כל אחת עם
שני מסיחים כתובים בארבע שפות. להמציא להן אפשרות רביעית במכונה זו
החמרה, לא תיקון.

ב״מפנה״ אותה תקלה בשאלת ״מה קרה קודם?״: נלקחו שלושה מועמדים ורק
אחר כך הוסרו כפילויות שנה, כך שכל התנגשות עלתה באפשרות. עכשיו אפס
מתוך 21,600.

**ומה שנבדק ונזרק.** בשאלות ההשלמה, כשהמילה החסרה פותחת את המשפט,
התשובה הייתה האפשרות היחידה שמתחילה באות גדולה ב-270 מתוך 2,615
שאלות — כלומר אפשר היה לענות נכון בלי לקרוא. התיקון הראשון השווה את
האותיות הגדולות בכל האפשרויות, ואיפס את הרמז לחלוטין — אבל הכפיל
פי שלושה מקרים שבהם שתי אפשרויות נכונות באמת (״Could I have the
menu״ מול ״Can I have״). הוא נזרק. מה שנשאר רק ממעט את המקרה
שהמילה החסרה פותחת משפט: הרמז ירד מ-10.3% ל-1.7%, והדו־משמעות ירדה
גם היא מ-1.13% ל-0.49%. שני המספרים השתפרו, אף אחד לא על חשבון
השני.

## math-teen t29 · math-uni u30 · math-uni2 v29 · math-uni3 g29

**רמה רביעית בארבע האפליקציות שהיו להן שלוש.**
בשלוש הרמות הקיימות הניסוח אומר במה להשתמש: ״פתרו בעזרת נוסחת
השורשים״, ״פתרו בשיטת ההצבה״, ״פשטו את השורש״. ברמה הרביעית לא
אומרים. זו המיומנות היחידה שלא הייתה קודם — לזהות לבד מה השאלה
דורשת — והיא מה שמפיל במבחן.

ב״ששלב״ שמה **בגרות**, והיא שני שלבים שלא נאמר מאיפה מתחילים:
ירידה של 25% ואז עלייה של 25% ומה השינוי הכולל; ‎√50 + √18‎, שמי
שינסה לכנס מיד לא יגיע; מהירות ממוצעת בשני קטעים, עם המלכודת
שממוצע המהירויות אינו התשובה.

בשלוש אפליקציות ״אקסיומה״ שמה **נימוק**, והיא שואלת אם מותר ולמה
ולא כמה. איזה מהמועמדים הוא פונקציה קדומה — אפשר לענות רק בגזירה
של כל אחד. האם מותר להפעיל את ניוטון־לייבניץ, כשבחצי מההגרלות יש
קוטב בתוך הקטע וההצבה הפורמלית מחזירה מספר שלילי לפונקציה חיובית.
מה קורה כשההסיאן נותן אפס, כלומר כשהכלי עצמו נשבר. והאם ההפך של
משפט לגראנז׳ נכון — שם ל-A₄ מסדר 12 אין תת־חבורה מסדר 6.

ההפרש נמדד ולא הוצהר. באקסיומה א׳ שיעור השאלות שמנסחות במפורש
באיזה כלי להשתמש יורד 65% → 48% → 35% → **אפס**, וניסוח ״איזה,
האם, מדוע״ עולה מאפס ל-57%. שיעור התשובות שהן מספר יורד מ-70%
ל-22%, ואורך האפשרות עולה פי ארבעה. מספר הכללים מדף הנוסחאות
שנדרשים ב״ששלב״ עולה מ-1.3 ל-2.6.

**באגים קיימים שהתגלו בדרך.** בגבולות באקסיומה א׳ הוצגו חמש
אפשרויות ב-337 מתוך 500 שאלות ברמה 1 וב-493 מתוך 500 ברמה 2 —
והחמישית קיבלה תווית ריקה ולא הייתה נגישה מהמקלדת. וב״ששלב״ מנגנון
התרגום לערבית חיפש את מפתח המילון בטקסט הגולמי במקום במכווץ, ולכן
כל מחרוזת עם רווח כפול נכשלה **בשקט** ונשארה עברית בממשק ערבי; כרטיס
הדיסקרימיננטה נחתך באמצע כי הסימנים ‎<‎ ו-‎>‎ שברו את הביטוי הרגולרי,
והוצג חציו בערבית וחציו בעברית.

**והמשוב עצמו היה בעברית.** ״נכון״, ״לא נכון״ ו״מה קרה כאן״ מופיעים
אחרי כל שאלה, בכל רמה, ולא עברו תרגום כלל — ההכרזה לקורא המסך אף
לא נשלחה למילון. תלמיד ערבי ראה אותם בעברית בכל תשובה. תוקן.

**מה שנשאר פתוח, ובמפורש:** ב״ששלב״ 588 מחרוזות — הסברי מסיחים
וטקסטי צעדים — עדיין אינן מתורגמות לערבית. זה פער קיים ולא רגרסיה,
והוא זהה בכל ארבע הרמות.

## math-teen t28

**ההקראה קוראת עכשיו גם את הרמז, את הסבר הטעות ואת ניסוח השאלה.**
עד היום ההקראה ידעה לומר את התרגיל ואת התשובות, אבל דווקא הטקסטים
שנועדו לעזור — הרמז, ההסבר שמגיע אחרי טעות, ולשון השאלה עצמה —
נמסרו למנוע כרצף סימנים. מי שנשען על ההקראה שמע קשקוש בדיוק ברגע
שבו נתקע. עכשיו גם הם נקראים כמילים, ונוספו הסימנים אחוז, חץ,
פלוס־מינוס, שונה, קטן או שווה וגדול או שווה — בעברית ובערבית.

**שני קולות לא מדברים יותר ביחד, ואפשר להשתיק.**
מי שלחץ הקראה ואז בחר תשובה שמע לפעמים את השאלה חוזרת מעל המשוב —
שני קולות באותו רגע, ואי אפשר להבין אף אחד מהם. זה נפתר. בזמן
שההקראה רצה הכפתור מתחלף ל״עצירת ההקראה״, ולחיצה עליו משתיקה מיד.

**במבחן, ההקראה אומרת גם את ארבע התשובות.**
בתרגול היא תמיד אמרה אותן; במבחן היא עצרה אחרי השאלה והשאירה את
התלמיד מול ארבע תשובות שלא הוקראו. תלמיד שקיבל התאמת הקראה קיבל
אותה בפועל רק בתרגול. עכשיו גם במבחן, ובאותו סדר שבו התשובות
מופיעות על המסך. בדרך התברר ש״משוואה ראשונה״ ו״משוואה שנייה״ לא
היו במילון הערבי כלל, ונאמרו בעברית — הן נוספו.

## math-uni u28 · math-uni2 v27 · math-uni3 g27

**רמות שהתשובה בהן הייתה קבועה.**
שבע רמות בשלוש אפליקציות האקסיומה נבנו כך שהתשובה הנכונה יצאה
תמיד אותה תשובה, ולכן אפשר היה לענות עליהן נכון בלי לדעת דבר:
במקדם המתאם ρ יצא תמיד 1, כי הקווריאנס נבנה כמכפלת סטיות התקן;
בתלות לינארית הווקטור השני היה תמיד כפולה של הראשון, כלומר
״תלויים״ תמיד; באורתוגונליות v היה תמיד המאונך של u; באינטגרל
לא אמיתי p היה תמיד גדול או שווה ל-1, כך שהתשובה תמיד ״מתבדר״
והכלל שהרמה מלמדת לא הודגם אף פעם; ובמשפט לגראנז׳ הסדר שנבדק
מעולם לא חילק את סדר החבורה. תוקנו — שני הכיוונים מוצגים עכשיו.

**אימות מתמטי בלתי תלוי.**
כל תשובה נבדקה בדרך שנייה ולא רק בקריאה: נגזרת נומרית מול הנגזרת
המוצהרת, גזירה של הפונקציה הקדומה מול האינטגרנד, קוודרטורת
סימפסון מול האינטגרל המסוים, גבול נומרי מול הגבול המוצהר, ו-f′=0
בנקודות הקיצון. 31,500 שאלות חדו״א ועוד 72,000 שאלות לינארית,
הסתברות ובדידה — **אפס תשובות שגויות**.

**הקראה ששיבשה מתמטיקה.** שבר שלם שלילי הוחזר עם מקף ASCII במקום
המינוס הטיפוגרפי, ומנוע ההקראה ממיר רק את השני — ולכן ‎−3‎ הוקרא
״שלוש״. ובטור ‎1/n^½‎ המבנה המקונן פורק ל-‎n^12‎, כלומר ״n בחזקת
שתים-עשרה״, ותשובת ״מתבדר״ נשמעה שגויה.

**מקדמים שנכתבו כמו טעות דפוס** — ‎1x⁶‎, ‎1sin x‎, ‎1ln x‎, ‎1e^x‎
ו-‎x^1‎ מפורש. תוקן בשישה מחוללים. באותה הזדמנות בוטלו מקדמים
שהפכו את המסיח ״שכחתם את הנגזרת הפנימית״ לתשובה הנכונה עצמה.

**שאלות שהציגו שלוש אפשרויות במקום ארבע** — 41% מהאינטגרל המסוים
ברמה 1, 49% ברמה 2, 34% מהבינומית ו-33% מהלוגריתמים. לולאת
ההגרלה החוזרת שכבר קיימת באקסיומה ב׳ ו-ג׳ הוחלה גם על א׳.

**112 נוסחאות בשלושת דפי הנוסחאות נסקרו אחת-אחת** — כולן נכונות;
שני ליקויי תצוגה במעריכים תוקנו בארבע השפות.

## english n26 · history m29 · math-app b33 · math-teen t26 · reader 14

**מבחנים כיתתיים ב״ניב״ שנשלחו לתלמידים בלי תשובות לסמן.**
שלוש תקלות נפרדות שכולן הגיעו לתלמיד ולא נראו אצל המורה:
תרגיל סידור המשפט נבנה מאריחים ואין לו אפשרויות בחירה — כשליש
משאלות המשפטים ברמות 2 ו-3 הגיעו בלי שום תשובה לסמן. שאלת ״מה
המשמעות״ הציגה את המשמעות עצמה בגוף השאלה, בזמן שהאפשרויות היו
המשמעויות. ושאלת הבנת הנקרא נבדקה לפי סוג הפריט בבנק ולא לפי סוג
השאלה, ולכן הקטע לא צורף מעולם: התלמיד ראה ״לפי הקטע:״ וארבע
אפשרויות, בלי קטע.

אחרי התיקון, 7,200 שאלות מבחן ב״ניב״ ו-5,760 ב״מפנה״, בכל ארבע
שפות הממשק: אפס בלי אפשרויות, אפס עם שתי תשובות נכונות, אפס בלי
תשובה נכונה, אפס אפשרויות כפולות, אפס שאלת קריאה בלי קטע.

**שאלות שנשאו את תשובתן בגוף השאלה.**
ברמז של תרגיל ״מה חסר במשפט״ הופיעה לפעמים המילה שנגרעה — גם
כמילה שלמה וגם בתוך מילה אחרת: ״first meeting״ מול תשובה meet,
ו״What is missing?״ מול תשובה What. וב״מפנה״, שאלת ״על איזה אירוע
מדובר?״ ברמה הקשה ביותר תיארה את קנוסה במילים ״לפני טירת קנוסה״.
13 מקרים ב״ניב״ ו-11 ב״מפנה״ — אפס בשניהם.

**קריסה ביציאה מתרגול ב״ניב״.**
בשאלת האזנה יוצאת הקראה אוטומטית רבע שנייה אחרי בניית השאלה.
היציאה למסך המסלולים בתוך החלון הזה אפסה את השאלה, וההקראה קראה
ממנה — TypeError והמסך נתקע. שוחזר ב-3 מתוך 6 ניסיונות, ואחרי
התיקון 0 מתוך 6.

**שתי שגיאות עובדתיות ב״מפנה״.**
המנדט על ארץ ישראל נמסר לבריטניה בוועידת סן רמו, וחבר הלאומים
אישר אותו ב-1922 — לא ב-1920. ותוצאות מלחמת ששת הימים נאמר בהן
שישראל שולטת מאז ״ביהודה, בשומרון וברצועה״ — לא נכון מאז 2005.

**רמה שהייתה מדורגת בשם בלבד.**
רמה 3 ב״ניב״ מוגדרת ״אנגלית בלבד״, אבל שני סוגי תרגיל עדיין הציגו
את המשמעות בשפת האם — כך ששאלה ברמה 3 על מילה נתונה הייתה זהה
לשאלה עליה ברמה 1. עכשיו המשמעות באנגלית, והתרגום מופיע במשוב
אחרי התשובה כדי שאיש לא ייתקע.

**הקורא — מונה משפטים, דילוג שהשתיק, וניווט מת.**
המונה ספר את המשפט הריק שאחרי הנקודה האחרונה: שלושה משפטים הוצגו
כ״1 מתוך 4״ ופס ההתקדמות נעצר על 75%. אומת על חמישה טקסטים —
4 מתוך 5 היו שגויים, עכשיו 5 מתוך 5 נכונים. לחיצה על ⏭ השתיקה את
הקורא לגמרי, כי ביטול האמירה הקודמת דווח אחרי שהחדשה כבר יצאה
והרג אותה. ו״משפט קודם״ נשאר מושבת אחרי מעבר אוטומטי בין משפטים.

**הוראות ההתקנה של קול נשי היו שגויות לחצי מהמכשירים.**
מסלול אנדרואיד היה של סמסונג בלבד — באנדרואיד של גוגל ההגדרה
יושבת תחת ״נגישות״; אייפד מדווח מאז iPadOS 13 כמו מק וקיבל הוראות
של ווינדוס; ומשתמש מק קיבל הוראות של ווינדוס תמיד, כי כל מה שאינו
אנדרואיד או אייפון נפל לשם. תוקן בכל האפליקציות ובכל ארבע השפות,
ואומת מול חמש מערכות הפעלה.

**דף בדיקת הקול** הכריז ״ההקראה לא התחילה״ אחרי לחיצה על ״עצירה״,
כי ביטול נספר ככישלון, ולא חזר בו אחרי שההקראה הצליחה.

## math-teen t25 · math-app b32

**פרק השברים — שאלות שאיבדו אפשרות, ופתרונות שדיברו על מספרים אחרים.**
בדיקה חישובית של 108,000 שאלות חשפה שבמחוללי השברים כמה מסיחים היו
שווים לתשובה הנכונה או זה לזה. השאלה עדיין הציגה תשובה נכונה אחת —
המסיח הכפול פשוט נשמט — ולכן במקום ארבע אפשרויות הוצגו שלוש: ניחוש
של אחד לשלושה במקום אחד לארבעה. במשוואה עם שבר וביחס ופרופורציה זה
קרה **בכל שאלה**.

בכיוון השני, בחלק מהשאלות נוצרו חמש אפשרויות. מערך התוויות באורך
ארבע, והמקלדת מקצה 1–4 — כך שהאפשרות החמישית קיבלה תווית ‎undefined‎
ולא הייתה נגישה מהמקלדת כלל. קרה ב-6,028 מתוך 108,000 שאלות.

**ופתרונות שלא תאמו לשאלה שעל המסך.** ברמה 1 ״המכנה המשותף הקטן
ביותר״ חושב מהמכנים שלפני הצמצום: ‎1/2 − 1/5‎ הוצג, והשלב הכריז
שהמכנה המשותף הוא 30. ברמה 2 כל ההסבר דיבר על השברים שלפני הצמצום.
ילד שעוקב אחרי הפתרון ראה מספרים שלא כתובים בשאלה.

אחרי התיקון, אותן 108,000 שאלות ב-18 נושאים: אפס שאלות בלי תשובה
נכונה, אפס עם שתי תשובות נכונות, אפס אפשרויות כפולות, אפס NaN, ואף
שאלה עם יותר מארבע אפשרויות.

**עוד עשר שגיאות תוכן שנמצאו באותה סריקה** — ובהן ״מטילים קובייה
בעלת שלוש פאות״ בהסתברות רמה 3, והסבר בהסתברות שסתר את המספר שלידו.

**מקום התשובה הנכונה ב״מתמטיקה לחכמים שמתקשים״.**
הערבוב היה ‎sort‎ עם משווה אקראי — משווה לא עקבי, ולכן הפיזור לא היה
אחיד: התשובה נחתה ראשונה ב-36% מהשאלות ואחרונה ב-31%, לעומת 16%
באמצע. ילד לומד ״ראשון או אחרון״ הרבה לפני שהוא לומד לחשב. עכשיו
25.15% / 24.94% / 24.88% / 25.03% על פני 48,000 שאלות.

**כותרת שנוחשה לפי מחרוזת אחרת.**
כותרת מקטע התצוגה בהגדרות נבחרה בבדיקה של ערך מפתח אחר, ולכן
משתמשי ערבית ורוסית ראו אותה באנגלית. נוסף מפתח משלה בארבע השפות.

## english n25 · history m28 · math-app b31 · reader 13

**קול נשי בכל האפליקציות, לא רק ברובן.**
ההעדפה לקול נשי הייתה קיימת בכל שמונה האפליקציות, אבל בשלוש מהן
היא נוסחה כבונוס בתוך ניקוד כללי — ובונוס של עשר נקודות נבלע מול
שלושים על שם יצרן. התוצאה: במכשיר שיש בו גם קול גברי ממותג וגם
קול נשי, ״ניב״ ו״מפנה״ בחרו את הגברי, ו״מתמטיקה לחכמים שמתקשים״
בחרה קול שמגדרו אינו ידוע על פני קול נשי מזוהה. ארבע האפליקציות
האחרות בחרו נשי באותו מכשיר עצמו.

עכשיו המגדר הוא המיון הראשון ולא סעיף בניקוד: קודם הקולות במגדר
המבוקש, אחריהם מי שלא זוהה, ורק אז ההפוך — ובתוך כל קבוצה מכריעה
האיכות, כמו קודם. בחירה ידנית של קול ממשיכה לגבור על הכול.

**וכשבאמת אין קול נשי במכשיר — נאמר למה.**
ווינדוס עם Asaf בלבד הוא המקרה הנפוץ בעברית. שם גובה הקול מורם,
וזו הדרך היחידה בדפדפן להזיז קול גברי לכיוון הנשי בלי הקלטות
ובלי שירות חיצוני. ההסבר הזה, יחד עם ההוראות להתקנת קול נשי
במערכת ההפעלה שבה המשתמש נמצא, היה קיים רק בארבע אפליקציות —
עכשיו הוא בכל שמונה, בכל שפות הממשק. מי שמתקין קול נשי מקבל אותו
מיד, בלי שינוי הגובה ובלי ההודעה.
## math-teen t25 · reader 13

**״שלב״ ו״מקריא קולי״ בערבית.**
שתי האפליקציות היחידות שנשארו בעברית בלבד מדברות עכשיו גם ערבית.
שתי השפות מימין לשמאל, ולכן הפריסה לא זזה — רק הטקסט מתחלף.

**במקריא הקולי מתחלף רק מה שמסביב.** הטקסט שמדביקים נשאר עברי,
ההקראה נשארת בעברית, והניקוד עובד כרגיל — זה כל תפקיד הכלי. מה
שהתרגם הוא הכפתורים, ההסברים וההודעות, כדי שמי שאינו קורא עברית
בשטף יבין איך להפעיל את הכלי שיקריא לו עברית.

**ב״שלב״ תורגם הכול:** 1,010 מחרוזות — שמות הנושאים, השאלות,
הפתרונות המודרכים, אבחון הטעויות, דף הנוסחאות, מסך ההתקדמות,
המבחן הכיתתי ומצב המורה. גם ההקראה: יש טבלת הגייה ערבית משלה,
ולכן תרגיל נאמר ״ثلاثة في أربعة״ ולא במילים עבריות בקול ערבי.

**איך זה בנוי, ולמה כך.** הדרך המתבקשת — לעטוף כל מחרוזת עברית
בקוד — נוסתה ונזנחה: חלק מהמחרוזות אינן טקסט אלא מזהים. ״יסודות״
הוא גם שם תחום וגם מפתח בטבלת צבעים, ותרגומו שבר את החיפוש בשקט.
לכן העברית נשארת בקוד, והתרגום קורה על המסך המוכן רגע לפני
שהוא מוצג. שאלה שנבנית עם מספרים אקראיים נחתכת על המספרים
ומתורגמת שבר־שבר.

**באג שנמצא בדרך:** נתיב התרגום של תכונות — aria-label ו-title —
לא כלל את הפיצול על מספרים, ולכן תווית לקורא מסך נשארה עברית
מתחת לממשק ערבי. בדיוק אצל מי שתלוי בה.

**נבדק:** סריקה של 18 נושאים בשלוש רמות, כל המסכים, מחזור מבחן
מלא ומצב מורה — אפס מחרוזות עבריות שנותרו בממשק הערבי. העברית
לא השתנתה.

## math-uni u27 · math-uni2 v26 · math-uni3 g26 · math-teen t24 · math-app b30 · english n24 · history m27 · reader 12

**קוד מורה — הסתרת בונה המבחן ומסך הציונים.**
על טאבלט כיתתי, או על טלפון שהילד לוקח לעצמו, בונה המבחן ומסך
הציונים היו במרחק הקשה אחת: אפשר היה למחוק מבחן, לראות ציונים של
חברים, או לפתוח מבחן ולראות מראש את השאלות. עכשיו אפשר לבחור קוד
בן 4 עד 8 ספרות, ואז שני המסכים נעלמים מהתפריט.

לפתיחה: מוסיפים ‎?teacher=1‎ לכתובת, או לוחצים בהגדרות. אפשר לבקש
שהמכשיר יזכור, ואפשר לצאת ממצב מורה בלחיצה. אם האפליקציה הייתה
מוסתרת יותר משתי דקות — היא ננעלת מעצמה, כי זה בדיוק הרגע שבו
הטאבלט עובר יד.

**זו אינה אבטחה, וכתוב כך גם במסך עצמו:** מי שיודע לפתוח כלי פיתוח
יעקוף אותה. זו נעילה מפני יד סקרנית. הקוד עצמו לא נשמר — נשמר רק
גיבוב שלו, ואין דרך לשחזר אותו. כל עוד לא בחרתם קוד, שום דבר לא
משתנה. הקישור שהתלמיד מקבל ממשיך לעבוד גם כשהמסכים נעולים.

**הדגשת המילה הנאמרת ב״מתמטיקה לחכמים שמתקשים״.**
בשאר האפליקציות המילה שנקראת בקול מודגשת, וכאן לא היה שום סימון.
הקושי היה שהטקסט שנאמר אינו הטקסט שמוצג: ״7 + 3״ נאמר ״שבע ועוד
שלוש״. עכשיו כל מילה מתורגמת בנפרד, וכך כשנשמע ״שבע״ — הספרה 7
היא זו שנדלקת על המסך.

**הדגשה שלא נראתה כלל.**
כשמנוע הדיבור אינו מדווח על גבולות מילים — קורה הרבה בעברית
באנדרואיד — האפליקציות ניסו להדגיש את כל המשפט, אבל הכלל העיצובי
לכך פשוט לא היה קיים. מי שהמנוע שלו שקט קיבל טקסט בלי שום סימון.

**הודעת תקלה שמחקה את ההדגשה.**
ביטול אמירה כדי לפנות מקום לבאה אחריה דיווח ״canceled״, וההודעה
הזאת ניקתה את ההדגשה של המשפט החדש מיד אחרי שהוכן.

**קול שנעלם בלי סימן.**
ב״מתמטיקה לחכמים שמתקשים״, בחירת קול שהדפדפן דחה ביטלה את כל
האמירה והאפליקציה השתתקה בלי שגיאה. עכשיו נאמר בכל מקרה, בשפה
הנכונה.

## math-uni u26 · math-uni2 v25 · math-uni3 g25 · math-teen t23 · math-app b29 · english n23 · history m26

**הורדת CSV של ציוני המבחן.**
עד היום היה אפשר להעתיק את הטבלה כטקסט. עכשיו יש כפתור שמוריד
קובץ CSV אמיתי שנפתח באקסל או בגיליון של גוגל: שורה לכל תלמיד,
ועמודה לכל שאלה — כדי לראות מיד באיזו שאלה נפלה כל הכיתה. הקובץ
נפתח עם סימן BOM, ולכן עברית, ערבית ורוסית נקראות בו נכון ולא
כג׳יבריש. פסיק, מרכאות או שורה חדשה בתוך שם של תלמיד לא שוברים
את הקובץ.

שם הקובץ מתועתק לאותיות לטיניות, כי כרום מוחק שם קובץ שיש בו תו
אחד שאינו ASCII ומוריד אותו בשם ״download״ בלי סיומת — קובץ
שהמחשב לא יודע לפתוח. מבחן בשם ״מבחן אלגברה — כיתה ט׳2״ יורד
עכשיו בשם mbchn algbrh kyth t 2 2026-08-29.csv.

## math-teen t22 · english n22 · history m25

**שלושה אייקונים בשלב לא צוירו כלל.**
שלושת האייקונים ״שיתוף״, ״העתקה״ ו״הוספה״ הוגדרו כסימון SVG מלא
בזמן שכל השאר הם מחרוזת נתיב, והפונקציה שעטפה אותם ייצרה נתיב
פסול. הכפתורים הופיעו בלי אייקון והדפדפן רשם שגיאה בכל ציור.

**קיצורי מקלדת בניב ובמפנה.**
באפליקציות המתמטיקה אפשר לענות עם 1–4, לשמוע שוב עם רווח ולעבור
עם Enter. בשתי האפליקציות האלה זה לא היה קיים, ומי שעובד עם מקלדת
בלבד היה תלוי בלחיצה מדויקת. עכשיו הן מתנהגות כמו כולן.

## math-uni u25 · math-uni2 v24 · math-uni3 g24 · math-teen t21 · math-app b28 · english n21 · history m24 · reader 11

**מסך התנאים נפתח עכשיו בשפה שלכם.**
מסך תנאי השימוש חוסם את האפליקציה עוד לפני שאפשר להגיע להגדרות,
והוא הופיע בעברית בלבד. מי שלא קורא עברית נתקל בקיר בלי דרך מעבר.
עכשיו יש בורר שפה בתוך המסך עצמו, והבחירה בו מחליפה גם את שפת
האפליקציה כולה.

**הכותרת התחתונה וקישור הדילוג מתורגמים.**
״תנאי שימוש וזכויות יוצרים״ ו״דילוג לתוכן״ נשארו עבריים בכל שפה,
כי הם יושבים מחוץ למסך המצויר. עכשיו הם מתחלפים עם שאר הממשק.

**הודעת תקלת הקול מתורגמת.**
ההודעה שמופיעה כשמנוע הדיבור של המכשיר נכשל הייתה בעברית בכל
אפליקציה ובכל שפה. עכשיו היא בארבע השפות, עם כיווניות נכונה.

**מסך ההגדרות של האפליקציות לתואר.**
״בדיקת קול״ והוראות התקנת קול נשי לאנדרואיד, לאייפון ולווינדוס
נוספו למילון ומתורגמות.

**מתמטיקה לחכמים שמתקשים — שני תיקונים.**
תיאורי הקולות בהגדרות הופיעו בעברית כשהממשק היה באנגלית, בגלל
מפתח שלא תאם. שמות המתחרים בלוח השיאים היו עבריים בכל שפה, וילד
שקורא ערבית או רוסית ראה מסך שלא נכתב בשבילו.

**נבדק:** כל מסך בכל אחת מארבע השפות בשש האפליקציות הרב־לשוניות —
אפס מחרוזות עבריות שנשארו. עבודה לא־מקוונת נבדקה בתשע האפליקציות.
מחזור הלמידה ומחזור המבחן הכיתתי נבדקו מקצה לקצה. התנהגות ההקראה
לא השתנתה.
