# אקסיומה ב׳ — שנה שנייה

מתמטיקה של שנה ב׳ בתואר ראשון — ההסבר מלא בארבע שפות, ולכל
מסיח כתוב למה הוא שגוי.

**מזהה:** `math-uni2` · **שלב:** `public` · **קובץ:** `index.html`, 12,736 שורות

## התוכן, כפי שנמדד

| | |
|---|---|
| שאלות שנבדקו | 10,800 |
| נושאים × רמות | 18 × 4 = 72 תאים |
| שפות | עברית, ערבית, רוסית, אנגלית |
| פסק דין | **PASS** |

מקור: `.claude/qa/reports/math-uni2.json`.

## איך נבנית שאלה — מחולל, לא מאגר

השאלה **מחושבת בזמן ריצה** מפרמטרים שהוגרלו; התשובה אינה מאוחסנת
בשום מקום, והווריאציה אינסופית. זה מה שמצדיק אפליקציה נפרדת ולא
נושא ב-`lomda` — הקריטריון ב-`CLAUDE.md`, ״מה מצדיק אפליקציה״.

הסכימה: `{ask, expr, ans, wrong:[W(טקסט, למה)], steps:[S(כותרת,
הסבר, נוסחה)], hint}`. לכל מסיח יש **למה** הוא שגוי, ולכל שאלה
שלבי פתרון — זה מה שהופך טעות לשיעור.

## תרגום — המחרוזת העברית היא המפתח

כל מחרוזת שנראית ללומד עוברת דרך מילון שבו **המחרוזת העברית עצמה
היא המפתח**. שינוי מילה בעברית בלי לעדכן את המפתח מוחק את התרגום
**בשקט, בלי שגיאה**. משנים את שניהם יחד.

## מצב מורה

המורה בונה מבחן, והקישור אורז **זרע** — לא עשרים שאלות. התלמיד
בונה מהזרע את אותן שאלות אצלו. לכן כל דבר שנכנס לבחירת השאלה
חייב להיות זהה בשני הצדדים, ו-`buildQ` מעביר תמיד `pure=true`
כדי שהחזרה המרווחת (שמסתכלת על היסטוריית הלמידה) לא תשנה למורה
ולתלמיד שאלות שונות. נמדד ב-6.9: לפני התיקון 39 שאלות מתוך 200
יצאו שונות. `node .claude/qa/exam.js` שומר על זה.

## מפתחות `localStorage`

| מפתח | מה |
|---|---|
| `axiom2-state-v1` | התקדמות הלומד |
| `shlav-exams-math-uni2-v1` | מבחנים שנבנו כאן |
| `shlav-exams-v1` | הישן, לקריאה בלבד |
| `axiom2-teacher` | מצב מורה |

**כל אפליקציה ומפתח משלה** — כולן יושבות באותו מקור, ולכן
מפתח שהועתק הוא מגירה משותפת. `node .claude/qa/storage.js` תופס זאת.

## בדיקות שנוגעות לה

    node .claude/qa/serve.js          # שרת מקומי, 127.0.0.1:8099
    node .claude/qa/smoke.js math-uni2   # דפדפן אמיתי — שגיאות JS
    node .claude/qa/cache.js          # גרסה מול מפתח קאש
    node .claude/qa/storage.js        # מפתח localStorage אחד לאפליקציה
    node .claude/qa/all.js            # הכול, ופסק דין אחד

## PWA — מה שהופך אותה לאפליקציה

`manifest.json` (‎3‎ אייקונים, `display: standalone`, `lang: he`, `dir: rtl`)
ו-`sw.js` שמצרף מראש **5 נכסים**. אחרי טעינה ראשונה היא עובדת
בלי אינטרנט, ואפשר להתקין אותה על מסך הבית.

**המטמון שלה הוא `math-uni2-` ושלה בלבד.** שנים־עשר קובצי ה-`sw.js`
מצרפים את אותם `/legal/terms.js` ו-`/legal/protect.js`, כל אחד
למטמון משלו — ולכן נגיעה ב-`legal/` מחייבת להעלות את **כל**
המפתחות יחד. ההסבר המלא ב-`CLAUDE.md`, ״מלכודת הקאש של `legal/`״.

## גרסה — שתי מחרוזות, לא אחת

    var BUILD="v70 · 2026-09-10";
    navigator.serviceWorker.register("sw.js?v=v70-pwa1");

`sw.js` גוזר את שם המטמון מ-`?v=` שבכתובת הרישום, **לא** מ-`BUILD`.
עדכון של אחד בלי השני משאיר את הלומד עם הקוד הישן.
`node .claude/qa/cache.js` נופל על אי־התאמה, וגם על מפתח שכבר
קיים ב-`origin/main` עם תוכן אחר.

## ״עזרה מהמורה״ (ג׳וש)

מחווטת בארבע נקודות: תגית `<script src="/tutor/tutor.js">`,
`TUTOR.mount(...)`, קריאת `TUTOR.open()` מהנתב, והנתיב ב-`PRE`
שב-`sw.js`. **הכפתור אינו מוצג** כל עוד `API` ריק ב-`tutor/tutor.js`;
ההפעלה היא `node .claude/qa/enable-tutor.js <כתובת>`. הצד השרתי
כולו ב-`tutor-api/worker.js`, והתפקיד של האפליקציה הזאת נמצא שם
ב-`ROLE`.

## מה אין כאן

אין `npm install`, אין build step, אין framework ואין תלות חיצונית.


---

*נוצר מקריאה בקוד ובדוחות ה-QA, 10.9.2026. כל מספר כאן נמדד בפקודה — אין הערכות.*
