# מתח — חשמל לתיכון

תרגול למגמת חשמל בתיכון (10 יח״ל), לפי ארבעת המקצועות של התכנית:
תורת החשמל, מיתוג, מערכות הספק והמרת אנרגיה. ארבע שפות, ולכל
מסיח כתוב למה הוא שגוי.

**מזהה:** `electric` · **שלב:** `build` (מאחורי שער פנימי) · **קובץ:** `index.html`

פתיחה בזמן בדיקה: `/electric/?internal=shlav-internal-electric` — פעם אחת למכשיר.

## מאיפה היא באה

הועתקה מ-`math-uni3` ב-24.9.2026 (משפחת `math-uni`: מחולל, לא מאגר).
המנוע — הקראה, רמזים, פתרון מודרך, מבחן כיתתי, מצב מורה, נגישות —
זהה. מה שהוחלף: המחוללים (סעיף 9), רשימת הנושאים (10), דף הנוסחאות
(11), כללי ההקראה ליחידות (`EL_SAY`), והמילון. המילון נבנה מחדש:
נשארו רק מפתחות שהקוד משתמש בהם.

## התוכן

16 נושאים × 4 רמות (בסיס, תרגיל, מבחן, נימוק):

| תחום | נושאים |
|---|---|
| תורת החשמל | חוק אוהם · נגדים בטור · נגדים במקביל · מחלק מתח · חוקי קירכהוף · הספק · קבלים · זרם חילופין |
| מיתוג | שערים לוגיים (כולל מגעים ודה־מורגן) · ספירה בינארית |
| מערכות הספק | זרם עומס ומפסקים (כולל חיבור 3×80 A) · כבלים ומפל מתח · הגנה מפני התחשמלות |
| המרת אנרגיה | נצילות ואנרגיה · מנוע השראה · שנאי |

הסכימה: `{ask, expr, ans, wrong:[W(טקסט, למה)], steps:[S(כותרת, הסבר, נוסחה)], hint, rule}`.

## מה עוד לא נעשה

- **התוכן לא נבדק בידי מורה לחשמל.** הערכים והנוסחאות נבדקו בקוד
  ובקריאה, ולא מול תכנית הלימודים של משרד החינוך.
- ברק מקבל תפקיד `electric` ב-`tutor-api/worker.js`, אבל השרת צריך
  פריסה (`deploy-tutor.yml`, ידני) לפני שהתפקיד חי.
- `content.js`: 0 FAIL, REVIEW — `.claude/qa/reports/electric.md`.
